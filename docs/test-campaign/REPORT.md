Sweeps A through J had never been run, and running them found the accessibility defect this campaign had already measured on 19 August and filed as a note instead of a defect: every switch on the status window reached the accessibility tree with no name at all.

`campaign.py check` exit 0: 33 pass · 0 fail · 0 n/a of 33, armed 33/33. `strict-check.py` exit 0, ratchet 32 to 33. `swift test`: 1563 tests in 186 suites. Sweeps A to L: 113 checks, 0 inconclusive cells.

The defect. Nine switches built as `Toggle("", isOn:)` with `.labelsHidden()` announced as nine unnamed checkboxes in a column; seven tool disclosures were all called "Details" with nothing saying which tool; the permissions list repeated "Open Settings" and "How" per grant. The naming rule is now `ProctorCore/AccessibilityNames.swift`, because there is no `ProctorUI` test target and a name written in a view body cannot be proved here, and `AccessibilityNameTests` holds distinctness over the real catalogue so a tenth switch cannot reintroduce it. Measured on the installed build through `proctor_find`: 9 named checkboxes, 21 buttons, no two alike, and the 7 that remain nameless are AppKit's own scrollbar and window chrome, each named by its subrole.

Sweeps. A forced six live states and named the one it did not, with what covers it instead. B put eight faults through the tools and every one came back as a code with a sentence and a remedy. C activated all 23 enabled controls on the status window and looked for the effect in the tree; the three that did not move a value are exactly the three consent-gated switches, where one press is meant to show the disclosure rather than flip anything. E, F, H and J are clean over 8, 3, 4 and 5 checks. G is two concurrent MCP sessions rather than two users, because there is one person and one agent here, and it says so.

Components. All 14 were enumerated and none carried a case, so the axis had no denominator. Each is now named by the armed effect-rung case that already exercises it, and the atlas names its cases back.

Two more instrument defects, both mine, both caught before their results counted. This sweep runner wrote its transcript over its own call results, so the window handle came back empty and every window-dependent check silently measured a "requires window" refusal and reported it clean. And the refusal predicate read only the top level of a response, so `proctor_act` batches, which report per step, looked like silent failures. Four such defects are now recorded in the sweep file, because the pattern is the point: a clean sweep is the first thing to distrust.

Not covered, and it is a gap rather than a clean row. Phase 8 never ran: `designOfRecord` is empty and `witness-worklist.py` answers `no pairs`, so no surface here has been judged against a design at all. A worklist file left over from an earlier run said otherwise, listing the status window against the app icon's 1024px render, and it is deleted rather than corrected, because a shot paired with an icon is not a comparison anybody should act on. The surface mocks that would supply the references are being built on `ai/wave-9`.

The machine is as I found it. The clipboard was saved and put back, every switch was toggled and toggled back, and the settings file the sweep created was removed, so all nine read `builtInDefault` again. The work is on local `main` in seven commits, unpushed.
