[
  {
    "id": "CASE-0001",
    "req": "REQ-001",
    "surface": "SURF-001",
    "flow": "FLOW-001",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/test-suite-pass.log",
      "evidence/mcp-doctor-report.json"
    ],
    "armed": true,
    "armedBy": "disabled MCP stdio dispatcher; verified tool calls fail with protocol error; restored",
    "note": "21 tools advertised over real MCP stdio (mcp_drive.py initialize + tools/list). Doctor structuredContent from the reinstalled artifact: agentBuild.commit ab53c5e5a47b, ready true, Accessibility and Screen Recording granted, Automation denied. The grants survived the Developer ID reinstall, which is what REQ-022 promises. swift test: 1526 tests in 176 suites passed."
  },
  {
    "id": "CASE-0002",
    "req": "REQ-001",
    "surface": "SURF-002",
    "flow": "FLOW-001",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/sweepL-privilege-separation.json",
      "evidence/test-suite-pass.log"
    ],
    "armed": true,
    "armedBy": "Swapped the launch profile and watched the advertised catalogue change: tools/list returns 21 names on full and exactly 10 on core.",
    "note": "Profile filtering measured over real MCP stdio. core advertises proctor_act, apps, assert, capture, doctor, find, menu, snapshot, wait, zoom. The earlier arming claim said proctor_guest is refused on core; it is not. tools/call accepts any real tool by name whatever the profile, which MCPServer.swift:123 states as the contract: the profile trims discovery so a host is not handed the whole catalogue as noise, and never blocks a host that widens its own surface. Verified: proctor_guest and proctor_policy both executed on a core-profile shim. The profile is a token-cost trim, not a privilege boundary, and sweep L records it as such."
  },
  {
    "id": "CASE-0003",
    "req": "REQ-002",
    "surface": "SURF-003",
    "flow": "FLOW-002",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/test-suite-pass.log"
    ],
    "armed": true,
    "armedBy": "forced window focus during process-directed step; verified focus assertion failed; restored",
    "note": "Background AXUIElement action execution without focus theft or Space disruption"
  },
  {
    "id": "CASE-0004",
    "req": "REQ-003",
    "surface": "SURF-004",
    "flow": "FLOW-004",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/overlay-exclusion.json",
      "evidence/test-suite-pass.log"
    ],
    "armed": true,
    "armedBy": "suppressed foreground activation; verified synthetic batch refused; restored",
    "note": "Synthetic hover batch raised HUD+takeover (CGWindowList: 3 overlay windows sharingState=0). Raster of the overlay is CASE-0008 n/a. Overlay-exclusion of the AUT capture is evidence/overlay-exclusion.json (verdict excluded, capturePathProvenLive true)."
  },
  {
    "id": "CASE-0005",
    "req": "REQ-004",
    "surface": "SURF-006",
    "flow": "FLOW-003",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/sck-capture-frame.json"
    ],
    "armed": true,
    "armedBy": "injected stale SCFrameStatus; verified trustworthy verdict evaluated false; restored",
    "note": "proctor_capture of Status win:1:0: SCFrameStatus complete, trustworthy true, 900x833 from 1280x1184, dirtyRectCount 1."
  },
  {
    "id": "CASE-0006",
    "req": "REQ-005",
    "surface": "SURF-006",
    "flow": "FLOW-003",
    "oracle": "raster-visual",
    "status": "pass",
    "capture": {
      "method": "ScreenCaptureKit window-scoped",
      "frameStatus": "complete"
    },
    "evidence": [
      "evidence/shots/surf-006-screen-capture.png"
    ],
    "armed": true,
    "armedBy": "forced 768px tier clamp on detail request; verified pixel assertion rejected; restored",
    "note": "Window-scoped SCK of TextEdit scratch.txt during overlay-exclusion baseline (1312x844, complete). Distinct bytes from the Status-window shot."
  },
  {
    "id": "CASE-0007",
    "req": "REQ-005",
    "surface": "SURF-007",
    "flow": "FLOW-003",
    "oracle": "raster-visual",
    "status": "pass",
    "capture": {
      "method": "ScreenCaptureKit window-scoped crop via proctor_zoom",
      "frameStatus": "complete"
    },
    "evidence": [
      "evidence/shots/surf-007-zoom.png"
    ],
    "armed": true,
    "armedBy": "clamped zoom crop to 1x scale; verified sub-pixel resolution assertion failed; restored",
    "note": "proctor_zoom region [16,48,300,160] of Status window: 600x320 at scale 2, status complete, trustworthy true."
  },
  {
    "id": "CASE-0008",
    "req": "REQ-006",
    "surface": "SURF-004",
    "flow": "FLOW-004",
    "oracle": "outcome",
    "status": "pass",
    "capture": {
      "method": "screencapture -l on the panel window number, exclusion lifted by PROCTOR_OVERLAY_CAPTURE",
      "frameStatus": "complete"
    },
    "evidence": [
      "evidence/overlay-capture-lifted.json",
      "evidence/overlay-capture-default.json"
    ],
    "armed": true,
    "armedBy": "Armed by the default, which refuses: with PROCTOR_OVERLAY_CAPTURE unset every overlay reports sharingState 0 and screencapture -l exits 1 with \"could not create image from window\". The same commands with the switch set produce these pixels.",
    "note": "704x460 at scale 2, 2517 distinct colours. The frame carries the 22pt character sprite, the step title \"Hovering over ...\", the target badge \"Synthetic event — TextEdit must stay in front\", the step counter, the elapsed clock and the Pause and Stop controls, which is what REQ-006 promises. Measured under PROCTOR_OVERLAY_CAPTURE=1; the shipping default excludes the panel from every channel, and that default is what CASE-0004 and CASE-0032 check. evidence/hud-capture-channels.json records the four channels that refuse when it is unset. Supporting measurements: evidence/overlay-capture-lifted.json (this capture) and evidence/overlay-capture-default.json (the refusal it is armed against). Citation reconciled in PRO-0109 (DEF-224)."
  },
  {
    "id": "CASE-0009",
    "req": "REQ-007",
    "surface": "SURF-005",
    "flow": "FLOW-004",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/test-suite-pass.log"
    ],
    "armed": true,
    "armedBy": "disconnected hardware event tap; verified contention yield timed out; restored",
    "note": "Hardware contention detection yields control within 50ms"
  },
  {
    "id": "CASE-0010",
    "req": "REQ-008",
    "surface": "SURF-005",
    "flow": "FLOW-004",
    "oracle": "outcome",
    "status": "pass",
    "capture": {
      "method": "screencapture -l on the panel window number, exclusion lifted by PROCTOR_OVERLAY_CAPTURE",
      "frameStatus": "complete"
    },
    "evidence": [
      "evidence/overlay-capture-lifted.json",
      "evidence/overlay-exclusion.json"
    ],
    "armed": true,
    "armedBy": "Armed by the default, which refuses: with PROCTOR_OVERLAY_CAPTURE unset every overlay reports sharingState 0 and screencapture -l exits 1 with \"could not create image from window\". The same commands with the switch set produce these pixels.",
    "note": "5120x2880, the full external display, 2739 distinct colours and every pixel carrying alpha. The frame shows the tint and the statement \"Proctor is driving \\\"TextEdit\\\"\" with \"Your clicks and keys still reach it — Pause and Stop are in Proctor's run panel\", which is what REQ-008 promises. Measured under PROCTOR_OVERLAY_CAPTURE=1. The complementary property, that the panel does not enter a window-scoped capture of the app under test at the default, is CASE-0004 evidence overlay-exclusion.json. Supporting measurements: evidence/overlay-capture-lifted.json and evidence/overlay-exclusion.json. Citation reconciled in PRO-0109 (DEF-224)."
  },
  {
    "id": "CASE-0011",
    "req": "REQ-009",
    "surface": "SURF-008",
    "flow": "FLOW-001",
    "oracle": "outcome",
    "status": "pass",
    "capture": {
      "method": "ScreenCaptureKit window-scoped",
      "frameStatus": "complete"
    },
    "evidence": [
      "evidence/shots/surf-008-status-window.png"
    ],
    "armed": true,
    "armedBy": "corrupted doctor health payload; verified status window rendered error row; restored",
    "note": "The window at 760x1000 so both sections are in one frame: seven tool rows, each saying what was established rather than that it exists — cua-driver reads 'Found and correctly signed, nothing has established that it works', which is the honesty this case is about. Shortcuts CLI sits under Tools rather than Permissions, which is the classification DEF-011 turned on. Citation pointed to published surf-008-status-window.png in PRO-0109 (DEF-224).",
    "captureMethod": "proctor_capture → ScreenCaptureKit, window-scoped",
    "frameStatus": "complete"
  },
  {
    "id": "CASE-0012",
    "req": "REQ-010",
    "surface": "SURF-009",
    "flow": "FLOW-001",
    "oracle": "raster-visual",
    "status": "pass",
    "capture": {
      "method": "ScreenCaptureKit window-scoped",
      "frameStatus": "complete"
    },
    "evidence": [
      "evidence/shots/surf-009-walkthrough.png"
    ],
    "armed": true,
    "armedBy": "mocked TCC denial in walkthrough; verified advance button remained disabled; restored",
    "note": "SCK of the walkthrough before Skip setup: Accessibility and Screen Recording marked On, Continue enabled, Skip setup present. 900x833 complete trustworthy."
  },
  {
    "id": "CASE-0013",
    "req": "REQ-011",
    "surface": "SURF-010",
    "flow": "FLOW-001",
    "oracle": "raster-visual",
    "status": "pass",
    "capture": {
      "method": "screencapture -R of extras region (not a window; SCK window-scoped does not apply)",
      "frameStatus": "complete"
    },
    "evidence": [
      "evidence/shots/surf-010-menubar.png"
    ],
    "armed": true,
    "armedBy": "cleared status item image; verified menu bar glyph existence check failed; restored",
    "note": "Menu extra captured at 1056x76. glass_probe hasExtrasMenuBar true on pid 38168. Popover anchoring not opened this run."
  },
  {
    "id": "CASE-0014",
    "req": "REQ-012",
    "surface": "SURF-003",
    "flow": "FLOW-002",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/test-suite-pass.log"
    ],
    "armed": true,
    "armedBy": "forced dirty frame generator during settle; verified timeout reported; restored",
    "note": "Multi-signal settling evaluates pixel and AX quietness"
  },
  {
    "id": "CASE-0015",
    "req": "REQ-013",
    "surface": "SURF-011",
    "flow": "FLOW-005",
    "oracle": "metamorphic",
    "status": "pass",
    "evidence": [
      "evidence/stability-sweep-5run.json"
    ],
    "armed": true,
    "armedBy": "injected state hash flip on step 3; verified firstDivergence reported step 3; restored",
    "note": "proctor_stability 5-run of campaign-setup-menu: tree-only deterministic true; includeTiles deterministic false, firstDivergence 0, stepInstability [0.25] (pixel animation)."
  },
  {
    "id": "CASE-0016",
    "req": "REQ-014",
    "surface": "SURF-011",
    "flow": "FLOW-005",
    "oracle": "metamorphic",
    "status": "pass",
    "evidence": [
      "evidence/test-suite-pass.log"
    ],
    "armed": true,
    "armedBy": "desynchronised AX element frame from layer geometry; verified agree assertion failed; restored",
    "note": "Tri-observer agree check compares AX tree, geometry, and pixels"
  },
  {
    "id": "CASE-0017",
    "req": "REQ-015",
    "surface": "SURF-012",
    "flow": "FLOW-006",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/audit-chain-verdict.json"
    ],
    "armed": true,
    "armedBy": "mutated HMAC signature on record 12; verified AuditLog.verify flagged corruption; restored",
    "note": "proctor_policy status: auditEncrypted true, auditSigned true, auditVerdict.clean true, completeness proven, entries 733, verified 158, preChain 575."
  },
  {
    "id": "CASE-0018",
    "req": "REQ-016",
    "surface": "SURF-012",
    "flow": "FLOW-006",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/test-suite-pass.log"
    ],
    "armed": true,
    "armedBy": "executed disallowed shell action in policy; verified PolicyEngine refused execution; restored",
    "note": "Policy engine action filters and redaction pattern enforcement"
  },
  {
    "id": "CASE-0019",
    "req": "REQ-017",
    "surface": "SURF-013",
    "flow": "FLOW-007",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/guest-autoroute-refusal.json"
    ],
    "armed": true,
    "armedBy": "provided invalid VM handle; verified proctor_guest returned error code; restored",
    "note": "proctor_guest list: 1 prlctl guest Windows 11 state invalid; lume --json unknown option. Capability surface answered."
  },
  {
    "id": "CASE-0020",
    "req": "REQ-018",
    "surface": "SURF-013",
    "flow": "FLOW-007",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/test-suite-pass.log"
    ],
    "armed": true,
    "armedBy": "executed tree assertion on delegated Cua tier; verified skipped verdict with reason; restored",
    "note": "Witness tier gating: delegated tier skips tree assertions with reason"
  },
  {
    "id": "CASE-0021",
    "req": "REQ-019",
    "surface": "SURF-004",
    "flow": "FLOW-007",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/guest-autoroute-refusal.json"
    ],
    "armed": true,
    "armedBy": "unset PROCTOR_GUEST during host takeover check; verified takeover executed on host; restored",
    "note": "LaunchAgent env PROCTOR_GUEST=gst-sequoia-01; synthetic hover on TextEdit refused: notImplemented, names gst-sequoia-01, remedy Unset PROCTOR_GUEST. Env restored after the call."
  },
  {
    "id": "CASE-0022",
    "req": "REQ-020",
    "surface": "SURF-014",
    "flow": "FLOW-008",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/test-suite-pass.log",
      "evidence/maestro-live-serialized.log"
    ],
    "armed": true,
    "armedBy": "passed malformed URL scheme to simctl; verified launch failed with non-zero exit; restored",
    "note": "iOS simulator deep link launching and Maestro flow repeat execution. The live lane was run on 20 Aug 2026 rather than carried from 15 Aug, against maestro 2.4.0 and a booted iPhone 16 Pro on iOS 18.2, and running it found DEF-015: it passed device nil and relied on exactly one simulator being booted, and its two tests drove one device concurrently so the determinism check scored its own repeats as divergent at command 4. Serialised and given a named device, both pass in 55.6s, green twice. What is still not gated by a default run is the live lane itself, which stays opt-in behind PROCTOR_LIVE_MAESTRO because a machine without Xcode, maestro or a booted simulator would go red for a fact about the machine."
  },
  {
    "id": "CASE-0023",
    "req": "REQ-021",
    "surface": "SURF-015",
    "flow": "FLOW-002",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/test-suite-pass.log"
    ],
    "armed": true,
    "armedBy": "injected non-Sendable actor mutation across task boundary; verified Swift compiler error; restored",
    "note": "Swift 6 complete concurrency safety across 1520 tests"
  },
  {
    "id": "CASE-0024",
    "req": "REQ-022",
    "surface": "SURF-016",
    "flow": "FLOW-001",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/gatekeeper.json",
      "evidence/gatekeeper-spctl.txt"
    ],
    "armed": true,
    "armedBy": "Watched to fail on the previous install: spctl rejected and stapler reported no ticket against the same two commands that now pass.",
    "note": "Re-measured after scripts/install.sh notarised and stapled a fresh build: spctl -a -vv /Applications/Proctor.app returns accepted, source=Notarized Developer ID, origin=Developer ID Application: Luke Rhodes (H4HGFL52W7); stapler validate returns \"The validate action worked!\"; codesign reports runtime hardened, timestamp 19 Aug 2026 21:59:14. The earlier fail was the same commands against the previous install (rejected, Unnotarized Developer ID, no ticket)."
  },
  {
    "id": "CASE-0025",
    "req": "REQ-023",
    "surface": "SURF-015",
    "flow": "FLOW-002",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/test-suite-pass.log"
    ],
    "armed": true,
    "armedBy": "stopped reflector in-process endpoint; verified reflectorIdle condition timed out; restored",
    "note": "proctor_inspect of Proctor Status returned reflectorUnavailable (expected: not in-process). Unit suite covers the bridge; live inspect of a foreign process is the documented ceiling."
  },
  {
    "id": "CASE-0026",
    "req": "REQ-024",
    "surface": "SURF-003",
    "flow": "FLOW-002",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/mcp-doctor-report.json"
    ],
    "armed": true,
    "armedBy": "disabled Obscura toolchain flag; verified browser route refused with missing engine; restored",
    "note": "doctor.obscura.available true at /Users/lukerhodes/.local/bin/obscura; browser lane ready."
  },
  {
    "id": "CASE-0027",
    "req": "REQ-026",
    "surface": "SURF-008",
    "flow": "FLOW-001",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/sweeps-kl.json",
      "evidence/glass-probe.json"
    ],
    "armed": true,
    "armedBy": "Closed Status via AXPress close; proctor_apps activate returned windows []; open -a left axWindowCount=0; Window ▸ Proctor restored the window. The activate/open path is the one that fails.",
    "note": "Fixed and verified on the installed artifact. With the window ordered out at an accessory launch (cg 0, axWindowCount 0, extras present), open -a /Applications/Proctor.app produced cg 1 window 640x592 and axWindowCount 1 titled Proctor. Before the fix the same sequence left axWindowCount 0 through both activate and open -a. WindowPresentation.shouldPresentMain asks whether a window titled Proctor is on screen instead of trusting AppKit hasVisibleWindows, which is true while only the extras item is up."
  },
  {
    "id": "CASE-0028",
    "req": "REQ-009",
    "surface": "SURF-008",
    "flow": "FLOW-001",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/sweeps-kl.json"
    ],
    "armed": true,
    "armedBy": "The kill is the control: while the agent answered, the same window showed Ready and the permission rows. launchctl bootout + SIGKILL flips it.",
    "note": "Re-measured 2026-08-19 against /Applications/Proctor.app with a monotonic clock: at t+0.71s and t+3.63s after bootout+SIGKILL the window showed the Agent down pill, \"The background agent is not answering\", the socket path and Start the agent / Re-check. The first capture that prompted DEF-002 was taken inside one 2s doctor tick (t is approximately 0.2s), so it caught the pre-tick frame rather than a stale claim. DEF-002 is retracted. Evidence citations reconciled in PRO-0109 (DEF-224)."
  },
  {
    "id": "CASE-0029",
    "req": "REQ-027",
    "surface": "SURF-008",
    "flow": "FLOW-001",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/sweepL-halfopen.json"
    ],
    "armed": true,
    "armedBy": "Watched to fail on the build before the fix: with the agent held by SIGSTOP the window went on showing Ready and green grants past 25 seconds, and a driven proctor_doctor never returned. The same SIGSTOP against the fixed build flips it.",
    "note": "SIGSTOP leaves the listener bound, so connect() succeeds and the reply never comes; the poll sat in read() and isChecking latched, so every later tick returned at the guard. SocketClient now takes an opt-in ioTimeoutSeconds, default 0 so the shim keeps blocking for batches that legitimately run for minutes, and AgentModel sets 5s on its doctor, activity and HUD calls. Measured after the fix: at t+7.12s the window reads Agent down and \"the Proctor agent did not answer within 5s\"; SIGCONT returns it to Ready. Evidence citations reconciled in PRO-0109 (DEF-224).",
    "defects": [
      "DEF-002"
    ]
  },
  {
    "id": "CASE-0030",
    "req": "REQ-006",
    "surface": "SURF-004",
    "flow": "FLOW-004",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/overlay-lifecycle.json",
      "evidence/glass-probe.json"
    ],
    "armed": true,
    "armedBy": "The run is the control. The same predicate over CGWindowListCopyWindowInfo returns nothing before the batch starts and nothing four seconds after it ends, and doctor reports hud.onScreen false at both ends.",
    "note": "The rung REQ-006 can actually be measured on this lane. Across 39 samples of the window server while a synthetic batch ran, the Run HUD went absent then present then absent: 352x230 at layer 25, alpha 1, sharingState 0, onscreen true. Its pixels stay unreachable (CASE-0008), so this asserts the panel reaching the display server rather than what it renders."
  },
  {
    "id": "CASE-0031",
    "req": "REQ-008",
    "surface": "SURF-005",
    "flow": "FLOW-004",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/overlay-lifecycle.json",
      "evidence/overlay-exclusion.json"
    ],
    "armed": true,
    "armedBy": "The batch is the control: zero shield windows before it and zero four seconds after, against one per display while it runs.",
    "note": "One takeover panel per display, each matching that display frame exactly (1728x1117 on the one display attached at measurement time; an earlier two-display run carried 1728x1117 and 2560x1440), layer 24, alpha 1, sharingState 0, and all cleared afterwards. The shield pixels stay unreachable (CASE-0010); the complementary property, that the panel does not enter a capture of the app under test, is CASE-0004 evidence overlay-exclusion.json."
  },
  {
    "id": "CASE-0032",
    "req": "REQ-028",
    "surface": "SURF-004",
    "flow": "FLOW-004",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/overlay-capture-default.json",
      "evidence/window-level-sharing.json",
      "evidence/PRO-0088/case-0032-population-before.json",
      "evidence/PRO-0088/glass/overlay-population.json"
    ],
    "armed": true,
    "armedBy": "Watched to fail on the build before the fix: the drawn pointer reported sharingState 1 and screencapture -l wrote a 3456x2234 file of it while the run panel and the takeover tint both reported 0 and refused.",
    "note": "All three overlays now report sharingState 0 at the default, on both displays: layer 24 at 1728x1117 and 2560x1440, layer 25 at 352x230, layer 1000 at 1728x1117 and 2560x1440. The pointer is the one that changes level at runtime, and the sharing type does not survive a level change, so setting it once at construction covered only the first placement; level and sharing are set together now. Ruled out first: an isolated six-level probe showed the window server honours .none at every level including screenSaver (evidence/window-level-sharing.json), so the level was not the cause.",
    "defects": [
      "DEF-005"
    ]
  },
  {
    "id": "CASE-0033",
    "req": "REQ-030",
    "surface": "SURF-017",
    "flow": "FLOW-009",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/tui-capture-100x30.json",
      "evidence/tui-capture-80x24.json"
    ],
    "armed": true,
    "armedBy": "Watched to fail on the build before the fix: the first render of the queue pane put '2 lanes' on the shelf where the design compiled '3 lanes', and the error pane placed its remedy keybar one row above the design's. Both were caught by the row-and-column comparison rather than by looking.",
    "note": "The running binary was captured through a pty at both sizes by tui-craft's tui_capture.py (kind=captured, term=xterm-256color) and compared against the Swift renderer cell by cell: identical at 100x30 and at 80x24, with any difference reported by its row and column. The 22 compiled design frames are reproduced exactly by the same renderer. NOT a raster claim, deliberately: a terminal has no pixels to photograph, and the artifact is a typed cell grid rather than an image. For this class of defect the grid is the stronger instrument — a one-column offset from a double-width glyph is invisible in a screenshot and exact against a ruler, which is why both sides measure with the same width function. The oracle is therefore an exact outcome: two independently produced grids are equal or they are not."
  },
  {
    "id": "CASE-0034",
    "req": "REQ-030",
    "surface": "SURF-017",
    "flow": "FLOW-009",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/tui-gates-100x30.txt",
      "evidence/tui-gates-80x24.txt"
    ],
    "armed": true,
    "armedBy": "Watched to fail on the build before the fix: tui_gates.py --strict reported 'no bold and no dim anywhere in the frame - every glyph carries the same weight, so the screen has no hierarchy that survives a monochrome terminal', because weight was applied only on the NO_COLOR path.",
    "note": "tui_gates.py --strict on both captures: 0 high findings, exit 0. Attribute inventory now reports bold and dim in use, so the role ladder survives losing colour - which matters because the 16-colour palette has no defined RGB mapping, so on many terminals colour is the channel that cannot be relied on."
  },
  {
    "id": "CASE-0035",
    "req": "REQ-029",
    "surface": "SURF-017",
    "flow": "FLOW-009",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/tui-one-latch.json"
    ],
    "armed": true,
    "armedBy": "Watched to fail on the build before the fix: an action string the surface does not have returned ok and did nothing, so a misspelled Stop was indistinguishable from a Stop that worked.",
    "note": "Pause, Resume and Stop from a supervision client reach RunControl.shared - the same object the HUD panel writes and every run loop reads between steps. The control is injected in the test rather than taken from the process, because PRO-0053 recorded a suite writing a shared latch reaching whichever other suite was stepping concurrently; the default argument carries the claim."
  },
  {
    "id": "CASE-0036",
    "req": "REQ-029",
    "surface": "SURF-017",
    "flow": "FLOW-009",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/tui-agent-too-old.json"
    ],
    "armed": true,
    "armedBy": "Found by capturing the running program against the installed agent, which predates this feature: the surface said 'The background agent is not answering' about an agent that had just answered, and offered to start a process that was already running.",
    "note": "An agent that answers and does not know proctor.watch is now its own state with its own remedy - upgrade rather than start. A frame that stops arriving reports itself stale rather than being drawn as current, because a stale queue drawn as live is the screen somebody decides against."
  },
  {
    "id": "CASE-0037",
    "req": "REQ-031",
    "surface": "SURF-010",
    "flow": "FLOW-005",
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/menu-bar-rendered.json"
    ],
    "armedBy": "Watched to fail on the build before the fix: proctor_menu against the running app returned 17 of 20 declared commands, and the three absent ones were exactly those SwiftUI never declared.",
    "note": "20 of 20 specified commands now render, read from the running app's own menu bar through proctor_menu and diffed against CommandSurface.all. The instrument had to be read correctly first: enumerated while the app was not frontmost, the Window and Help menus come back unpopulated, which looks identical to five more missing commands."
  },
  {
    "id": "CASE-0038",
    "req": "REQ-018",
    "surface": "SURF-001",
    "flow": "FLOW-009",
    "oracle": "metamorphic",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/cli-json-identical.json"
    ],
    "armedBy": "Watched to fail by comparing without stripping wall-clock fields: the obscura and tools blocks differ on checkedAt between two calls seconds apart, so the relation is only true of the stable payload and the comparison says which fields it removed.",
    "note": "The same relation from two front ends: proctor --json and the MCP tools/call result for proctor_doctor agree across 23 top-level keys and 51,195 characters. Settles PRO-0073 A2, which was carried as needing a live agent."
  },
  {
    "id": "CASE-0039",
    "req": "REQ-032",
    "surface": "SURF-008",
    "flow": "FLOW-005",
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/surf-008-status-window.png",
      "evidence/shots/mock/surf-008-status-window.png"
    ],
    "armedBy": "Watched to fail on the build before the fix: the health report carried three grants and the design drew Input Monitoring as one of them, so the row the design specifies had no data behind it.",
    "note": "Measured through the product: a wave-9 agent on its own socket reports Input Monitoring granted, and the readiness pane draws it. The operator's installed agent was never replaced, so this was measured without an install."
  },
  {
    "id": "CASE-0040",
    "req": "REQ-029",
    "surface": "SURF-017",
    "flow": "FLOW-009",
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/tui-stop-halts-mcp-run.json",
      "evidence/tui-live-run.json",
      "evidence/tui-live-stopped.json"
    ],
    "armedBy": "Watched to fail twice on the way: a batch of hover steps was refused for want of foreground and ran in 0.239s, so there was no run to stop, and the first capture of a running TUI showed the empty state because nothing was in flight. Both are the vacuous pass this case exists to avoid.",
    "note": "A real keystroke into a TUI under a pty halted a run an MCP client had started. The caller received haltedByPerson after five of ten steps, with the remedy 'Nothing failed. Somebody watching the run decided to end it.' One latch, reached from a terminal. Settles PRO-0074 A4 and A5, both carried as needing a live agent. Recorded at the outcome rung rather than interactive-glass: the keystroke was actuated for real into a running program and the effect was read out of the MCP caller's own result, but a pty is not a display server, and interactive-glass owes one."
  },
  {
    "id": "CASE-0041",
    "req": "REQ-033",
    "surface": "SURF-017",
    "flow": "FLOW-009",
    "oracle": "outcome",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/tui-live-readiness.json",
      "evidence/tui-live-switches.json"
    ],
    "armedBy": "Watched to fail on the build before the fix: both panes drew their empty state against a live agent, which reads as a quiet machine rather than as a pane nobody wired.",
    "note": "Captured against a live agent: readiness draws four grants and five lanes with what each still wants, switches draws all eight with their real source and timing. History remains empty by design — the trail is sealed and no client can read it — and that is an open question rather than a fixed gap."
  },
  {
    "id": "CASE-0042",
    "req": "REQ-032",
    "surface": "SURF-008",
    "flow": "FLOW-005",
    "oracle": "raster-visual",
    "status": "pass",
    "armed": true,
    "evidence": [
      "evidence/shots/surf-008-status-window.png"
    ],
    "note": "The window draws four permission rows, Input Monitoring among them, reading a wave-9 agent on its own socket. `open -n --env PROCTOR_SOCKET=…` is what made it reachable: launching the binary directly from this harness exits immediately, and open passes an environment where a bare exec does not. The operator's installed app was never replaced.",
    "armedBy": "Watched to fail on the build before the fix, three times over: the window drew three rows against a health report carrying four, and two earlier captures of it were of a UI bound to the operator's installed agent, which predates the fix and reports three.",
    "captureMethod": "proctor_capture → ScreenCaptureKit, window-scoped",
    "frameStatus": "complete",
    "capture": {
      "method": "proctor_capture → ScreenCaptureKit window-scoped",
      "frameStatus": "complete"
    }
  },
  {
    "id": "CASE-0043",
    "req": "REQ-033",
    "surface": "SURF-017",
    "flow": "FLOW-009",
    "oracle": "outcome",
    "lane": "macos",
    "status": "pass",
    "evidence": [
      "evidence/tui-history-pane.txt"
    ],
    "armed": true,
    "note": "The supervision TUI's history pane is fed by proctor_history, the same internal socket verb the app's History window calls. TUISurface.history(from:) maps the agent's projection to rows and derives nothing the reply did not name, so a field the agent withholds stays withheld rather than being reconstructed on the client side. The pane separates three states a single empty frame used to collapse: a quiet machine, a trail this Mac could not open, and a history that opened short - the last states its count on the shelf. Rendered at the 80x24 floor as well as at 100x30, and the shelf keeps both facts at 80 columns.",
    "armedBy": "Watched to fail with the behaviour removed, three ways. Forcing the pane's sealed branch to false collapsed a sealed trail and a quiet machine onto one identical frame and both assertions bit; dropping the shelf note lost '2 entries could not be opened'; and making history(from:) return no rows failed the mapping assertion. Restored and green."
  },
  {
    "id": "CASE-0044",
    "req": "REQ-034",
    "surface": "SURF-018",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/cli/verb-derivation.txt",
      "evidence/cli/verbs.txt"
    ],
    "armed": true,
    "armedBy": "CLISurfaceTests derives verbs from ToolCatalogue.all rather than a list; adding a tool without a verb reddens the derivation test. Measured live: 21 catalogue tools, 26 verbs, zero tools with no verb.",
    "note": "First case the cli lane has ever carried. The lane was declared in campaign.json from the start and had no requirement, no surface and no case behind it."
  },
  {
    "id": "CASE-0045",
    "req": "REQ-035",
    "surface": "SURF-018",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/cli/audit-via.txt"
    ],
    "armed": true,
    "armedBy": "FrontEndAttributionTests pins the name->front-end map, and a peer whose executable is neither proctor-cli nor proctor-shim records nil rather than defaulting to one of them.",
    "note": "Whole trail read: 1159 records, via=cli on 3, via=mcp on 19, None on the 1137 that predate the field. Derived from proc_pidpath of the peer rather than from the request."
  },
  {
    "id": "CASE-0046",
    "req": "REQ-036",
    "surface": "SURF-018",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/cli/exit-codes.txt",
      "evidence/cli/exit-codes-after-fix.txt"
    ],
    "armed": true,
    "armedBy": "Restored DEF-019's original predicate and re-ran the suite: 5 issues across the failed assertion, the skipped assertion, the timed-out wait and the failed kill. The passing-check control did NOT redden, which is what stops an over-broad fix passing the same test.",
    "note": "DEF-019 found here and fixed. Measured against the live agent before and after: a failed assert and a timed-out wait both exited 0, now both exit 1; a genuinely passing assert still exits 0; usage 2, agent unreachable 3, doctor ready 0."
  },
  {
    "id": "CASE-0047",
    "req": "REQ-037",
    "surface": "SURF-019",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/test-suite-pass.log"
    ],
    "armed": true,
    "armedBy": "Deleted the guest-forwarding funnel in Dispatch.swift and re-ran: 3 assertions red in GuestDispatchWiringTests, including the host answering the call itself and the guest link never being reached.",
    "note": "Seam level, and recorded as seam level. A session attached to a guest is driven through Dispatcher.handle rather than through the forwarding function directly, and the host actuates nothing. Executing inside a live guest is CASE-0056 and is blocked."
  },
  {
    "id": "CASE-0048",
    "req": "REQ-038",
    "surface": "SURF-020",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/test-suite-pass.log"
    ],
    "armed": true,
    "armedBy": "The PRO-0076 verifier set the lane's isCounted to false; the suite hung and produced no verdict, which scripts/test.sh treats as a failure through its absent-verdict rule. Caught, but as a hang rather than a red assertion, and recorded as the weaker oracle it is.",
    "note": "Capacity 1/2/3 admit 1/2/3; one grant scan cannot admit three into two; one session per named guest serialises even with a slot free; a third attach queues rather than being refused; a linux guest is not counted against the macOS cap."
  },
  {
    "id": "CASE-0049",
    "req": "REQ-039",
    "surface": "SURF-020",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/test-suite-pass.log"
    ],
    "armed": true,
    "armedBy": "Two mutations after the gap-fix refactor rather than before it, because the refactor routed three stop call sites through one audited path and a green suite says least about code that has just moved. Dropping startedByThisAgent from the release guard: 2 red. Removing the audited boot-timeout stop: 4 red, including the audit row's outcome and bundle id.",
    "note": "A guest a person started, or one another session holds, is waited for and never stopped to free a slot. Every stop this agent performs on its own initiative goes through guestMutate and lands in the trail."
  },
  {
    "id": "CASE-0050",
    "req": "REQ-040",
    "surface": "SURF-019",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/test-suite-pass.log"
    ],
    "armed": true,
    "armedBy": "GuestAttachmentTests exercises the derivation both directions and for a nil platform; the PRO-0076 verifier additionally checked the regression direction of the infer fix.",
    "note": "Carries DEF-020's fix. The tier is read from what the provider says rather than from the guest's name, so a provider reporting darwin yields macOS and the native tier."
  },
  {
    "id": "CASE-0051",
    "req": "REQ-041",
    "surface": "SURF-021",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/overlay/entitlement-on-glass.txt"
    ],
    "armed": true,
    "armedBy": "Removed the AgentProcess term and re-ran the suite while polling the window server: 2 windows appeared from swiftpm-testing-helper, one at the tint's level mid-fade and one at the drawn pointer's, and 3 tests went red. With the term in place the same run paints nothing.",
    "note": "Measured against the window server's own list rather than against a value. The baseline was established after killing an orphaned helper from an earlier run that was still holding two such windows, so zero means zero."
  },
  {
    "id": "CASE-0052",
    "req": "REQ-042",
    "surface": "SURF-005",
    "oracle": "metamorphic",
    "status": "pass",
    "evidence": [
      "evidence/test-suite-pass.log"
    ],
    "armed": true,
    "armedBy": "TakeoverDwellTests pins the relation rather than a duration: showing again while up returns raising=false and moves the deadline forward, and a stop cancels rather than defers.",
    "note": "A relation across calls, not a value: N shows inside the floor raise once and lower once. Found a second defect while testing it, where a stop during a lingering statement never lowered it because takeoverEnd returned on the takeoverShown guard first."
  },
  {
    "id": "CASE-0053",
    "req": "REQ-043",
    "surface": "SURF-005",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/test-suite-pass.log"
    ],
    "armed": true,
    "armedBy": "PointerPlaneCoveredTests: making isCovered ignore the window order reddens the covered case, and making Proctor's own panels count as covering reddens the annotation case.",
    "note": "The drawn pointer over a covered target. Dimming marks an uncertain position and cannot mark a pointer in the wrong plane entirely, so the fallback splits on whether anything covers the target and draws nothing when something does."
  },
  {
    "id": "CASE-0054",
    "req": "REQ-044",
    "surface": "SURF-005",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/test-suite-pass.log"
    ],
    "armed": true,
    "armedBy": "Reverted the label to its single wording: the off-panel-screen test reddens on both the menu-bar assertion and the no-run-panel one. Separately, letting the no-panel case fall through to RunHUDPlacement's primary-screen answer reddens the nowhere test.",
    "note": "The statement is drawn on every display and the run panel stands on one, so the control the line names changes with the screen reading it. A run with no panel standing resolves to nowhere rather than to the primary screen, and every display then names the menu bar."
  },
  {
    "id": "CASE-0055",
    "req": "REQ-038",
    "surface": "SURF-020",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/test-suite-pass.log"
    ],
    "armed": true,
    "armedBy": "Deleted the guestPool line from the doctor reply assembly and re-ran: 2 assertions red in DoctorReplyWiringTests, on the reply's own object rather than on session.poolStatus().",
    "note": "The pool as proctor_doctor reports it: capacity, held, holders and waiting. Separate from CASE-0048 because the clause names the reply and the seam function is a different thing from the wire it reaches."
  },
  {
    "id": "CASE-0056",
    "req": "REQ-037",
    "surface": "SURF-019",
    "oracle": "interactive-glass",
    "status": "pass",
    "evidence": [
      "evidence/guest/a1-guest-calculator-4x7.png",
      "evidence/guest/a1-attached-session-transcript.txt",
      "evidence/guest/a1-guest-doctor.json"
    ],
    "armed": true,
    "note": "SETTLED LIVE 2026-08-21 on `proctor-guest`, a fresh lume guest built by route (c) of the three recorded below: `lume create proctor-guest --ipsw tahoe.ipsw --unattended tahoe --cpu 4 --memory 8`, which configures the user, SSH, autologin and no screen lock by preset. macOS 26.6.2 arm64. That preset is what unblocked it: `stat -f %Su /dev/console` reads `lume` rather than `root`, so there IS an Aqua session and `launchctl bootstrap gui/501` succeeds, which is precisely what `proctor-mac-node` could not do. The Developer ID signed, notarised and stapled wave-9 build is installed at /Applications/Proctor.app inside the guest (guest's own spctl: source=Notarized Developer ID); both TCC grants were switched on in the guest's own GUI session, driven over VNC, and survived a rebuild because the designated requirement is team-scoped. THE MEASUREMENT: one persistent MCP session against the host agent — attach names {kind: guest, name: proctor-guest, tier: native}; proctor_apps activate launches Calculator INSIDE the guest (pid 1085, win:1:0) while the host has none; proctor_act runs 5 of 5 key steps and its machine names the guest; proctor_find reads AXStaticText '4×7' and '28' from the guest's own accessibility tree; the VNC framebuffer shows the same two strings; detach. Host Calculator absent throughout, which is A2 in the same run. FOUR DEFECTS THIS FOUND, all fixed here: the lume provider called `ls --json` where 0.5.3 wants `--format json`, so the whole guest lane reported 'no guest matches' while the guest was listed and running; Dispatch.swift kept its own copy of the valid-action list and it never gained attach or detach, so every attach a real client sent was refused as unknown; the packaged app's SwiftPM resource bundle was unreachable on any Mac but the build machine, crash-looping the guest agent on the first call that drew the character; and a relayed guest reply carried the guest's own {kind: host}, telling the caller their step ran on their Mac. NOT reproduced: the capabilities note warns Tahoe guests render no application windows (trycua/cua #870) — Calculator, System Settings and Setup Assistant all rendered here. NOTE ON THE PROCEDURE: the spec's recipe said to attach with proctor-cli and then make an actuating call. That cannot work — an attachment is keyed to the peer process (pid:startTime), so a one-shot CLI attaches and exits, and the next call silently runs on the host. Measured: it launched Calculator on this Mac. The proof needs one persistent session, which is what an MCP client is.",
    "lane": "guest-glass",
    "capture": {
      "method": "lume VNC framebuffer read of the guest's virtual display; corroborated by the guest's own accessibility tree through proctor_find",
      "frameStatus": "unreported (VNC carries no per-frame status; corroborated by the guest's own accessibility tree returning the same two strings the pixels show)"
    }
  },
  {
    "id": "CASE-0057",
    "req": "REQ-036",
    "surface": "SURF-018",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/cli/sweep-refusal-honesty.txt"
    ],
    "armed": true,
    "armedBy": "Re-ran the sweep with the refusal condition removed, so every call reached the live agent: all three predicates failed on all 21 rows. The first version of this sweep is kept in the evidence with the reason it was worthless, because its predicate examined 21 rows and could not have failed on any of them.",
    "note": "Refusal honesty across every verb, denominator 21 (the five service verbs are never invoked: they mutate the machine). Every verb exits 3 when the agent is unreachable, carries a remedy, and names the socket it tried so an operator can see which agent was missing. Write posture: the socket does not exist, so nothing could be actuated."
  },
  {
    "id": "CASE-0058",
    "req": "REQ-042",
    "surface": "SURF-005",
    "oracle": "metamorphic",
    "status": "pass",
    "evidence": [
      "evidence/mutation-wave10.txt",
      "evidence/mutation-wave10.json"
    ],
    "armed": true,
    "armedBy": "The three surviving mutants were re-applied together after the tests were written and the suite went red with 6 issues across the three new tests, then restored to green at 1,802.",
    "note": "Mutation assay over the seven files wave 10 added or changed. PARTIAL by denominator: 11 of 40 selected mutants ran, 40 of 188 sites selected by seed 20260820. 3 survived and all three are now closed. One of the 8 kills was a 900s timeout under CPU contention from another project building concurrently and is recorded as untrustworthy rather than counted, so the honest kill count is 7 of 10 scored. Survivors are trustworthy in both directions: starvation can turn a survivor into a false kill but cannot turn a kill into a false survivor."
  },
  {
    "id": "CASE-0059",
    "req": "REQ-017",
    "surface": "SURF-013",
    "oracle": "effect-witness",
    "status": "pass",
    "armed": true,
    "armedBy": "Sabotage run in the same test body: executable pointed at a path that does not exist. The armed variant instead pointed it at the WORKING script, and the sabotage half went red with 3 issues — providerErrors nil, count 1.0 not 0, and a sentinel [8676] present. The live-count assertion was inverted in the same run and printed (livePIDs.count -> 2) < 0. docs/test-campaign/evidence/PRO-0077/arming-run.txt",
    "evidence": [
      "Tests/ProctorAgentTests/EffectWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0077/witness-run.txt",
      "docs/test-campaign/evidence/PRO-0077/arming-run.txt",
      "docs/test-campaign/evidence/PRO-0077/gate.txt"
    ],
    "witness": {
      "recorder": "sentinel files written by each spawned /bin/sh child with its own $$, read back off the filesystem with FileManager and Data(contentsOf:) — no code under test participates in the reading",
      "effect": "subprocess",
      "count": 2
    },
    "note": "Driven through Session.guest(action:\"list\") twice, with LumeProvider built by its CONVENIENCE initialiser — the production path, which binds run to Self.liveRun and reaches a real Process() through Session.runBounded. The three-argument init(executable:timeoutMs:run:) is the fake seam and was deliberately not used. Two drives, two sentinels, two distinct pids, neither this process's. PORTABLE FLOOR, not the kernel bar: no dtrace, no eslogger, no execve census — this suite has no privilege for one, and that is the lane's ceiling rather than a silence. The case asserts the SPAWN and not the parsed listing: runBounded bounds its output drain at 5s after the child exits, and this repo has already measured a loaded parallel run truncating a child's stdout (see the comment in IOSDeviceList.parse, 2026-08-20), so a stdout assertion here would be load-sensitive while the filesystem effect is durable. The listing's content is REQ-017's outcome rung and is covered by GuestProviderTests.",
    "capture": {
      "method": "ScreenCaptureKit window-scoped, signed Developer ID agent pid 97657, target Calculator pid 14545 CG window 123357",
      "frameStatus": "SCFrameStatus=complete, framesWaited 2"
    }
  },
  {
    "id": "CASE-0060",
    "req": "REQ-020",
    "surface": "SURF-014",
    "oracle": "effect-witness",
    "status": "pass",
    "armed": true,
    "armedBy": "Sabotage run in the same test body: simctl pointed at a path that does not exist, and readDevices raises the tool's own refusal. The armed variant pointed it at the WORKING script and the sabotage half went red with 2 issues — no error thrown, and a sentinel [8941] present. The live-count assertion was inverted in the same run and printed (pids.count -> 2) < 0. docs/test-campaign/evidence/PRO-0077/arming-run.txt",
    "evidence": [
      "Tests/ProctorAgentTests/EffectWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0077/witness-run.txt",
      "docs/test-campaign/evidence/PRO-0077/arming-run.txt",
      "docs/test-campaign/evidence/PRO-0077/gate.txt"
    ],
    "witness": {
      "recorder": "sentinel files written by each spawned /bin/sh child with its own $$, read back off the filesystem with FileManager and Data(contentsOf:)",
      "effect": "subprocess",
      "count": 2
    },
    "note": "Driven through Session.ios(action:\"list\") twice, against a Session whose ToolProbes reports the witness script as simctl, so the call reaches Session.runSimctl -> runBounded -> a real Process() in SessionIOSProcess.swift. Kept separate from CASE-0059 rather than sharing one case: they are different providers, and one case covering both would let one provider's silence hide behind the other's noise. Two drives, two sentinels, two distinct pids, neither this process's. PORTABLE FLOOR, not the kernel bar. Asserts the spawn and not the parsed device listing, for the drain reason recorded on CASE-0059."
  },
  {
    "id": "CASE-0061",
    "req": "REQ-015",
    "surface": "SURF-012",
    "oracle": "effect-witness",
    "status": "pass",
    "armed": true,
    "armedBy": "Sabotage run in the same test body: chmod 0500 on the trail directory, so withAuditFileLock cannot create audit.lock and appendRawLocked cannot O_CREAT the trail. The armed variant used 0700 instead and the zero-check went red at (landed -> 2753) == 0. The line-count assertion was inverted in the same run and printed (lines.count -> 3) == -1 against 3 records emitted. docs/test-campaign/evidence/PRO-0077/arming-run.txt",
    "evidence": [
      "Tests/ProctorAgentTests/EffectWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0077/witness-run.txt",
      "docs/test-campaign/evidence/PRO-0077/arming-run.txt",
      "docs/test-campaign/evidence/PRO-0077/gate.txt"
    ],
    "witness": {
      "recorder": "the trail file's presence, byte size and mtime before and after, plus its bytes read with a FRESH FileHandle(forReadingFrom:) and each line opened with AuditSeal.open in ProctorCore using the injected private half — never AuditLog.readTrail or openedTail, which are the code under test confirming itself",
      "effect": "filesystem-write",
      "count": 3
    },
    "note": "Driven through Session.attachResolved + Session.act with three real steps, with the session's sink set to the production AuditLog.append. The seam is the TEST-PROCESS INTERLOCK in Session.auditSink, not the write path: AuditLog.append still seals with CryptoKit AES-GCM, chains, signs, and lands the bytes with Darwin.open(O_WRONLY|O_APPEND|O_CREAT), Darwin.write and fsync in PolicyStore.swift, unchanged. Three records emitted by the agent, three lines on disk, all three opened with the seal. The line count is checked against what the AGENT emitted rather than against a number the test chose, so no assertion stands on a value the test wrote (DEF-019). PORTABLE FLOOR: no kernel file-open census. The witness runs on a dedicated Thread because AuditLog's seams are process-wide and TrailIsolation must be held across the drive; an async holder of that lock wedged the whole run on 2026-08-21, and the comment above withRedirectedTrail carries the measurement."
  },
  {
    "id": "CASE-0062",
    "req": "REQ-009",
    "surface": "SURF-008",
    "oracle": "effect-witness",
    "status": "pass",
    "armed": true,
    "armedBy": "Sabotage run in the same test body: the socket path is unlinked between bind and connect, so Darwin.connect has nothing to reach. The armed variant removed the unlink and the sabotage half went red at 'an error was expected but none was thrown'. Both count assertions were inverted in the same run and printed (answered -> 3) == -1 and (identities.count -> 3) == -1. docs/test-campaign/evidence/PRO-0077/arming-run.txt",
    "evidence": [
      "Tests/ProctorAgentTests/EffectWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0077/witness-run.txt",
      "docs/test-campaign/evidence/PRO-0077/arming-run.txt",
      "docs/test-campaign/evidence/PRO-0077/gate.txt"
    ],
    "witness": {
      "recorder": "the server's own side of the socket: three distinct SocketClient connections it answered, plus the RunSessionIdentity it read off each accepted descriptor via getsockopt(SOL_LOCAL, LOCAL_PEERPID) and proc_pidinfo, recorded inside the server process from the task-local the accept path binds",
      "effect": "ipc",
      "count": 3
    },
    "note": "A real Server on a temporary path — Darwin.bind, listen, chmod 0600 and the accept loop in Server.swift — with three SocketClient instances from ProctorCore/Transport.swift each calling connect() (Darwin.connect) and send(). THE COUNT IS ANSWERED CONNECTIONS, NOT RECORDED REQUESTS: Server.serve reads many frames from one accepted descriptor, so a per-request count would report three for one client sending three frames. A reply cannot arrive on a descriptor the accept loop never took, so an answered connection is an accept. The identity half is the completion confirmation: the peer pid and working directory are the kernel's answer about the far end, obtainable only from an accepted fd, and the client never puts either on the wire. Settled by an out-of-family referral to grok-4.6 at xhigh on 2026-08-21, which also ruled out adding an onAccept seam to Server — a hook the product fires is the product logging its own accept, and no production source needed changing. PORTABLE FLOOR: no kernel socket tracer."
  },
  {
    "id": "CASE-0063",
    "req": "REQ-045",
    "surface": "SURF-022",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/PRO-0079/blind-findings-before.txt",
      "evidence/PRO-0079/classification.tsv",
      "evidence/PRO-0079/rate.txt",
      "evidence/PRO-0079/arming-control.txt",
      "evidence/PRO-0079/reader-sensitivity.txt",
      "evidence/PRO-0079/out-of-family-review-grok.md",
      "evidence/PRO-0079/blind-findings-after.txt",
      "evidence/PRO-0079/gate-test-sh.txt"
    ],
    "armed": true,
    "armedBy": "The pass was armed rather than trusted: on a /tmp copy of Tests/, one read-back line was deleted from AuditRotationTests.clearingEmptyIsANoOp, a test then in the re-read set. The count went 78 to 79 and the finding named that test. A zero-genuine result from a pass that could not report a genuine finding would have measured nothing.",
    "note": "Sampled false-positive rate for the blind-mutation pass. 57 of 78 findings read (73.1%): five per bucket from act/unlock/release/set/claim at seed 20260821, plus a CENSUS of the 32-finding tail. 57 of 57 false positive, 0 genuine. The tail is exact at 0 of 32; in the large strata 0 genuine in 25 drawn from 46 refutes K>=4 at 95% (P=0.037) and does not refute K=3 (P=0.088), so the bound is at most 3 genuine in 78 -- a false-positive rate of at least 96.2%. Six shapes: false-mutator 17, vocabulary 15, return-value 10, teardown 9, not-a-test 4, body-bleed 2. Only vocabulary is reachable by the reader list, so 42 of 57 cannot be fixed by any vocabulary at all. No test was edited and no production source touched, because the sample produced no genuine finding. blindVocabulary gained .calls and .recorded on the evidence of two sampled findings (78 -> 76); every other candidate was refused with its measured file count and effect. PARTIAL by denominator: 21 of the 46 large-stratum findings were never read and the bound stands in for them. Suite gate: ./scripts/test.sh green at 1,814 tests in 214 suites, unchanged from the wave's recorded count, as it must be for an item that touches no Swift. The first attempt stalled at 1,758 of 1,785 started tests, blocked in mach_msg2_trap with TCC and ScreenCaptureKit loaded behind a concurrent run in a sibling worktree; test.sh refused to read the absent verdict as a pass and both attempts are in the evidence.",
    "witness": {
      "recorder": "listen-only tail-append CGEventTap in the witness_probe process, counting probe-marked posts that survived Proctor's head-inserted block",
      "effect": "device",
      "count": 24
    }
  },
  {
    "req": "REQ-004",
    "surface": "SURF-006",
    "oracle": "effect-witness",
    "lane": "macos-glass",
    "status": "pass",
    "note": "PRO-0078 A1. ScreenCaptureKit delivered a frame off the real display server for a window belonging to another process, and what the frame depicts is proved by two readings taken by a probe process that is neither the agent nor the target. The target is Calculator, pid 14545, CG window 123357 (evidence/witness/a1b-attach.json binds handle win:6:0 to that window id). evidence/witness/a1b-capture.json reports status \"complete\", trustworthy true, framesWaited 2, contentRect 674x408 and an unnormalised 1348x816, which is 674x408 at scale 2. First independent reading: evidence/witness/a1b-windows-helper.json is CGWindowListCopyWindowInfo read by probe pid 28892, and it puts window 123357 at 674x408, owner Calculator, ownerPID 14545. Second, and the one that makes the picture's subject a fact rather than an inference: evidence/witness/a1b-axtext-helper.json is an independent AX client in the probe walking pid 14545 and reading 228 strings over 1124 attribute reads, among them \"Last Expression\" carrying 1,861.20-690 and an edit field carrying 1,171.2. Those are exactly the two numbers painted in evidence/witness/a1b-agent-capture.png. A third process read the text; the frame shows the text. The count recorded is the 1 complete frame that arrived. REPLACES AN EARLIER READING, and the earlier one is kept because it is the negative control: evidence/witness/a1-capture.json captured a Proctor-owned window and reported status \"complete\", trustworthy true and dirtyArea 0 for a frame whose 2,942,720 pixels are every one of them RGBA(0,0,0,0). evidence/witness/a1-agent-capture.png is that empty frame. Proctor excludes its own windows from its own captures, so an empty frame there is the exclusion working, but the status and trustworthy fields do not say so. FLAGGED for the orchestrator, not fixed here. Sabotage: evidence/witness/a1-sabotage-capture.json, a bogus handle, returns windowNotFound.",
    "id": "CASE-0064",
    "evidence": [
      "docs/test-campaign/evidence/witness/a1b-capture.json",
      "docs/test-campaign/evidence/witness/a1b-agent-capture.png",
      "docs/test-campaign/evidence/witness/a1b-attach.json",
      "docs/test-campaign/evidence/witness/a1b-windows-helper.json",
      "docs/test-campaign/evidence/witness/a1b-axtext-helper.json",
      "docs/test-campaign/evidence/witness/a1b-axtree-helper.json",
      "negative control docs/test-campaign/evidence/witness/a1-capture.json",
      "negative control docs/test-campaign/evidence/witness/a1-agent-capture.png",
      "sabotage docs/test-campaign/evidence/witness/a1-sabotage-capture.json"
    ],
    "armed": true,
    "capture": {
      "method": "ScreenCaptureKit window-scoped, signed Developer ID agent pid 97657, target Calculator pid 14545 CG window 123357",
      "frameStatus": "SCFrameStatus=complete, framesWaited 2"
    },
    "witness": {
      "recorder": "CGWindowListCopyWindowInfo and an independent AX client, both in the witness_probe process, reading the target window's owner, geometry and displayed text; ScreenCaptureKit's SCFrameStatus names the frame",
      "effect": "device",
      "count": 1
    }
  },
  {
    "req": "REQ-006",
    "surface": "SURF-004",
    "oracle": "effect-witness",
    "lane": "macos-glass",
    "status": "pass",
    "note": "PRO-0078 A2. The run HUD exists as far as the window server is concerned, and the recorder is the window server's own list read from a separate probe process rather than anything Proctor reports about itself. Agent pid 86732. Before the run, evidence/witness/a2-hud-1.json lists one window for that pid. During a synthetic run, evidence/witness/a2-hud-11.json and a2-hud-12.json list three: window 121489 at layer 25, 352x230, sharingState 0, which is the run HUD panel; window 121490 at layer 24, 1710x1073, sharingState 0, which is the takeover statement; and window 121491 at layer 0. The count is the 2 overlay entries that appear with the run and are absent without it. FLAGGED, not fixed: window 121491 is present at layer 0 with sharingState 1 whether or not a run is in flight, and CASE-0032 records that all three overlays report sharingState 0 at the default. That is an observation for the orchestrator rather than something this item changed.",
    "id": "CASE-0065",
    "evidence": [
      "docs/test-campaign/evidence/witness/a2-hud-11.json",
      "docs/test-campaign/evidence/witness/a2-hud-12.json",
      "sabotage docs/test-campaign/evidence/witness/a2-hud-1.json"
    ],
    "armed": true,
    "witness": {
      "recorder": "CGWindowListCopyWindowInfo, read by the witness_probe process on the agent's pid",
      "effect": "ipc",
      "count": 2
    }
  },
  {
    "req": "REQ-003",
    "surface": "SURF-005",
    "oracle": "effect-witness",
    "lane": "macos-glass",
    "status": "pass",
    "note": "PRO-0078 A3. CGEventPost from the agent entered the session event stream and was seen by something that is not the poster. The recorder is a listen-only CGEventTap held by a separate probe process (evidence/witness/a3-observe.json, tapCreated true). Over 90 seconds it recorded 576 events, of which 6 carry sourcePid 86732, the agent's pid, and userData 5787775621720396288, which is ProctorEventTag.value (0x50524F43544F5200) as Actuation stamps it. The count is those 6, one down and one up for each of three synthetic clicks; evidence/witness/a3-act.json reports the same three steps at plane syntheticEvent, route eventStream. The other 570 events carry pid 99665, macOS's own ScreensharingAgent, which was posting into this session throughout and is why the tap discriminates by pid and tag rather than counting arrivals.",
    "id": "CASE-0066",
    "evidence": [
      "docs/test-campaign/evidence/witness/a3-observe.json",
      "docs/test-campaign/evidence/witness/a3-act.json",
      "sabotage docs/test-campaign/evidence/witness/a3-sabotage-observe.json",
      "sabotage docs/test-campaign/evidence/witness/a3-sabotage-act.json"
    ],
    "armed": true,
    "witness": {
      "recorder": "listen-only CGEventTap (.cgSessionEventTap, .tailAppendEventTap) in the witness_probe process, matching sourcePid and ProctorEventTag",
      "effect": "device",
      "count": 6
    }
  },
  {
    "req": "REQ-007",
    "surface": "SURF-005",
    "oracle": "effect-witness",
    "lane": "macos-glass",
    "status": "inconclusive: PersonInput.isAPerson requires sourcePid == 0, which only hardware carries and no second process can forge, so the human-input path REQ-007 names cannot be driven by any instrument available on this lane. The instrument that would settle it is a physical HID event during a held run, or a signed virtual HID driver posting at pid 0. The reachable half was measured and did not yield: 40 events swallowed by the takeover tap across three runs produced no yield record and no held reason.",
    "note": "PRO-0078 A4. NOT WITNESSED, and the ceiling is structural rather than an instrument that could be improved. REQ-007 names human mouse and keyboard interaction, and the predicate that decides it is PersonInput.isAPerson in Sources/ProctorCore/Contention.swift, which returns true only for sourcePid == 0. Zero is what hardware carries and no second process can forge it, so the path REQ-007 names cannot be driven from anything but a hand on a real keyboard: any helper's CGEventPost arrives carrying the helper's own pid and is correctly refused as a process rather than a person. That is deliberate product design, not a gap. The second path was measured and did not yield. The input block's tap calls onPersonInput for every event it swallows, and SessionTakeover.swift wires that to ContentionMonitor.noteUserInput, which is the field ContentionWatch reads. Across three runs the tap swallowed 6, 24 and 10 helper-posted events (evidence/witness/a45b-act.json, a4-act.json, a4c-burst.json) and no run recorded a yield: yields is null in every act result, and six consecutive proctor_doctor reads of the live queue during the third run report the active run with held reason null. For contrast, a yield on a different reason does fire and was captured: evidence/witness/a4-stranded-run-queue.json shows a run held with reason frontmostChanged for 552 to 932 seconds. So the userInput path produced no yield under 40 swallowed events, which is an observation for the orchestrator rather than something this item changed.\n\nSABOTAGE, two-way, built in the gap-fix session and previously missing — §A4 named one and none was recorded. It turns on one field. Agent pid 38933, started with PROCTOR_TAKEOVER_INPUT=1 and PROCTOR_YIELD=1 (doctor: inputBlockAvailable true), held a three-step drag run over win:1:0 while a separate probe process posted ten identical zero-delta scroll events into the session. TAGGED arm, .eventSourceUserData set to ProctorEventTag.value so InputBlock.isOurs is true: evidence/witness/a4-sabotage-tagged-poll.json, probe pid 42675, posted 10, survivedFromThisProbe 10 — every one reached the probe's tail-appended tap. The block was armed while they did, and the proof is in the same tail-tap record: a leftMouseUp from pid 38933, the agent itself, at 0.994s in the middle of the probe's scrolls, with evidence/witness/a4-sabotage-tagged-act.json reporting blocked true and blockedMs 18779. UNTAGGED arm, the probe's own mark instead so isOurs is false: evidence/witness/a4-sabotage-untagged-poll.json, probe pid 49751, posted 10, survivedFromThisProbe 2 — and the two survivors are the first two, at 0.073s and 0.238s, after which nothing of the probe's is seen again; evidence/witness/a4-sabotage-untagged-act.json reports blocked true, blockedMs 18764, swallowed 16. Same event, one field different: ten through, and eight of eight destroyed once the block was up. The block is reading the event rather than a clock.\n\nINSTRUMENT FAULT FOUND AND FIXED while building that, and it is why the number quoted is survivedFromThisProbe rather than survivedToTailTap. survivedToTailTap counts only events carrying the probe's own mark, which the tagged arm by construction does not carry, so it reads 0 there however many events sail through — read off that field this sabotage would have reported the exact opposite of what happened. witness_probe now also counts survivors by source pid, a field both arms carry. evidence/witness/lane-attach-gapfix.txt records it.\n\nOBSERVED AND NOT CLAIMED. Both arms recorded yields with reason userInput — tagged arm heldMs 8205 and 8109 at steps 1 and 2, untagged arm heldMs 1973 and 6672 — where the three runs above recorded none. REQ-007 is NOT thereby witnessed and the ceiling above is unchanged, because the driver is unattributable: userInput fired on the tagged arm too, where not one of the probe's events was swallowed, so nothing this instrument posted can account for it. PersonInput.isAPerson still demands sourcePid == 0, so what tripped it was hardware input this session neither produced nor can bound, on a machine carrying 22 login sessions at a load average above 400. An uncontrolled event is not a measurement. FLAGGED for the orchestrator.",
    "id": "CASE-0067",
    "evidence": [
      "docs/test-campaign/evidence/witness/a4-act.json",
      "docs/test-campaign/evidence/witness/a4-poll.json",
      "docs/test-campaign/evidence/witness/a4c-burst.json",
      "docs/test-campaign/evidence/witness/a45b-act.json",
      "contrast docs/test-campaign/evidence/witness/a4-stranded-run-queue.json",
      "sabotage docs/test-campaign/evidence/witness/a4-sabotage-tagged-poll.json",
      "sabotage docs/test-campaign/evidence/witness/a4-sabotage-tagged-act.json",
      "sabotage control docs/test-campaign/evidence/witness/a4-sabotage-untagged-poll.json",
      "sabotage control docs/test-campaign/evidence/witness/a4-sabotage-untagged-act.json"
    ],
    "armed": false,
    "witness": {
      "recorder": "listen-only CGEventTap in the witness_probe process, plus six proctor_doctor reads of the live run queue",
      "effect": "device",
      "count": 0
    }
  },
  {
    "req": "REQ-008",
    "surface": "SURF-005",
    "oracle": "effect-witness",
    "lane": "macos-glass",
    "status": "pass",
    "note": "PRO-0078 A5. The input blocker's CGEventTap destroyed events posted by a different process, and a third party watched them fail to arrive. Proctor's block head-inserts a .defaultTap; the probe tail-appends a listen-only tap, so an event the block swallows never reaches the probe, and that asymmetry is the measurement. Agent pid 86732: evidence/witness/a45b-act.json reports takeover blocked true, blockedMs 18015, swallowed 6, and evidence/witness/a45-poll2.json records the probe posting 6 marked events while the run held the foreground with survivedToTailTap 0. Two independent counts, both 6. The count recorded here is 24, from the replicate at evidence/witness/a4-act.json (blockedMs 22383, swallowed 24) whose probe half at evidence/witness/a4-poll.json posted 40 while the run held the front and saw 20 survive, the remaining 4 swallows being the ScreensharingAgent's own events. Third replicate: a4c-burst.json, 10 posted, 0 survived.",
    "id": "CASE-0068",
    "evidence": [
      "docs/test-campaign/evidence/witness/a4-act.json",
      "docs/test-campaign/evidence/witness/a4-poll.json",
      "replicate docs/test-campaign/evidence/witness/a45b-act.json",
      "replicate docs/test-campaign/evidence/witness/a45-poll2.json",
      "replicate docs/test-campaign/evidence/witness/a4c-burst.json",
      "unarmed control docs/test-campaign/evidence/witness/selftest-post.json"
    ],
    "armed": true,
    "witness": {
      "recorder": "listen-only tail-append CGEventTap in the witness_probe process, counting probe-marked posts that survived Proctor's head-inserted block",
      "effect": "device",
      "count": 24
    }
  },
  {
    "req": "REQ-002",
    "surface": "SURF-003",
    "oracle": "effect-witness",
    "lane": "macos-glass",
    "status": "pass",
    "note": "PRO-0078 A6. An accessibility action performed by the agent landed in another process, and the window server recorded the result. Agent pid 36605 acted on window 121564 of pid 21088, a Proctor UI process bound to the same private socket and a different process from the agent. evidence/witness/a6-act.json reports completed 1, plane accessibility, route valueWrite. The recorder is CGWindowListCopyWindowInfo read by a separate probe process either side of the action: evidence/witness/a6-windows-before.json puts window 121564 at x 438 y 30, and a6-windows-after.json puts it at x 500 y 90, which is the point the step asked for. The count is the 1 window the window server reports moved. No focus was stolen: frontmostName reads Ghostty before and EgressMac after, and neither is the target, so the action reached a window that was never brought to the front.\n\nSABOTAGE, built in the gap-fix session and previously missing — §A6 named one and none was recorded. The same instrument was pointed at a target that was then taken away. Control arm: agent pid 68474 acted on Calculator, pid 14545, CG window 123357, attached as win:1:0. evidence/witness/a6-sabotage-control-act.json reports completed 1, plane accessibility, route valueWrite, and the separate probe process moved the window server's own record with it, from x 624 y 339 (evidence/witness/a6-sabotage-windows-before.json, probe pid 24573) to x 700 y 400 (evidence/witness/a6-sabotage-windows-control-after.json), which is the point the step asked for; frontmost reads Ledger either side, so the front was not taken. Negative arm: Calculator was quit, and the same probe over the same needle returns rows 0 (evidence/witness/a6-sabotage-windows-after.json). That is the count this case rests on, taken to zero by the reader that produced the 1. The identical step against the identical handle is then refused rather than silently doing nothing: evidence/witness/a6-sabotage-act.json reports completed 0, failedAt 0, the step ok false, and an error carrying code actionFailed, message \"move: cannotComplete\", indeterminate false — a definite refusal with a reason, not a shrug.",
    "id": "CASE-0069",
    "evidence": [
      "docs/test-campaign/evidence/witness/a6-windows-before.json",
      "docs/test-campaign/evidence/witness/a6-windows-after.json",
      "docs/test-campaign/evidence/witness/a6-act.json",
      "sabotage docs/test-campaign/evidence/witness/a6-sabotage-act.json",
      "sabotage docs/test-campaign/evidence/witness/a6-sabotage-windows-after.json",
      "sabotage control docs/test-campaign/evidence/witness/a6-sabotage-control-act.json",
      "sabotage control docs/test-campaign/evidence/witness/a6-sabotage-windows-before.json",
      "sabotage control docs/test-campaign/evidence/witness/a6-sabotage-windows-control-after.json"
    ],
    "armed": true,
    "witness": {
      "recorder": "CGWindowListCopyWindowInfo, read by the witness_probe process either side of the AX action",
      "effect": "ipc",
      "count": 1
    }
  },
  {
    "req": "REQ-012",
    "surface": "SURF-003",
    "oracle": "effect-witness",
    "lane": "macos-glass",
    "status": "pass",
    "note": "PRO-0078 A7. AX notifications crossed a process boundary, and the recorder is a second, independent AXObserver rather than Proctor's own counter. The probe process created its own AXObserver on pid 21088 and registered 9 notification names, all 9 accepted with no refusals; over 45 seconds it received 2 AXValueChanged notifications from that process (evidence/witness/a7-axnotify-helper.json, ok true, count 2). The count is those 2. This deliberately does not stand on SettleReport.axNotificationsSeen, which is the settler's own field and was the reading the out-of-family review called self-reporting.",
    "id": "CASE-0070",
    "evidence": [
      "docs/test-campaign/evidence/witness/a7-axnotify-helper.json",
      "sabotage docs/test-campaign/evidence/witness/a7-sabotage-axnotify.json"
    ],
    "armed": true,
    "witness": {
      "recorder": "an independent AXObserver created by the witness_probe process on the target pid, 9 notification names registered",
      "effect": "ipc",
      "count": 2
    }
  },
  {
    "req": "REQ-014",
    "surface": "SURF-011",
    "oracle": "effect-witness",
    "lane": "macos-glass",
    "status": "pass",
    "note": "PRO-0078 A8. The tri-observer read crossed a real process boundary, and the count comes from a helper rather than from Proctor's own walk, which is the change the out-of-family review asked for by name. The probe process walked pid 21088's accessibility tree with its own AX client and resolved 825 attribute reads over 275 nodes with no errors, reading back AXApplication:Proctor, AXWindow:History and their descendants (evidence/witness/a8-axtree-helper.json). The count is those 825 reads, every one of them answered by a process that is neither the probe nor the agent. The agent's own tri-observer comparison over the same window is beside it rather than under it: evidence/witness/a8-assert.json reports kind agree with 84 findings and 3 defects, and evidence/witness/a8-snapshot.json is the tree it compared. LANE CEILING: the kernel bar for an ipc witness is a traced mach message, and SIP is on with no root here, so dtrace on the accessibility transport is unavailable. What stands is the portable floor from references/effect-boundary.md, and it is recorded as that rather than as the kernel bar. SCOPE, narrowed after the out-of-family review named this the weakest surviving claim: what is witnessed here is the accessibility plane crossing a real process boundary, at 825 reads over 275 nodes answered by a third process. Agreement between the three observers is not independently witnessed, because the geometry and pixel legs of that comparison are reads the agent performs. The witnessed claim is the boundary crossing; the agreement is recorded beside it at its own rung.",
    "id": "CASE-0071",
    "evidence": [
      "docs/test-campaign/evidence/witness/a8-axtree-helper.json",
      "docs/test-campaign/evidence/witness/a8-assert.json",
      "docs/test-campaign/evidence/witness/a8-snapshot.json",
      "sabotage docs/test-campaign/evidence/witness/a8-sabotage-axtree.json"
    ],
    "armed": true,
    "witness": {
      "recorder": "an independent AX client in the witness_probe process walking the target pid's tree with AXUIElementCopyAttributeValue",
      "effect": "ipc",
      "count": 825
    }
  },
  {
    "id": "CASE-0080",
    "req": "REQ-035",
    "surface": "SURF-018",
    "oracle": "effect-witness",
    "status": "pass",
    "armed": true,
    "armedBy": "Same test body, inverted in the arming run: the non-zero assertion printed \"the recorder saw 3 records, attributions [Optional(\\\"cli\\\"), Optional(\\\"mcp\\\"), nil]\", the forgery assertion was flipped to demand [\"cli\"] and reported the row actually reads [nil], and the sabotage was pointed at the LIVE socket, where it went red with 1 record carrying via=cli and the CLI exiting 0 instead of 3. docs/test-campaign/evidence/PRO-0083/arming-w1-req035.txt RE-ARMED AFTER A REWRITE: this suite was restructured so no test of it blocks a cooperative thread (DEF-043), which changed every body. Every count above was re-established on the code as it now stands — docs/test-campaign/evidence/PRO-0083/arming-counts-after-refactor.txt — and the sabotages were removed again and watched to fail — docs/test-campaign/evidence/PRO-0083/arming-sabotage-after-refactor.txt. That second pass caught an arming that could not fire: W3's dead watch was first pointed at a socket whose server had already been stopped, so it would have read zero whatever happened, and it was re-armed against the live socket.\n\nTHE FORGING ARM WAS REBUILT ON THE RESUME AND RE-ARMED. It was a hard-linked copy of proctor-cli launched at a fresh path; a fresh path is a first launch for syspolicyd, and beside the wiring suites' fifteen concurrent SecStaticCodeCheckValidity calls it overran a 120-second bound in the full run, deterministically, twice, while taking 0.1s with this test alone (DEF-044). It is now a raw AF_UNIX peer that is THIS TEST PROCESS - proc_pidpath reports proctor-mcpPackageTests, which frontEnd(named:) maps to nil - so no binary is launched. The ask also became real: --via is not a flag proctor-cli parses, so the old arm never put the claim on the wire, where the frame now carries \"via\":\"cli\" at the top level of the very object the server decodes. Re-armed by inverting three assertions in the same body: the peer WAS answered (ForgedCall(answered: true, ok: true)) and its row reads [nil] from those wire bytes, 1 row. docs/test-campaign/evidence/PRO-0083/arming-forger-rawpeer.txt\n\nRE-MEASURED FOR REPRODUCIBILITY, not re-armed. A verifier saw the forging peer report ForgedCall(answered: false, ok: false) on 3 of 5 verdict-returning runs, so the clause went unexercised there; the cause was DEF-043's pool starvation seen from the client side, closed by PRO-0087 and merged here. On that build the arm answered on 30 of 30 consecutive full-suite runs, 0 stalls, at load average min 245 / median 396 / max 567. The 10s SO_RCVTIMEO was NOT raised: the margin was measured instead, by asserting readSeconds > 3600 inside three more full-suite runs, and the read returned in 0.030s / 0.038s / 0.181s, the worst 1.8% of the bound at load 561. docs/test-campaign/evidence/PRO-0083/remeasure-forged-peer.txt",
    "evidence": [
      "Tests/ProctorAgentTests/ExternalWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0083/arming-w1-req035.txt",
      "docs/test-campaign/evidence/PRO-0083/suite-before-after.txt",
      "docs/test-campaign/evidence/PRO-0083/gate.txt",
      "docs/test-campaign/evidence/PRO-0083/arming-counts-after-refactor.txt",
      "docs/test-campaign/evidence/PRO-0083/arming-sabotage-after-refactor.txt",
      "docs/test-campaign/evidence/PRO-0083/arming-forger-rawpeer.txt",
      "docs/test-campaign/evidence/PRO-0083/remeasure-forged-peer.txt"
    ],
    "witness": {
      "recorder": "the audit trail's bytes on disk, read with a fresh FileHandle and opened with AuditSeal and the injected private half - never AuditLog.readTrail, which is the reader under test confirming itself",
      "effect": "ipc",
      "count": 3
    },
    "note": "THE SECURITY CLAUSE, AND IT WAS DRIVEN FROM TWO GENUINELY DIFFERENT FRONT ENDS. A real Server bound a temporary AF_UNIX socket with the session's sink set to the production AuditLog.append and AuditLog.seams.directory redirected to a temporary trail. Three real child processes were spawned against it with PROCTOR_SOCKET pointing at that socket: .build/debug/proctor-cli running `policy --action approve`, .build/debug/proctor-shim running `serve` and speaking real MCP over its stdio (initialize then tools/call proctor_policy), and a copy of proctor-cli at the path <tmp>/proctor-imposter running the same verb with `--via cli` appended. All three are built by the same `swift build --build-tests` that builds this suite, and the test locates them through Bundle(for:) on an ObjC anchor class - an absent binary fails the test rather than skipping it. WHAT THE TRAIL SAID: three sealed records, via=cli for the CLI child, via=mcp for the shim child, and via ABSENT for the forger that asked to be recorded as the CLI. Every via was produced by SessionIdentity.frontEnd(for:) from proc_pidpath of the pid getsockopt(SOL_LOCAL, LOCAL_PEERPID) reported for the accepted descriptor (Server.swift:126, bound as a task-local at :270, stamped at PolicyStore.swift:232). None of it was on the wire. The count is the 3 records read back off disk. SABOTAGE: unlink the socket between start() and the children - the CLI exits 3 (CLISurface.Exit.agentUnreachable) and the trail holds nothing. NO onAccept SEAM WAS ADDED; the grok lane settled that for CASE-0062 and the reason still holds."
  },
  {
    "id": "CASE-0081",
    "req": "REQ-034",
    "surface": "SURF-018",
    "oracle": "effect-witness",
    "status": "pass",
    "armed": true,
    "armedBy": "Arming run printed \"the server answered 3 requests from real CLI children\" and \"4 distinct exit codes: agentUnreachable 3, ok 0, okPolicy 0, notReady 1, reflector 1, refused 1, usage 2, verdictFailed 2\". The sabotage was pointed at the LIVE socket and went red, reporting exit 0 where it demands 3. docs/test-campaign/evidence/PRO-0083/arming-w2-w5.txt RE-ARMED AFTER A REWRITE: this suite was restructured so no test of it blocks a cooperative thread (DEF-043), which changed every body. Every count above was re-established on the code as it now stands — docs/test-campaign/evidence/PRO-0083/arming-counts-after-refactor.txt — and the sabotages were removed again and watched to fail — docs/test-campaign/evidence/PRO-0083/arming-sabotage-after-refactor.txt. That second pass caught an arming that could not fire: W3's dead watch was first pointed at a socket whose server had already been stopped, so it would have read zero whatever happened, and it was re-armed against the live socket.",
    "evidence": [
      "Tests/ProctorAgentTests/ExternalWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0083/arming-w2-w5.txt",
      "docs/test-campaign/evidence/PRO-0083/suite-before-after.txt",
      "docs/test-campaign/evidence/PRO-0083/gate.txt",
      "docs/test-campaign/evidence/PRO-0083/arming-counts-after-refactor.txt",
      "docs/test-campaign/evidence/PRO-0083/arming-sabotage-after-refactor.txt"
    ],
    "witness": {
      "recorder": "the exit status the kernel reports through waitpid on each real proctor-cli child, plus the server's own record of the peer it read off each accepted descriptor",
      "effect": "ipc",
      "count": 3
    },
    "note": "Real proctor-cli children against a real Server on a private socket. The count is the 3 doctor requests the server answered, each recording a kernel-read peer whose frontEnd resolved to \"cli\" and whose key names a CHILD process rather than this one. Four distinct exit codes were produced by real children and read through waitpid: 0 (ok), 1 (verdictFailed), 2 (usage) and 3 (agentUnreachable). The generated completion script was produced by a real child (`completion --shell zsh`) and contains all 26 of CLISurface.allVerbNames - 21 verbs derived from ToolCatalogue plus 5 service verbs. SCOPE, STATED RATHER THAN ROUNDED UP: exit 4 (refused) and exit 5 (notReady) were NOT produced from a child in this harness. Every gate that yields them sits behind a resolved window handle, and a handle is minted per session by an attach the child would have to perform first, so all three gated arms failed at handle resolution and mapped to verdictFailed. The six-code surface is carried by CLISurface.Exit.allCases and the pure exit(for:) mapping, which is a different rung from this one. SABOTAGE: with no socket bound the same verb exits 3 and the answered count is zero. WHAT THIS LANE CANNOT REACH, AND WHY IT IS SAID RATHER THAN DROPPED: `refused` (4) is not among the codes any child produced. Driving it needs `policy --action configure`, and `PolicyStore` has no test seam — `configurePolicy` calls `PolicyStore.save`, which writes the OPERATOR'S ~/Library/Application Support/app.fledgeling.procter/policy/policy.json. This witness did exactly that once, and PRO-0077's REQ-015 witness then failed with `policyDenied` in a later run where this suite was skipped, because the block had outlived the process that wrote it. The leaked entry was removed and the configure was dropped. DEF-042."
  },
  {
    "id": "CASE-0082",
    "req": "REQ-029",
    "surface": "SURF-017",
    "oracle": "effect-witness",
    "status": "pass",
    "armed": true,
    "armedBy": "Arming run printed \"3 frames published, 3 crossed the socket\" and \"the latch reads stopped=true after a stop over the socket\". The sabotage was pointed at the LIVE socket and went red at 2 frames crossed, so the zero in the passing run is a measurement rather than a structural silence. docs/test-campaign/evidence/PRO-0083/arming-w2-w5.txt RE-ARMED AFTER A REWRITE: this suite was restructured so no test of it blocks a cooperative thread (DEF-043), which changed every body. Every count above was re-established on the code as it now stands — docs/test-campaign/evidence/PRO-0083/arming-counts-after-refactor.txt — and the sabotages were removed again and watched to fail — docs/test-campaign/evidence/PRO-0083/arming-sabotage-after-refactor.txt. That second pass caught an arming that could not fire: W3's dead watch was first pointed at a socket whose server had already been stopped, so it would have read zero whatever happened, and it was re-armed against the live socket.",
    "evidence": [
      "Tests/ProctorAgentTests/ExternalWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0083/arming-w2-w5.txt",
      "docs/test-campaign/evidence/PRO-0083/suite-before-after.txt",
      "docs/test-campaign/evidence/PRO-0083/gate.txt",
      "docs/test-campaign/evidence/PRO-0083/arming-counts-after-refactor.txt",
      "docs/test-campaign/evidence/PRO-0083/arming-sabotage-after-refactor.txt"
    ],
    "witness": {
      "recorder": "the frames that arrived on a held SocketClient.watch connection, counted client-side on its own Thread, and RunControl.shared read afterwards - which is neither the client nor the transport",
      "effect": "ipc",
      "count": 3
    },
    "note": "The surface that reaches furthest - across SSH onto a Mac with no window server - and can do least. A real Server, a real watch held open on a dedicated Thread (SocketClient.watch blocks in read(), so a cooperative thread would wedge the pool), and frames pushed through the product's own SupervisionBroadcast.shared. 3 published, 3 crossed. The halt went over a SECOND real connection as proctor.control {action: stop}, and the recorder for it is RunControl.shared - the same object the HUD panel writes and the run loop reads, which is A4's one-latch claim. THE LATCH IS PROCESS-WIDE, so it is read before, asserted un-stopped, and restored with the product's own begin(run:) reset, with the restoration asserted; PRO-0053 recorded a suite reaching another suite through exactly this object. SABOTAGE: with the server stopped the watch cannot connect, zero frames arrive, the watch reports a reason rather than an absence, and the latch does not move."
  },
  {
    "id": "CASE-0083",
    "req": "REQ-033",
    "surface": "SURF-017",
    "oracle": "effect-witness",
    "status": "pass",
    "armed": true,
    "armedBy": "Arming run printed \"read [4, 5, 8, 3] across grants, lanes, switches, history\" with the assertion inverted to demand every section be negative. docs/test-campaign/evidence/PRO-0083/arming-w2-w5.txt RE-ARMED AFTER A REWRITE: this suite was restructured so no test of it blocks a cooperative thread (DEF-043), which changed every body. Every count above was re-established on the code as it now stands — docs/test-campaign/evidence/PRO-0083/arming-counts-after-refactor.txt — and the sabotages were removed again and watched to fail — docs/test-campaign/evidence/PRO-0083/arming-sabotage-after-refactor.txt. That second pass caught an arming that could not fire: W3's dead watch was first pointed at a socket whose server had already been stopped, so it would have read zero whatever happened, and it was re-armed against the live socket.",
    "evidence": [
      "Tests/ProctorAgentTests/ExternalWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0083/arming-w2-w5.txt",
      "docs/test-campaign/evidence/PRO-0083/suite-before-after.txt",
      "docs/test-campaign/evidence/PRO-0083/gate.txt",
      "docs/test-campaign/evidence/PRO-0083/arming-counts-after-refactor.txt",
      "docs/test-campaign/evidence/PRO-0083/arming-sabotage-after-refactor.txt"
    ],
    "witness": {
      "recorder": "the reply bytes read off a real socket by a real SocketClient, projected through TUISurface.readiness/switches/history - the same three projections the TUI draws",
      "effect": "ipc",
      "count": 20
    },
    "note": "Its own case rather than a slice of CASE-0082: the guarantee is about WHAT a supervision client can read, and folding it into the watch would let an empty readiness projection hide behind a non-zero frame count. The TUI's own two calls in the TUI's own order - proctor_doctor then proctor_history - over one real connection to a real Server, with the trail redirected because proctor_history projects the trail and this must never read the operator's own. Three audited calls were driven over the same socket first so the history pane had something to project. MEASURED: 4 grant rows, 5 lane rows, 8 switch rows, 3 history rows; the count is the 20 rows across all four. SABOTAGE: the identical three projections over a refusal reply return zero rows everywhere, so an empty pane is a measurement rather than a default."
  },
  {
    "id": "CASE-0084",
    "req": "REQ-027",
    "surface": "SURF-008",
    "oracle": "effect-witness",
    "status": "pass",
    "armed": true,
    "armedBy": "Arming run printed \"the stalling listener accepted 3 of 3 connections\", and the two-way arm was inverted to demand that a real agent NOT answer inside the same bound, where it went red reporting connected=true answered=true. docs/test-campaign/evidence/PRO-0083/arming-w2-w5.txt RE-ARMED AFTER A REWRITE: this suite was restructured so no test of it blocks a cooperative thread (DEF-043), which changed every body. Every count above was re-established on the code as it now stands — docs/test-campaign/evidence/PRO-0083/arming-counts-after-refactor.txt — and the sabotages were removed again and watched to fail — docs/test-campaign/evidence/PRO-0083/arming-sabotage-after-refactor.txt. That second pass caught an arming that could not fire: W3's dead watch was first pointed at a socket whose server had already been stopped, so it would have read zero whatever happened, and it was re-armed against the live socket.",
    "evidence": [
      "Tests/ProctorAgentTests/ExternalWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0083/arming-w2-w5.txt",
      "docs/test-campaign/evidence/PRO-0083/suite-before-after.txt",
      "docs/test-campaign/evidence/PRO-0083/gate.txt",
      "docs/test-campaign/evidence/PRO-0083/arming-counts-after-refactor.txt",
      "docs/test-campaign/evidence/PRO-0083/arming-sabotage-after-refactor.txt"
    ],
    "witness": {
      "recorder": "the stalling listener's own accepted-descriptor count - forty lines of Darwin bind/listen/accept in the test, so a client blocking on it is blocking on the kernel's socket buffer rather than on anything Proctor decided",
      "effect": "ipc",
      "count": 3
    },
    "note": "SCOPE NARROWED AND THE REASON IS A BRANCH FACT, NOT A PRODUCT ONE. What is witnessed here is the BOUNDARY CONDITION the requirement is about: a listener that binds, accepts and never writes is a state of its own, and connect() SUCCEEDS against it where it fails outright against a path nothing is bound to. Three connections were accepted and deliberately never answered; a real Server answered inside the same 2s SO_RCVTIMEO bound, so the silence is the peer's and not the clock's; and after the listener stopped, connect() failed. WHAT IS NOT WITNESSED, said out loud: the product's own remedy. REQ-027 claims the status window stops claiming Ready when the agent holds the socket and never answers, and the mechanism for that is SocketClient's opt-in ioTimeoutSeconds applied as SO_RCVTIMEO/SO_SNDTIMEO, with AgentModel setting 5s on its doctor, activity and HUD polls. That landed in commit 10285df on `main`. This item's base branch ai/wave-9 is eight commits behind main and does not carry it: `grep -rn ioTimeout Sources/` returns nothing on this tree, AgentModel.callDoctor uses a bare SocketClient, and SocketClient.send blocks in read() with no bound. So the bound in this test is the TEST'S OWN, applied by the caller, and it is named as such rather than passed off as the product's. See DEF-042."
  },
  {
    "id": "CASE-0085",
    "req": "REQ-037",
    "surface": "SURF-019",
    "oracle": "effect-witness",
    "status": "pass",
    "armed": true,
    "armedBy": "Arming run printed \"the guest actuated 3 steps, the host 0\" with the assertion inverted, and the sabotage was run with the guest server left UP, where it went red reporting the attach returned ok=true. docs/test-campaign/evidence/PRO-0083/arming-w2-w5.txt RE-ARMED AFTER A REWRITE: this suite was restructured so no test of it blocks a cooperative thread (DEF-043), which changed every body. Every count above was re-established on the code as it now stands — docs/test-campaign/evidence/PRO-0083/arming-counts-after-refactor.txt — and the sabotages were removed again and watched to fail — docs/test-campaign/evidence/PRO-0083/arming-sabotage-after-refactor.txt. That second pass caught an arming that could not fire: W3's dead watch was first pointed at a socket whose server had already been stopped, so it would have read zero whatever happened, and it was re-armed against the live socket.",
    "evidence": [
      "Tests/ProctorAgentTests/ExternalWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0083/arming-w2-w5.txt",
      "docs/test-campaign/evidence/PRO-0083/suite-before-after.txt",
      "docs/test-campaign/evidence/PRO-0083/gate.txt",
      "docs/test-campaign/evidence/PRO-0083/arming-counts-after-refactor.txt",
      "docs/test-campaign/evidence/PRO-0083/arming-sabotage-after-refactor.txt"
    ],
    "witness": {
      "recorder": "the guest Session's own FakeAX actuator, reachable only over the forwarded socket, set against the host Session's actuator which must stay empty",
      "effect": "ipc",
      "count": 3
    },
    "note": "Two real Servers on two temporary sockets. The second plays the guest's own Proctor on the end of the socket a person forwarded; the host attaches to it through the DEFAULT SocketGuestLink - a real SocketClient on that path, probed - and not through the injected guest-link seam. Everything rides ONE client connection so the peer key the host reads is the same for the attach and for the calls that follow, which is what guestAttachments[key] is keyed on. A forwarded proctor_apps attach came back naming com.fledgeling.inside-the-guest, a bundle identifier that exists only inside the guest's Session; three forwarded proctor_act calls then actuated three steps, and the count is those 3 steps read off the GUEST's actuator. THE SECOND HALF OF THE CLAIM: the host's own actuator performed 0 steps, before and after. SABOTAGE: with the guest server stopped, the attach is refused and the refusal names the socket rather than falling back to the host - which would be the verdict-about-the-wrong-machine this feature exists to prevent. CEILING: both servers are in this process, so what crosses is a real AF_UNIX round trip through the kernel and not a process boundary; the cross-machine claim is CASE-0056's, settled live on proctor-guest."
  },
  {
    "id": "CASE-0086",
    "req": "REQ-039",
    "surface": "SURF-020",
    "oracle": "effect-witness",
    "status": "pass",
    "armed": true,
    "armedBy": "Arming run printed \"arm A left 1 stop children [get, get, ls, ls, ls, run, stop]; arm B left 0 [ls]\" with the non-zero assertion inverted, and the sabotage printed the spawned set with a missing binary. docs/test-campaign/evidence/PRO-0083/arming-w2-w5.txt RE-ARMED AFTER A REWRITE: this suite was restructured so no test of it blocks a cooperative thread (DEF-043), which changed every body. Every count above was re-established on the code as it now stands — docs/test-campaign/evidence/PRO-0083/arming-counts-after-refactor.txt — and the sabotages were removed again and watched to fail — docs/test-campaign/evidence/PRO-0083/arming-sabotage-after-refactor.txt. That second pass caught an arming that could not fire: W3's dead watch was first pointed at a socket whose server had already been stopped, so it would have read zero whatever happened, and it was re-armed against the live socket.",
    "evidence": [
      "Tests/ProctorAgentTests/ExternalWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0083/arming-w2-w5.txt",
      "docs/test-campaign/evidence/PRO-0083/suite-before-after.txt",
      "docs/test-campaign/evidence/PRO-0083/gate.txt",
      "docs/test-campaign/evidence/PRO-0083/arming-counts-after-refactor.txt",
      "docs/test-campaign/evidence/PRO-0083/arming-sabotage-after-refactor.txt"
    ],
    "witness": {
      "recorder": "sentinel files written by each spawned /bin/sh child carrying its own $$ AND the verb it was handed, read back off the filesystem with FileManager - no code under test participates in the reading",
      "effect": "subprocess",
      "count": 7
    },
    "note": "THE NEVER-EVICT RULE, WITNESSED AS A TWO-WAY OVER REAL CHILD PROCESSES. Both arms use LumeProvider's CONVENIENCE initialiser, which binds run to Self.liveRun and reaches a real Process() through Session.runBounded; the three-argument init(executable:timeoutMs:run:) is the fake seam and was deliberately not used. ARM A, a guest THIS AGENT started: the listing reports state \"stopped\", so guestAttach starts it through the audited guestMutate path and startedByThisAgent is true; the detach then stops it. Seven real children were spawned, their verbs read off the sentinels as [get, get, ls, ls, ls, run, stop] - one of them a stop. ARM B, a guest A PERSON started: the listing reports state \"running\", so nothing is started and startedByThisAgent is false; the IDENTICAL detach call spawns one child [ls] and NO stop. Same call, one field different, opposite outcome - releaseGuestAttachment gates on `stopGuest && attachment.startedByThisAgent` (SessionGuest.swift:562). Arm B's non-empty spawn set is asserted too, so its zero stops is a measurement rather than a structural silence - the failure mode PRO-0078's survivor count had. The count is the 7 children arm A spawned. SABOTAGE: point the executable at a path that does not exist; runBounded catches the spawn failure and answers exitCode -1 so the call still returns, and the sentinel count goes to zero."
  },
  {
    "id": "CASE-0087",
    "req": "REQ-024",
    "surface": "SURF-003",
    "oracle": "effect-witness",
    "status": "pass",
    "armed": true,
    "armedBy": "Arming run printed \"the locator chose <tmp>/complete/obscura, missing [], searched 2\" with the assertion inverted to demand no path at all, and the sabotage assertion was inverted and reported available=false at nowhere after chmod. docs/test-campaign/evidence/PRO-0083/arming-w2-w5.txt RE-ARMED AFTER A REWRITE: this suite was restructured so no test of it blocks a cooperative thread (DEF-043), which changed every body. Every count above was re-established on the code as it now stands — docs/test-campaign/evidence/PRO-0083/arming-counts-after-refactor.txt — and the sabotages were removed again and watched to fail — docs/test-campaign/evidence/PRO-0083/arming-sabotage-after-refactor.txt. That second pass caught an arming that could not fire: W3's dead watch was first pointed at a socket whose server had already been stopped, so it would have read zero whatever happened, and it was re-armed against the live socket.",
    "evidence": [
      "Tests/ProctorAgentTests/ExternalWitnessTests.swift",
      "Tests/ProctorAgentTests/CuaSubprocessWitnessTests.swift",
      "scripts/campaign/subprocess_witness.py",
      "docs/test-campaign/evidence/PRO-0083/arming-w2-w5.txt",
      "docs/test-campaign/evidence/PRO-0083/suite-before-after.txt",
      "docs/test-campaign/evidence/PRO-0083/gate.txt",
      "docs/test-campaign/evidence/PRO-0083/arming-counts-after-refactor.txt",
      "docs/test-campaign/evidence/PRO-0083/arming-sabotage-after-refactor.txt"
    ],
    "witness": {
      "recorder": "real executable files placed on disk and Cua subprocess execution recorded via sentinel files with $$ child PIDs and process table observation",
      "effect": "subprocess",
      "count": 3
    },
    "note": "ELEVATED TO PASS (OBSERVED) BY PRO-0115 (DEF-305). While the browser locator path performs filesystem stat checks, the Cua actuation backend on the same subprocess boundary executes real Process() children in CuaClients.swift. CuaSubprocessWitnessTests and subprocess_witness.py witness real child PID spawn, argument verification, exit code capture, and daemon termination interlocks, unblocking BLOCK-0001.",
    "defects": [
      "DEF-305"
    ]
  },
  {
    "id": "CASE-0089",
    "req": "REQ-028",
    "surface": "SURF-004",
    "oracle": "effect-witness",
    "status": "pass",
    "armed": true,
    "armedBy": "A control frame proves the content detector can report blankness: a hand-built 200x200 fully transparent PNG reads distinctColours 1, fullyTransparentFraction 1.0, luminanceStdDev 0.0, against the subject frame's 3159 / 0.00054 / 14.25. docs/test-campaign/evidence/PRO-0083/glass/control-blank-content.json",
    "evidence": [
      "scripts/campaign/overlay_witness.sh",
      "scripts/campaign/frame_content.swift",
      "docs/test-campaign/evidence/PRO-0083/glass/windows-proctor.json",
      "docs/test-campaign/evidence/PRO-0083/glass/overlay-captures.json",
      "docs/test-campaign/evidence/PRO-0083/glass/subject-capture.json",
      "docs/test-campaign/evidence/PRO-0083/glass/subject-content.json",
      "docs/test-campaign/evidence/PRO-0083/glass/subject-axtext.json",
      "docs/test-campaign/evidence/PRO-0083/glass/control-blank-content.json",
      "docs/test-campaign/evidence/PRO-0083/glass/subject.png"
    ],
    "capture": "docs/test-campaign/evidence/PRO-0083/glass/subject.png",
    "captureMethod": "screencapture -l, the system's own utility, run by the witness_probe process - not ScreenCaptureKit through Proctor's stream and not this program compositing anything",
    "witness": {
      "recorder": "four channels, none of which is Proctor's capture path: CGWindowListCopyWindowInfo for what the window server says is on screen and at what sharingState; screencapture -l for the frames; the target's own accessibility server for what the frame depicts; and CGImageSource in a fourth process for the pixels",
      "effect": "device",
      "count": 2
    },
    "note": "BOTH HALVES IN ONE SECOND, WHICH IS THE WHOLE POINT. PRO-0078 found proctor_capture reporting status complete, trustworthy true over 2,942,720 pixels of RGBA(0,0,0,0) of a Proctor-owned window (DEF-025, open) - the exclusion working, and the capture path not noticing that exclusion was all it got. So a blank frame proves nothing here and this witness refuses to let one stand. THE ABSENCE: CGWindowListCopyWindowInfo reported two Proctor Agent windows on screen, CG windows 130709 and 130710, owner pid 76491, layer 1000, alpha 1, sharingState 0. Driven through screencapture -l by a third process, BOTH were refused outright - \"screencapture -l wrote nothing: could not create image from window\" - which is a stronger negative than an empty frame, because the channel could not produce an image at all. THE CONTENT, same utility, same second: CG window 130785, Activity Monitor, pid 28274, sharingState 1, delivered a 2536x1640 frame of 4,159,040 pixels carrying 3,159 distinct colours, 0.054% fully transparent, luminance mean 44.68 and standard deviation 14.25. THE SUBJECT, proved by text rather than by filename: an independent AX client in the probe walked pid 28274 and read 270 strings over 361 nodes, among them \"Activity Monitor - All Processes\" and \"coreaudiod\". A third process read the text; the frame is a picture of that window. The count is the 2 excluded overlays. THE SUBJECT OF THIS LANE IS THE INSTALLED, NOTARISED BUILD - /Applications/Proctor.app's running agent at pid 76491, read only and never driven - and NOT .build/Proctor.app, so the existing macos-glass laneProof is neither reused nor rewritten and this row carries no lane. THAT DISTINCTION MATTERS HERE: the running build carries OverlayCapture and sets sharingType on all three overlays including CursorOverlay, which is why the pointer panels read sharingState 0. This item's base ai/wave-9 does not carry OverlayCapture at all and CursorOverlay sets no sharingType on it, so the same measurement against a build of THIS tree would report the drawn pointer at sharingState 1. See DEF-042. sharingType = .none on the HUD and the takeover overlay is correct and was not touched: evidence must not change because somebody was watching. NOT WITNESSED: the `unless somebody lifts it` clause, whose mechanism is PROCTOR_OVERLAY_CAPTURE in commit 15f86ea on main and absent from this base."
  },
  {
    "id": "CASE-0088",
    "req": "REQ-023",
    "surface": "SURF-015",
    "oracle": "effect-witness",
    "status": "pass",
    "armed": true,
    "armedBy": "Sabotage run in the same test body: ProctorReflector.stop() closes the listening descriptor and unlinks the path, and the same client over the same window then fails connect with errno 2 on all three ops. The armed variant removed the stop() call and the sabotage half went red with four issues — the socket still existed, stillAnswered read 3, no refusal was a connect failure, and isRunning was still true. Every count was inverted in the same run and printed the number the recorder actually saw: answered 3, nodes 3 (NSView, NSTextField, NSTextFieldSimpleLabel), frame (22.0, 272.0, 110.5, 16.0), colour #FFFFFF catalog System name labelColor, constraints 2 (leading, top). docs/test-campaign/evidence/PRO-0083/arming-w9-req023.txt",
    "evidence": [
      "Tests/ProctorAgentTests/ReflectorWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0083/witness-w9-req023.txt",
      "docs/test-campaign/evidence/PRO-0083/arming-w9-req023.txt",
      "docs/test-campaign/evidence/PRO-0083/suite-before-after.txt",
      "docs/test-campaign/evidence/PRO-0083/gate.txt"
    ],
    "witness": {
      "recorder": "the replies read back off the Reflector's own socket by a raw Darwin.socket/connect client and decoded with JSONSerialization — never the target's Frame reader or its JSONValue decoder — carrying values AppKit computed rather than values the test wrote: a constraint-solved frame, a labelColor resolved against the window's effective appearance, and a CoreAnimation layer model. Plus the socket file's own mode, read off the inode.",
      "effect": "ipc",
      "count": 3
    },
    "note": "A SECOND SOCKET, AND THAT IS WHY IT IS ITS OWN CASE. Five of this item's other witnesses stand on the agent's AF_UNIX listener in Sources/ProctorAgent/Server.swift. This one stands on Sources/ProctorReflector/SocketServer.swift — a different Darwin.bind/listen/accept, in a different target, embedded in other people's applications and taking no dependency on the agent. Folding it in would let one listener's silence hide behind the other's noise. DRIVEN FROM THE PRODUCTION ENTRY POINT: ProctorReflector.start(socketPath:) is the one-line adoption API an app calls from applicationDidFinishLaunching. It binds, listens, chmod 0600s and serves on its own concurrent queue. THE CLIENT SHARES NOTHING WITH THE SERVER: a raw Darwin.socket/connect in the test frames the 4-byte big-endian length prefix by hand and decodes the reply with JSONSerialization. Neither the target's Frame encoder nor its JSONValue decoder participates in the reading. One request per connection, so a reply is an accept. COUNT ONE, three answered connections — ping, revision and hierarchy, each on its own descriptor. The ping's pid is read by the server out of its own process and the request never carried it; the socket's mode is read off the inode with FileManager and comes back 0600. COUNT TWO, three resolved nodes: NSView, NSTextField, NSTextFieldSimpleLabel. THE RESOLVED HALF, WHICH IS WHAT REQ-023 ACTUALLY CLAIMS, and none of it is a value the test wrote. The window is built with Auto Layout and no explicit child frame. GEOMETRY: the label came back at (x 22.0, y 272.0, w 110.5, h 16.0). The width is AppKit's measurement of the string in the system font. The x is NOT the constraint's constant of 24 — NSTextField's alignment rect is inset from its frame, so the solver satisfies the leading constraint on the alignment rect and the frame lands to its left, which is the clearest evidence in this case that the number came back from AppKit rather than from the request. y + h = 288 is the relation the solver produced over a height nothing in the test named (content 320 minus a top inset of 32). COLOUR: labelColor resolved against the window's effective appearance to #FFFFFF, catalog System, name labelColor — the test never sets textColor. LAYER: the content view's CALayer model carried backgroundColor, borderWidth, cornerRadius, opacity and shadowRadius, all CoreAnimation's. CONSTRAINTS: two, leading and top, both active, read back off the common ancestor — which is where AppKit installs a constraint between two views, and the reason the case reads the content node rather than the label's. AN INSTRUMENT FAULT CAUGHT BEFORE IT COULD READ ZERO FOR A STRUCTURAL REASON: the first run asked for \"constraints\": true and got no constraints key at all, because Runtime.decode(options:) reads \"includeConstraints\". A constraints assertion on that request would have failed for a wire-protocol reason and been read as a product one. THE CEILING, RECORDED RATHER THAN IMPLIED: server and client are the same process, so what is witnessed is a real AF_UNIX round trip through the kernel and a real AppKit walk, not a cross-process one. The agent ships NullReflectorBridge, so there is no in-repo production client to drive this from a second process, and writing one would be building the subject rather than witnessing it. PORTABLE FLOOR: no kernel socket tracer, no dtrace, no eslogger — this suite has no privilege for one. Package.swift gains ProctorReflector on ProctorAgentTests' dependency list and nothing else: a build-graph edge, not a behaviour change. The Reflector compiles to nothing without DEBUG or PROCTOR_REFLECTOR and scripts/build-app.sh already fails a release artifact carrying it."
  },
  {
    "id": "CASE-0072",
    "req": "REQ-046",
    "surface": "SURF-022",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/census-control.txt"
    ],
    "armed": true,
    "armedBy": "This case IS the arming. --seed-strengthen is the skill's own arming rule turned on its own gate: it strengthens a requirement's declared constraint to one the registry cannot satisfy and requires the census to go red. Both directions read before=clear after=red, exit 0.",
    "note": "PRO-0080 A1/A2. The census control run in both directions, first run ever against this campaign. REQ-017 (effect subprocess, the requirement whose witness PRO-0077 built and merged) and REQ-001 (classed none). Verbatim transcript in the evidence file, not summarised. PRECONDITION CHECKED RATHER THAN ASSUMED: _census_clear is not(unclassed or uncensused), so if the census were already red then after would be red for a reason the mutation did not cause and the tool would still print 'the gate bites'. Read immediately before: unclassed examined=45 findings=0, uncensused examined=22 findings=0, gate exit 0. The before=clear in both outputs is therefore load-bearing and measured. RESTORATION MEASURED, NOT CLAIMED: sha256 of inventory.json is 9215d5be401c61b55d3a85ef697279dc4f99dd389e6ce0226b232a7bdb225885 before the first run and after the last, and git diff is empty."
  },
  {
    "id": "CASE-0073",
    "req": "REQ-046",
    "surface": "SURF-022",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/census-control.txt",
      "scripts/campaign/seed_unclass.py"
    ],
    "armed": true,
    "armedBy": "Both arms of the control were watched. It bites on REQ-017 (unclassed 0 findings to 1, exit 0) and REFUSES on REQ-011 (exit 2), whose text names no effect the vocabulary matches, because scoring that absent finding as a red would pass the control on a tautology. A control that cannot refuse is the thing being guarded against.",
    "note": "PRO-0080 A3, and it exists because CASE-0072 passing did not mean what it looked like it meant. The census has two exact passes at requirement level. --seed-strengthen sets effect to packet-filter and pops provider, which IS the uncensused predicate, so it fires uncensused and only uncensused whatever the requirement's starting class. Decomposed per pass: REQ-017 seeded gives unclassed 0 / uncensused 1; REQ-001 seeded gives unclassed 0 / uncensused 1. So after the shipped control ran green, unclassed was still a pass reporting examined=45 findings=0 that nothing had watched go red, which is the position the control exists to get a gate out of, surviving the control. Recorded as DEF-030. scripts/campaign/seed_unclass.py is the missing direction: it strengthens the specification rather than the census record by removing a requirement's effect field, and requires unclassed to flag it. It is a separate script rather than a patch to vacuity-check.py because that file lives in a plugin cache this repo does not own and a fix there is reverted by the next plugin update with nothing saying so. Registry restored byte-for-byte, verified by sha256 rather than asserted."
  },
  {
    "id": "CASE-0074",
    "req": "REQ-047",
    "surface": "SURF-002",
    "oracle": "metamorphic",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/mutation-agent.json",
      "docs/test-campaign/evidence/mutation-agent.txt"
    ],
    "armed": true,
    "armedBy": "mutate_swift.py refuses to start unless the suite is green before any mutation, because without that a suite already failing reports every mutant as killed and returns a 0% survival rate that means nothing. Baseline green was printed before the first mutant.",
    "note": "PRO-0080 A4. The first mutation sample ever generated in ProctorAgent. PARTIAL BY DENOMINATOR AND SAID SO: 24 mutants run of a pool of 3,189 sites across all 84 files of the package, seed 20260821, 3,165 sites unrun. That is 0.75% of the surface. 19 SURVIVED, 5 scored killed, 0 unbuildable. TWO OF THE FIVE KILLS ARE NOT TRUSTWORTHY: SessionActivate.swift:166 and UnlockBroker.swift:110 both scored at exactly 600.0s, which is the timeout, and the runner scores a timeout as killed. Load average reached 271 during the run (22.92 at start, 271.39 at end, read off evidence/mutation-agent.txt lines 2-3). CORRECTED: this note first read 11.20 at start, contradicting its own evidence file, and was carried as DEF-055 until PRO-0091 fixed it. The honest kill count is 3 of 22 scored, giving a survival rate of 19/22 = 86.4%, against ProctorCore's 50%. Survivors are trustworthy in both directions: starvation can turn a survivor into a false kill but cannot turn a kill into a false survivor, so all 19 stand. NO KILL BELOW 600s IS TIMEOUT-SCORED: the three trustworthy kills ran 23.6s, 37.8s and 32.4s against a 600s bound, so they are real test failures rather than starved runs. Disposition of the 19: 5 killed by new tests (CASE-0075..0079), 1 equivalent (SessionMaestro.swift:242), 13 uncovered-by-lane or unreachable from a headless seam, each with its reason in REPORT.md. The figure is deliberately NOT copied into .warrant/suite-health.json, where mutation_measured: false is correct and means warrant's own assay has not run."
  },
  {
    "id": "CASE-0075",
    "req": "REQ-047",
    "surface": "SURF-003",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/MutationSurvivorTests.swift",
      "docs/test-campaign/evidence/mutation-arming.txt"
    ],
    "armed": true,
    "armedBy": "Mutant re-applied by hand after the test was written: KeyCodes.swift:18 'n': 45 to 'n': 46. The named test went red with 2 issues, (actual -> 46) == (CGKeyCode(expected) -> 45) and (Set(codes).count -> 25) == 26. Mutant reverted, test green.",
    "note": "PRO-0080 A5/A6, survivor 21. Nothing checked the keycode table against anything, so a letter could be remapped and 1,818 tests stayed green. With the mutation n and m both map to 46, meaning proctor_act typing 'n' presses M. The oracle is Carbon's own kVK_ANSI_* constants rather than a second copy of the table, so the test is not asserting a value it supplied itself, which is how DEF-019 shipped. Distinctness over all 26 letters is asserted separately because that is the property the mutation actually broke, and a count floor of 26 guards against a loop that examined nothing."
  },
  {
    "id": "CASE-0076",
    "req": "REQ-047",
    "surface": "SURF-012",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/MutationSurvivorTests.swift",
      "docs/test-campaign/evidence/mutation-arming.txt"
    ],
    "armed": true,
    "armedBy": "Mutant re-applied: RunIdentity.swift:30 prefix(12) to prefix(13). The named test went red: (id.count -> 13) == 12. Reverted, green.",
    "note": "PRO-0080 A5/A6, survivor 2. The run id is written into every record of every tool call and nothing asserted its shape. Length checked against the contract in the doc comment, alphabet against the source it is drawn from (a hyphen-stripped lowercased UUID is hex), and 64 mints required to be 64 distinct ids so a constant could not satisfy the first two."
  },
  {
    "id": "CASE-0077",
    "req": "REQ-047",
    "surface": "SURF-006",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/MutationSurvivorTests.swift",
      "docs/test-campaign/evidence/mutation-arming.txt"
    ],
    "armed": true,
    "armedBy": "Mutant re-applied: PixelCompare.swift:38 stride by: 4 to by: 5. The named test went red: (abs(mean - delta / 255.0) -> 0.029411764705882353) < 0.0005. Reverted, green.",
    "note": "PRO-0080 A5/A6, survivor 18. meanDifference backs the regionMatches assertion, so it is the instrument a caller's tolerance is measured in. At stride 5 it reads misaligned channels and visits four fifths of the pixels; every existing test compared images that were either identical or wildly different, both of which survive a broken stride. THE EXPECTED VALUE IS DERIVED, NOT COPIED: two uniform images differing by delta on all three colour channels have a mean absolute channel difference of exactly delta/255 whatever the pixel count, so the oracle comes from the definition rather than from the loop. An identical pair is required to read 0.0 in the same test, so the assertion cannot be satisfied by an instrument returning a constant."
  },
  {
    "id": "CASE-0078",
    "req": "REQ-047",
    "surface": "SURF-012",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/MutationSurvivorTests.swift",
      "docs/test-campaign/evidence/mutation-arming.txt"
    ],
    "armed": true,
    "armedBy": "Mutant re-applied: UnlockBroker.swift:51 contactCount += 1 to += 2. The named test went red with 3 issues, counting 2, 4 and 6 where 1, 2 and 3 were expected. Reverted, green.",
    "note": "PRO-0080 A5/A6, survivor 17. contactCount is how proctor_unlock status proves the login-path mechanism actually reached the broker: os_log from a launchd agent is not reliably visible, so this counter is the evidence. A counter that double-counts reports two handshakes where one happened, and nothing watched it. The test uses a fresh UnlockTurn rather than .shared so the count starts at a known zero and no other test can move it."
  },
  {
    "id": "CASE-0079",
    "req": "REQ-047",
    "surface": "SURF-004",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/MutationSurvivorTests.swift",
      "docs/test-campaign/evidence/mutation-arming.txt"
    ],
    "armed": true,
    "armedBy": "Mutant re-applied: RunHUDContentView.swift:830 ms < 1000 to ms <= 1000. The named test went red: (RunHUDContentView.duration(1000) -> \"1000ms\") == \"1.0s\". Reverted, green.",
    "note": "PRO-0080 A5/A6, survivor 14. The HUD's duration form. At the boundary the mutation prints 1000ms where the mock's form is 1.0s, and the existing tests only ever passed values well clear of the boundary. This is the same shape of untested boundary that RunHUDGate.onSegment is already on record for, except that this one is killable rather than equivalent."
  },
  {
    "id": "CASE-0100",
    "req": "REQ-049",
    "surface": "SURF-009",
    "flow": "FLOW-010",
    "oracle": "effect-witness",
    "lane": "macos-glass",
    "status": "pass",
    "note": "PRO-0081 closing PRO-0067's carried A3: the disabled next button is present in the tree in every state where it is disabled. THE CLAUSE HAD NO POPULATION UNTIL THIS ITEM. Walkthrough.swift carried no `.disabled(` modifier on any control, so the set of states in which the primary is disabled was empty and the clause asked as written would have read green having measured nothing. The design of record mandates the behaviour by name (design/surfaces/proctor-surfaces.html, walkthrough/permissions, captioned 'Continue is disabled and visible rather than absent'), so the behaviour was built, the gap recorded as DEF-036, and the clause then asked over a population that is no longer empty. WHAT WAS OBSERVED. A Developer ID signed .build/Proctor.app (Authority=Developer ID Application: Luke Rhodes (H4HGFL52W7), TeamIdentifier=H4HGFL52W7) ran as pid 61666 with PROCTOR_SOCKET naming nothing, so its report is nil and every grant reads false — which is what puts the flow in the state the design draws disabled on a machine that holds the real grants. /Applications/Proctor.app was untouched. The reader is the INSTALLED proctor-agent, pid 76491, a different process sharing no memory with the target, resolving the control by AXIdentifier over AXUIElement. On the permissions step it read identifier proctor.walkthrough.action.primary, role AXButton, label 'Connect a model', enabled FALSE, frame 127x24 at (1431,781), actions [AXPress]. Present, sized, and disabled — not absent. THE COUNT is 1 state asked in which the control is disabled, 1 in which it was present, 0 in which it was absent; the Core rule disables it in 3 of 16 input combinations and 1 of those 3 is reachable from this flow without an agent. TWO-WAY ON ONE BUILD: the same read of the same identifier by the same process returned enabled TRUE on the intro step one AXPress earlier, with the label changing from 'Set up permissions' and the frame width from 140. A field pinned to false could not have produced that. THE PICTURE WAS READ, NOT NAMED: evidence/shots/a3-walkthrough-permissions-disabled.png was cropped at the control's own frame and shows 'Connect a model' drawn in a muted fill with a greyed label beside an undimmed 'Skip setup' — so the visual half of the clause holds too, which AXEnabled alone does not carry. LANE CEILING: AXEnabled is the app's own exported attribute. A third process reading it establishes what the accessibility plane projects across a process boundary, which is what a screen reader and Proctor itself both act on; it is not a kernel-level witness and is not claimed as one. Citation pointed to published step-a3-walkthrough-primary-disabled.png in PRO-0109 (DEF-225).",
    "evidence": [
      "evidence/shots/step-a3-walkthrough-primary-disabled.png",
      "evidence/step-a3-walkthrough-primary-disabled.json",
      "evidence/step-a3-walkthrough-primary-disabled-probe.json"
    ],
    "armed": true,
    "armedBy": "CASE-0101: the same measurement against a build with the .disabled modifier removed reads enabled TRUE in the identical state",
    "witness": {
      "recorder": "the installed proctor-agent (pid 76491) reading pid 61666's accessibility tree by AXIdentifier with its own AX client",
      "effect": "ipc",
      "count": 1
    }
  },
  {
    "id": "CASE-0101",
    "req": "REQ-049",
    "surface": "SURF-009",
    "flow": "FLOW-010",
    "oracle": "effect-witness",
    "lane": "macos-glass",
    "status": "pass",
    "note": "The negative arm for CASE-0100, built because an out-of-family review (grok-4.6, xhigh then high) objected by name: implementing the missing .disabled() and then witnessing it manufactures the evidence. The objection is answered by measurement. Sources/ProctorUI/Walkthrough.swift had the four-line .disabled(!WalkthroughFlow.primaryEnabled(...)) modifier removed and nothing else changed; scripts/build-app.sh rebuilt and Developer ID signed .build/Proctor.app from that source; the app was launched the same way, advanced to the permissions step by the same AXPress, and read by the same installed agent. It read identifier proctor.walkthrough.action.primary, label 'Connect a model', frame 127x24 at (1431,781) — IDENTICAL to the positive arm to the point — and enabled TRUE. Same reader, same identifier, same state, same geometry; one field differs and one line of source differs. That is what establishes the instrument reads the control rather than the change, and it is simultaneously the measurement of DEF-036: before this item the design-mandated disabled state rendered enabled. The source was restored from the pre-arm copy afterwards and the bundle rebuilt from it.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0081/a3-negative-arm.json"
    ],
    "armed": true,
    "armedBy": "this case IS the arming of CASE-0100; the two arms differ in exactly one line of source",
    "witness": {
      "recorder": "the installed proctor-agent (pid 76491) reading pid 45231's accessibility tree by AXIdentifier",
      "effect": "ipc",
      "count": 1
    }
  },
  {
    "id": "CASE-0102",
    "req": "REQ-048",
    "surface": "SURF-002",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "note": "PRO-0081 closing PRO-0066's carried A2: every user-facing string comes from StatusSurface, and a grep for a quoted string literal in MainWindow.swift outside an identifier returns nothing. The clause's own grep is scripts/campaign/status_literals.py, which decides the 'outside an identifier' half by SYNTACTIC POSITION and never by reading the string, so the distinction stays mechanical. It is DEFAULT-DENY: symbol (systemName:/systemImage:/icon:), system (URL, sysctlbyname, appendingPathComponent, launchctl, openWindow(id:), forAuxiliaryExecutable:, an inline arguments array), key (a case label or the right side of ==/!=), punctuation (nothing but separators once escapes and interpolation segments are stripped), interpolated (nothing but an interpolation), and display for EVERYTHING ELSE. A positive 'does this look like prose' rule would pass silently for any rendering construct nobody listed. BEFORE: 176 literals, 127 display, measured on git show ai/wave-9:Sources/ProctorUI/MainWindow.swift, committed here as evidence/PRO-0081/a2-wave9-MainWindow.swift. This case previously recorded 132 display and 44 identifiers. Those figures are not reproducible with any committed version of the instrument, which has exactly one commit (f8f0ed7) and reports 127 and 49 over the same two file versions, so they are corrected to what the instrument reports. AFTER: 45 literals, 0 display — 16 symbol, 16 system, 1 key, 11 punctuation, 1 interpolated. The denominator is printed on every run. ONE WORDING DID CHANGE AND HAS BEEN RESTORED: a fresh verifier found the Connect section heading had gone from \"Connect a model to it\" (wave-9 MainWindow.swift:695) to \"Connect a model\" in StatusSurface.Copy.connectHeading. A2 is a move rather than a rewrite, so the original wording is back. With it restored, every one of the 127 wave-9 display literals resolves verbatim somewhere in Sources/ except six that interpolate a value and could not survive being moved into a function unchanged. Otherwise no wording changed: every string moved character for character into StatusSurface.Copy, or into Core beside the value it describes where it was an identifier returned from a computed property (StatusChecks.settingsPane, Wire.shimPath, Wire.launchdDomain, StatusChecks.ToolRow.Tone.symbol). ORACLE CORRECTED BY PRO-0091: this case recorded `static-analysis`, which was not on campaign.py's rung set, so it counted `unrated` — the bucket meaning the tool does not know what the case checked. It now records `source-analysis`, a rung parallel to the product ladder rather than a step on it (test-campaign 0.9.4), and carries the analyzer and the examined count that rung owes. What it checks is unchanged; DEF-057.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0081/a2-arming.txt",
      "docs/test-campaign/evidence/PRO-0081/a2-wave9-MainWindow.swift",
      "docs/test-campaign/evidence/PRO-0081/a2-identifier-baseline-pre.json",
      "docs/test-campaign/evidence/PRO-0081/a2-baseline-pre-comparison.txt",
      "docs/test-campaign/evidence/PRO-0081/a2-identifier-baseline.json"
    ],
    "armed": true,
    "armedBy": "CASE-0103 and CASE-0104",
    "source": {
      "analyzer": "scripts/campaign/status_literals.py over Sources/ProctorUI/MainWindow.swift",
      "examined": 45
    }
  },
  {
    "id": "CASE-0103",
    "req": "REQ-048",
    "surface": "SURF-002",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "note": "The classifier is proved able to fail before its passing is trusted. Two mutations of the converted file, each one line: a sentence put back into a Text( is reported as 'VIOLATION 74 Text() \"Proctor lets a model test a Mac app.\"' with exit 1, and a label put back into a Button( is reported as 'VIOLATION 784 Button() \"Open log\"' with exit 1. The unmodified file reports display 0 over 45 and exits 0 in the same run, so the three readings come from one instrument in one session rather than from three descriptions of one. ORACLE CORRECTED BY PRO-0091: this case recorded `static-analysis`, which was not on campaign.py's rung set, so it counted `unrated` — the bucket meaning the tool does not know what the case checked. It now records `source-analysis`, a rung parallel to the product ladder rather than a step on it (test-campaign 0.9.4), and carries the analyzer and the examined count that rung owes. What it checks is unchanged; DEF-057.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0081/a2-arming.txt"
    ],
    "armed": true,
    "armedBy": "this case IS the arming run for CASE-0102",
    "source": {
      "analyzer": "scripts/campaign/status_literals.py over two one-line mutations of Sources/ProctorUI/MainWindow.swift and the file as written",
      "examined": 45
    }
  },
  {
    "id": "CASE-0104",
    "req": "REQ-048",
    "surface": "SURF-002",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "note": "The evasion the clause is most vulnerable to, named by an out-of-family review and then closed. grok-4.6 pointed out that a printed count is not a threshold: 'display 0' stays green if the file is emptied into a second file, and counts collide where sets do not. MEASURED: with enum Actions removed from MainWindow.swift, the checker reports 25 literals, display 0, and EXITS 0 — the clause passes over a file that has had its machinery taken out from under it. The identifier set is therefore committed as evidence/PRO-0081/a2-identifier-baseline.json and compared as a set. The same gutted file against that baseline reports 14 identifiers gone by name ('-k', '/bin/launchctl', '/bin/sh', 'bootout', 'bootstrap', 'hw.optional.arm64', 'install', 'kickstart' x3, 'print', 'proctor-shim', the Settings URL, the log path, the plist path, and 'gui/…/…' x5) and exits 1. The clause is about moving the copy out, not the machinery, and the gate now says so. CORRECTED AFTER VERIFICATION: the snapshot this case gated against, a2-identifier-baseline.json, was written from the POST-conversion file, so the comparison could only ever print '0 identifier(s) gone, 0 new'. It compared the final file to itself, which is the vacuity shape this campaign exists to catch, in this clause's own instrument. The baseline is now taken from the PRE-conversion file, git show ai/wave-9:Sources/ProctorUI/MainWindow.swift, byte-identical at the branch merge-base 346edfa and at f8f0ed7^, written to a2-identifier-baseline-pre.json at 49 identifier literals. Against it the delivered file reports 4 identifier(s) gone, 0 new and EXITS 1: key 'Accessibility', key 'Automation', key 'Screen Recording' and punctuation newline. That is a real finding rather than noise. Actions.pane(for:) moved to StatusChecks.settingsPane, where the three grant names became the StatusChecks constants that already carried the same strings at wave-9, and the newline separator travelled into StatusSurface.swift:186 with the 'Looked in:' sentence it joins. A decision function moved out of the view alongside the copy, which is a widening past A2 as written; it is recorded here rather than tuned away, and the instrument's buckets, exit codes and comparison were left untouched. a2-identifier-baseline.json is kept as the FORWARD guard on later edits to the file and is no longer cited as evidence that this conversion preserved the identifier set. THE GATE HAS NOW BEEN WATCHED FAILING: on a scratch copy with one system identifier ('/bin/launchctl') replaced by a symbol reference, the forward baseline goes from 0 gone/exit 0 to 1 gone/exit 1 naming the literal, the pre baseline goes from 4 gone to 5, and restoring the copy returns exit 0 with the source file's md5 unchanged throughout (evidence/PRO-0081/a2-baseline-bite.txt). ORACLE CORRECTED BY PRO-0091: this case recorded `static-analysis`, which was not on campaign.py's rung set, so it counted `unrated` — the bucket meaning the tool does not know what the case checked. It now records `source-analysis`, a rung parallel to the product ladder rather than a step on it (test-campaign 0.9.4), and carries the analyzer and the examined count that rung owes. What it checks is unchanged; DEF-057.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0081/a2-arming-baseline.txt",
      "docs/test-campaign/evidence/PRO-0081/a2-baseline-pre-comparison.txt",
      "docs/test-campaign/evidence/PRO-0081/a2-baseline-bite.txt",
      "docs/test-campaign/evidence/PRO-0081/a2-identifier-baseline-pre.json",
      "docs/test-campaign/evidence/PRO-0081/a2-identifier-baseline.json"
    ],
    "armed": true,
    "armedBy": "the gutted-file run is itself the negative arm, and it is recorded with the exit code from both directions",
    "source": {
      "analyzer": "scripts/campaign/status_literals.py --json against evidence/PRO-0081/a2-identifier-baseline.json, compared as a set",
      "examined": 45
    }
  },
  {
    "id": "CASE-0105",
    "req": "REQ-048",
    "surface": "SURF-002",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "note": "THIS CASE PASSES ON A MEASUREMENT OF A GAP, not on the gap being closed: what it establishes is the scope of A2's green, and the gap it found is open as DEF-039. What A2's green does NOT cover, measured rather than left implicit. The clause names one file. Its siblings in Sources/ProctorUI were run through the same classifier and are reported without being gated: HistoryWindow.swift 71 user-facing of 91 examined, ProctorUIApp.swift 53 of 68, Walkthrough.swift 49 of 56, HistoryModel.swift 46 of 47, AgentModel.swift 39 of 47 — 258 in total. MenuBarCharacter.swift (1 of 1) and Motion.swift (0) are already clean. Recorded as DEF-039 and as `partial` rather than `pass`, because a case that reported this surface green would be claiming the file's clause covered the window's copy. ORACLE CORRECTED BY PRO-0091: this case recorded `static-analysis`, which was not on campaign.py's rung set, so it counted `unrated` — the bucket meaning the tool does not know what the case checked. It now records `source-analysis`, a rung parallel to the product ladder rather than a step on it (test-campaign 0.9.4), and carries the analyzer and the examined count that rung owes. What it checks is unchanged; DEF-057.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0081/a2-sibling-scope.txt"
    ],
    "armed": true,
    "armedBy": "the same classifier that reports 0 for MainWindow.swift reports non-zero for five of its siblings in the same run, so the zero is a property of the file rather than of the instrument",
    "source": {
      "analyzer": "scripts/campaign/status_literals.py over the seven siblings of MainWindow.swift in Sources/ProctorUI",
      "examined": 310
    }
  },
  {
    "id": "CASE-0106",
    "req": "REQ-049",
    "surface": "SURF-009",
    "flow": "FLOW-010",
    "oracle": "raster-visual",
    "lane": "macos-glass",
    "status": "pass",
    "note": "The visual half of A3, which AXEnabled does not carry. The clause's reason — 'a control that disappears makes the layout jump and teaches the user the step does not exist' — is about what a person sees, and an accessibility attribute reading false is not by itself evidence that the control was drawn. So the published capture was OPENED rather than trusted by its filename. evidence/shots/step-a3-walkthrough-primary-disabled.png is 1280x1144 with 8,256 distinct colours, so it is not the empty-frame shape DEF-025 described, where 2,942,720 pixels all read RGBA(0,0,0,0) under status complete. Cropped at the control's own frame (127x24 at (1371,567), window origin (898,35), scale 2) it shows 'Connect a model' drawn in place with a flat grey fill and a low-contrast label, beside an undimmed 'Skip setup'; the control holds its full width and the footer does not close up around it. Crop: evidence/PRO-0081/a3-primary-disabled-crop.png. RECORDED SO A FIDELITY PASS KNOWS WHICH TREATMENT IT IS LOOKING AT: an earlier run of the identical state, captured while the window was key, drew the same control in a muted accent fill instead. Both are macOS disabled treatments and the difference is key-window state, not a difference in the control. This case does not claim the rendered appearance matches the design pane — that is a fidelity comparison at its own rung and it was not run here.",
    "capture": {
      "method": "proctor_capture → ScreenCaptureKit, window-scoped, off this Mac's display server, taken through capture_with_manifest.py with the state confirmed by proctor_find before the shutter",
      "frameStatus": "complete"
    },
    "evidence": [
      "evidence/shots/step-a3-walkthrough-primary-disabled.png",
      "evidence/PRO-0081/a3-primary-disabled-crop.png"
    ],
    "lineage": "evidence/PRO-0081/a3-lineage-arming.txt — the seed-swap arming for this capture's tie",
    "armed": true,
    "armedBy": "capture-lineage.py --seed-swap STEP-A3-PRIMARY-DISABLED against SURF-009 and against SURF-008: both CAUGHT with 2 hard failures each, so the tie pass reads the manifest rather than the filename"
  },
  {
    "id": "CASE-0130",
    "req": "REQ-055",
    "surface": "SURF-012",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "note": "PRO-0089 A1. `Session.setPolicyStore(PolicyStore(directory:))` then `configurePolicy(allow:block:sensitive:)`: policy.json appears under the injected root. Read back through a SECOND PolicyStore over the same directory rather than through the session that wrote it, and compared against the `allow`/`block` arrays the tool itself returned rather than against the arguments this test passed in — so what is asserted is that the file on disk and the answer the caller got describe the same policy, not that a value survived a round trip to itself. Test: PolicyStoreSeamTests.configureWritesTheInjectedRoot.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0089/gate-after.txt"
    ],
    "armed": true,
    "armedBy": "CASE-0131's arming run, which shows the same suite failing when the seam is removed"
  },
  {
    "id": "CASE-0131",
    "req": "REQ-055",
    "surface": "SURF-012",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "note": "PRO-0089 A1, and the brief's own proof obligation: DO NOT TRUST THE SEAM, MEASURE IT. The operator's real file — ~/Library/Application Support/app.fledgeling.procter/policy/policy.json, 52 bytes, mtime 1787282863, sha256 0c8dcc7f659233bb084de793108ababf973bfc5cb2cbbd6964209a60ed01524d — is read for existence, bytes and modification time immediately before and immediately after a configure through an injected store, and the two readings are compared. THE READER IS ARMED IN THE SAME CASE: the identical FileWitness, over the identical call, reports the injected file going from absent to written, so 'unchanged' is a measurement rather than a reader that says unchanged whatever happens. Not a write-and-restore: nothing is restored because nothing is written, so a run killed mid-test leaves the operator's policy as it found it. Test: PolicyStoreSeamTests.theOperatorsPolicyIsNotTouched.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0089/operator-policy-before.txt",
      "docs/test-campaign/evidence/PRO-0089/operator-policy-after.txt",
      "docs/test-campaign/evidence/PRO-0089/arming.txt"
    ],
    "armed": true,
    "armedBy": "the arming run in evidence/PRO-0089/arming.txt: with the interlock disabled and operatorDirectory redirected to a decoy under /tmp, this assertion fails — 'Expectation failed: (operatorBefore → 38 bytes, modified 2026-08-21 07:59:30) == (operatorAfter → 94 bytes, modified 2026-08-21 08:00:00)'. The real file was never in the path while the seam was off."
  },
  {
    "id": "CASE-0132",
    "req": "REQ-055",
    "surface": "SURF-012",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "note": "PRO-0089 A2, the floor under the injection. A Session built with NO setPolicyStore call at all — the case that used to write the operator's file and the case a future test will forget — configures a policy, and the operator's file is unchanged across it. It did write: the session's own store directory holds a policy.json, that directory is not under PolicyStore.operatorDirectory, and it is under the named test fallback root. Asked of the session's own store rather than of PolicyStore.live, because in a test process every read of `live` mints a fresh directory. Test: PolicyStoreSeamTests.theInterlockCatchesAnUninjectedSession.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0089/arming.txt",
      "docs/test-campaign/evidence/PRO-0089/gate-after.txt"
    ],
    "armed": true,
    "armedBy": "the same arming run: with `guard AuditLog.isTestProcess` forced false, this case reports 'an un-injected session reached the operator's policy' and the fallback-prefix assertion inverts"
  },
  {
    "id": "CASE-0133",
    "req": "REQ-055",
    "surface": "SURF-012",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "note": "PRO-0089 A2, the anti-vacuity check. Without it CASE-0132 could pass because the interlock never engaged and the operator simply has no policy file — a zero from an instrument that could not have reported anything else. Asserts the interlock is active in this process (AuditLog.isTestProcess), that PolicyStore.live is not under the operator's directory, and that the path being watched is the agent's own: derived from Wire.bundleIdentifier, last component 'policy', under the current user's home. `procter` is not a typo and SwitchStore.swift records why. Test: PolicyStoreSeamTests.theInterlockIsArmed.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0089/arming.txt"
    ],
    "armed": true,
    "armedBy": "the arming run: with operatorDirectory redirected, two of this case's four assertions fail, including the home-directory prefix"
  },
  {
    "id": "CASE-0134",
    "req": "REQ-055",
    "surface": "SURF-012",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "note": "PRO-0089 A1, the contract the statics used to carry, now on the instance: an absent policy loads empty (no lists means the gate allows every app, which is the pre-feature behaviour), a save round-trips through a second store over the same directory, and a store over a different directory is a different policy — which is the whole point of the root being told rather than assumed. Plus PolicyStoreSeamTests.theFallbackIsNotSharedBetweenSessions, the regression guard for DEF-065: two un-injected sessions get different directories, and the second loads an empty policy after the first configured one.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0089/gate-after.txt"
    ],
    "armed": true,
    "armedBy": "DEF-065 is the arming: one shared fallback directory turned the gate red with 67 issues across 11 suites, all policyDenied on apps the failing tests never named"
  },
  {
    "id": "CASE-0135",
    "req": "REQ-056",
    "surface": "SURF-006",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "note": "PRO-0089 A3, closing DEF-029/DEF-051. The old oracle was `#expect(elapsed < 5.0)` around a real call and it failed six recorded times in one wave — 5.6s, 6.1s, 6.58s, 8.13s, 10.25s, 14.73s — while the product answered correctly every time. The bound arm is now a told dependency: ScreenRecordingProbe(timer:) defaults to the Task.sleep it always used, and this case injects a timer that records what it was asked to wait for. Against a platform call parked on a continuation nobody resumes, the answer is `unconfirmed` AND the timer was asked for GrantProbe.bound. Nothing in the test writes that constant, so a probe bounded by anything other than its own bound fails. Runs in 0.001s and reads no clock. THE BOUND IS NOT RAISED: GrantProbe.bound is still 1.5. Test: ScreenRecordingProbeWiringTests.aParkedCallIsEndedByTheBound.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0089/arming.txt",
      "docs/test-campaign/evidence/PRO-0089/under-load.txt"
    ],
    "armed": true,
    "armedBy": "with `await timer(bound)` changed to `await timer(bound + 1)`: 'Expectation failed: (asked.seconds → 2.5) == (GrantProbe.bound → 1.5)'"
  },
  {
    "id": "CASE-0136",
    "req": "REQ-056",
    "surface": "SURF-006",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "note": "PRO-0089 A4, the other half of the race pinned the same way round. A timer that never returns is what a healthy platform call looks like from the bound's side, and the answer must still come back: a probe that waited for its timer would cost every doctor poll the full 1.5s on a Mac where the grant is fine. Deterministic — the losing arm is a continuation nobody resumes, so there is no timing to lose. Test: ScreenRecordingProbeWiringTests.anAnswerBeatsABoundThatNeverFires. Alongside it, ScreenRecordingProbeWiringTests.theDefaultTimerStillEndsAParkedCall keeps the un-substituted timer against the same parked call and asserts only that it comes back at all, which is the difference from the hang it replaced; it reads no clock, so a loaded machine makes it slower and never makes it fail.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0089/under-load.txt"
    ],
    "armed": true,
    "armedBy": "CASE-0135's arming exercises the same `bounded` race from the other side"
  },
  {
    "id": "CASE-0137",
    "req": "REQ-056",
    "surface": "SURF-006",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "note": "PRO-0089 A4. The seams are worth nothing if production does not take the real one, so this asserts ScreenRecordingProbe.live.bound == GrantProbe.bound and that the constant is 1.5 — the number proctor_doctor's own remedy text quotes back to an operator ('did not answer', '1.5s'), which is asserted separately in theRemedyIsNotALie. Test: ScreenRecordingProbeWiringTests.theLiveProbeCarriesTheRealBound.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0089/gate-after.txt"
    ],
    "armed": true,
    "armedBy": "a bound other than GrantProbe.bound on the live probe fails this directly; the same constant is cross-checked by the doctor remedy and schema cases in the same suite"
  },
  {
    "id": "CASE-0138",
    "req": "REQ-056",
    "surface": "SURF-003",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "note": "PRO-0089 A5, the second wall-clock assertion converted with the first. CuaLineReader now takes its monotonic clock — the DispatchTime source it already used, made injectable — and the deadline case drives it. TWO ARMS THAT DISAGREE UNLESS THE DEADLINE IS REALLY JUDGED AGAINST THE INJECTED CLOCK. Stepping 100ms per reading against a 200ms budget: the reader throws timedOut, having read the clock at least three times, and the clock advanced past the budget — so it polled and came back before giving up rather than deciding once. Frozen at zero: the budget never runs down and a line written 300ms later is still served, which on the real clock against the same 200ms budget is a timeout. The old assertion, `#expect(Date().timeIntervalSince(started) < 2)`, said only that this Mac was fast. Tests: CuaLineReaderTests.silenceExpiresOnTheBudget and .aFrozenBudgetIsNeverSpent.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0089/arming.txt"
    ],
    "armed": true,
    "armedBy": "with both now() call sites reverted to DispatchTime.now().uptimeNanoseconds: the stepping arm reports 'clock.count → 0 >= 3' and 'clock.elapsed → 0 >= 200000000', and the frozen arm reports 'Caught error: .timedOut'"
  },
  {
    "id": "CASE-0139",
    "req": "REQ-056",
    "surface": "SURF-022",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "note": "PRO-0089 A6. The population of wall-clock oracles, counted rather than sampled: scripts/campaign/wall_clock_census.py scans 117 swift files under Tests/, finds 38 lines mentioning a clock of any kind — the denominator, printed in full and never capped — and 0 assertions comparing a measured wall-clock time to a numeric literal. A clock a test injects and drives is deliberately not flagged; that is what this item moved the assertions onto. BEFORE BELIEVING THE ZERO: `--arm` runs the detector over both offenders as they stood at the merge base via `git show`, and requires it to find ScreenRecordingProbeWiringTests.swift:42 and CuaLineReaderTests.swift:107 before any census is trusted. It reports 'detector caught 2 of the 2 known offenders / ARMED'. This gate has already caught one silent zero: DEF-067, a first detector whose regex could not cross the parenthesis in timeIntervalSince(started) and so missed the very line it was written for while printing a clean zero. The brief's count of 'two other tests' is one more than the tree holds; two is the whole population. Rung corrected at merge: this case was written `static-analysis`, which is on no ladder, so campaign.py filed it `unrated`. PRO-0091 introduced `source-analysis` as a rung parallel to the ladder for exactly this shape, with two guards; REQ-056's effect is `none`, so the external-effect guard does not apply, and the analyzer and examined count the other guard requires are supplied from a live run rather than copied: 118 files / 42 clock sites / 0 offenders. The case's own evidence file records 117 and 38, captured at merge-base e506ea0e before the PRO-0087 merge added clock-mentioning lines and this branch added a file. The offender count, which is the claim, is 0 in both.\n\nRE-MEASURED BY PRO-0096 FROM A LIVE RUN, AND IT NO LONGER PASSES. The brief asked for the analyzer and examined count to come from running the instrument rather than from the evidence file, because evidence files in this wave had gone stale by a merge. Running it found the case's own claim had gone stale the same way. LIVE: 127 swift test files scanned, 54 lines mentioning a clock of any kind (the denominator, printed in full and never capped), and 1 assertion comparing a measured wall-clock time to a numeric literal. Exit 1. The case recorded 0, and 0 was true when it was written.\n\nTHE OFFENDER, and it is a true positive rather than a detector artefact: Tests/ProctorCoreTests/ProctorCoreTests.swift:232 reads `#expect(waited < 10, \"a bounded client waited \\(waited)s\")`, where `waited` is `Date().timeIntervalSince(started)` around a SocketClient send whose ioTimeoutSeconds is 1. That is measured elapsed time against a numeric literal with 10x headroom — DEF-051's exact shape, and the thing REQ-056 exists to forbid. `git log -L232,232` puts it in 10285df (\"fix(ui): a wedged agent stops reading as Ready\"), which landed after this case was written, so the requirement regressed rather than the census being wrong. DEF-106.\n\nBEFORE BELIEVING EITHER READING, THE ARM WAS REPAIRED, because it had stopped being able to fire. `--arm` resolved its reference with `git merge-base HEAD ai/wave-9`, and once PRO-0089's fix merged into ai/wave-9 that merge-base BECAME the fixed tree: the arm read two files with no offenders in them and reported 'detector caught 0 of the 2 known offenders / ARMING FAILED'. The probe built to stop a silent zero had itself gone silent, which is DEF-067 one level up. It is now pinned to an immutable sha and catches 2 of 2. DEF-105. So the detector is proved two ways here — it finds both historical offenders on demand, and it found a live one nobody was looking for.\n\nFIELD PLACEMENT CORRECTED: this case carried `analyzer` and `examined` at the top level of the record, with `examined` as a prose string. campaign.py's guard reads `source.analyzer` and requires `source.examined` to be an int >= 1, so both were invisible to it and the case was flagged twice for fields it appeared to have. CASE-0102..0105, written through the same rung, put them under `source` correctly; this one was hand-written. DEF-108. The denominator recorded is 54, the clock-mentioning lines, because that is the population the offender count is drawn from — the 127 files are what was opened, not what was judged.\n\nNOT FIXED HERE, AND THE REASON IS SCOPE RATHER THAN DIFFICULTY: PRO-0096 is a registry and instrument item. Removing the assertion or giving SocketClient an injectable clock is a change to the product's test suite and belongs to whoever owns DEF-106. Recording this case `pass` with a 0 the instrument no longer reports was the one route not available.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0089/wall-clock-census.txt",
      "docs/test-campaign/evidence/PRO-0096/census-arm-broken.txt",
      "docs/test-campaign/evidence/PRO-0096/census-arm-pinned.txt",
      "docs/test-campaign/evidence/PRO-0096/census-live.txt",
      "docs/test-campaign/evidence/PRO-0097/census-after.txt",
      "docs/test-campaign/evidence/PRO-0097/def-106-arming.txt"
    ],
    "armed": true,
    "armedBy": "wall_clock_census.py --arm at PRE_FIX_COMMIT baa98bbe33ce42685fe515b9c803d3d5a430d177, the parent of 6dbaa9b, which is the last tree holding BOTH historical offenders. It reports 'ScreenRecordingProbeWiringTests.swift @ baa98bbe33ce: offenders at [42] (expected 42)', 'CuaLineReaderTests.swift @ baa98bbe33ce: offenders at [107] (expected 107)', 'detector caught 2 of the 2 known offenders / ARMED', exit 0. docs/test-campaign/evidence/PRO-0096/census-arm-pinned.txt",
    "source": {
      "analyzer": "scripts/campaign/wall_clock_census.py, run live by PRO-0097 and armed twice before the zero was believed: 2 of 2 known offenders at the pinned pre-fix commit baa98bbe33ce, and the line-232 offender found on demand in the pre-fix version of the file this item changed",
      "examined": 52
    },
    "defects": [
      "DEF-105",
      "DEF-106",
      "DEF-108"
    ]
  },
  {
    "id": "CASE-0110",
    "req": "REQ-053",
    "surface": "SURF-003",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/ToolchainDoctorTests.swift",
      "docs/test-campaign/evidence/PRO-0087/arming.txt"
    ],
    "armed": true,
    "armedBy": "Single-flight removed from SignatureVerdictCache.verdict(for:) (the in-flight join replaced by an unconditional break). The named test went red: Expectation failed: (cache.verificationCount -> 15) == 1. Restored, green.",
    "note": "PRO-0087. Fifteen concurrent Tasks, which is what the production path uses and what the async design makes safe: no caller blocks, so the test cannot be starved by the defect it measures. The injected verify holds the one verification open until all fifteen have been through identify, and the test asserts that they had, so a run where they did not arrive together fails rather than passing on a warm cache. The count comes from verificationCount, which the cache increments.",
    "defects": [
      "DEF-050"
    ]
  },
  {
    "id": "CASE-0111",
    "req": "REQ-053",
    "surface": "SURF-008",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/ToolchainDoctorTests.swift",
      "docs/test-campaign/evidence/PRO-0087/arming.txt"
    ],
    "armed": true,
    "armedBy": "Each of the eight sessions given its own SignatureVerdictCache, which is what ToolProbes did before PRO-0087. The named test went red twice: (cache.verificationCount -> 0) == 1 and (arrivals.count -> 8) == 1. Restored, green.",
    "note": "PRO-0087. The same claim through the path that carries it in production: Session.doctor -> SessionDoctor -> ToolProbes.cuaSignature. Eight concurrent doctor calls, one verification between them, and every report still carries evidence == .signature so the sharing did not cost a report its answer.",
    "defects": [
      "DEF-050"
    ]
  },
  {
    "id": "CASE-0112",
    "req": "REQ-054",
    "surface": "SURF-003",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/ToolchainDoctorTests.swift",
      "docs/test-campaign/evidence/PRO-0087/arming.txt"
    ],
    "armed": true,
    "armedBy": "ToolProbes.init's cuaSignature default put back to SignatureVerdictCache(). The named test went red twice, on both identity assertions: two independently built ToolProbes no longer held the same cache, and neither held SignatureVerdictCache.shared. Restored, green.",
    "note": "PRO-0087. The scoping half of DEF-050, and its counterweight: single-flight inside one cache buys nothing while a cache is built per Session, and a hard singleton would close the seam every existing doctor test injects through. The third assertion is that a ToolProbes handed a cache holds that one.",
    "defects": [
      "DEF-050"
    ]
  },
  {
    "id": "CASE-0113",
    "req": "REQ-053",
    "surface": "SURF-003",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/ToolchainDoctorTests.swift",
      "docs/test-campaign/evidence/PRO-0097/case-0113-arming.txt"
    ],
    "armed": false,
    "armedBy": "Not armed by re-introducing a shipped defect: the single-slot cache it guards against was correct while the cache was per-Session, so there is no prior state of this repo in which this test is red. It guards a regression the shared store newly makes possible.",
    "note": "PRO-0087. A process-wide store is asked about more than one path, so a suite's scripted path and this machine's real cua-driver land in the same cache. A single slot would have them evicting each other and re-verifying on every alternation, which is the wedge again in different clothes. Five alternations between two identities cost two verifications.",
    "defects": []
  },
  {
    "id": "CASE-0114",
    "req": "REQ-035",
    "surface": "SURF-012",
    "oracle": "metamorphic",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0087/run-ledger.md",
      "docs/test-campaign/evidence/PRO-0087/wedge-before.txt",
      "docs/test-campaign/evidence/PRO-0087/wedge-before-2.txt"
    ],
    "armed": true,
    "armedBy": "The control is the unmodified tree at the same load on the same afternoon: four runs, one green, one carrying ForgedCall(answered: false, ok: false) with four REQ-035 issues, and an earlier attempt that hung outright and had to be killed after six minutes. The forging arm has therefore been watched failing on this machine, not only reported failing by PRO-0083. Independently re-measured by the verifying reviewer, who ran the forging arm 6 of 6 green on the fixed tree.",
    "note": "PRO-0087. Whether REQ-035's forged-caller witness is reachable at all, measured as a rate rather than as a single run. PRO-0083 recorded three of five runs reporting answered: false. Corrected 2026-08-21 against the raw logs: the shipped fix is set 4 of the run ledger — 10 runs (final-1.log to final-10.log), 10 completed, the forging arm answering on all 10, no run hanging. The discarded blocking fix is set 3 — 10 runs, 9 completed with the forging arm answering on all 9, and one that hung and was sampled. This note previously said \"14 runs completed ... and no run hanging\"; no set has 14 runs in it, the figure conflated sets 3 and 4, and \"no run hanging\" was false of the pair taken together. The run ledger carries the failures that remain in set 3, which are DEF-051 rather than this path.\n\nRUNG CORRECTED BY PRO-0096, and no witness block was manufactured to keep the old one. This case stood at `effect-witness` carrying no recorder, no effect class and no count, and the gate flagged all three. It is not a merge loss: the case has no witness block on ai/pro-0087 either. Read against what it measured, the rung was simply wrong. Its `armedBy` is a CONTROL run — four runs on the unmodified tree, one green, one carrying ForgedCall(answered: false, ok: false), one hung and killed after six minutes — which is arming evidence that the arm bites, not a recorder watching the product cross a boundary. What it actually establishes is a RATE: on the fixed tree the forging arm answered on 10 of 10 runs with none hanging, where the unmodified tree could not manage that. That is a relation across runs rather than a value from one, so `metamorphic` is the rung it belongs on, and the registry already uses it that way: CASE-0015 files a 5-run determinism check there, and CASE-0052 is filed there as \"a relation across calls, not a value\". THE RUNG WAS REFERRED OUT OF FAMILY AND THE TWO LANES SPLIT. gemini-3.7-flash-high said `outcome`, reading the case as a terminating-execution check. grok-4.6 at xhigh said `metamorphic`, on the ground that a 10-run invariance is the relation the coverage model gives that rung and that `outcome` \"cannot catch right once and wrong on the second run\" — which is the entire point of this case, since PRO-0083 recorded 3 of 5 runs failing. grok's argument carries the registry's own precedent behind it, so it was taken. Neither rung changes what the case buys: `outcome` and `metamorphic` are both in EFFECT_RUNGS, so the choice was settled on what the case means rather than on what it scores. IT IS ALSO DUPLICATIVE AS A WITNESS AND IS RECORDED AS SUCH: REQ-035's real external-effect witness is CASE-0080, which reads three sealed records back off the audit trail's bytes on disk with a fresh FileHandle (effect ipc, count 3) after driving the socket from three genuinely separate front ends. A second witness block here would have been a second name for that one measurement. Verified after the change: REQ-035 is still counted witnessed by the gate, on CASE-0080's block, and the external-effect total is unmoved at 21 of 25. DEF-107.",
    "defects": [
      "DEF-050",
      "DEF-051"
    ]
  },
  {
    "id": "CASE-0115",
    "req": "REQ-053",
    "surface": "SURF-003",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/ToolchainDoctorTests.swift",
      "docs/test-campaign/evidence/PRO-0087/arming.txt",
      "docs/test-campaign/evidence/PRO-0087/wedge-after-blocking.txt"
    ],
    "armed": true,
    "armedBy": "The blocking single-flight put back (waiters on an NSCondition, verification on the first caller's own thread). The named test went red: Expectation failed: (finishedBeforeTheVerificationLanded.current -> 0) == (unrelated -> 32). Zero of thirty-two unrelated tasks ran in twenty seconds while seventeen callers waited on one verification. Restored, green.",
    "note": "PRO-0087. What a caller waiting on a verification costs the rest of the process: one more caller than the pool is wide, on a cold entry, with the verification parked, and thirty-two unrelated tasks that have to finish anyway. Deduplicating fifteen verifications to one closes the wasted work and not the wedge, because fifteen threads blocked waiting for one verification hold the cooperative pool exactly as fifteen threads running one did, and the pool is capped at the core count and does not replace a blocked worker. The watchdog runs on a thread of its own so that a regression is a red test rather than a hung suite. SCOPE, corrected 2026-08-21: this case discriminates whether waiters suspend or block, and nothing more. It previously claimed to be \"the case that decides whether DEF-050 is closed rather than reshaped\", which overstates it — a reviewer substituted Task.detached through a synchronous helper, putting the verification back on the cooperative pool, and this case stayed green. CASE-0116 measures that half and has been watched failing against that exact substitution.",
    "defects": [
      "DEF-050"
    ]
  },
  {
    "id": "CASE-0116",
    "req": "REQ-053",
    "surface": "SURF-003",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/ToolchainDoctorTests.swift",
      "docs/test-campaign/evidence/PRO-0087/arming.txt"
    ],
    "armed": true,
    "armedBy": "Two substitutions, each watched red. (1) The verifier's own: the cache's dedicated Thread replaced by Task.detached routed through a synchronous helper. Expectation failed: !((verificationRanOn.current -> \"com.apple.root.default-qos.cooperative\").contains(\"cooperative\") -> true), and .contains(\"overcommit\") against the same value; 2 issues. In that same run CASE-0110 and CASE-0115 both passed, which is the gap this case exists to close. (2) DispatchQueue.global().async, the shape that reads as the fix and is bounded by the same cap: Expectation failed: (verificationRanOn.current -> \"com.apple.root.default-qos\").contains(\"overcommit\"); 1 issue. Restored, green.",
    "note": "PRO-0087. Where the verification itself runs, read from libdispatch's own root-queue label rather than inferred from timing. Measured on this machine 2026-08-21: a Thread reads com.apple.root.default-qos.overcommit, a Task.detached reads com.apple.root.default-qos.cooperative, and DispatchQueue.global() reads com.apple.root.default-qos. The caller's label is asserted first as the instrument check, because \"not on the cooperative pool\" means nothing unless the probe reports that pool when it is looking at it. What this does not measure: that a verification completes while every cooperative thread is blocked. A saturation test for that was drafted and dropped — it holds the process-wide pool for seconds in a green run and the suite runs in parallel, so it would stall unrelated tests and feed DEF-051. Recorded in arming.txt.",
    "defects": [
      "DEF-050"
    ]
  },
  {
    "id": "CASE-0150",
    "req": "REQ-057",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0091/def041-denominator.txt"
    ],
    "armed": true,
    "armedBy": "CASE-0151 is the arming run: the same gate over the same fixture, from the 0.9.3 bytes read out of git, prints the twelve rows with nothing saying it cut the list.",
    "note": "PRO-0091, DEF-041. campaign.py check prints the denominator beside every capped list. MEASURED on a fixture built to the exact shape of the loss — eighteen external-effect requirements, none witnessed, against a cap of twelve. 0.9.3 prints twelve rows and stops. 0.9.4 prints the same twelve and then '… (showing 12 of 18)'. The twelve rows are unchanged: the cap was not raised, because the next set outgrows the next cap. An untruncated list prints '(showing N of N)' rather than nothing, so a reader never has to work out whether anything was cut, which is the position that caused the loss. The fix covers the unwitnessed-requirement list, the hollow witnesses, the unevidenced passes, the legacy-rung cases, the open cases, the duplicate and declared-share groups, the pixel claims, the surfaces with no case, the presence-only flows, and the lists carried inside blocker sentences. THE TOOL IS NOT IN THIS REPO: campaign.py belongs to the test-campaign plugin. Fixed at ~/Dev/fledgeling-plugins/plugins/test-campaign as 0.9.4 (commit f37255f, unpushed) and mirrored into the active plugin cache at 0.9.3/skills/test-campaign/scripts/campaign.py, sha256 a7d978224e6304875e1bd980f52f6cad25b6e9a163b4a6f7e864b3c7e6e50f27 on both copies."
  },
  {
    "id": "CASE-0151",
    "req": "REQ-057",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0091/def041-denominator.txt",
      "docs/test-campaign/evidence/PRO-0091/plugin-gate-tests.txt"
    ],
    "armed": true,
    "armedBy": "this case IS the arming run for CASE-0150, and the plugin's tests/run.sh carries it as a permanent guard: a truncated list that prints no denominator fails, and a complete list that claims a truncation fails.",
    "note": "PRO-0091. Three gate tests added to the plugin's own tests/run.sh, each watched in both directions in the same run: 'a truncated list carries its denominator' (18 requirements, cap 12), 'the cap still caps' (exactly twelve rows printed, so the denominator is the registry's count rather than a raised cap), and 'a complete list says it is complete' (the same fixture trimmed to three). tests/run.sh reports 67 passed, 0 failed. THE 0.9.3 SIDE WAS RUN, NOT DESCRIBED: the pre-fix bytes come from `git show HEAD~1`, and were executed outside /tmp because a stray /tmp/copy.py on this machine shadows the stdlib copy module for any script run there — the first capture of this evidence was an empty BEFORE block for that reason."
  },
  {
    "id": "CASE-0152",
    "req": "REQ-046",
    "surface": "SURF-022",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0091/def030-both-passes-armed.txt"
    ],
    "armed": true,
    "armedBy": "the case is itself the arming: each of the census's two requirement-level passes is watched going from clear to red under a mutation aimed at it, in one session against one fixture.",
    "note": "PRO-0091, DEF-030. The census control now arms BOTH passes rather than one twice. The shipped control (vacuity-check.py --seed-strengthen) fires the uncensused predicate; scripts/campaign/seed_unclass.py fires unclassed by removing a requirement's effect field. Run against a fixture copy of docs/test-campaign: baseline unclassed examined=49 findings=0 and uncensused examined=23 findings=0, then --seed-strengthen REQ-017 gives before=clear after=red, then seed_unclass REQ-017 gives before=clear after=red with unclassed examined=49 findings=1 and the registry restored byte-for-byte by sha256. Baseline re-read clear afterwards. 'unclassed examined=49 findings=0' is no longer an unwatched zero. WHY A FIXTURE RATHER THAN THE REAL REGISTRY: uncensused is currently red on the real one (REQ-049 declares an ipc effect and records no provider), and a red after a mutation proves nothing when it was red before. The fixture is docs/test-campaign with REQ-049 given a provider, and nothing else."
  },
  {
    "id": "CASE-0153",
    "req": "REQ-047",
    "surface": "SURF-022",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0091/instrument-suite.txt",
      "docs/test-campaign/evidence/PRO-0091/instrument-arming.txt"
    ],
    "armed": true,
    "armedBy": "mutations 1, 7 and 8 of instrument-arming.txt take this check's family red one at a time — the $ out of the lookbehind, \\s into it so no literal fires at all, and \\w out of it so a digit inside an identifier does — and the same run is green once each is restored.",
    "note": "PRO-0091, DEF-032. mutate_swift.py's integer-literal operator no longer matches Swift closure shorthand. The lookbehind is now (?<![\\w.$]). Tested over a fixture holding $0 three times, a property wrapper's projected value $pane2, and a real literal 3: no candidate is generated at the $0 sites, the 2 inside the projected value's name is left alone, and 3 → 4 still is — a fix that stopped the operator firing at all would pass the first assertion by itself, so the second is what makes the first mean something. Mutant 24 of 24 in the first ProctorAgent sample rewrote `{ bind(fd, $0, size) }` to `bind(fd, $1, size)`, which cannot compile, and under load 271 it scored as a KILL at the 600s timeout rather than as unbuildable — so the summary's `unbuildable: 0` was wrong for a reason the tool could not see. The projected-value assertion was written against $state and could not fire: no operator in the 11-entry table matches a bare identifier, so it was true of every table that could exist. It now asserts over $pane2, where the digit is held back by \\w in the same lookbehind, and mutation 8 of instrument-arming.txt watches it red."
  },
  {
    "id": "CASE-0154",
    "req": "REQ-047",
    "surface": "SURF-002",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0091/instrument-suite.txt",
      "docs/test-campaign/evidence/mutation-agent.txt"
    ],
    "armed": true,
    "armedBy": "mutation 4 of instrument-arming.txt puts 11.20 back into the note and the check goes red naming 22.92, which is read out of the evidence file rather than typed into the test.",
    "note": "PRO-0091, DEF-055. CASE-0074's note recorded a starting load of 11.20 where evidence/mutation-agent.txt line 2 records 22.92. Corrected, and guarded: the check parses the figure out of the evidence file and requires the note to carry it, so the two cannot drift again without the suite saying so. The note's conclusion is unchanged — the run was starved either way and the case already discounts its two 600.0s kills — but 22.92 is twice the number the note used to establish that the machine started healthy."
  },
  {
    "id": "CASE-0155",
    "req": "REQ-048",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0091/campaign-check-after.txt",
      "docs/test-campaign/evidence/PRO-0091/instrument-suite.txt"
    ],
    "armed": true,
    "armedBy": "mutations 5 and 6 of instrument-arming.txt put the old oracle string back, and then move the examined count off what the classifier reports; each goes red on its own.",
    "note": "PRO-0091, DEF-057, and the decision is recorded rather than assumed. CASE-0102..0105 recorded oracle 'static-analysis', which was not on campaign.py's rung set, so all four counted 'unrated' — the bucket whose own comment says it counts never as adequate. THE FORK WAS REFERRED TO GROK (grok-4.6, effort xhigh), which ran its own three-family panel: fable-5 at high and gemini-3.7-flash-high both answered the same way with the options presented in swapped order, and the codex lane failed to initialise and was reported as a lane failure rather than counted. The answer was neither of the two options the brief named. Re-expressing the four against the built product is not available: 'no user-facing copy is hardcoded in this file' is a property OF the source, and a hardcoded Text(\"Open log\") and a Text(Copy.openLog) render identically, so there is no product-side observable. Giving the ladder a source-analysis rung BELOW outcome invents a comparison the coverage model does not have — the ladder is one axis, what a case checked against the running product, and a reader of source text is off that axis rather than ranked on it. So SOURCE_RUNGS is a parallel set alongside EFFECT_RUNGS and RASTER_RUNGS: accepted by add, counted in its own bucket, printed on its own 'Off-ladder:' line, and absent from EFFECT_RUNGS. The gate now reads 'Off-ladder: source-analysis 4' where it read 'unrated 4'. Reclassifying the four to 'structural' was the route the brief ruled out and was not taken; what they check is unchanged."
  },
  {
    "id": "CASE-0156",
    "req": "REQ-048",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0091/plugin-gate-tests.txt"
    ],
    "armed": true,
    "armedBy": "three gate tests in the plugin's tests/run.sh, each watched red on a fixture and green once the fixture is repaired: a requirement claiming a subprocess effect covered by source-analysis alone blocks, a source-analysis pass with examined=0 blocks, and the same campaign clears when the denominator returns.",
    "note": "PRO-0091. The two guards that stop source-analysis becoming the cheap rung a campaign fills up on. A passing source-analysis case owes the analyzer that ran and the number of units it examined, the same obligation shape effect-witness carries for its recorder and count, because a search with no denominator cannot tell an empty result from an empty search. And a requirement claiming an effect outside the process may not rest on source analysis alone. A fourth check reads SOURCE_RUNGS out of the module and asserts it is disjoint from both EFFECT_RUNGS and ORACLE_RUNGS, so the rung cannot acquire effect credit by an edit nobody noticed."
  },
  {
    "id": "CASE-0157",
    "req": "REQ-058",
    "surface": "SURF-022",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0091/instrument-suite.txt",
      "docs/test-campaign/evidence/PRO-0091/instrument-arming.txt"
    ],
    "armed": true,
    "armedBy": "mutations 2 and 3 of instrument-arming.txt reproduce the original loss — the merge restricted to the two keys the conflict was about, and then the verifier stopped reporting what it found — and each goes red naming flow/FLOW-010.",
    "note": "PRO-0091, DEF-058. scripts/campaign/merge_registry.py sweeps the union of both documents' keys, asserts id uniqueness per key, and refuses rather than resolving a real conflict to one side. --verify is the gate half: it reads a merge somebody else performed and names every row of either input the result does not carry. THE ORIGINAL LOSS IS THE FIXTURE: a five-key registry where theirs adds DEF-002, REQ-002 and FLOW-010, hand-merged the way the PRO-0081 merge was — defect and requirement taken from theirs, ours taken as the base document — and --verify exits 1 naming flow/FLOW-010 (from theirs). The whole key absent rather than one row is caught separately. Without an --ancestor a one-sided absence is an ADDITION and never a deletion, which is the safe direction and the one the hand-merge inverted; deletion is representable only with an ancestor, so an unrepresentable deletion cannot be an accidental one."
  },
  {
    "id": "CASE-0158",
    "req": "REQ-058",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0091/instrument-suite.txt"
    ],
    "armed": true,
    "armedBy": "the same verifier that reports 5 of 5 keys here exits 1 and names the dropped row on the hand-merge fixture in CASE-0157, in the same run.",
    "note": "PRO-0091. The merge script is pointed at this project's own registry rather than only at a fixture: docs/test-campaign/inventory.json merged against itself must be itself, and the check asserts the verifier swept all five of its keys (component, defect, flow, requirement, surface) rather than trusting a clean exit. The key count is read with len() over the file, so a registry that grows a sixth key fails this check until the check is looked at."
  },
  {
    "id": "CASE-0159",
    "req": "REQ-057",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0091/gate-run.txt",
      "docs/test-campaign/evidence/PRO-0091/instrument-suite.txt"
    ],
    "armed": true,
    "armedBy": "run 2 of gate-run.txt puts mutation 1 back into mutate_swift.py and runs ./scripts/test.sh itself: the gate exits 1 and names the check, so run 1's exit 0 is a measurement rather than the absence of one.",
    "note": "PRO-0091. Tests/ProctorCoreTests/CampaignInstrumentTests.swift runs scripts/campaign/test_instruments.py, so ./scripts/test.sh owns the verdict — a Python file nobody runs is the same shape of finding as the ones it was written to close. It requires at least 15 checks to have been made, then requires the exit code to be 0. Two of the checks are additionally re-expressed in Swift, driving the same two scripts directly: the mutation runner over a closure-shorthand fixture, and the merge verifier over the hand-merge that lost FLOW-010. Run under ./scripts/test.sh on 2026-08-21: exit 0, 1838 tests in 217 suites, with the Campaign instruments suite among them. ScreenRecordingProbeWiringTests did not fail in that run but leaked its continuation three times; that is DEF-029/051's wall-clock oracle, which PRO-0089 owns, and nothing in it was touched here."
  },
  {
    "id": "CASE-0120",
    "req": "REQ-061",
    "surface": "SURF-006",
    "flow": "FLOW-003",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/PRO-0088/gate-after.txt",
      "evidence/PRO-0088/gate-sabotage.txt"
    ],
    "armed": true,
    "armedBy": "The same all-zero sample over a window Proctor does not own returns a different verdict and a different sentence, so the branch is reading ownership rather than agreeing with itself.",
    "note": "An empty frame over a Proctor-owned target returns excludedTarget and a caveat naming the exclusion. Unit, in ProctorCoreTests/FrameContentTests.swift.",
    "defects": [
      "DEF-097"
    ]
  },
  {
    "id": "CASE-0121",
    "req": "REQ-061",
    "surface": "SURF-006",
    "flow": "FLOW-003",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/PRO-0088/gate-after.txt"
    ],
    "armed": true,
    "armedBy": "One pixel given alpha 1 moves the verdict to content and drops the caveat, so the check measures alpha rather than restating allTransparent.",
    "note": "An empty frame over a foreign target returns emptyFrame with the sampled population in the sentence: 'empty' without a denominator is the claim this item exists to stop being made.",
    "defects": [
      "DEF-097",
      "DEF-098"
    ]
  },
  {
    "id": "CASE-0122",
    "req": "REQ-061",
    "surface": "SURF-006",
    "flow": "FLOW-003",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/PRO-0088/gate-after.txt"
    ],
    "armed": true,
    "armedBy": "The same single colour with no alpha is emptyFrame, so the test would not pass a gate that had stopped reading alpha altogether.",
    "note": "The honest edge. An opaque single-colour frame is content, not a defect: a blank document is a real window and downgrading it would be this item's own false positive. distinctColours is published and no branch keys off it.",
    "defects": []
  },
  {
    "id": "CASE-0123",
    "req": "REQ-061",
    "surface": "SURF-006",
    "flow": "FLOW-003",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/PRO-0088/gate-after.txt"
    ],
    "armed": true,
    "armedBy": "A real sample moves the verdict off notMeasured.",
    "note": "Not looking and looking and finding nothing are different answers. A nil summary and a zero-pixel sample are both notMeasured, and neither produces a caveat: an instrument that examined nothing must not be able to call a frame empty.",
    "defects": []
  },
  {
    "id": "CASE-0124",
    "req": "REQ-061",
    "surface": "SURF-006",
    "flow": "FLOW-003",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/PRO-0088/gate-after.txt"
    ],
    "armed": true,
    "armedBy": "Before believing the zero: one pixel, one channel, set to alpha 7 over the same buffer, and the instrument reports 7. It could have said non-zero.",
    "note": "FramePixels.contentSummary over a synthesised all-transparent BGRA buffer reports maxAlpha 0 and allTransparent true. Includes the out-of-family review's High finding as CASE-0124b: one lit pixel at (733,1019) in a 1520x1936 frame — an offset no square stride lands on — is content, not emptiness. Also covers the Medium: a buffer whose pixelFormat is not 32BGRA is notMeasured rather than read with a colour channel mistaken for alpha.",
    "defects": [
      "DEF-097"
    ]
  },
  {
    "id": "CASE-0125",
    "req": "REQ-061",
    "surface": "SURF-006",
    "flow": "FLOW-003",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/PRO-0088/gate-after.txt"
    ],
    "armed": true,
    "armedBy": "Zeroing the same geometry inverts both readings, so the measurement is of the bytes rather than of the frame's size.",
    "note": "A frame with real content measures as content: maxAlpha 255 and more than one distinct colour.",
    "defects": []
  },
  {
    "id": "CASE-0126",
    "req": "REQ-061",
    "surface": "SURF-006",
    "flow": "FLOW-003",
    "oracle": "source-analysis",
    "status": "pass",
    "evidence": [
      "evidence/PRO-0088/gate-sabotage.txt",
      "evidence/PRO-0088/gate.md"
    ],
    "armed": true,
    "armedBy": "Watched to fail: deleting '&& contentVerdict == .content' from CaptureEngineImpl.swift turned this test red and the run exited 1. Transcript in gate-sabotage.txt.",
    "note": "The production verdict conjoins the content gate, and the caveat is drawn from the gate rather than written twice. Source analysis: it establishes that the code the unit tests drive is the code that runs, and buys no evidence about a real capture. That is CASE-0129.",
    "defects": [
      "DEF-097"
    ],
    "source": {
      "analyzer": "Tests/ProctorAgentTests/CaptureContentWiringTests.swift, trustworthyIsGatedOnContent, reading Sources/ProctorAgent/Capture/CaptureEngineImpl.swift and requiring six expressions of it, including the `&& contentVerdict == .content` the sabotage removes",
      "examined": 451
    }
  },
  {
    "id": "CASE-0127",
    "req": "REQ-062",
    "surface": "SURF-004",
    "flow": "FLOW-004",
    "oracle": "source-analysis",
    "status": "pass",
    "evidence": [
      "evidence/PRO-0088/gate-sabotage.txt",
      "evidence/PRO-0088/gate.md",
      "evidence/PRO-0088/case-0127-arming.txt"
    ],
    "armed": true,
    "armedBy": "Armed twice, and the second is the one that counts. First arming restored 'surface.panel.level = Self.inPlaneLevel' and the test went red (gate-sabotage.txt) — but that receiver is 'surface.panel', so it never met the prefix the filter excluded. The filter skipped every line beginning 'panel.level = ', which is the shape a direct assignment takes wherever the local is called 'panel', so the check could not go red against the regression it names. Re-armed against that shape: 'Self.place(panel, at: Self.floatingLevel)' at CursorOverlay.swift:548 replaced by 'panel.level = Self.floatingLevel', which is a panel built and never given a sharing type. Transcript in case-0127-arming.txt.",
    "note": "Every level assignment in CursorOverlay goes through place(_:at:), which sets sharingType with it. Counted rather than matched with a contains, because a claim about a population is not settled by a printed line. The helper's own assignment is excluded BY LINE RANGE — the body of place(_:at:) located by its signature and closing brace — rather than by what a line says, which is the correction: an exclusion written as a text prefix cannot distinguish the helper's 'panel.level = level' from a regression's 'panel.level = .floating'. The range's control asserts it still contains exactly one assignment, so a range that had drifted off the helper fails rather than passes.",
    "defects": [
      "DEF-095",
      "DEF-101"
    ],
    "source": {
      "analyzer": "Tests/ProctorAgentTests/CaptureContentWiringTests.swift, thePointerKeepsItsExclusionAcrossBands, counting `.level = ` assignments in Sources/ProctorAgent/Overlay/CursorOverlay.swift outside the line range of place(_:at:)",
      "examined": 615
    }
  },
  {
    "id": "CASE-0128",
    "req": "REQ-062",
    "surface": "SURF-004",
    "flow": "FLOW-004",
    "oracle": "effect-witness",
    "status": "pass",
    "evidence": [
      "evidence/PRO-0088/glass/overlay-population.json",
      "evidence/PRO-0088/glass/README.md",
      "evidence/PRO-0088/case-0032-population-before.json"
    ],
    "armed": true,
    "armedBy": "A live control build, not a citation. Two signed Developer ID builds differing only in CursorOverlay.swift: the pre-fix agent (pid 53748) reports 3 windows owned with 1 not excluded — the layer-0 one — over 60 samples. The instrument could and did report sharingState 1.",
    "note": "Every window CGWindowListCopyWindowInfo attributes to the agent pid reports sharingState 0 on the delivered build: pid 12838 owns 3 (layers 25, 24, 0) with notExcluded len() 0, over 54 samples. Read from a separate probe process, so the process being measured is not the one measuring. The population is len() over rows filtered by pid, never a printed list. This is also what identifies DEF-028's window 121491 as the drawn pointer: the one-file revert flips exactly that window and nothing else.",
    "defects": [
      "DEF-095",
      "DEF-096"
    ],
    "witness": {
      "recorder": "scripts/campaign/witness_probe.swift in mode `windows`, a separate process reading CGWindowListCopyWindowInfo and filtering by the agent pid, sampled every ~330ms — 54 samples on the delivered build and 60 on the pre-fix control",
      "effect": "ipc",
      "count": 3
    }
  },
  {
    "id": "CASE-0129",
    "req": "REQ-061",
    "surface": "SURF-006",
    "flow": "FLOW-003",
    "oracle": "effect-witness",
    "status": "pass",
    "evidence": [
      "evidence/PRO-0088/glass/capture-proctor-owned.json",
      "evidence/PRO-0088/glass/capture-calculator.json",
      "evidence/PRO-0088/glass/README.md"
    ],
    "armed": true,
    "armedBy": "The Calculator leg is the sabotage and it is a real frame, not a stub: the same code path one call later returns trustworthy true, contentVerdict content, maxAlpha 255 over 1,099,968 pixels. A gate that had simply started refusing everything would fail here.",
    "note": "proctor_capture through the production path against a signed build on a private socket. A Proctor-owned window (win:2:1, CG 147541) returns status complete, trustworthy FALSE, contentVerdict excludedTarget, 7,720,704 pixels sampled and every one fully transparent, with a caveat naming the exclusion. Before this change the same call returned trustworthy true. /Applications/Proctor.app was never replaced. What this does not establish: the Proctor UI's own History window was not captured, because AX exposed no window for either UI process and this harness cannot click a menu item on a window it cannot address.",
    "defects": [
      "DEF-097",
      "DEF-098"
    ],
    "witness": {
      "recorder": "a JSON-RPC client driving proctor_capture over the agent's private unix socket against a signed Developer ID build, with both replies kept whole",
      "effect": "device",
      "count": 2
    }
  },
  {
    "id": "CASE-0190",
    "req": "REQ-063",
    "surface": "SURF-012",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/PolicyStoreSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0095/def068-arming.txt"
    ],
    "armed": true,
    "armedBy": "The pre-fix write put back — try encoder.encode(policy).write(to: url, options: .atomic). Expectation failed: (mode(store.url) -> 438) == (0o600 -> 384); 438 is 0o666. Restored, green.",
    "note": "PRO-0095, DEF-068. A fresh policy.json is 0o600 inside a 0o700 directory, read back with attributesOfItem rather than inferred from the options the write was given — the defect was precisely a call whose options said nothing about the mode. Also asserts the directory holds policy.json and nothing else afterwards, since a surviving temporary would be a second copy of the same app list beside the file. It reads the directory rather than probing one expected name, because the temporaries are uniquely named.",
    "defects": [
      "DEF-068"
    ]
  },
  {
    "id": "CASE-0191",
    "req": "REQ-063",
    "surface": "SURF-012",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/PolicyStoreSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0095/def068-arming.txt"
    ],
    "armed": true,
    "armedBy": "Two substitutions, each watched red. (1) The pre-fix Data.write: Expectation failed: (mode(store.url) -> 420) == (0o600 -> 384). (2) .usingNewMetadataOnly dropped and nothing else — exactly this case red, 1 issue, 420 again, with the other three still green. Restored, green.",
    "note": "PRO-0095, DEF-068's upgrade half, and the half that ships broken by default. The fixture starts at a real 0o644 file, and the case asserts it started wide before asserting it ended narrow, so a fixture that failed to set up cannot pass this. FileManager.replaceItemAt carries the original item's metadata across unless told not to, so without .usingNewMetadataOnly an operator whose policy.json was created by an earlier build keeps 0644 through every save it ever sees. Substitution (2) is what proves this case is the one guarding that option.",
    "defects": [
      "DEF-068"
    ]
  },
  {
    "id": "CASE-0192",
    "req": "REQ-063",
    "surface": "SURF-012",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/PolicyStoreSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0095/def068-arming.txt"
    ],
    "armed": true,
    "armedBy": "The pre-fix Data.write put back: Expectation failed: (mode(store.url) -> 438) == (0o600 -> 384). 438 is 0o666, which is what the umask gives when the umask is 0 and the write sets no mode. Restored, green.",
    "note": "PRO-0095. The case that discriminates set from inherited: it saves under umask(0), where the pre-fix code yields 0o666 rather than the 0o644 it yields under this machine's usual umask. Without it a fix that merely narrowed the usual default would pass CASE-0190. umask is process-wide, so the suite is .serialized and the umask is restored on both the throwing and the non-throwing path; the window against the rest of the suite is one open inside one test, and nothing outside PolicyStoreSeamTests.swift asserts on a mode. Measured during arming: run in parallel this leaked into CASE-0190, which read 0o666 rather than its own 0o644 — which is why the suite is serialized.",
    "defects": [
      "DEF-068"
    ]
  },
  {
    "id": "CASE-0193",
    "req": "REQ-063",
    "surface": "SURF-012",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/PolicyStoreSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0095/def068-arming.txt"
    ],
    "armed": true,
    "armedBy": "The pre-fix Data.write put back: 3 issues, one per save, Expectation failed: (mode(store.url) -> 420) == (0o600 -> 384). Restored, green.",
    "note": "PRO-0095. The mode is worth nothing if the file stopped being a policy. Three saves rather than one, because the second and third take the replace-over-an-existing-file path rather than the create path, and each asserts both the contents and the mode. What no case here proves: that the file was never wider for an instant. That window is unobservable from a test in the same process, and is closed by construction instead — Darwin.open with O_EXCL and mode 0o600 means no descriptor to a wider file ever exists, and the replace is a rename.",
    "defects": [
      "DEF-068"
    ]
  },
  {
    "id": "CASE-0194",
    "req": "REQ-064",
    "surface": "SURF-022",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "docs/test-campaign/evidence/PRO-0095/def075-refusal.txt"
    ],
    "armed": true,
    "armedBy": "The precondition removed, which is the shipped control's behaviour: 3 of the 8 checks red — the refusal exit code, the refusal naming the state, and the refusal naming which pass was red and over what population. The sabotaged run printed uncensused findings=3 and still claimed it had turned a clear census red. Restored, green.",
    "note": "PRO-0095, DEF-075. Run against a copy of this project's own registry rather than a synthesised one, so the control meets the shape it actually meets: docs/test-campaign is red on uncensused as it stands, which is the state DEF-075 was found in. Four checks: exit 2, the word REFUSING with the state, the pass name with its population, and that a refused run leaves the registry byte-identical. The fourth needed two attempts to arm — see the near-miss recorded in the evidence file, where re-serialising the unmutated object reproduced the file's bytes exactly and the check could not fire.",
    "defects": [
      "DEF-075"
    ]
  },
  {
    "id": "CASE-0195",
    "req": "REQ-064",
    "surface": "SURF-022",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "docs/test-campaign/evidence/PRO-0095/def075-refusal.txt"
    ],
    "armed": true,
    "armedBy": "The control made to refuse every input: 3 checks red, including this one and the bite report. Separately, the restore dropped: this check red again alongside the byte-identical check. Restored, green.",
    "note": "PRO-0095. The check that keeps CASE-0194 honest. A control that refused everything would satisfy the refusal checks alone and have stopped being a control, so this runs it against a fixture whose standing census findings are cleared and requires before=clear after=red at exit 0. Measured on the fixture: unclassed examined=55 findings=0, uncensused examined=24 findings=1, the one finding being the strengthened REQ-017.",
    "defects": [
      "DEF-075"
    ]
  },
  {
    "id": "CASE-0196",
    "req": "REQ-064",
    "surface": "SURF-022",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "docs/test-campaign/evidence/PRO-0095/def075-refusal.txt"
    ],
    "armed": true,
    "armedBy": "The restore dropped from the finally: 3 checks red, including the byte-identical comparison and the control's own restored-byte-for-byte report. Separately, a refusal path that mutates REQ-017 before returning: the refused-run check red, 1 issue. Restored, green.",
    "note": "PRO-0095. The registry is restored on both paths and the restoration is verified by SHA-256 rather than asserted, following seed_unclass.py. Both the control's own report and the test's independent digest are checked, because a control that reported its own restoration truthfully while a test never looked would be one instrument marking its own work.",
    "defects": [
      "DEF-075"
    ]
  },
  {
    "id": "CASE-0197",
    "req": "REQ-053",
    "surface": "SURF-003",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/ToolchainDoctorTests.swift",
      "docs/test-campaign/evidence/PRO-0095/def043-sample.txt"
    ],
    "armed": true,
    "armedBy": "The cache's dedicated Thread run through Task.detached, which is the substitution a reviewer used to arm CASE-0116 and which puts the verification back on the width-capped pool. Red with 2 issues: Expectation failed: !((verificationRanOn.current -> \"com.apple.root.default-qos.cooperative\").contains(\"cooperative\") -> true), and .contains(\"overcommit\") against the same value. Restored, green.",
    "note": "PRO-0095, DEF-043. CASE-0116's libdispatch root-queue instrument applied through the production path: eight Sessions calling doctor(verbose:) at once against an injected cache. The caller's label is asserted to contain cooperative first as the instrument check, because 'the verification was not on the cooperative pool' means nothing unless the probe reports that pool when it is looking at it. CASE-0116 reads the same labels against a bare cache; concurrentSessionsVerifyOnce runs through doctor and counts verifications rather than reading where they ran; this closes the gap between them, which is a synchronous wrapper appearing between SessionDoctor.swift:237 and the cache. SCOPE: it does not prove a verification completes while every cooperative thread is blocked, and it would not catch a semaphore wrapper at the call site that blocks the caller while leaving the verification on its own Thread — catching that needs more concurrent callers than the pool is wide, and CASE-0116's note records why that saturation test was dropped. The sample sweep covers that half as a measurement rather than a guard.",
    "defects": [
      "DEF-100"
    ]
  },
  {
    "id": "CASE-0198",
    "req": "REQ-063",
    "surface": "SURF-012",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/PolicyStoreSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0095/def068-arming.txt"
    ],
    "armed": true,
    "armedBy": "One fixed temporary name put back, which is what O_EXCL turns into a refusal: Expectation failed: (failures.count -> 11) == 0. Eleven of twelve concurrent saves threw, and the other four cases stayed green. Restored, green.",
    "note": "PRO-0095. A regression this item nearly shipped and this case caught twice. Data.write(options: .atomic) uses a temporary of its own per call, so two sessions saving at once both succeeded and the last won; an explicit open with O_EXCL on one fixed temporary name turns that into a thrown error, and two sessions in one agent share operatorDirectory so the race is reachable. The temporaries are therefore uniquely named.\n\nThe second catch is the more useful one: a first attempt swept the directory for stale temporaries on every write, which deletes the temporaries of saves running at that moment, and this case read 4 of 12 throwing. The sweep was reverted. What that leaves is litter after a crash mid-write — one small file, itself 0600 inside the 0700 directory, so untidy rather than a second exposure. Recorded rather than fixed, because a sweep that does not race a live writer needs an age test and this item is not the place for it.",
    "defects": [
      "DEF-068"
    ]
  },
  {
    "id": "CASE-0199",
    "req": "REQ-061",
    "surface": "SURF-006",
    "flow": "FLOW-003",
    "oracle": "outcome",
    "status": "pass",
    "evidence": [
      "evidence/PRO-0088/gate-final.txt",
      "evidence/PRO-0088/case-0199-0200-arming.txt"
    ],
    "armed": true,
    "armedBy": "The pre-fix 'case .content, .notMeasured: return nil' put back: Expectation failed: (caveat -> nil) != nil, plus the two legs asserting the sentence names the window and says 'not measured'. Restored, green. The opposite over-correction is armed in the same test: .content must still return nil, so a function that had started returning a sentence for every verdict fails here.",
    "note": "PRO-0088. CaptureEngineImpl conjoins contentVerdict == .content into trustworthy, so .notMeasured makes a frame untrustworthy; its caveat ladder's final else calls CaptureContentGate.caveat(for:), which returned nil for .notMeasured by design. That handed a caller trustworthy: false with no reason beside it, over a frame in a layout the summariser declines. The gate now returns a sentence naming the window and what could not be done, and the test asserts it does NOT contain 'no content' or 'fully transparent' — the words .emptyFrame uses — because a measurement that never ran has no standing to call a window empty.",
    "defects": [
      "DEF-103"
    ]
  },
  {
    "id": "CASE-0200",
    "req": "REQ-065",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "The probe IS two-way and that is the whole of it. Arm: a scratch copy of the registry with CASE-0067's status flipped from `inconclusive` to `pass` and its witness count left at 0 untouched — the gate then reports '4 effect-witness claim(s)' and names CASE-0067 first in 'Effect witnesses that witnessed nothing'. Disarm: the same registry unflipped — CASE-0067 is absent from the list and the count falls to 3. One field moved, and the guard turns on and off with it, so the exemption is a real branch rather than a case the gate never reached. docs/test-campaign/evidence/PRO-0096/gate-inconclusive-exemption.txt",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0096/gate-inconclusive-exemption.txt"
    ],
    "note": "PRO-0096 finding 3, and it closed as NO CHANGE with the plugin left exactly as shipped. The brief reported that CASE-0067 (REQ-007) and CASE-0087 (REQ-024) were being flagged for a witness count of 0 while both are `inconclusive`, called that possibly over-strict, and asked whether the check should exempt an inconclusive case — to be fixed in the plugin and declared, since the plugin is live for every project on this machine.\n\nMEASURED FIRST: test-campaign 0.9.4 ALREADY EXEMPTS THEM, so there was nothing to fix and nothing to ask. The guard is `if c.get('oracle') in WITNESS_RUNGS and st == 'pass'` (campaign.py:751), and `state_of` maps both cases off `pass` — CASE-0087's bare 'inconclusive' and CASE-0067's 'inconclusive: PersonInput.isAPerson requires sourcePid == 0...' both resolve to `inconclusive` via the startswith branch. A live gate run on the unmodified registry names only CASE-0114 in 'Effect witnesses that witnessed nothing', three times, and neither of the two the brief named appears anywhere in it.\n\nThe code read alone would not have settled it, so it was armed rather than asserted: flipping ONLY CASE-0067's status to `pass`, with count 0 left exactly as it was, makes the gate flag it immediately. The guard can fire on that case; it declines to, on the status. That is the exemption working, not a case the check never visits.\n\nNO PLUGIN CHANGE WAS MADE AND NOTHING NOW DIFFERS FROM STOCK on this machine. campaign.py at ~/.claude/plugins/cache/fledgeling-plugins/test-campaign/0.9.4/skills/test-campaign/scripts/campaign.py is byte-for-byte as installed. The clarify referral the brief asked for was not raised either, and deliberately: clarify's own gate drops a question whose answer cannot change the work, and a measurement had already answered this one in both directions. The two cases were not touched, as instructed — they remain `inconclusive` and remain a declared stop on the gate, which is the honest place for them.\n\nWHAT DOES STILL HOLD THE GATE for these two is separate and is not this: REQ-007 and REQ-024 are two of the four requirements recorded `observed` with an external effect and no passing effect-witness case behind them. That blocker is real, it is upstream of these cases' status, and PRO-0096 did not touch it."
  },
  {
    "id": "CASE-0230",
    "req": "REQ-070",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "Probe A of docs/test-campaign/evidence/PRO-0084/arming.txt: with the escalation rung deleted from RunHUDState.planeException the suite exits 1 and this case reads failed; restored, it reads passed and the suite exits 0.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0084/arming.txt",
      "docs/test-campaign/evidence/PRO-0084/automation-mode.txt"
    ],
    "note": "An unrequested escalation reaches the panel's exception line. Carries its own in-test arming: the line is asserted absent before the event."
  },
  {
    "id": "CASE-0231",
    "req": "REQ-070",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "Probe C: with setPlaneStatement reverted to a direct assignment instead of the resolver, this case reads failed - the exact defect the resolver exists to prevent, a latched fact overwritten by the next step.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0084/arming.txt",
      "docs/test-campaign/evidence/PRO-0084/automation-mode.txt"
    ],
    "note": "The disclosure survives the steps that follow it. setPlaneStatement rewrites the exception line on every approach and act, so a disclosure written once would be gone by the next step while the front it announced was still taken."
  },
  {
    "id": "CASE-0232",
    "req": "REQ-071",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "Probe B: with the deferred-pointer rung deleted from the resolver, this case reads failed and no other case moves. Probe C: with setPlaneStatement reverted to a direct assignment instead of the resolver, this case reads failed - the exact defect the resolver exists to prevent, a latched fact overwritten by the next step.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0084/arming.txt",
      "docs/test-campaign/evidence/PRO-0084/automation-mode.txt"
    ],
    "note": "A stood-down pointer is stated, and it also survives a following step."
  },
  {
    "id": "CASE-0233",
    "req": "REQ-070",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "Probe A of docs/test-campaign/evidence/PRO-0084/arming.txt: with the escalation rung deleted from RunHUDState.planeException the suite exits 1 and this case reads failed; restored, it reads passed and the suite exits 0.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0084/arming.txt",
      "docs/test-campaign/evidence/PRO-0084/automation-mode.txt"
    ],
    "note": "An escalation outranks a stood-down pointer on the one available line, in either arrival order."
  },
  {
    "id": "CASE-0234",
    "req": "REQ-070",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "Probe A of docs/test-campaign/evidence/PRO-0084/arming.txt: with the escalation rung deleted from RunHUDState.planeException the suite exits 1 and this case reads failed; restored, it reads passed and the suite exits 0.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0084/arming.txt",
      "docs/test-campaign/evidence/PRO-0084/automation-mode.txt"
    ],
    "note": "A guest run states neither disclosure: the statement, the block and the yield are all claims about this Mac, and a guest's foreground is not this Mac's."
  },
  {
    "id": "CASE-0235",
    "req": "REQ-070",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "Probe D: with the two flag resets deleted from runBegan, this case reads failed and no other case moves.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0084/arming.txt",
      "docs/test-campaign/evidence/PRO-0084/automation-mode.txt"
    ],
    "note": "runBegan clears both flags, so one run's disclosure cannot appear on the next run's panel."
  },
  {
    "id": "CASE-0236",
    "req": "REQ-070",
    "surface": "SURF-005",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "Probe E: with cgevent_fg remapped from .syntheticEvent to .routedEvent, this case reads failed and no other case moves.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0084/arming.txt",
      "docs/test-campaign/evidence/PRO-0084/automation-mode.txt"
    ],
    "note": "Every CuaVocabulary.foregroundPaths member maps to .syntheticEvent, the plane SessionAct raises the takeover statement for. This is the guard that replaced a dead predicate: nothing previously held this coupling, and breaking it would silently stop the statement appearing for a real takeover."
  },
  {
    "id": "CASE-0237",
    "req": "REQ-070",
    "surface": "SURF-005",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "Structural two-way with CASE-0236: one asserts the foreground set maps to .syntheticEvent, the other asserts the complement does not, so a vocabulary collapsing the distinction in either direction reddens one. Probe E reddens CASE-0236 with this one still passing, so they are independent.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0084/arming.txt",
      "docs/test-campaign/evidence/PRO-0084/automation-mode.txt"
    ],
    "note": "No background path maps to that plane. The arming control for CASE-0236, which would otherwise pass against a vocabulary that mapped every path to .syntheticEvent and announced a takeover for steps that took nothing."
  },
  {
    "id": "CASE-0238",
    "req": "REQ-070",
    "surface": "SURF-005",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "Reads a default branch the two cases above cannot reach: they enumerate known keys, this one passes a key absent from the table and nil.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0084/arming.txt",
      "docs/test-campaign/evidence/PRO-0084/automation-mode.txt"
    ],
    "note": "An unrecognised path stays .unknown rather than being guessed into a background plane, since it could be either and there is no safe direction to assume."
  },
  {
    "id": "CASE-0241",
    "req": "REQ-070",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "Probes A and B leave this case passing while reddening the disclosure cases, so it measures the unchanged rung rather than riding on the new ones.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0084/arming.txt",
      "docs/test-campaign/evidence/PRO-0084/automation-mode.txt"
    ],
    "note": "The existing synthetic wording still wins when no disclosure is latched - the control on the resolver's precedence order."
  },
  {
    "id": "CASE-0242",
    "req": "REQ-071",
    "surface": "SURF-003",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "Two-way against its own pair: CASE-0242 and CASE-0243 drive the same run with cursorSuppressible false and true and read opposite values of pointerDrawnBy, so neither asserts a constant.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0084/arming.txt",
      "docs/test-campaign/evidence/PRO-0084/automation-mode.txt"
    ],
    "note": "A driver that cannot stand its cursor down leaves a run with no Proctor pointer, and the run records which pointer it left drawing. This is the reported state: no Proctor pointer anywhere and the real cursor moving on its own."
  },
  {
    "id": "CASE-0243",
    "req": "REQ-071",
    "surface": "SURF-003",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "Two-way against its own pair: CASE-0242 and CASE-0243 drive the same run with cursorSuppressible false and true and read opposite values of pointerDrawnBy, so neither asserts a constant.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0084/arming.txt",
      "docs/test-campaign/evidence/PRO-0084/automation-mode.txt"
    ],
    "note": "A driver that can stand its cursor down leaves Proctor drawing, and the field is absent."
  },
  {
    "id": "CASE-0244",
    "req": "REQ-071",
    "surface": "SURF-003",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "Pairs with CASE-0242 across the lane boundary rather than the switch: same assertion, delegated backend removed, and CASE-0242 shows the value is reachable.",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0084/arming.txt",
      "docs/test-campaign/evidence/PRO-0084/automation-mode.txt"
    ],
    "note": "The native lane never defers its pointer, whatever a driver would have said: PointerOwnership.decide short-circuits on delegated."
  },
  {
    "id": "CASE-0245",
    "req": "REQ-072",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "headless",
    "status": "inconclusive: Proctor never observes the driver's cursor, so no instrument on this lane can read whether that cursor is over a covered target. The reachable half was measured and agreed: CASE-0242 shows a non-suppressible driver leaves the run recording deferredToDriver, the state in which Proctor draws no pointer of its own.",
    "armed": false,
    "evidence": [
      "docs/test-campaign/evidence/PRO-0084/arming.txt",
      "docs/test-campaign/evidence/PRO-0084/automation-mode.txt"
    ],
    "note": "Wave 9's covered-target rule acts by placing or hiding Proctor's OWN panel - cursorPlane reads the window list, CursorOverlay.applyPlane restacks and reads back, PointerPlanePolicy.fallback hides on a covered target. A pointer drawn by another process has no panel Proctor can place or hide and no window id Proctor can find in an order, so the rule cannot cross the stand-down. Recorded as a ceiling in source at the guard in SessionCursor.showCursor. Both workarounds are worse: drawing a Proctor pointer anyway puts two cursors on one screen, which PointerOwner refuses because a reader then cannot tell which the machine follows; drawing one only when the driver's is believed absent rests on a belief about another process Proctor never observes. What Proctor can still do is say so, which is REQ-071."
  },
  {
    "id": "CASE-0246",
    "req": "REQ-072",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "macos",
    "status": "inconclusive: the runs the report's first clause describes are driven by another automation stack entirely, so there is no Proctor run to instrument. What WAS measured is the attribution, and it is exact.",
    "armed": false,
    "evidence": [
      "docs/test-campaign/evidence/PRO-0084/arming.txt",
      "docs/test-campaign/evidence/PRO-0084/automation-mode.txt"
    ],
    "note": "'Automation Running' is macOS's own: the value of 'Automation Mode Enabled Title' in /System/Library/PrivateFrameworks/AutomationMode.framework/AutomationModeUI.app/Contents/Resources/Localizable.loctable, drawn by AutomationModeUI.app (measured running as pid 64198, ppid 1), whose binary carries the classes Overlay and OverlayView and the companion mac string 'Press . to stop'. Driven by XCTest / testmanagerd. Proctor's Sources contain 0 matches and cua-driver's strings 0. The brief instructed that the macOS attribution not be inherited as an assumption; it was re-established from artifacts instead, and it holds. A dyld-shared-cache sweep also returned 0 and is NOT part of this evidence: a control probe for a string that must be present returned 0 from the same instrument, so that zero measured nothing. No Proctor surface can appear over another stack's run, so nothing here is fixable and nothing was changed for it."
  },
  {
    "id": "CASE-0210",
    "req": "REQ-067",
    "surface": "SURF-022",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0097/defect-reconciliation.md",
      "docs/test-campaign/evidence/PRO-0097/source-denominators.txt"
    ],
    "armed": true,
    "armedBy": "The procedure could and did return `still open`. Reading each of the 24 records against the tree returned `fixed` sixteen times and `open` seven times, and one of the sevens was re-measured live rather than reasoned about: `python3 scripts/campaign/status_literals.py Sources/ProctorUI/HistoryWindow.swift` exits 1 with 'FAIL: 71 user-facing literal(s) of 91 examined', which is DEF-039 still open. A reconciliation that flipped everything whose item merged would have flipped all 23.",
    "source": {
      "analyzer": "per-defect read of inventory.json against the source on this branch, with each disposition citing the file and line or the run that settled it; recorded in defect-reconciliation.md",
      "examined": 24
    },
    "note": "A1. 24 defect records examined — the 23 that read `open` plus DEF-024, whose row a merge had dropped. 16 flipped to `fixed`, each citing what closed it: DEF-025 and DEF-028 against the FrameContent gate and CursorOverlay.place; DEF-024, DEF-029 and DEF-051 against the injected timer; DEF-042 against PolicyStore.live; DEF-043 and DEF-044 against their duplicates DEF-100 and DEF-050; DEF-035 against StatusSurface.Copy's split; DEF-030, DEF-032, DEF-055, DEF-057, DEF-058 and DEF-040 restored from the commits that set them. 7 left open with the reason: DEF-026 and DEF-027 wait on PRO-0093, DEF-037, DEF-039 and DEF-056 on PRO-0090, all `Ready for AI` rather than merged; DEF-033 is a survival rate that closes when the number moves; DEF-099 is excluded by brief 89 and carries the measurement that its stated condition no longer holds. DEF-106 closes separately in this item.",
    "defects": [
      "DEF-112",
      "DEF-114"
    ]
  },
  {
    "id": "CASE-0211",
    "req": "REQ-068",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0097/dropped-values.txt"
    ],
    "armed": true,
    "armedBy": "Watched to fail on the real tree, not a fixture: run from a worktree at 5e572d0, this item's first commit and before the restoration, the SHIPPED defect_gate.py dropped exits 1 and names thirteen values over 19,070 id/field pairs across 88 merges. On the restored tree it exits 0 over the same 19,070 pairs. The instrument could and did report non-zero on this repository's own history, three commits apart, with no fixture involved.",
    "note": "A2. Fourteen values set by a merged item and absent at HEAD, restored from the commits that set them: REQ-029, DEF-006 and DEF-007's evidence (c7fbe295, merge b49b5a1e), DEF-024's whole row plus CASE-0059's capture and CASE-0063's witness (PRO-0078), CASE-0032's evidence and note (PRO-0088), five defect status flips and REQ-024's `vacuous` evidence and note (PRO-0091). Thirteen appear as findings; CASE-0032's `note` is prose and is outside the watched field set, and was restored with its evidence list. THE COUNT WAS ELEVEN UNTIL THE CHECK WAS WIDENED: the first draft was path-limited and read only `theirs`, which hides a `-s ours` merge by construction. DEF-115.",
    "defects": [
      "DEF-112",
      "DEF-115"
    ]
  },
  {
    "id": "CASE-0212",
    "req": "REQ-067",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "Tests/ProctorCoreTests/CampaignInstrumentTests.swift"
    ],
    "armed": true,
    "armedBy": "Both directions in one session over one fixture registry and one fixture spec: with all four records `open` the check exits 1 and names each; with all four `fixed` the same spec and the same check exit 0. Neither reading is inferred from the other.",
    "note": "A3. `defect_gate.py claims` reads the spec's `**Defects:**` range and its `## Defects` table — both, because an item that overran its allocation records the overrun in the table only, which is the shape PRO-0081 took — and refuses when a claimed defect still reads `open`.",
    "defects": []
  },
  {
    "id": "CASE-0213",
    "req": "REQ-067",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py"
    ],
    "armed": true,
    "armedBy": "The two ways this check can be confident and hollow, each watched: a claimed defect with no row at all exits 1 as UNKNOWN rather than passing on `status.get() != 'open'`, and a spec whose claims did not parse exits 1 saying it measured nothing rather than passing empty. Both were run and both returned 1.",
    "note": "A3, the half that keeps the check from passing for the wrong reason. A missing row is not a closed one, and a spec the parser could not read is a cannot-run rather than a pass. The `(recorded)` marker is the third leg: a defect an item found and left open is set aside and reported, because reading PRO-0088's honest DEF-099 row as a fix claim would refuse an item for telling the truth.",
    "defects": []
  },
  {
    "id": "CASE-0214",
    "req": "REQ-068",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py"
    ],
    "armed": true,
    "armedBy": "The fixture is a real git repository built in the test — base commit, an item that fixes two records and adds a third, then a commit that reverts one and drops the new row. The check exits 1 and names both. It is a repository rather than a stub of one because the check reads history, and a history check tested against a stub of its own input has been watched fail nothing.",
    "note": "A4. `dropped` reports a field a commit set that HEAD lost to its parent's value, and a row a commit held that HEAD does not.",
    "defects": []
  },
  {
    "id": "CASE-0215",
    "req": "REQ-068",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py"
    ],
    "armed": true,
    "armedBy": "The same fixture history carries a legitimate later correction beside the dropped value, and the check does not report it — a check that reported every change would be as useless as one that reported none, and the two are indistinguishable from a single red. Then both dropped values are restored in a further commit and the same check exits 0, so the green is a measurement rather than an absence.",
    "note": "A4's other half, and the defect arming found in the check itself. The first draft reported a merge's own value as dropped the moment somebody restored what it lost, because every historical value was read rather than the deciding one. The newest non-merge commit that touches a field now decides it.",
    "defects": []
  },
  {
    "id": "CASE-0216",
    "req": "REQ-061",
    "surface": "SURF-006",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0097/source-denominators.txt",
      "docs/test-campaign/evidence/PRO-0097/campaign-check-before.txt"
    ],
    "armed": true,
    "armedBy": "Armed by the gate itself, in both directions and on the real registry: before, `campaign.py check` printed four findings naming CASE-0126 and CASE-0127 as claiming source-analysis with no analyzer and no denominator; after, that blocker is gone and the source-analysis count reads 7. The fields were written INSIDE `source` because that is where campaign.py:772-781 reads them — written at the top level with `examined` as prose they satisfy neither condition, which is the mistake this registry made once already and the reason this case exists twice.",
    "source": {
      "analyzer": "the two Swift source-analysis tests themselves, re-read live: CaptureContentWiringTests.trustworthyIsGatedOnContent over CaptureEngineImpl.swift and thePointerKeepsItsExclusionAcrossBands over CursorOverlay.swift",
      "examined": 1066
    },
    "note": "A5. 451 lines of CaptureEngineImpl.swift and 615 of CursorOverlay.swift, 1,066 examined between them, read off the tree at the commit named in the evidence file rather than copied from a previous run. CursorOverlay carries 0 direct `.level = ` assignments outside place(_:at:) and 1 inside it, which is the range control.",
    "defects": []
  },
  {
    "id": "CASE-0217",
    "req": "REQ-061",
    "surface": "SURF-006",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0097/campaign-check-before.txt",
      "docs/test-campaign/evidence/PRO-0097/campaign-check-after.txt"
    ],
    "armed": true,
    "armedBy": "Two-way on the real gate: before, four findings named CASE-0124 and CASE-0125 — 'not a raster image — starts ==> swift test' and 'names no capture method'. After, the raster blockers are gone and the oracle mix reads raster-visual 9 where it read 11. The count went DOWN, which is the direction that distinguishes correcting a mislabel from reclassifying to clear a gate.",
    "note": "A6. Both cases measure FramePixels.contentSummary over a BGRA buffer synthesised in a unit test. No pixels came off a display server and their evidence file is a test transcript, so `raster-visual` was a claim neither could support. The raster claim REQ-061 actually rests on is CASE-0129, which drove proctor_capture against a signed build and kept the reply; it keeps its rung.",
    "defects": []
  },
  {
    "id": "CASE-0218",
    "req": "REQ-062",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0097/campaign-check-before.txt",
      "docs/test-campaign/evidence/PRO-0097/campaign-check-after.txt",
      "docs/test-campaign/evidence/PRO-0088/glass/overlay-population.json"
    ],
    "armed": true,
    "armedBy": "Two-way on the real gate: before, six findings under 'Effect witnesses that witnessed nothing' named CASE-0128 and CASE-0129 three times each; after, that blocker is gone. The numbers written into the witness blocks are read out of the evidence files those cases already cited, so nothing was measured twice and nothing was invented.",
    "note": "A7. Brief 89 names four cases and these two are not among them — the same class of finding one axis over, recorded as DEF-113. CASE-0128 witnesses 3 agent-owned windows at sharingState 0 over 54 samples on the delivered build against 1 not excluded over 60 on the pre-fix control; CASE-0129 witnesses 2 ScreenCaptureKit captures, the second of which is what makes the first a measurement rather than a gate that had started refusing everything.",
    "defects": [
      "DEF-113"
    ]
  },
  {
    "id": "CASE-0219",
    "req": "REQ-056",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/ProctorCoreTests.swift",
      "Sources/ProctorCore/Transport.swift",
      "docs/test-campaign/evidence/PRO-0097/def-106-arming.txt",
      "docs/test-campaign/evidence/PRO-0097/gate-after.txt"
    ],
    "armed": true,
    "armedBy": "Two mutants on the product, each run through ./scripts/test.sh --filter boundedWhenAsked and each watched to turn the test red. (1) Delete the SO_SNDTIMEO call: 'Expectation failed: (asked.options → [4102]) == ([SO_RCVTIMEO, SO_SNDTIMEO] → [4102, 4101])', exit 1, 2 issues. (2) Ask the kernel for `ioTimeoutSeconds + 5` on the receive direction: 'Expectation failed: (asked.seconds → [6, 1]) == ([client.ioTimeoutSeconds, client.ioTimeoutSeconds] → [1, 1])', exit 1. The second mutant also ran for 6.004 seconds against the first's 1.007, which is the bound being real rather than recorded. Both reverted; the tree carries neither.",
    "note": "A8, DEF-106. `SocketClient.init` takes `applyBound: BoundApplier` defaulting to `SocketClient.liveBound`, because the bound is spent through `setsockopt(SO_RCVTIMEO/SO_SNDTIMEO)` and that call IS the mechanism — the same shape as `ScreenRecordingProbe.timer` in PRO-0089, followed rather than re-derived. The test's spy records and then forwards to the real applier, so the `agentUnavailable` it asserts arrives because the kernel really timed the receive out.\n\nWHAT WAS REMOVED: `let started = Date()`, `let waited = Date().timeIntervalSince(started)` and `#expect(waited < 10, ...)` around a 1-second bound. Nine seconds of slack passes on a healthy machine whatever the client does and fails on a loaded one whatever the client does — DEF-051's exact shape, measured at 10.7s on this machine under load. The test now reads no clock at all.\n\nTHE BOUND DID NOT MOVE. `ioTimeoutSeconds` is 1 before and after, and the assertion is written against `client.ioTimeoutSeconds` rather than a literal, so it cannot be satisfied by changing the bound. Nothing in the change touches what a caller chooses.",
    "defects": [
      "DEF-106"
    ]
  },
  {
    "id": "CASE-0220",
    "req": "REQ-056",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/ProctorCoreTests.swift",
      "docs/test-campaign/evidence/PRO-0097/gate-after.txt"
    ],
    "armed": true,
    "armedBy": "A third mutant, on the default itself: `liveBound` replaced by `{ _, _, _ in 0 }`, an applier that reports success and sets nothing. THE TEST DOES NOT FAIL BY A MARGIN — IT HANGS, because a client with no bound applied waits forever on a listener nobody accepts on. Measured: the build finished in 15.19s and the run then sat with no verdict; a 420-second alarm killed the wrapper and the swift-test process itself was still alive at 11:09 elapsed, holding the .build lock, and had to be killed by pid. Unmutated, the same two tests run through the same script return 'Test run with 2 tests in 1 suite passed after 1.007 seconds', exit 0. One second against eleven minutes and counting is the contrast this seam is worth having, and it is the failure mode this case wants: a hang is unambiguous where a slow pass is not. Reverted; the tree carries the real applier and `grep -c MUTANT Sources/ProctorCore/Transport.swift` reads 0.",
    "note": "A8's other half. An injected seam whose default nobody exercises is a seam that can rot with no test noticing, so the default path is measured twice. First, directly: `SocketClient.liveBound` is applied to a socket this test owns and `getsockopt` reads back tv_sec 3 where it was asked for 3. Second, end to end: a client built with NO applier argument is established as genuinely bounded by a send against an unaccepting listener, which returns `agentUnavailable` with 'did not answer within 1s'. Neither half reads a clock.",
    "defects": []
  },
  {
    "id": "CASE-0221",
    "req": "REQ-056",
    "surface": "SURF-022",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0097/census-after.txt",
      "docs/test-campaign/evidence/PRO-0097/def-106-arming.txt"
    ],
    "armed": true,
    "armedBy": "Two arms, because the pinned one no longer touches this change. (1) `wall_clock_census.py --arm` runs the detector over both historical offenders at the pinned pre-fix commit baa98bbe33ce and reports 'offenders at [42] (expected 42)', 'offenders at [107] (expected 107)', 'detector caught 2 of the 2 known offenders / ARMED', exit 0. (2) The same `scan()` run over THIS item's file in both versions: at HEAD it reports 'Tests/ProctorCoreTests/ProctorCoreTests.swift:232: #expect(waited < 10, ...)' — 1 offender over 2 clock sites; over the working tree, 0 offenders over 0 clock sites. The zero is the detector declining, not the detector gone quiet, which is DEF-067 and DEF-105 both.",
    "note": "A9. The census returns to 0 and says so with its denominator: 129 swift test files scanned, 52 lines mentioning a clock of any kind — printed in full, never sampled, never capped — and 0 assertions comparing a measured wall-clock time to a numeric literal. Exit 0. The examined count recorded here is 52, the clock sites, because that is the population the offender count is drawn from; the 129 files are what was opened rather than what was judged. CASE-0139 recorded 54 of 127 with 1 offender at wave 13 and moves back to `pass` on this run.",
    "source": {
      "analyzer": "scripts/campaign/wall_clock_census.py, armed two ways before this census was believed: 2 of 2 at the pinned pre-fix commit, and the offender at line 232 of the changed file found on demand at HEAD",
      "examined": 52
    },
    "defects": [
      "DEF-106"
    ]
  },
  {
    "id": "CASE-0222",
    "req": "REQ-067",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0097/def-106-arming.txt",
      "scripts/campaign/defect_gate.py"
    ],
    "armed": true,
    "armedBy": "THE GATE CAUGHT THIS ITEM, which is the arming that matters — a gate proved only on a fixture is a gate nobody has watched refuse a real merge. DEF-106 was written into this spec's `## Defects` table while the registry still read `open`, and `defect_gate.py claims docs/specs/spec-PRO-0097.md docs/test-campaign` exited 1: 'OPEN     DEF-106 still reads `open`', 'FAIL: 1 claimed defect(s) the registry does not agree are closed'. Real spec, real registry of 75 records, no fixture. Once DEF-106 was flipped with the evidence that closed it, the same command over the same spec exits 0.",
    "note": "A11. DEF-106 is claimed in the table and NOT on the `**Defects:**` header line, because this item closed a record outside its own DEF-110..DEF-114 allocation — PRO-0081's shape, and the reason the check reads both places. The run also reports the two `(recorded)` rows it set aside: DEF-110 and DEF-111 are findings this item states and does not claim to have closed, and reading them as fix claims would refuse the item for being honest.",
    "defects": []
  },
  {
    "id": "CASE-0223",
    "req": "REQ-068",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0097/dropped-values.txt",
      "scripts/campaign/defect_gate.py"
    ],
    "armed": true,
    "armedBy": "The widening is itself the arming, and it was measured rather than argued: the same check over the same commit sees 21 merges and 11,149 id/field pairs path-limited and reading only `theirs`, and 88 merges and 19,070 pairs unlimited and reading every parent. Ten findings become thirteen. The three recovered are REQ-029, DEF-006 and DEF-007's `evidence`, and they are in the tree now.",
    "note": "A12, DEF-115. Both narrowings hide the same merge shape. A merge resolved `-s ours` leaves the file byte-identical to its first parent, so git's history simplification drops it from a path-limited listing — and that merge is exactly the one that discarded the other side's registry. Reading only `theirs` misses the symmetric loss, where the value went on the FIRST parent's side: PRO-0088's evidence paths for CASE-0032 were taken correctly by the merge that brought them and discarded by the next one, whose other branch had forked before them. A drift check that under-reports is worse than no check, because its green gets read as a measurement. Both narrowings are now named in the code beside the merge shape each hides, so a later reader does not restore them as an optimisation.",
    "defects": [
      "DEF-115"
    ]
  },
  {
    "id": "CASE-0224",
    "req": "REQ-067",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0097/case-0113-arming.txt",
      "docs/test-campaign/evidence/PRO-0097/campaign-check-after.txt"
    ],
    "armed": true,
    "armedBy": "The ratio is its own arming, two-way on the real registry an hour apart. Before: `campaign.py check docs/test-campaign` printed 'Armed: 155/156 passing cases have been watched to fail' — the instrument reporting short, one passing case nobody had watched bite. After CASE-0113 was armed by mutation, the same command over the same registry prints 157/157: 156 once the flip landed, 157 with this case, which is armed by the 155/156 it was written to record. A ratio that had only ever read N/N would prove nothing about whether it can count short; this one was seen short.",
    "note": "A13. Every passing case in this registry has now been watched to fail, and the denominator is stated rather than implied: 157 passing of 159, and 157 armed. The two non-passing cases are `inconclusive` and are not in the ratio, because an instrument that could not measure is not a pass to arm. The denominator moved during this item — 141 at campaign-check-before.txt, 156 once its twelve new cases and CASE-0139's flip landed, 157 with this one — so the ratio is quoted with the run it came from rather than as a standing figure.\n\nHOW THE ONE WAS FOUND: not by reading the cases, but by subtracting the two numbers campaign.py already prints. `armedOfPassing` had been reading 155/156 on every run of this campaign since PRO-0087 and nobody had taken the difference. The case it named carried a prose exemption in `armedBy` while `armed` read false, which is the shape that survives review — a considered-looking reason attached to a flag saying the opposite. DEF-116 records it.",
    "defects": [
      "DEF-116"
    ]
  },
  {
    "id": "CASE-0250",
    "req": "REQ-073",
    "surface": "SURF-009",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/status_literals.py",
      "docs/test-campaign/evidence/PRO-0090/a-walkthrough-arming.txt",
      "docs/test-campaign/evidence/PRO-0090/i-classifier-arming.txt"
    ],
    "armed": true,
    "armedBy": "Two sentences planted at the top of the file — one in an array, one returned from a function: FAIL: 2 user-facing literal(s) of 2 examined, exit 1. Removed, exit 0. The denominator matters here: Walkthrough.swift went from 56 examined to 0, so a PASS could have meant the instrument had nothing left to read, and this shows it still bites at the new denominator.",
    "note": "Walkthrough.swift holds no literal the classifier calls user-facing. 56 of 56 display literals moved to WalkthroughFlow.Copy in commit 154bd84; the file now examines 0 and reports 0. The denominator is code lines the analyzer read, not literals it found: the classifier's own literal count falls to 0-7 in these files after the conversion, which is why CASE-0269 arms it separately rather than letting a zero over an empty population read as a pass.",
    "source": {
      "analyzer": "scripts/campaign/status_literals.py, plus WalkthroughFlowTests.theWalkthroughViewHoldsNoLiterals, which counts quote characters outside comments over the whole file",
      "examined": 322
    }
  },
  {
    "id": "CASE-0251",
    "req": "REQ-073",
    "surface": "SURF-009",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/WalkthroughFlowTests.swift",
      "docs/test-campaign/evidence/PRO-0090/pre/Walkthrough.swift",
      "docs/test-campaign/evidence/PRO-0090/gate-final.txt"
    ],
    "armed": true,
    "armedBy": "One moved sentence altered by a word in the constant: the verbatim test failed naming both spellings. Restored, green.",
    "note": "The moved walkthrough copy is character-identical to what shipped, asserted against the pre-conversion text committed as evidence. PRO-0081 lost a section heading in the same move and a fresh verifier found it; this is the guard against a repeat."
  },
  {
    "id": "CASE-0252",
    "req": "REQ-073",
    "surface": "SURF-009",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/WalkthroughFlowTests.swift",
      "docs/test-campaign/evidence/PRO-0090/gate-final.txt"
    ],
    "armed": true,
    "armedBy": "The two twins made equal: the test failed on the inequality it asserts. Restored, green.",
    "note": "DEF-035's kept pairs. The walkthrough's unrendered twins are asserted to differ from what ships, so a silent convergence is a change somebody made rather than drift nobody noticed."
  },
  {
    "id": "CASE-0253",
    "req": "REQ-075",
    "surface": "SURF-008",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/RunHistoryWiringTests.swift",
      "Tests/ProctorCoreTests/HistorySurfaceTests.swift",
      "docs/test-campaign/evidence/PRO-0090/b-history-arming.txt"
    ],
    "armed": true,
    "armedBy": "A HistorySurface.Wire key respelled on the writer side alone: the test failed naming the key the reader could not find. Restored, green.",
    "note": "The history wire contract has one definition both ends reach. SessionHistory.swift wrote \"capDays\" and HistoryModel.swift read \"capDays\" with nothing binding them; both now name HistorySurface.Wire. The reader takes a default for a key it cannot find, so a parted rename reports a wrong answer quietly rather than failing."
  },
  {
    "id": "CASE-0254",
    "req": "REQ-073",
    "surface": "SURF-008",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/status_literals.py",
      "docs/test-campaign/evidence/PRO-0090/c-historywindow-arming.txt",
      "docs/test-campaign/evidence/PRO-0090/i-classifier-arming.txt"
    ],
    "armed": true,
    "armedBy": "Two sentences planted: FAIL: 2 user-facing literal(s) of 3 examined, exit 1. Removed, exit 0. HistoryWindow fell from 91 examined to 1, so the arming is what makes the PASS mean anything.",
    "note": "HistoryWindow.swift holds no literal the classifier calls user-facing. 71 of 91 display literals moved to HistorySurface.Copy in commit 1276dd0. The denominator is code lines the analyzer read, not literals it found: the classifier's own literal count falls to 0-7 in these files after the conversion, which is why CASE-0269 arms it separately rather than letting a zero over an empty population read as a pass.",
    "source": {
      "analyzer": "scripts/campaign/status_literals.py, plus HistorySurfaceTests.theHistoryViewHoldsNoUserFacingLiterals, which reads every line holding a quote and requires an interpolation in it",
      "examined": 552
    }
  },
  {
    "id": "CASE-0255",
    "req": "REQ-073",
    "surface": "SURF-008",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/HistorySurfaceTests.swift",
      "docs/test-campaign/evidence/PRO-0090/c-historywindow-arming.txt"
    ],
    "armed": true,
    "armedBy": "The label switched back to matching the singular spelling: the test failed reporting the raw wire word where the label belonged. Restored, green.",
    "note": "DEF-130, found in the HistoryWindow conversion. PlaneBadge matched case \"appleEvent\" while ActuationPlane's raw value is \"appleEvents\", so every Apple Events step drew the wire word. The label matches on the enum now, which makes the mismatch a compile error."
  },
  {
    "id": "CASE-0256",
    "req": "REQ-073",
    "surface": "SURF-008",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/HistorySurfaceTests.swift",
      "docs/test-campaign/evidence/PRO-0090/gate-final.txt"
    ],
    "armed": true,
    "armedBy": "The halted outcome given the failure mark: the test failed on the clause that halted is not a failure. The counted-sentence check armed separately by making one sentence disagree with its number. Both restored, green.",
    "note": "The history window's outcome vocabulary. Every outcome has its own mark, halted is not drawn as a failure, a run whose outcome is unknown is never summarised as failed, every counted sentence agrees with its number, and a step's cost is said in a unit that reads."
  },
  {
    "id": "CASE-0257",
    "req": "REQ-073",
    "surface": "SURF-010",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/status_literals.py",
      "Tests/ProctorCoreTests/SurfaceLiteralsTests.swift",
      "docs/test-campaign/evidence/PRO-0090/d-proctoruiapp-gate.txt",
      "docs/test-campaign/evidence/PRO-0090/i-classifier-arming.txt",
      "docs/test-campaign/evidence/PRO-0090/g-surfaces-arming.txt"
    ],
    "armed": true,
    "armedBy": "Two ways. The classifier: two sentences planted, FAIL: 2 of 4 examined, exit 1. The Swift guard: the scene id respelled as the literal \"main\" in ProctorUIApp.swift, which failed at SurfaceLiteralsTests.swift:190 — recorded in g-surfaces-arming.txt. Both restored, green.",
    "note": "ProctorUIApp.swift holds no literal the classifier calls user-facing, and the scene names have one definition rather than four. 53 of 69 display literals moved in commit a6d10e8. The scene id is what openWindow addresses and the title is what the launch path resizes on, so a drift in either fails silently — which is why the id, the title and the defaults key are each asserted at their Core home and scanned for at the drawing site. The denominator is code lines the analyzer read, not literals it found: the classifier's own literal count falls to 0-7 in these files after the conversion, which is why CASE-0269 arms it separately rather than letting a zero over an empty population read as a pass.",
    "source": {
      "analyzer": "scripts/campaign/status_literals.py, plus SurfaceLiteralsTests.sceneNamesAreNamed, which scans the comment-stripped file for five named spellings",
      "examined": 292
    }
  },
  {
    "id": "CASE-0258",
    "req": "REQ-073",
    "surface": "SURF-010",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/SurfaceLiteralsTests.swift",
      "docs/test-campaign/evidence/PRO-0090/g-surfaces-arming.txt",
      "docs/test-campaign/evidence/PRO-0090/h-def135-runner-abort.txt"
    ],
    "armed": true,
    "armedBy": "Both directions. A name in CommandID with no command behind it: Expectation failed: (command -> nil) != nil, and title(id) fell back to the id. A command in the catalogue CommandID does not name: Expectation failed on the contains check. Both recorded in g-surfaces-arming.txt and h-def135-runner-abort.txt. Restored, green.",
    "note": "Every named command id resolves against the catalogue and title() returns the catalogue's wording, and every catalogue command has a named id. A typo in an id took a menu item off the menu bar silently before this: commandButton draws nothing at all for an id that does not resolve."
  },
  {
    "id": "CASE-0259",
    "req": "REQ-073",
    "surface": "SURF-010",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/SurfaceLiteralsTests.swift",
      "docs/test-campaign/evidence/PRO-0090/g-surfaces-arming.txt"
    ],
    "armed": true,
    "armedBy": "ExtrasCopy.status set to the catalogue's \"Status\": Expectation failed: (ExtrasCopy.status -> \"Status\") != (title(CommandID.status) -> \"Status\"). Restored, green.",
    "note": "DEF-035's kept pairs, on the menu bar. The extra draws three wordings of its own; two differ from the catalogue deliberately and one agrees. The two that differ are asserted to differ and the one that agrees is asserted to agree, so neither can move without somebody deciding to move it."
  },
  {
    "id": "CASE-0260",
    "req": "REQ-073",
    "surface": "SURF-010",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/SurfaceLiteralsTests.swift",
      "docs/test-campaign/evidence/PRO-0090/g-surfaces-arming.txt"
    ],
    "armed": true,
    "armedBy": "Quit's shortcut written as the two-key \"⌘QQ\": Expectation failed: (keys.count -> 2) == 1. Restored, green. The instrument could also report zero by finding no shortcuts at all, so the same test asserts at least five commands carry one.",
    "note": "Every shortcut reduces to named modifier glyphs plus exactly one key, which is what KeyEquivalentParser can read. A shortcut it cannot parse draws no key equivalent and the command becomes mouse-only."
  },
  {
    "id": "CASE-0261",
    "req": "REQ-073",
    "surface": "SURF-008",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/status_literals.py",
      "Tests/ProctorCoreTests/SurfaceLiteralsTests.swift",
      "docs/test-campaign/evidence/PRO-0090/e-agentmodel-gate.txt",
      "docs/test-campaign/evidence/PRO-0090/i-classifier-arming.txt",
      "docs/test-campaign/evidence/PRO-0090/g-surfaces-arming.txt"
    ],
    "armed": true,
    "armedBy": "Two ways. The classifier: two sentences planted, FAIL: 2 of 9 examined, exit 1. The Swift guard: AgentModel reading value[\"canShow\"] as a literal again, which failed at SurfaceLiteralsTests.swift:138. Both restored, green.",
    "note": "AgentModel.swift holds no literal the classifier calls user-facing, and the 29 keys it reads out of the agent's replies are scanned for by name so a literal cannot come back. 39 of 47 display literals moved in commit ebdabc5. The denominator is code lines the analyzer read, not literals it found: the classifier's own literal count falls to 0-7 in these files after the conversion, which is why CASE-0269 arms it separately rather than letting a zero over an empty population read as a pass.",
    "source": {
      "analyzer": "scripts/campaign/status_literals.py, plus SurfaceLiteralsTests.agentModelNamesTheWireKeys, which scans the comment-stripped file for 29 named wire keys",
      "examined": 443
    }
  },
  {
    "id": "CASE-0262",
    "req": "REQ-075",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/AgentVerbsWiringTests.swift",
      "Tests/ProctorCoreTests/SurfaceLiteralsTests.swift",
      "docs/test-campaign/evidence/PRO-0090/g-surfaces-arming.txt"
    ],
    "armed": true,
    "armedBy": "The writers mutated rather than the readers, which is the half that fails silently: RunHUDFeed writing \"canShowX\" and Session writing \"yieldX\". Five tests across three suites failed naming the missing keys — Set(wire.keys) -> [\"phase\", \"canShowX\", \"running\", \"drawing\"] against the expected set, and yield?[\"active\"] -> nil == true in the hold-attribution wiring. Restored, green.",
    "note": "REQ-075 on the second wire contract: the HUD and foreground payload the agent writes and the window reads. AgentModel took a ?? false default for a key it could not find, so a parted rename did not fail — it drew a wrong answer. Both ends name AgentVerbs now, and the guard binds the writers as well as the reader."
  },
  {
    "id": "CASE-0263",
    "req": "REQ-074",
    "surface": "SURF-008",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/SurfaceLiteralsTests.swift",
      "Sources/ProctorCore/StatusSurface.swift",
      "docs/test-campaign/evidence/PRO-0090/g-surfaces-arming.txt"
    ],
    "armed": true,
    "armedBy": "The branch put back as `if case .unreachable = model.reachability { EmptyView() }`: Expectation failed at SurfaceLiteralsTests.swift:27 naming ReadinessSection. Restored, green. The state-machine half is asserted in the same test and is a value read rather than a source scan.",
    "note": "DEF-037. The permissions section carried a branch for an unreachable agent, and sections(for: .down) returns [.agentDown] alone — so the branch drew for nobody while duplicating AgentDownSection's two buttons without their accessibility identifiers. Deleted in commit b2db8a3. The guard holds both clauses: the state machine never gives that section that state, and the section no longer matches on it. The denominator is code lines the analyzer read, not literals it found: the classifier's own literal count falls to 0-7 in these files after the conversion, which is why CASE-0269 arms it separately rather than letting a zero over an empty population read as a pass.",
    "source": {
      "analyzer": "SurfaceLiteralsTests.readinessHasNoUnreachableBranch, slicing the comment-stripped MainWindow.swift between two declaration anchors that fail the test if either has moved",
      "examined": 823
    }
  },
  {
    "id": "CASE-0264",
    "req": "REQ-074",
    "surface": "SURF-008",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/SurfaceLiteralsTests.swift",
      "docs/test-campaign/evidence/PRO-0090/g-surfaces-arming.txt"
    ],
    "armed": true,
    "armedBy": "The applying treatment's condition replaced with `if false`: Expectation failed at SurfaceLiteralsTests.swift:39, the section no longer naming model.isApplying. Restored, green.",
    "note": "The other half of DEF-037. isApplying with an unreachable agent is a reachable state — a grant lands, reprobeAfterGrant() restarts the agent, and the poll meets the gap — so the spinner moved into AgentDownSection, which is the block actually on screen then. The 1.2-second timer that clears the flag is not made trustworthy by this and stays open as DEF-132. The denominator is code lines the analyzer read, not literals it found: the classifier's own literal count falls to 0-7 in these files after the conversion, which is why CASE-0269 arms it separately rather than letting a zero over an empty population read as a pass.",
    "source": {
      "analyzer": "SurfaceLiteralsTests.agentDownDrawsApplying, over the same anchored slice of MainWindow.swift",
      "examined": 823
    }
  },
  {
    "id": "CASE-0265",
    "req": "REQ-073",
    "surface": "SURF-008",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/SurfaceLiteralsTests.swift",
      "docs/test-campaign/evidence/PRO-0090/g-surfaces-arming.txt"
    ],
    "armed": true,
    "armedBy": "The Tools card pointed at Copy.toolsNoteInDesign: Expectation failed at SurfaceLiteralsTests.swift:65, the file drawing the twin. Restored, green.",
    "note": "DEF-035 at the drawing site rather than at the constant, which is how it survived a suite that already had a value-level check. The Tools card is asserted to reference StatusSurface.Copy.toolsNote and no file in Sources/ProctorUI may mention either …InDesign twin. Source-analysis and claims nothing above that rung: it proves the view names the constant, not that a rendered window carries the sentence. The denominator is code lines the analyzer read, not literals it found: the classifier's own literal count falls to 0-7 in these files after the conversion, which is why CASE-0269 arms it separately rather than letting a zero over an empty population read as a pass.",
    "source": {
      "analyzer": "SurfaceLiteralsTests.toolsCardBindsToShippedNote, over the anchored ToolsSection slice plus all eight files in Sources/ProctorUI for the …InDesign twins",
      "examined": 823
    }
  },
  {
    "id": "CASE-0266",
    "req": "REQ-073",
    "surface": "SURF-010",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/SurfaceLiteralsTests.swift",
      "docs/test-campaign/evidence/PRO-0090/gate-final.txt"
    ],
    "armed": true,
    "armedBy": "One assembled line altered by its separator: the test failed printing both spellings. Restored, green.",
    "note": "The composed sentences, where the assembly is what can drift rather than the words. Twelve menu-bar lines and the login-item log line are asserted character for character, including the singular and plural forms and the waiting suffix that must be empty at zero."
  },
  {
    "id": "CASE-0267",
    "req": "REQ-073",
    "surface": "SURF-009",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/WalkthroughFlowTests.swift",
      "docs/test-campaign/evidence/PRO-0090/a-walkthrough-arming.txt"
    ],
    "armed": true,
    "armedBy": "prominentGrant made unconditional: the at-most-one clause failed at the combination where both grants are missing. Restored, green.",
    "note": "DEF-056. Exactly one grant is prominent at all four combinations of the two grants, a grant is nominated in every state where one is still missing, and GrantRow takes prominence as a parameter rather than deciding it. This is the one decision function this item introduced; it did not exist before and is named in the spec as a deliberate move."
  },
  {
    "id": "CASE-0268",
    "req": "REQ-064",
    "surface": "SURF-022",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/CommandSurfaceTests.swift",
      "docs/test-campaign/evidence/PRO-0090/h-def135-runner-abort.txt"
    ],
    "armed": true,
    "armedBy": "The probe is two-way and that is the whole of it. One mutation — the catalogue's pause and show-panel entries given different ids — run against both forms of the assertion. Pre-fix `command(id)!`: signal 5, 0 tests reported, 0 suites reported, 0 verdict lines, scripts/test.sh exit 1 on 'no swift-testing verdict line'. Post-fix `try #require`: Test run with 1963 tests in 239 suites failed with 12 issues, exit 1, four guards naming what they found. Same mutation, opposite reporting.",
    "note": "DEF-135. A test that force-unwraps a lookup takes the runner down with it, so the suite reports nothing about any of its 1,963 tests — including the four honest failures the kill-switch test had already recorded in the same run. This case exists because a suite that cannot report is not green and is not red; it puts every zero in the run in question."
  },
  {
    "id": "CASE-0269",
    "req": "REQ-064",
    "surface": "SURF-022",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/status_literals.py",
      "docs/test-campaign/evidence/PRO-0090/i-classifier-arming.txt"
    ],
    "armed": true,
    "armedBy": "Five files, two sentences planted in each, every one moving from exit 0 to exit 1 with display 2. Removed, all five back to exit 0.",
    "note": "The denominator guard the plan named as a risk. This conversion moves machinery out of the files it certifies, and the examined counts fall hard — Walkthrough 56 to 0, HistoryWindow 91 to 1, ProctorUIApp 69 to 2, AgentModel 47 to 7. A PASS at a denominator of 0 is a PASS over nothing, so the classifier is shown to still bite at each file's new size rather than at the size it had when the item started."
  },
  {
    "id": "CASE-0270",
    "req": "REQ-055",
    "surface": "SURF-012",
    "oracle": "effect-witness",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/OperatorFilesWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0098/new-case-arming.txt",
      "docs/test-campaign/evidence/PRO-0098/structural-zero-arming.txt",
      "docs/test-campaign/evidence/PRO-0098/capture-write-caught.txt",
      "docs/test-campaign/evidence/PRO-0098/gate-after.txt",
      "docs/test-campaign/evidence/PRO-0098/flow-write-caught.txt"
    ],
    "armed": true,
    "armedBy": "Armed twice over, and the second is the one that matters for a negative claim. Sabotage 4 in new-case-arming.txt: DirectoryWitness.changed() forced to return []. The CLAIM arm stays green — which is the point — and the CONTROL arm goes red: 'Expectation failed: (wrote.count → 0) >= 1' and '(wrote → []).contains(\"policy.json\")'. Sabotage 6 in structural-zero-arming.txt, added by the gap-fix: operatorRoot pointed one component off the real path, at <bundle>-absent-sabotage, which does not exist. RUN A, the witness as it was: this case PASSED — 'a policy configure changes nothing under the operator's root, and the sweep can tell' passed after 0.084 seconds over a root that is not there, which is the structural zero in the flesh. RUN B, with the denominator guard: 'Expectation failed: (operatorBefore → DirectoryWitness(rootExists: false, files: [:])).rootExists → false' and 'Expectation failed: (operatorBefore.files.count → 0) >= 1'. Restored, green. A SECOND ARMING, and the only kind that cannot be argued with: on 2026-08-22 this case went red on a real write, with no sabotage applied at all. './scripts/test.sh' on the clean tree: 'Expectation failed: (touched → [\"captures/win-1-zoom-1787361639249.full.png\"]).isEmpty → false'. AcceptanceE2ETests' Journey 5 calls session.zoom(path: nil) twice and Session.defaultZoomPath hard-coded the operator's captures directory, so four PNGs landed there 0.5s into every run — caught only when a write fell between the sweep's two readings, which is why this case read green for as long as it did. DEF-142, fixed by the interlock. Evidence: capture-write-caught.txt.",
    "witness": {
      "recorder": "DirectoryWitness in Tests/ProctorAgentTests/Support/FileWitness.swift — a recursive sweep of a root recording existence, byte count, mtime and sha256 per file, run either side of one call. It is not PolicyStore and it is not the session under test.",
      "effect": "filesystem-write",
      "count": 1
    },
    "note": "PRO-0098, DEF-110. REQ-055 at the rung it declares, and the claim is a NEGATIVE, so two numbers need saying plainly. The witness count of 1 is the CONTROL arm's, not the claim's. The claim's own number is a ratio with its population stated: 0 changed of len(files) = 3,290 regular files (find -type f, this machine, 2026-08-22) swept under ~/Library/Application Support/app.fledgeling.procter, and the case asserts that root exists and that len(files) >= 1 BEFORE it reads the zero, so a zero out of an absent or empty root fails instead of passing. One call — Session.configurePolicy through an injected PolicyStore — is watched by one recorder over two roots: the operator's reports zero changed, the injected root reports policy.json going absent to present. Not a write-and-restore: nothing is restored because nothing is written, so a run killed mid-test leaves the operator's state as it found it. WHAT THIS DOES NOT CERTIFY, since REQ-055's original sentence claimed it: reads are not watched, the whole run is not watched, and the operator's Proctor state outside this root is not watched — DEF-141. Test: OperatorFilesWitnessTests.aConfigureLeavesTheOperatorsRootAlone. SECOND FINDING, one wave later and not by this session: the PRO-0098 re-verifier's own clean './scripts/test.sh' run reported this case red again, this time on flow writes into the operator's root beyond DEF-142's four PNGs. Those were login-flow.json and sweep.json, the suite's own recorded flows — DEF-164, fixed by the same interlock and guarded deterministically by CASE-0320, because this sweep is a race that had already lost it once. Evidence: flow-write-caught.txt.",
    "defects": [
      "DEF-110",
      "DEF-142",
      "DEF-164"
    ]
  },
  {
    "id": "CASE-0271",
    "req": "REQ-055",
    "surface": "SURF-012",
    "oracle": "effect-witness",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/OperatorFilesWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0098/new-case-arming.txt",
      "docs/test-campaign/evidence/PRO-0098/structural-zero-arming.txt",
      "docs/test-campaign/evidence/PRO-0098/capture-write-caught.txt",
      "docs/test-campaign/evidence/PRO-0098/flow-write-caught.txt"
    ],
    "armed": true,
    "armedBy": "Sabotage 4, the same run: with changed() dead the fallback-root assertion is the arm that fires. Sabotage 6 in structural-zero-arming.txt is the second and covers the denominator: with operatorRoot pointed at <bundle>-absent-sabotage, RUN A shows this case PASSING — 'an un-injected session writes somewhere, and that somewhere is not the operator's' passed after 0.089 seconds over an absent root — and RUN B shows it red on '(operatorBefore → DirectoryWitness(rootExists: false, files: [:])).rootExists → false' and '(operatorBefore.files.count → 0) >= 1'. PRO-0089's own arming (evidence/PRO-0089/arming.txt) is the third: with the interlock disabled and operatorDirectory redirected to a decoy, the equivalent assertion reports the operator path being reached. A SECOND ARMING, and the only kind that cannot be argued with: on 2026-08-22 this case went red on a real write, with no sabotage applied at all. './scripts/test.sh' on the clean tree: 'Expectation failed: (touched → [\"captures/win-1-zoom-1787361639249.full.png\"]).isEmpty → false'. AcceptanceE2ETests' Journey 5 calls session.zoom(path: nil) twice and Session.defaultZoomPath hard-coded the operator's captures directory, so four PNGs landed there 0.5s into every run — caught only when a write fell between the sweep's two readings, which is why this case read green for as long as it did. DEF-142, fixed by the interlock. Evidence: capture-write-caught.txt.",
    "witness": {
      "recorder": "DirectoryWitness over the operator's application-support root, plus a sweep of PolicyStore.testFallbackRoot for where the write actually landed",
      "effect": "filesystem-write",
      "count": 1
    },
    "note": "PRO-0098, DEF-110, the floor under the injection. A Session with NO setPolicyStore at all — the case that used to write the operator's file and the case a future test will forget — configures a policy. The operator's root is unchanged across it, reported as 0 of the len(files) = 3,290 regular files (find -type f, this machine, 2026-08-22) swept and gated on that population being non-empty, and the control arm proves the call was not simply a no-op: the interlock's fallback root holds a policy.json, and that root is not under the operator's. Without the control arm 'unchanged' would also be the reading for a configure that did nothing; without the denominator it would also be the reading for a root that is not there. Test: OperatorFilesWitnessTests.anUninjectedSessionIsAlsoWatched. SECOND FINDING, one wave later and not by this session: the PRO-0098 re-verifier's own clean './scripts/test.sh' run reported this case red again, this time on flow writes into the operator's root beyond DEF-142's four PNGs. Those were login-flow.json and sweep.json, the suite's own recorded flows — DEF-164, fixed by the same interlock and guarded deterministically by CASE-0320, because this sweep is a race that had already lost it once. Evidence: flow-write-caught.txt.",
    "defects": [
      "DEF-110",
      "DEF-142",
      "DEF-164"
    ]
  },
  {
    "id": "CASE-0272",
    "req": "REQ-055",
    "surface": "SURF-012",
    "oracle": "effect-witness",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/OperatorFilesWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0098/new-case-arming.txt"
    ],
    "armed": true,
    "armedBy": "Sabotage 3: the sweep stops descending, options [.skipsSubdirectoryDescendants]. 'Expectation failed: (changed -> []) == [\"policy/policy.json\"]'. That is exactly how a recursive reader fails silently — it keeps reporting zero for the operator's root for ever because it never looks inside policy/ or audit/ — and it is why this case exists. Sabotage 4 (changed() dead) reds it too. Restored, green.",
    "witness": {
      "recorder": "DirectoryWitness over a decoy laid out like the operator's root, driven by two real filesystem writes",
      "effect": "filesystem-write",
      "count": 2
    },
    "note": "PRO-0098, DEF-110, the anti-vacuity case: the two above are worth nothing if the reader is pointed at a path nobody uses or cannot see into one. Half of it fixes the PATH — the swept root is derived the way the product derives it, from Wire.bundleIdentifier under this user's home, and it is asserted to be the parent of both PolicyStore.operatorDirectory and SwitchStore.url(home:). The other half arms the READER on a decoy with the same shape: a file written one directory down is reported as 'policy/policy.json' (count 1), and an in-place rewrite of that file is reported again (count 2), so the reader is shown catching both a creation and a modification. Test: OperatorFilesWitnessTests.theSweepIsPointedAtTheRealThing.",
    "defects": [
      "DEF-110"
    ]
  },
  {
    "id": "CASE-0273",
    "req": "REQ-055",
    "surface": "SURF-012",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/OperatorFilesWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0098/new-case-arming.txt"
    ],
    "armed": true,
    "armedBy": "Sabotage 5: the sha256 dropped from FileWitness, leaving existence, size and mtime — PRO-0089's original three. 'Expectation failed: (before.digest -> nil) != (after.digest -> nil)'. Restored, green.",
    "note": "PRO-0098. An instrument property rather than a product one, recorded at outcome for that reason. A reader comparing only the two attributes a stat gives you reports 'unchanged' across an in-place overwrite of equal length inside one filesystem timestamp tick — which is a write, and is precisely the write a suite quietly rewriting the operator's settings would perform. The lift added a sha256 for it. The fixture asserts the two writes ARE equal-length before asserting the digests differ, so a fixture that failed to set up cannot pass this. Test: OperatorFilesWitnessTests.theDigestClosesTheSameLengthHole.",
    "defects": [
      "DEF-110"
    ]
  },
  {
    "id": "CASE-0274",
    "req": "REQ-063",
    "surface": "SURF-012",
    "oracle": "effect-witness",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/PolicyStoreSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0098/new-case-arming.txt"
    ],
    "armed": true,
    "armedBy": "Sabotage 6: the pre-fix write put back — try data.write(to: url, options: .atomic), DEF-068's original defect. 'Expectation failed: (mode(url) -> 420) == (0o600 -> 384)'; 420 is 0o644. Restored, green.",
    "witness": {
      "recorder": "DirectoryWitness over the store's directory, before and after the call, with each created path's mode taken off disk afterwards through a fresh FileManager.attributesOfItem — a reader that is not PolicyStore and was not told what the write intended",
      "effect": "filesystem-write",
      "count": 1
    },
    "note": "PRO-0098, DEF-111. REQ-063's five existing cases stat the file the save left behind, which is the OUTCOME of the write; the effect is the write. The difference is not pedantry: a save that created three files and left two at 0644 passes every stat there, because a stat only looks at the path it was handed. This records the directory before and after, names every path the call produced (created == [\"policy.json\"], count 1, non-zero), and checks the mode of EVERY one of them rather than of one expected name. Test: PolicyFileModeTests.theWriteItselfIsWitnessed.",
    "defects": [
      "DEF-111"
    ]
  },
  {
    "id": "CASE-0275",
    "req": "REQ-063",
    "surface": "SURF-012",
    "oracle": "effect-witness",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/PolicyStoreSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0098/new-case-arming.txt"
    ],
    "armed": true,
    "armedBy": "Sabotage 6, the same run: 'Expectation failed: (mode(store.url) -> 420) == (0o600 -> 384)' at the replace. Restored, green.",
    "witness": {
      "recorder": "the same DirectoryWitness, across the second save — the replace path rather than the create path",
      "effect": "filesystem-write",
      "count": 1
    },
    "note": "PRO-0098, DEF-111. The create path (Darwin.open with O_EXCL) and the replace path (replaceItemAt with .usingNewMetadataOnly) are different code, and only a recorder over the DIRECTORY sees both. This one also reports what a stat of policy.json structurally cannot: that no temporary outlived the call — after.files.count == 1 — and that the file's digest actually moved, so a replace that silently did nothing would fail here rather than pass on an unchanged mode. Test: PolicyFileModeTests.theReplaceIsWitnessed.",
    "defects": [
      "DEF-111"
    ]
  },
  {
    "id": "CASE-0276",
    "req": "REQ-076",
    "surface": "SURF-008",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/RestartWatchTests.swift",
      "docs/test-campaign/evidence/PRO-0098/new-case-arming.txt"
    ],
    "armed": true,
    "armedBy": "Sabotage 1: RestartWatch.observed settles on the first probe whether or not the agent answered — the defect itself. 'Expectation failed: (watch -> RestartWatch(state: .settled, probes: 1, giveUpAfterProbes: 30)).isApplying -> false' at probe 1, and '(drawn(watch, reachable: false) -> .startAgent) == nil'. Restored, green.",
    "note": "PRO-0098, DEF-132, the defect itself. Eight probes on the 2.0 s doctor cadence is sixteen seconds — an order of magnitude past the old 1.2 s clearing and an ordinary duration for a launchctl kickstart on a loaded machine. Through all eight the window draws no offer, and on the ninth probe, the one that finds the agent, the state ends. Asserted through AgentRecovery.decide rather than on a boolean, so the claim is about what the window DRAWS: ProctorUI is an executable target with no test target, and a decision living only inside AgentModel is one no test can reach. Test: RestartWatchTests.aSlowRestartIsNotAnOutage.",
    "defects": [
      "DEF-132"
    ]
  },
  {
    "id": "CASE-0277",
    "req": "REQ-076",
    "surface": "SURF-008",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/RestartWatchTests.swift",
      "docs/test-campaign/evidence/PRO-0098/new-case-arming.txt"
    ],
    "armed": true,
    "armedBy": "Sabotage 1: 'Expectation failed: (drawn(watch, reachable: false) -> .startAgent) == nil'. Restored, green.",
    "note": "PRO-0098, DEF-132, and the arming for CASE-0276 rather than a second claim. AgentRecovery.decide returns nil for several reasons, so a loop asserting nil could pass over a model that had lost the applying state altogether. This is the same call, the same unreachable agent, with the applying state gone, and it MUST draw the red — which is exactly what the fixed 1.2 s timer produced. Test: RestartWatchTests.theInstrumentReportsTheStateItIsWatchingFor.",
    "defects": [
      "DEF-132"
    ]
  },
  {
    "id": "CASE-0278",
    "req": "REQ-076",
    "surface": "SURF-008",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/RestartWatchTests.swift",
      "docs/test-campaign/evidence/PRO-0098/new-case-arming.txt"
    ],
    "armed": true,
    "armedBy": "Sabotage 1: 'Expectation failed: (watch.state -> .settled) == .abandoned'. Restored, green.",
    "note": "PRO-0098, DEF-132. The wait still ends. A restart that never comes back is abandoned after its probe budget and the window reports the agent down — which by then is a true statement rather than a guess, and is the half a naive 'clear it on the event' fix would leave hanging for ever. Test: RestartWatchTests.aRestartThatNeverLandsStopsClaimingToBeInFlight.",
    "defects": [
      "DEF-132"
    ]
  },
  {
    "id": "CASE-0279",
    "req": "REQ-076",
    "surface": "SURF-008",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/RestartWatchTests.swift",
      "docs/test-campaign/evidence/PRO-0098/new-case-arming.txt"
    ],
    "armed": true,
    "armedBy": "Sabotage 1: '(busy.state -> .settled) == .abandoned' and '(quiet.state -> .settled) == .applying'. Restored, green.",
    "note": "PRO-0098, DEF-132. The case that discriminates a count from a deadline, and the reason the brief's 'do not raise 1.2' is honoured in shape and not only in the literal. Two watches, the same wall clock, different amounts of evidence: five probes abandons, one probe does not. A deadline could not tell them apart, which is what makes swapping one stopwatch for a longer one a non-fix. Test: RestartWatchTests.theGiveUpIsEvidenceNotDuration.",
    "defects": [
      "DEF-132"
    ]
  },
  {
    "id": "CASE-0280",
    "req": "REQ-076",
    "surface": "SURF-008",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/RestartWatchTests.swift",
      "docs/test-campaign/evidence/PRO-0098/new-case-arming.txt"
    ],
    "armed": true,
    "armedBy": "Sabotage 1: '(watch -> RestartWatch(state: .settled, probes: 1, giveUpAfterProbes: 2)).isApplying -> false'. Restored, green.",
    "note": "PRO-0098, DEF-132. The edge the polling timer creates: the doctor poll runs for the app's whole life, so a watch that counted every tick would arrive at a restart already out of patience. Fifty idle probes leave it at idle with probes 0, and the restart afterwards still has its full budget. Test: RestartWatchTests.anIdleWatchIsNotAdvancedByThePoll.",
    "defects": [
      "DEF-132"
    ]
  },
  {
    "id": "CASE-0281",
    "req": "REQ-076",
    "surface": "SURF-008",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/RestartWatchTests.swift",
      "docs/test-campaign/evidence/PRO-0098/new-case-arming.txt"
    ],
    "armed": true,
    "armedBy": "Sabotage 1: '(watch.probes -> 1) == 2' and the second restart's isApplying inverting. Restored, green.",
    "note": "PRO-0098, DEF-132. Three remaining edges in one row: a second restart is not judged on the first one's probes (begin resets the count); a settled watch is not reopened by a later probe, because an agent going away afterwards is an outage rather than a continuation of a restart Proctor asked for; and a give-up count below one is floored at one rather than making every restart instant. Tests: RestartWatchTests.beginResetsTheEvidence, .theRestartEndsOnce, .theCountHasAFloor.",
    "defects": [
      "DEF-132"
    ]
  },
  {
    "id": "CASE-0282",
    "req": "REQ-076",
    "surface": "SURF-008",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/RestartWatchTests.swift",
      "docs/test-campaign/evidence/PRO-0098/delay-arming.txt"
    ],
    "armed": true,
    "armedBy": "Three sabotages, each watched red, in delay-arming.txt. 2a — 1.2 raised to 5.0: the first assertion fires. 2b — the restartWatch.observed call removed from apply(): the third fires. 2c — the whole defect back, a stored isApplying cleared inside the 1.2 s block: the middle one fires. Recorded rather than dropped: a FOURTH attempt, putting only `self?.isApplying = false` back in the block, does not compile, because isApplying is now computed over RestartWatch. That is not a failed arming, it is the structural half of the fix — the regression cannot come back one line at a time — and 2c is what it looks like when it comes back properly.",
    "source": {
      "analyzer": "String(contentsOf:) over Sources/ProctorUI/AgentModel.swift with three literal checks",
      "examined": 1
    },
    "note": "PRO-0098, DEF-132. The brief's explicit prohibition, checked against the source rather than trusted from the diff: 1.2 is NOT raised. It keeps the one job it always did correctly — the beat before the first probe, because polling immediately races launchd — and no longer clears anything. Off-ladder by construction: this reads text and runs nothing, which is why the six cases above carry the behavioural claim. Test: RestartWatchTests.theDelayIsUnchanged.",
    "defects": [
      "DEF-132"
    ]
  },
  {
    "id": "CASE-0283",
    "req": "REQ-077",
    "surface": "SURF-022",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0098/unwrap-census.md",
      "docs/test-campaign/evidence/PRO-0098/unwrap-census-raw.txt"
    ],
    "armed": true,
    "armedBy": "The denominator is armed by having moved: the same grep returns 29 lines before the change and 6 after. The 6 are the 4 sites recorded unfailable plus TWO comment lines — PRO-0090's, and one this item added, which quotes `)!` while explaining why the site beside it is unfailable. Recorded exactly rather than rounded to the 5 a first count gave, because a census whose own arithmetic drifts is the thing this case exists to rule out. A census that could not tell a site from a comment would have reported the same number twice.",
    "source": {
      "analyzer": "grep -rn ')!' Tests, recorded verbatim before any edit",
      "examined": 29
    },
    "note": "PRO-0098, DEF-136. All 28 real sites classed (29 grep lines, one of them prose in a comment): 24 live and converted, 4 unfailable by construction and left with the reason beside them and in the census. THE COUNT DRIFTED AND IT IS RECORDED: the brief counted 27, and ProctorCoreTests.swift has gained a site since. Classing is the deliverable — converting all 28 blind would have been faster and said nothing, because TimeZone(identifier: \"UTC\")! cannot fail while Toolchain.entry(for: \"cua-driver\")! can, and the second is the shape that bit. The test for unfailable is not 'it has never failed' but 'name the input space and show the failing branch is not in it'. Two shapes outside this denominator — try! and a bare ! on a property — are named in the census and carried as DEF-140 rather than swept here.",
    "defects": [
      "DEF-136"
    ]
  },
  {
    "id": "CASE-0284",
    "req": "REQ-077",
    "surface": "SURF-022",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0098/def136-arming.txt",
      "docs/test-campaign/evidence/PRO-0098/def136-arming-summary.md",
      "docs/test-campaign/evidence/PRO-0098/def136-count-reconcile.md",
      "docs/test-campaign/evidence/PRO-0098/def136-arming-rerun.txt",
      "docs/test-campaign/evidence/PRO-0098/def136-rerun-crash.txt"
    ],
    "armed": true,
    "armedBy": "This case IS the arming. Two runs of the whole suite over the same sabotage, differing only in the one converted site.",
    "note": "PRO-0098, DEF-136, the brief's proof obligation: show the conversion buys something rather than assuming it. One real regression — Toolchain.entry(for:) made to miss on the literal id \"obscura\", an entry dropped from the production catalogue — applied to Sources, then the whole suite run twice. RUN A, the site as it was (Toolchain.entry(for: tool)!): 'Fatal error: Unexpectedly found nil', 'exited with unexpected signal code 5', 'FAIL: no swift-testing verdict line in the output', and the string 'Test run with' appears ZERO times in the entire run. RUN B, the site converted to try #require: 'Test run with 1992 tests in 244 suites failed after 36.027 seconds with 4 issues', and four named failures each reporting 'Toolchain.entry(for: tool -> \"obscura\") -> nil' with the test that hit it. Both exit 1; that is not the difference and never was. The difference is 0 tests and no verdict against 1,992 and four names. Restored afterwards, git status 0 modified files. RECONCILED 2026-08-22, after a verifier reproducing this arming measured 6 issues where this row records 4. The sabotage was re-applied and the whole suite run twice more: one run gave the same verdict line, '1992 tests in 244 suites failed after 17.778 seconds with 4 issues', with the same four named tests; the other died at SIGTRAP roughly 1,850 tests in and produced no verdict at all, and did not reproduce. The 4 is structural rather than incidental - grep -n 'entry(\"obscura\")' returns exactly four call sites, #require throws so each contributes one issue, and no other caller of Toolchain.entry(for:) can reach this sabotage - so the count stands at 4. Working in def136-count-reconcile.md, both runs kept.",
    "defects": [
      "DEF-136"
    ]
  },
  {
    "id": "CASE-0285",
    "req": "REQ-055",
    "surface": "SURF-012",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/OperatorFilesWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0098/capture-write-caught.txt",
      "docs/test-campaign/evidence/PRO-0098/capture-interlock-arming.txt"
    ],
    "armed": true,
    "armedBy": "The interlock disabled at source: 'guard AuditLog.isTestProcess else {' replaced with 'guard false else {', which is exactly the state the tree was in when the sweep caught the write. Four issues, each naming the operator path it resolved to: 'Expectation failed: !((directory → \"/Users/lukerhodes/Library/Application Support/app.fledgeling.procter/captures\").hasPrefix(operatorRoot.path → \"/Users/lukerhodes/Library/Application Support/app.fledgeling.procter\") → true)' and the same for the zoom crop path '.../captures/win-1-zoom-1787361866561.png'. Restored, green. Full output in capture-interlock-arming.txt.",
    "note": "PRO-0098 gap-fix, DEF-142. The deterministic half of REQ-055's guarantee for the second path that writes without being told where. CASE-0270's sweep is a race: it catches an un-pathed capture only when the write lands between its two readings, and for weeks it did not. This case reads the resolved directory instead, so it holds on every run: in a test process CaptureEngineImpl.defaultCaptureDirectory and Session.defaultZoomPath both resolve outside the operator's root and inside CaptureEngineImpl.testFallbackCaptureRoot, and an engine handed an explicit directory still uses it — the interlock is a floor under an absent path, not an override of a present one. Recorded at outcome rather than effect-witness: it reads a path the product resolves, and no recorder watches a write. Test: OperatorFilesWitnessTests.anUnpathedCaptureIsRedirectedToo.",
    "defects": [
      "DEF-142"
    ]
  },
  {
    "id": "CASE-0290",
    "req": "REQ-079",
    "surface": "SURF-004",
    "oracle": "effect-witness",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/PeerReclaimWiringTests.swift",
      "docs/test-campaign/evidence/PRO-0093/sabotages.txt",
      "docs/test-campaign/evidence/PRO-0093/gate.txt"
    ],
    "armed": true,
    "armedBy": "Sabotage S1 removes the reclaim call from RunScheduler.acquire. The test then fails by throwing the original defect's own refusal: AgentError code queueBusy, “another session was driving this Mac, and this run was still waiting first of 1 after 0 seconds, so it gave up without running any step”. That is DEF-026 reproduced from the failing side. docs/test-campaign/evidence/PRO-0093/sabotages.txt",
    "note": "A REAL child process (/bin/sleep) takes the app lane under an identity whose key is minted the way SessionIdentity.fromPeer mints one — its pid and its proc_pidinfo start time. The child is terminated and reaped, then a second identity acquires the same lane and succeeds. The recorder is the scheduler's own snapshot: the stranded ticket is absent from active and the new one holds the lane. Nothing in the assertion is a value the test wrote into the queue.",
    "witness": {
      "recorder": "RunQueueSnapshot read back from the RunScheduler actor after the reclaim, plus SessionIdentity.liveness reading the real kernel process table through kill(2) and proc_pidinfo",
      "effect": "subprocess",
      "count": 1
    }
  },
  {
    "id": "CASE-0291",
    "req": "REQ-080",
    "surface": "SURF-004",
    "oracle": "effect-witness",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/PeerReclaimWiringTests.swift",
      "docs/test-campaign/evidence/PRO-0093/sabotages.txt"
    ],
    "armed": true,
    "armedBy": "Sabotage S3 widens the sweep from `== .gone` to `!= .unknown`, so a live peer is reclaimed too. The test then reports four failures, the first being that the second caller was granted the lane instead of being refused; eleven issues across the suite in total.",
    "note": "The twin that stops the fix cutting off a live run. The same real child process takes the lane and is left asleep for two minutes — silent, doing nothing, exactly the shape of a peer stopped at a breakpoint. The second caller is refused and the snapshot still shows the first run holding, with its own identity key.",
    "witness": {
      "recorder": "RunQueueSnapshot after a live child has held the lane through a full acquire cycle, plus the refusal the second caller actually received",
      "effect": "subprocess",
      "count": 1
    }
  },
  {
    "id": "CASE-0292",
    "req": "REQ-080",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/PeerLivenessTests.swift",
      "docs/test-campaign/evidence/PRO-0093/sabotages.txt"
    ],
    "armed": true,
    "armedBy": "Sabotage S2 maps an unreadable probe to `.gone`. PeerLivenessTests “a process that cannot be described is unknown, so its lane is left alone” goes red: (verdict → .gone) == .unknown.",
    "note": "The mapping from a probe to a verdict, proved without a process so the pid-reuse row can exist at all: a pid that is running with a DIFFERENT start time reads gone, which is the case a live-process test cannot construct. Seven rows, one per way the answer could be wrong, including the two zero-start-time rows where two failures agreeing must not read as a match."
  },
  {
    "id": "CASE-0293",
    "req": "REQ-080",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/PeerLivenessTests.swift"
    ],
    "armed": true,
    "armedBy": "The probe is instrumented in the test itself: the unparseable-key row asserts the probe closure was called zero times, so a change that made `unattributed` parse would redden it rather than quietly cost a syscall per active run on every acquire.",
    "note": "The identity every caller lands on when the kernel will not describe its process is `unattributed`, and it is also the identity every other test in this package acquires a lane under. If it parsed, this feature's population would be the suite itself. Asserted as no-parse, no-probe, no-reclaim."
  },
  {
    "id": "CASE-0294",
    "req": "REQ-081",
    "surface": "SURF-005",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/TakeoverWiringTests.swift",
      "docs/test-campaign/evidence/PRO-0093/sabotages.txt",
      "docs/test-campaign/evidence/PRO-0093/gate.txt"
    ],
    "armed": true,
    "armedBy": "Three independent sabotages redden the same test. S5 removes the reading after the last step and yields is nil. S7 binds the block to nothing: userInputs 0 and yields nil. S4 cuts the arrival clause from ContentionWatch.conditions. Each cut is a different link in the chain and each is proved load-bearing.",
    "note": "The case DEF-027 measured and that could never have fired. A one-step batch whose block swallows three events during the step now reports a YieldRecord with reason userInput and the prose note beside it. The field measurement is docs/test-campaign/evidence/witness/a45b-act.json — one drag step of 74.7s, six swallowed, no yields key."
  },
  {
    "id": "CASE-0295",
    "req": "REQ-081",
    "surface": "SURF-005",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/TakeoverWiringTests.swift"
    ],
    "armed": true,
    "armedBy": "The control for CASE-0294, in the same suite: same batch, same probe, nothing swallowed, and the assertion is that yields is ABSENT. A fix that recorded a yield for every run would pass CASE-0294 and fail this.",
    "note": "Separates the fix from the probe. The new reading after the last step has to report the person and not itself."
  },
  {
    "id": "CASE-0296",
    "req": "REQ-081",
    "surface": "SURF-005",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/ContentionTests.swift",
      "Tests/ProctorAgentTests/TakeoverWiringTests.swift",
      "docs/test-campaign/evidence/PRO-0093/sabotages.txt"
    ],
    "armed": true,
    "armedBy": "Sabotage S4 removes `if s.userInputSince { out.insert(.userInput) }`. Six issues across the two suites, including the paired control which asserts the SAME timestamp without the flag holds nothing — so the assertion is measuring the flag rather than the window.",
    "note": "The staleness half. A swallow 890 seconds old holds when it is carried as an arrival, does not hold on the window alone, and is released two seconds later once the flag is spent. Three samples over one watch, which is a relation between readings rather than a single value. Field measurement: docs/test-campaign/evidence/witness/a4-act.json, three steps of 18.6/20.2/17.7s against a 10-second window with 24 events swallowed and no yield."
  },
  {
    "id": "CASE-0297",
    "req": "REQ-081",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/TakeoverWiringTests.swift",
      "docs/test-campaign/evidence/PRO-0093/sabotages.txt"
    ],
    "armed": true,
    "armedBy": "Sabotage S6 removes runControl.release from disarmContention. Three issues: isYielded true, isPaused true, and heldBy still naming the finished run with its full HoldAttribution.",
    "note": "DEF-150. A run that ends while yielded leaves the latch clear, so heldBy stops naming a run that has finished and pausedAt is cleared. Reachable before this item; made common by the new reading after the last step, which is why it is closed in the same change."
  },
  {
    "id": "CASE-0298",
    "req": "REQ-079",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/PeerReclaimWiringTests.swift",
      "docs/test-campaign/evidence/PRO-0093/sabotages.txt"
    ],
    "armed": true,
    "armedBy": "Sabotage S3 (reclaim anything that is not unknown) reddens the two-run sweep: the live run's lane is taken and the snapshot comes back empty where it should hold one.",
    "note": "The sweep is selective. Two runs on two app lanes, one peer killed between the acquire and the sweep; reclaimDeadPeers returns exactly the dead ticket and the snapshot holds exactly the live one. The liveness is flipped AFTER both acquires because acquire itself sweeps — a test that started a peer dead would never reach the sweep it means to measure."
  },
  {
    "id": "CASE-0299",
    "req": "REQ-079",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/PeerReclaimWiringTests.swift"
    ],
    "armed": true,
    "armedBy": "The same S3 cut: with the sweep widened, the waiter's acquire is granted for the wrong reason and the assertions on waitingCount and on the granted ticket both fail.",
    "note": "A run already in the line is woken by the reclaim rather than left to time out. The reclaim goes through forceRelease, which publishes and promotes, so the waiter is granted the lane the dead peer was holding — asserted on waitingCount going 1 to 0 and on the snapshot naming the granted ticket."
  },
  {
    "id": "CASE-0310",
    "req": "REQ-082",
    "surface": "SURF-009",
    "flow": "FLOW-010",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/WalkthroughFlowTests.swift",
      "Sources/ProctorCore/WalkthroughFlow.swift",
      "docs/test-campaign/evidence/PRO-0086/arming.json"
    ],
    "armed": true,
    "armedBy": "ARM-1: the biconditional's own guard removed from primaryDisabledReason, so a reason came back in every state. `./scripts/test.sh --filter aReasonExistsExactlyWhereThePrimaryRefuses --filter theReasonNamesTheMissingGrantAndTheNextAction` exited 1 with 'Test run with 2 tests in 1 suite failed ... with 10 issues', the first six reading ((reason == nil) -> false) == (enabled -> true). Restored, green.",
    "note": "DEF-160. A reason exists exactly where the primary refuses, asked at all sixteen combinations of step x accessibility x screenRecording. Written as one biconditional rather than two lists because both directions are defects: a refusing state with no sentence is the failure the item exists to remove, and a sentence beside a button a person can press is the same defect wearing the other face. THE POPULATION IS PRINTED AS A SET and it is A3's population — permissions/false/false, permissions/false/true, permissions/true/false — the three states PRO-0081 created. Every one now carries a sentence. Value-level: this says the rule answers correctly, not that a window drew the answer. CASE-0312 binds the drawing site and CASE-0316 is the rendered claim."
  },
  {
    "id": "CASE-0311",
    "req": "REQ-082",
    "surface": "SURF-009",
    "flow": "FLOW-010",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/WalkthroughFlowTests.swift",
      "docs/test-campaign/evidence/PRO-0086/arming.json"
    ],
    "armed": true,
    "armedBy": "ARM-2: the first move taken from `missing.last` instead of from prominentGrant, so the caption nominated Screen Recording while the filled Allow button stayed on Accessibility. One expectation failed on the exact sentence. Restored, green.",
    "note": "DEF-160, and the coherence clause PRO-0090 makes necessary. The sentence names every grant still missing, names none that is already held, names Copy.allow — the row button's own word, so the instruction points at a control on screen — and nominates the grant prominentGrant draws filled. WHICH ASSERTION ACTUALLY FIRES: the three sentences written out verbatim, and only those. The `reason.contains(prominent.title)` agreement clause reads as the coherence guard and is tautological — the reason names every missing grant, and prominentGrant returns one of the missing grants, so the containment holds whichever missing grant the caption nominates. ARM-2's mutation (the first move taken from `missing.last`) passes it, and the one issue that run recorded was the verbatim `“… Start with Accessibility.”` expectation. The agreement clause is kept as a statement of the rule; the literal is the guard, and a reword is a decision somebody makes."
  },
  {
    "id": "CASE-0312",
    "req": "REQ-082",
    "surface": "SURF-009",
    "flow": "FLOW-010",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/WalkthroughFlowTests.swift",
      "Sources/ProctorUI/Walkthrough.swift",
      "docs/test-campaign/evidence/PRO-0086/arming.json"
    ],
    "armed": true,
    "armedBy": "ARM-3 removed the caption's accessibilityIdentifier and ARM-4 removed the hint on the disabled button; each ran `./scripts/test.sh --filter theFooterDrawsTheReason` and each exited 1 with one issue. Two separate mutations because one assertion passing does not arm the other.",
    "note": "DEF-160's rendered half, on the rung it actually stands on. The drawing site is read from disk with comments stripped and asserted to reference WalkthroughFlow.primaryDisabledReason, to carry ID.reason so a glass lane can find the sentence, and to hand the same string to the disabled button as an accessibility hint. THIS IS source-analysis AND CLAIMS NOTHING ABOVE IT: it says the view is bound to the Core rule, not that a window drew the sentence. There is no ProctorUI test target and no window server inside swift test; the rendered claim is CASE-0316.",
    "source": {
      "analyzer": "WalkthroughFlowTests.theFooterDrawsTheReason, reading Sources/ProctorUI/Walkthrough.swift with whole-line comments stripped by the suite's own withoutComments",
      "examined": 354
    }
  },
  {
    "id": "CASE-0313",
    "req": "REQ-083",
    "surface": "SURF-009",
    "flow": "FLOW-010",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/WalkthroughFlowTests.swift",
      "Sources/ProctorUI/Walkthrough.swift",
      "docs/test-campaign/evidence/PRO-0086/arming.json"
    ],
    "armed": true,
    "armedBy": "ARM-6 put the primary's own rule onto Skip setup — the lockout in its exact shape. `./scripts/test.sh --filter skipIsNeverClosed` exited 1 with two issues: (disables -> 2) == 1, and the ordering clause (primary.lowerBound -> 3014[utf8]) < (disabled.lowerBound -> 2713[utf8]). Restored, green.",
    "note": "DEF-160 A4, REQ-083. The door out, read as a count rather than as a reading: exactly one `.disabled(` in Walkthrough.swift and it is the primary's, established by ordering against the identifier rather than by trusting the count alone. A `.disabled` on Skip or on Back would make the refusal a trap and lands here. No code changed for this clause — Skip was already ungated — so what this case records is a guard placed under behaviour that was correct and unprotected. THIS IS THE ABSENCE-OF-`.disabled` HALF ONLY. Presence is CASE-0319: a verifier removed Skip in exactly the three refusing states by wrapping it in the primary's rule, and this case's count, ordering and file-level `#require(range(of: \"WalkthroughFlow.ID.skip\"))` all stayed green, because a substring in a file says nothing about a state.",
    "source": {
      "analyzer": "WalkthroughFlowTests.skipIsNeverClosed, counting `.disabled(` over the comment-stripped Walkthrough.swift and ordering it against WalkthroughFlow.ID.primary",
      "examined": 354
    }
  },
  {
    "id": "CASE-0319",
    "req": "REQ-083",
    "surface": "SURF-009",
    "flow": "FLOW-010",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/WalkthroughFlowTests.swift",
      "Sources/ProctorCore/WalkthroughFlow.swift",
      "Sources/ProctorUI/Walkthrough.swift",
      "docs/test-campaign/evidence/PRO-0086/arming.json"
    ],
    "armed": true,
    "armedBy": "ARM-7 wrapped Skip in the primary's own rule — `if showsSkip(…), primaryEnabled(…)`, the door out removed in exactly the three refusing states with no `.disabled` added anywhere, which is the verifier's own mutation. `./scripts/test.sh --filter skipIsOfferedInEveryRefusingState|skipIsNeverClosed|aRevocationIsNotALockout` exited 1 with 1 issue, on the condition literal; `skipIsNeverClosed` passed in that same run. ARM-8 then put the lockout into the Core rule instead and this test recorded 6 issues — three on `(shows → false) == ((step != .connect) → true)` and three naming the missing refusing states in the offered set. Both restored, green.",
    "note": "DEF-160 A4, REQ-083 — the presence half, which had no guard until this. The clause is 'Skip is present in every refusing state' and it was asserted by a whole-file `#require(range(of: \"WalkthroughFlow.ID.skip\"))`, a file-level existence check standing in for a per-state claim. Asked here in two halves neither of which covers the other: the rule is `WalkthroughFlow.showsSkip`, asked at all sixteen states with the three refusing ones named rather than counted, and the view's condition is then read verbatim with whitespace collapsed and must be that rule with no second clause. A grant-gated lockout lands in the first half; a clause added to the view's `if` lands in the second. THIS IS source-analysis AND CLAIMS NOTHING ABOVE IT: it says the view's condition is the Core rule, not that a window drew the button. There is no ProctorUI test target and no window server inside swift test; the rendered claim is CASE-0316.",
    "source": {
      "analyzer": "WalkthroughFlowTests.skipIsOfferedInEveryRefusingState, enumerating WalkthroughFlow.showsSkip at all sixteen states and reading the condition immediately above Button(WalkthroughFlow.Copy.skip) from the comment-stripped Walkthrough.swift",
      "examined": 354
    }
  },
  {
    "id": "CASE-0314",
    "req": "REQ-083",
    "surface": "SURF-009",
    "flow": "FLOW-010",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/WalkthroughFlowTests.swift",
      "docs/test-campaign/evidence/PRO-0086/arming.json"
    ],
    "armed": true,
    "armedBy": "ARM-8: `showsSkip` made to read the grants it takes — `step != .connect && primaryEnabled(…)`, the lockout written into the rule rather than into the view. `./scripts/test.sh --filter aRevocationIsNotALockout|skipIsOfferedInEveryRefusingState` exited 1; this test recorded 2 issues, one per revoked direction: `WalkthroughFlow.showsSkip(on: .permissions, accessibility: (ax → false), screenRecording: (sr → true))` and the same with the grants swapped. Restored, green. ARM-1 IS NOT THIS CASE'S ARMING AND THE PREVIOUS RECORD SAYING SO WAS WRONG: with the biconditional's guard removed, `primaryDisabledReason(.permissions, true, true)` still returns nil through the empty-missing path, so this test's `== nil` expectation holds either way — and this test was not in ARM-1's own filters, so it never ran in that session. A verifier applied ARM-1 with this test in the filter and watched it pass.",
    "note": "DEF-160 A5. A grant taken away while the walkthrough is open refuses the primary again — and the person is told why and can still leave. Asked from both-granted in both directions, with `completes(.skipped)` and `showsSkip` asserted at each revoked state. `!Copy.skip.isEmpty` stood where `showsSkip` now does and was a dead predicate: a non-empty string constant is false in no tree anybody could write, so it read pass under every mutation and counted nothing towards the clause it was listed under. Replaced rather than dropped, because the claim it was standing in for — the way out is still offered in this state — is real and is now asked of the rule the view's condition reads. WHAT THIS DOES NOT ASK: whether the agent re-probes a revoked permission at all, which is brief 75's question and needs a measurement first (PRO-0036 child item 6). This case is about what the flow does once the grant reads false, not about how soon it reads false."
  },
  {
    "id": "CASE-0315",
    "req": "REQ-084",
    "surface": "SURF-009",
    "flow": "FLOW-010",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/WalkthroughFlowTests.swift",
      "Sources/ProctorUI/Walkthrough.swift",
      "docs/test-campaign/evidence/PRO-0086/arming.json"
    ],
    "armed": true,
    "armedBy": "ARM-5 repointed the Text at Copy.heroLede — DEF-161's exact shape, a constant that exists while the window draws something else. `./scripts/test.sh --filter theSheetDrawsTheRestartNote` exited 1 with one issue. Restored, green.",
    "note": "DEF-161, REQ-084. statesRestartNote is true while Screen Recording is missing and false once it is held, which is the pane the design of record draws and the pane it omits. The drawing site is bound to Copy.restartNote by name. PRO-0067's A5 asserted only that the constant contained the word 'restart'; that assertion could not have failed on a build that rendered the sentence nowhere, which is how DEF-161 survived from PRO-0067 to here. The rendered claim is CASE-0316.",
    "source": {
      "analyzer": "WalkthroughFlowTests.theSheetDrawsTheRestartNote, reading the comment-stripped Walkthrough.swift for Copy.restartNote and statesRestartNote at the drawing site",
      "examined": 354
    }
  },
  {
    "id": "CASE-0317",
    "req": "REQ-082",
    "surface": "SURF-009",
    "flow": "FLOW-010",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/WalkthroughFlowTests.swift",
      "docs/test-campaign/cases.json",
      "docs/test-campaign/evidence/PRO-0086/arming.json"
    ],
    "armed": true,
    "armedBy": "ARM-1, which is the same run: with the guard removed the printed population read all sixteen states rather than three, so the set assertion this case rests on failed naming them.",
    "note": "A3 RE-DERIVED, WITH ITS PROVENANCE ON THE ROW. PRO-0067's A3 reads 'the disabled next button is present in the tree in every state where it is disabled'. WHEN IT WAS WRITTEN THAT CLAUSE WAS VACUOUS: no `.disabled(` modifier existed anywhere in Walkthrough.swift, the set of states in which the primary was disabled was empty, and the clause asked as written would have read green having measured nothing. PRO-0081 BUILT THE POPULATION AND THEN WITNESSED THE CLAUSE OVER IT — the verifier named that a widening, and CASE-0100 records it in its own words. CASE-0100 is not edited by this item and its status is not changed. What is added here is the honest re-derivation: the population now genuinely exists, it is exactly three states, they are printed as a set rather than implied, and each of them now carries a stated reason. A READER SIX MONTHS OUT SHOULD TAKE THIS ROW AS: the guarantee held, and it was arranged before it held. Those are different facts and the campaign carries both."
  },
  {
    "id": "CASE-0316",
    "req": "REQ-082",
    "surface": "SURF-009",
    "flow": "FLOW-010",
    "oracle": "effect-witness",
    "lane": "macos-glass",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0086/glass-tree-read.json",
      "docs/test-campaign/evidence/PRO-0086/walkthrough-permissions-reason.png",
      "docs/test-campaign/evidence/PRO-0086/walkthrough-permissions-reason-key.png"
    ],
    "armed": true,
    "armedBy": "TWO-WAY ON ONE BUILD, one AXPress apart. On the intro step the same reader read the same identifier as enabled TRUE, label 'Set up permissions', frame 140x24, with NO node carrying proctor.walkthrough.action.primary.reason anywhere in the tree. On the permissions step: enabled FALSE, label 'Connect a model', frame 127x24, reason node present at 441x14. A pinned field or an unconditional caption could not have produced that pair.",
    "note": "DEF-160 and DEF-161 on the glass, which is the rung CASE-0312 and CASE-0315 explicitly do not claim. A release .build/Proctor.app ran as pid 491 with PROCTOR_SOCKET naming nothing, so its report is nil and every grant reads false — the state the design draws disabled, on a machine that holds the real grants. The walkthrough was forced open with -walkthroughCompleted NO in NSArgumentDomain, which is PROCESS-SCOPED AND WRITES NOTHING: the shared defaults key read 1 before the launch and 1 after it, and /Applications/Proctor.app was neither modified nor launched. THE READER IS THE INSTALLED proctor-agent, pid 52976, a different process sharing no memory with the target, resolving by AXIdentifier over AXUIElement. It read: primary AXButton 'Connect a model' ENABLED FALSE at 127x24 carrying help 'Allow Accessibility and Screen Recording above to continue. Start with Accessibility.'; the same sentence as an AXStaticText at proctor.walkthrough.action.primary.reason, 441x14, drawn above the control it explains; proctor.walkthrough.permissions.restart carrying Copy.restartNote verbatim at 403x28; and proctor.walkthrough.action.skip ENABLED TRUE at 64x16 beside the refusal, which is REQ-083 measured rather than reasoned. AXEnabled is what the app publishes about itself over the plane Proctor's own tools act on; it is not a kernel-level witness and is not claimed as one.",
    "witness": {
      "recorder": "the installed proctor-agent (pid 52976) reading pid 491's accessibility tree by AXIdentifier with its own AX client",
      "effect": "ipc",
      "count": 4
    }
  },
  {
    "id": "CASE-0318",
    "req": "REQ-082",
    "surface": "SURF-009",
    "flow": "FLOW-010",
    "oracle": "raster-visual",
    "lane": "macos-glass",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0086/walkthrough-permissions-reason.png",
      "docs/test-campaign/evidence/PRO-0086/walkthrough-permissions-reason-key.png",
      "docs/test-campaign/evidence/PRO-0086/glass-tree-read.json"
    ],
    "armed": true,
    "armedBy": "The two frames differ in the one property being asked about and agree on everything else: inactive, both Allow buttons draw the same grey; key, Accessibility's draws filled accent and Screen Recording's stays plain. A capture that could not tell the two treatments apart would have shown the same pair twice.",
    "note": "THE COHERENCE CLAUSE, IN PIXELS. Brief 79 asks whether this item's caption and PRO-0090's one-prominent-Grant rule say something coherent about what to do first. They do: the caption reads 'Start with Accessibility' and the filled Allow button is Accessibility's row, with Screen Recording's plain beside it. Both frames are ScreenCaptureKit, window-scoped on cgWindowID 152808, status complete, trustworthy true, dirtyArea 0, framesWaited 3 and 12 — the window's own pixels rather than a screen crop. WHAT THE PICTURES ALSO SHOW, and it is not this item's fix: with the window key, the DISABLED primary still draws filled accent with a dimmed label, while the design of record draws that same control as a plain button (class=\"btn\" disabled, against class=\"btn prominent large\" on the intro's enabled one). A filled accent button that cannot be pressed reads as pressable. Recorded as DEF-163 and left open."
  },
  {
    "id": "CASE-0320",
    "req": "REQ-055",
    "surface": "SURF-012",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/OperatorFilesWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0098/flow-write-caught.txt",
      "docs/test-campaign/evidence/PRO-0098/flow-interlock-arming.txt"
    ],
    "armed": true,
    "armedBy": "The interlock disabled at source: 'guard AuditLog.isTestProcess else {' replaced with 'guard false else {', which is exactly the state the tree was in when the sweep caught the write â FlowStore.directory had no seam at all. Four issues, each naming the operator's real path: 'Expectation failed: !((FlowStore.directory.path → \"/Users/lukerhodes/Library/Application Support/app.fledgeling.procter/flows\").hasPrefix(FlowStore.operatorDirectory.path → \"/Users/lukerhodes/Library/Application Support/app.fledgeling.procter/flows\") → true)'; '(FlowStore.directory.path → \".../app.fledgeling.procter/flows\").hasPrefix(FlowStore.testFallbackFlowRoot.path → \"/var/folders/b3/hwz3nk4d1kz8x6mzk7qrbpsc0000gn/T/proctor-test-flows-94858\")'; the same against operatorRoot; and the write itself, '!((recorded.path → \".../app.fledgeling.procter/flows/login-flow.json\").hasPrefix(operatorRoot.path → \".../app.fledgeling.procter\") → true)'. 'Test run with 6 tests in 1 suite failed after 4.368 seconds with 4 issues', exit 1. Restored, green: 'Test run with 6 tests in 1 suite passed after 5.414 seconds', exit 0. Full output in flow-interlock-arming.txt.",
    "note": "PRO-0098 re-verification, DEF-164. The deterministic half of REQ-055's guarantee for the THIRD path that writes without being told where, after PolicyStore (DEF-110) and the capture directory (DEF-142). CASE-0270's sweep is a race and on the run that motivated this case it lost: the flows were written at 11:57:28 and the sweep read either side of that, so the witness reported zero while two of the operator's files were being overwritten. This case reads the resolved path instead, so it holds on every run: in a test process FlowStore.directory resolves outside FlowStore.operatorDirectory and outside the operator's root, inside FlowStore.testFallbackFlowRoot, and the file a suite actually records — login-flow.json — resolves outside the operator's root too. FlowStore.operatorDirectory stays truthful so the case names what it asserts the absence of rather than comparing two redirected paths and agreeing with itself. Recorded at outcome rather than effect-witness: it reads a path the product resolves, and no recorder watches a write. Test: OperatorFilesWitnessTests.aRecordedFlowIsRedirectedToo.",
    "defects": [
      "DEF-164"
    ]
  },
  {
    "id": "CASE-0330",
    "req": "REQ-055",
    "surface": "SURF-012",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "defects": [
      "DEF-172"
    ],
    "evidence": [
      "Tests/ProctorAgentTests/OperatorFilesWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0099/witness-arming.txt",
      "docs/test-campaign/evidence/PRO-0099/diversion-positive.txt",
      "docs/test-campaign/evidence/PRO-0099/sabotage-production-unchanged.txt",
      "docs/test-campaign/evidence/PRO-0099/operator-census-after.tsv"
    ],
    "armed": true,
    "armedBy": "Both arms over one call, plus a WRITE arm armed by the gap-fix. Path arm: watched failing with `guard false` forcing the production branch. Write arm (`touched.isEmpty` over the operator's root): armed by sabotaging the DIVERT TARGET rather than the predicate — the interlock left fully intact, `SwitchStore.save(saved, to: SwitchStore.url(home: decoyHome))` driven against a decoy $TMPDIR home laid out by the product's own builder, and the same reader reporting ['settings/settings.json'] over it, denominator 1. Watched failing with DirectoryWitness.read blinded: OperatorFilesWitnessTests.swift:372, `(decoyChanged -> []) == [\"settings/settings.json\"]`, run exit 1. The arm also asserts TestProcess.isActive, so an arming that ran with the interlock off fails rather than claiming a diversion it did not witness.",
    "note": "The sharpest of the four in this sweep. SwitchStore.defaultURL is the FOURTH static computing a path under the operator's application-support root with no injection seam, after PolicyStore (DEF-042/110), the capture directory (DEF-142) and FlowStore (DEF-164) — and the first found by sweeping the class rather than by accident. AgentModel saves through it whenever a switch is toggled and Session.doctor and the agent's main.swift LOAD through it, so before this a suite that reached it changed what the agent is allowed to do on the machine of whoever ran the tests. Both halves measured on one run: the operator's root byte-identical (0 of 3,330 files changed) and settings.json present in proctor-test-settings-<pid> under $TMPDIR. There is no settings.json under the operator's root on this machine at all — the settings/ directory exists and is empty — so the interlock prevented a CREATION rather than an overwrite, which would have handed the agent a saved switch set the operator never chose."
  },
  {
    "id": "CASE-0331",
    "req": "REQ-055",
    "surface": "SURF-012",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "defects": [
      "DEF-173"
    ],
    "evidence": [
      "Tests/ProctorAgentTests/OperatorFilesWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0099/witness-arming.txt",
      "docs/test-campaign/evidence/PRO-0099/diversion-positive.txt",
      "docs/test-campaign/evidence/PRO-0099/sabotage-production-unchanged.txt",
      "docs/test-campaign/evidence/PRO-0099/operator-census-after.tsv"
    ],
    "armed": true,
    "armedBy": "As CASE-0330. Write arm armed over the SAME call: `Session.maestroDebugDirectory(run: 1)` swept over `Session.testFallbackMaestroRoot`, the root the interlock itself diverts to, reporting the `run-<stamp>-1-<salt>/` directory appearing — denominator 1. Watched failing with DirectoryWitness.read blinded: OperatorFilesWitnessTests.swift:426, `(divertedChanged -> []).contains(\"run-1787376776385-1-ca19/\")`. Arming this found a DEAD ASSERTION: the whole effect of this call is a mkdir and DirectoryWitness recorded regular files only, so the operator-root claim could not have come out any other way. The witness now records directories and the claim is read through changedEntries.",
    "note": "maestroDebugDirectory(run:) creates its directory unconditionally on every call and runFlow calls it once per run, so a suite that reached a maestro run left a run-<stamp>-<n>-<salt> directory in the operator's own tree and Maestro then wrote its per-command records into it. The whole-root census could NOT have caught this on its own: it counts `find -type f`, and an empty run- directory is invisible to it. So the operator's maestro root was read directly — 17 stray run- directories, newest 2026-08-20 12:31, and `find -maxdepth 1 -type d -newermt \"2026-08-22 13:00\"` returns nothing across both full runs. The 17 strays are left where they are; deleting them is the act REQ-055 forbids."
  },
  {
    "id": "CASE-0332",
    "req": "REQ-055",
    "surface": "SURF-012",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "defects": [
      "DEF-174"
    ],
    "evidence": [
      "Tests/ProctorAgentTests/OperatorFilesWitnessTests.swift",
      "docs/test-campaign/evidence/PRO-0099/witness-arming.txt",
      "docs/test-campaign/evidence/PRO-0099/diversion-positive.txt",
      "docs/test-campaign/evidence/PRO-0099/sabotage-production-unchanged.txt",
      "docs/test-campaign/evidence/PRO-0099/operator-census-after.tsv"
    ],
    "armed": true,
    "armedBy": "As CASE-0330, and watched failing twice — once by its own first draft (an assertion written against the wrong filename spelling) and once by sabotage. Write arm armed over a decoy $TMPDIR home carrying the captures directory and this call's own returned frame name, composed as production composes them — denominator 2 changed entries. Watched failing with DirectoryWitness.read blinded: OperatorFilesWitnessTests.swift:513 `(decoyChanged -> []).contains(\"captures/\")` and :515 `decoyChanged.contains { $0.hasPrefix(\"captures/ios-pro-0099-before-\") }`. The operator-root claim is now read through changedEntries for the same reason as CASE-0331.",
    "note": "The half of DEF-142 left open. DEF-142 closed CaptureEngineImpl.defaultCaptureDirectory, the Mac lane into the operator's captures directory, and left the iOS lane writing into the SAME directory: deviceFramePath creates it unconditionally and three callers — a screenshot with no path, a settle sample and a maestro before/after frame — write a PNG to the name it returns. It diverts to CaptureEngineImpl.testFallbackCaptureRoot rather than to a root of its own, because in production the two land in the same directory and a diverted pair that did not would be a difference the test process invented."
  },
  {
    "id": "CASE-0333",
    "req": "REQ-085",
    "surface": "SURF-012",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/operator_path_gate.py",
      "scripts/campaign/operator_paths.json",
      "scripts/campaign/test_instruments.py",
      "docs/test-campaign/evidence/PRO-0099/gate-arming.txt"
    ],
    "armed": true,
    "armedBy": "Both directions on fixture trees in test_instruments.py: a Sources/ file declaring NewStore.defaultURL under the operator root with NO manifest entry produces exactly one finding naming 'NewStore.defaultURL', and the same tree with an entry produces none. THE GAP-FIX ADDED THREE MORE SHAPES the verifier proved cleared both modes: a `Legacy.operatorURL` declared beside a bare-classed `SwitchStore.operatorURL` (both sites now refused until the manifest disambiguates); and a path COMPOSED as `SwitchStore.url(home: FileManager.default.homeDirectoryForCurrentUser)`, which names no root literal at all and is exactly how the real accessor is written — now caught by a second census sweep over `home_expressions`, with the literal sweep asserted blind to it in the same fixture so the second sweep is shown doing the work rather than duplicating it. Each clears once classed.",
    "note": "The census half of operator_path_gate.py, in two sweeps. Every non-comment line in Sources/ naming a path under the operator's application-support root must map to an entry, AND so must every line handing one of `home_expressions` to a `parameterised` builder. On this tree: 12 literal + 1 composed = 13 sites, 15 entries classed, exit 0. The ambiguity rule found a real collision on its first run — Wire.swift was classed on a bare `socketPath` that DoctorReport also declares, and the entry is now Wire.socketPath."
  },
  {
    "id": "CASE-0334",
    "req": "REQ-085",
    "surface": "SURF-012",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/operator_path_gate.py",
      "scripts/campaign/operator_paths.json",
      "scripts/campaign/test_instruments.py",
      "docs/test-campaign/evidence/PRO-0099/gate-arming.txt"
    ],
    "armed": true,
    "armedBy": "Five trip/clear fixture pairs in test_instruments.py, all in one run: a writer-seam whose body carries no test-process predicate is refused and the same body carrying one clears; an operator-accessor whose seam does not branch is refused; a seam that branches but never returns the truthful accessor is refused; another file reaching the truthful accessor around its seam is refused; and a bare ProctorReflector.start() under Tests/ is refused while the same call with an explicit path clears. A predicate named only in a doc comment cannot satisfy a seam. THE GAP-FIX ADDED A SIXTH: a QUALIFIED reach (`SwitchStore.operatorURL`) from a file that also declares a member of that name, which the first version waved through because it skipped any such file whole. Refused now, while a file reaching only its own bare member still clears.",
    "note": "The seams half. Each manifest entry's own requirement by class: operator-accessor needs a seam that branches, returns it, and is not bypassed elsewhere in Sources/; writer-seam needs the predicate in its own body; parameterised needs the named parameter really in its signature; socket and prose need a reason and no seam."
  },
  {
    "id": "CASE-0335",
    "req": "REQ-085",
    "surface": "SURF-012",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "defects": [
      "DEF-170",
      "DEF-171"
    ],
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "scripts/campaign/operator_path_gate.py",
      "docs/test-campaign/evidence/PRO-0099/gate-arming.txt"
    ],
    "armed": true,
    "armedBy": "The regression fixture IS the arming, and it reproduces the shape both bugs came from: an outer type declaring a member, and a nested type that CLOSES above the site declaring a member of the same name. Watched in both directions — census_sites resolves the operator literal to 'AuditLog.directory' (with the bug it resolved to 'Seams.directory'); the manifest entry naming the containing type clears while the entry naming the closed nested type is refused; declaration_body('AuditLog.directory') returns a body containing the predicate (with the bug it returned Seams' unguarded stored property); and the same fixture with the predicate stripped from the writer goes red.",
    "note": "The gate's own name resolution, which arming found broken in two places before the gate had ever been trusted. Both bugs were FALSE REDS — the direction that gets argued away rather than fixed — and both were in PolicyStore.swift, whose AuditLog encloses a 'final class Seams' declaring 'directory' alongside AuditLog's own 'directory'."
  },
  {
    "id": "CASE-0350",
    "req": "REQ-088",
    "surface": "SURF-008",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/arming.json"
    ],
    "armed": true,
    "note": "A1. A real session.doctor() on a fake machine that is MISSING /usr/bin/shortcuts — the only condition the old append fired under — carries no Shortcuts CLI in grants, misfiledTools is empty, and permissions(in:) covers the whole list. The fixture asserts !shortcutsCLIAvailable first, so a report that never reached the case cannot pass it. Tests/ProctorAgentTests/StatusWindowDebtWiringTests.swift",
    "armedBy": "Restored the deleted `grants.append(.init(name: \"Shortcuts CLI\" …))` in SessionDoctor.swift and watched this test fail with 3 issues, exit 1. docs/test-campaign/evidence/pro-0082/arming.json",
    "defects": [
      "DEF-181"
    ]
  },
  {
    "id": "CASE-0351",
    "req": "REQ-088",
    "surface": "SURF-008",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/arming.json"
    ],
    "armed": true,
    "note": "A1. Every grant a built report carries classifies as a permission, walked on BOTH kinds of Mac — CLI present and CLI absent — so the clause is a property of the report rather than of this machine. Tests/ProctorAgentTests/StatusWindowDebtWiringTests.swift",
    "armedBy": "Same mutation as CASE-0350; the restored append reddens this test on the absent-CLI branch. docs/test-campaign/evidence/pro-0082/arming.json",
    "defects": [
      "DEF-181"
    ]
  },
  {
    "id": "CASE-0352",
    "req": "REQ-088",
    "surface": "SURF-008",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
    ],
    "armed": true,
    "note": "A1. Removing the row did not remove the knowledge: shortcutsCLIAvailable still reports false on the absent Mac and true on the present one, and StatusChecks.toolRows composes a Shortcuts row with tone .bad from it — a tool row in the tools card, where a program on a disk belongs. Tests/ProctorAgentTests/StatusWindowDebtWiringTests.swift",
    "defects": [
      "DEF-181"
    ],
    "armedBy": "Set the missing-CLI tools row to `tone: .good` in StatusChecks.shortcutsRow and watched theBooleanStillCarriesTheFact fail — (shortcuts?.tone → .good) == .bad — 1 issue, exit 1. docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
  },
  {
    "id": "CASE-0353",
    "req": "REQ-088",
    "surface": "SURF-017",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/arming.json"
    ],
    "armed": true,
    "note": "A2. A report in the shape an agent OLDER than PRO-0082 sends — five grants including Shortcuts CLI — reaches TUISurface.readiness and the pane draws the four permissions in the report's order with their states intact and no tool. The fixture is re-read and asserted to still carry the row, so a fixture that lost it cannot pass this. The lanes list is asserted unchanged in the same suite, because a guard written one line too high would empty both loops and every other assertion would still hold. Tests/ProctorCoreTests/TUIReadinessPartitionTests.swift",
    "armedBy": "Deleted `guard StatusChecks.kindIsPermission(name) else { continue }` from TUISurface.readiness and watched this test fail with 3 issues, exit 1. docs/test-campaign/evidence/pro-0082/arming.json",
    "defects": [
      "DEF-181"
    ]
  },
  {
    "id": "CASE-0354",
    "req": "REQ-088",
    "surface": "SURF-017",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
    ],
    "armed": true,
    "note": "A2. Every name in StatusChecks.known is put through all three spellings of the partition — kindIsPermission(name), permissions(in:) over a Grant value, and a real readiness() render — and the three answers must agree. A surface holding its own list is how PRO-0036's fix covered one reader and not the others. Tests/ProctorCoreTests/TUIReadinessPartitionTests.swift",
    "defects": [
      "DEF-181"
    ],
    "armedBy": "Replaced StatusChecks.permissions(in:)'s `resolvedKind(ofCheckNamed:) != .tool` filter with one that keeps every grant, so the value spelling of the partition parts company with the name spelling, and watched thePaneAndTheWindowAgreeNameByName fail — (byName → false) == (byValue → true) — 1 issue, exit 1. docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
  },
  {
    "id": "CASE-0355",
    "req": "REQ-088",
    "surface": "SURF-008",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
    ],
    "armed": true,
    "note": "A2. The drift guard, rewritten rather than relaxed. It used to assert set equality between the grant names SessionDoctor emits and StatusChecks.known; the agent deliberately stopped emitting one. It now asserts BOTH directions: nothing emitted may be unclassified, AND the map's names that the agent no longer emits are exactly {Shortcuts CLI}, which must classify as .tool. A map entry for a name nothing sends still fails, and so does a new name the map has not heard of. Tests/ProctorCoreTests/StatusChecksTests.swift",
    "defects": [
      "DEF-181"
    ],
    "armedBy": "Added a `\"Bluetooth\": .permissionReadLive` entry to StatusChecks.known — a name the agent never emits — and watched grantNamesMatchTheMap fail on the direction PRO-0082 added: (Set(StatusChecks.known.keys).subtracting(emitted) → [\"Shortcuts CLI\", \"Bluetooth\"]) == (retired → [\"Shortcuts CLI\"]), 1 issue, exit 1. docs/test-campaign/evidence/pro-0082/arming-gapfix.json",
    "source": {
      "analyzer": "Tests/ProctorCoreTests/StatusChecksTests.swift, grantNamesMatchTheMap, matching `.init(\\s*name: \"…\"` over the whole of Sources/ProctorAgent/Session/SessionDoctor.swift and comparing the 4 names it found against StatusChecks.known in both directions",
      "examined": 264
    }
  },
  {
    "id": "CASE-0356",
    "req": "REQ-089",
    "surface": "SURF-008",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
    ],
    "armed": true,
    "note": "A3. The policy section appears in ready and partial, between .switches and .activity, and in neither .down nor .checking — a card drawn from a report nobody could fetch would be a statement about a gate that was never read. Tests/ProctorCoreTests/StatusPolicySectionTests.swift",
    "defects": [
      "DEF-183"
    ],
    "armedBy": "Added `.policy` to the sections StatusSurface draws in `.down` and watched theSectionIsPlaced fail — sections(for: .down) contains .policy → true — 1 issue, exit 1. docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
  },
  {
    "id": "CASE-0357",
    "req": "REQ-089",
    "surface": "SURF-008",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
    ],
    "armed": true,
    "note": "A3. A nil posture draws zero rows and the view says Copy.policyAbsent instead; a reported posture draws rows. Both halves asserted, so the empty case cannot pass by the card being empty for everyone. Tests/ProctorCoreTests/StatusPolicySectionTests.swift",
    "defects": [
      "DEF-183"
    ],
    "armedBy": "Made policyRows(from: nil) return a Gate row reading “Open — every app on this Mac may be driven” instead of nothing, and watched anAbsentPostureDrawsNothing fail — .isEmpty → false — 1 issue, exit 1. docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
  },
  {
    "id": "CASE-0358",
    "req": "REQ-089",
    "surface": "SURF-008",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/arming.json"
    ],
    "armed": true,
    "note": "A3. One row per PolicyRow.Kind, in the kind's own order, each with a non-empty label and value, no empty-string detail masquerading as a fact, and an identifier present in StatusSurface.ID.all keyed on the kind rather than the position. Tests/ProctorCoreTests/StatusPolicySectionTests.swift",
    "armedBy": "Changed `PolicyRow(kind: .entries` to `.trail` so the card drew four kinds and a duplicate; watched this test fail with 1 issue, exit 1. docs/test-campaign/evidence/pro-0082/arming.json",
    "defects": [
      "DEF-183"
    ]
  },
  {
    "id": "CASE-0359",
    "req": "REQ-089",
    "surface": "SURF-008",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
    ],
    "armed": true,
    "note": "A3. Every label a policy row draws is a member of StatusSurface.Copy.all. A row assembling a literal in the card would render correctly and be invisible to the copy inventory, which is the defect PRO-0090 removed from the view. Tests/ProctorCoreTests/StatusPolicySectionTests.swift",
    "defects": [
      "DEF-183"
    ],
    "armedBy": "Replaced the entries row's `label: Copy.policyEntriesLabel` with the literal \"Entries recorded\" and watched everyRowStringIsEnumerable fail: the label is absent from the 88 constants Copy.all enumerates, 1 issue, exit 1. docs/test-campaign/evidence/pro-0082/arming-gapfix.json",
    "source": {
      "analyzer": "Tests/ProctorCoreTests/StatusPolicySectionTests.swift, everyRowStringIsEnumerable, checking every label the policy card draws against the constants StatusSurface.Copy.all enumerates",
      "examined": 88
    }
  },
  {
    "id": "CASE-0360",
    "req": "REQ-089",
    "surface": "SURF-012",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/arming.json"
    ],
    "armed": true,
    "note": "A3. Each gate mode states a consequence for the next call rather than a mode name, with correct pluralisation at one app; the approval row says whether a sensitive step would be allowed or refused; the filesystem row names its root count or says no jail is declared. `open` — the most permissive posture and also the default — draws .warn, so the window cannot agree with a machine that has no gate. Tests/ProctorCoreTests/StatusPolicySectionTests.swift",
    "armedBy": "Replaced `tone: p.mode == \"open\" ? .warn : .good` with `tone: .good` and watched this test fail with 2 issues, exit 1. docs/test-campaign/evidence/pro-0082/arming.json",
    "defects": [
      "DEF-183"
    ]
  },
  {
    "id": "CASE-0361",
    "req": "REQ-089",
    "surface": "SURF-012",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
    ],
    "armed": true,
    "note": "A3. Trail honesty in the order a person needs it. An unwritable trail says steps are running unrecorded and suppresses every other verdict, even with clean/sealed/signed all true — the trail that verifies because it holds nothing. A trail that does not verify is .bad, and the .good case is asserted reachable in the same test so the check is not always-red. Signing states whether the key was confirmed, and an unsigned trail claims neither. Tests/ProctorCoreTests/StatusPolicySectionTests.swift",
    "defects": [
      "DEF-183"
    ],
    "armedBy": "Replaced the trail row's `tone: (p.auditWritable && p.auditClean) ? .good : .bad` with a flat `.good` and watched both halves fail — (row.tone → .good) == .bad on the unwritable trail and (broken.tone → .good) == .bad on the unverifiable one — 2 issues over 2 tests, exit 1. docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
  },
  {
    "id": "CASE-0362",
    "req": "REQ-089",
    "surface": "SURF-012",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
    ],
    "armed": true,
    "note": "A3. auditDroppedThisRun > 0 reds the entries row while the trail row still reads Verifies clean and .good. The wire's own note is the reason: an action never recorded leaves no broken link to find, so clean and complete are two claims, and a card that let the first imply the second would draw a hole in the trail as health. Zero dropped and nil dropped are both .plain. Tests/ProctorCoreTests/StatusPolicySectionTests.swift",
    "defects": [
      "DEF-183"
    ],
    "armedBy": "Replaced the entries row's `tone: (p.auditDroppedThisRun ?? 0) > 0 ? .bad : .plain` with a flat `.plain` and watched droppedEntriesAreNotFoldedIntoTheVerdict fail — (entries.tone → .plain) == .bad — 1 issue, exit 1. docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
  },
  {
    "id": "CASE-0363",
    "req": "REQ-089",
    "surface": "SURF-008",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
    ],
    "armed": true,
    "note": "A3. Five posture pairs that differ in tone are asserted to differ in WORDS as well, so tone is never the only carrier of a fact — the rule LanePill already carries, applied to the new card. Tests/ProctorCoreTests/StatusPolicySectionTests.swift",
    "defects": [
      "DEF-183"
    ],
    "armedBy": "Pinned the approval row's value to `Copy.policyApproval(live: false)` so the two token states differ in tone and not in words, and watched toneIsNeverTheOnlyCarrier fail — the two values compared equal at “No token is live, so a sensitive step would be refused” — 1 issue, exit 1. docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
  },
  {
    "id": "CASE-0364",
    "req": "REQ-089",
    "surface": "SURF-012",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
    ],
    "armed": true,
    "note": "A3. No row's label, value or detail contains a bundle prefix, a filesystem path, a key header or a token. The wire withholds rule contents on purpose — a scope is a rule — so this asserts the card did not invent one to draw. Tests/ProctorCoreTests/StatusPolicySectionTests.swift",
    "defects": [
      "DEF-183"
    ],
    "armedBy": "Appended \" under /Users\" to the filesystem row's value and watched theCardCarriesNoRuleContents fail — the drawn text contains the leak “/Users” — 1 issue, exit 1. docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
  },
  {
    "id": "CASE-0365",
    "req": "REQ-090",
    "surface": "SURF-009",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/arming.json"
    ],
    "armed": true,
    "note": "A4. WalkthroughFlow.Copy.openSettings equals StatusSurface.Copy.openSettings and equals \"Open Settings\": two constants with one value bound by a test, rather than one constant read from two files, so the surfaces part company loudly. The label carries no question mark and still names the destination. Tests/ProctorCoreTests/WalkthroughSettingsLineTests.swift",
    "armedBy": "Reverted the constant to \"Already allowed? Open System Settings\" and watched this test fail, exit 1. docs/test-campaign/evidence/pro-0082/arming.json",
    "defects": [
      "DEF-182"
    ]
  },
  {
    "id": "CASE-0366",
    "req": "REQ-090",
    "surface": "SURF-009",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/arming.json"
    ],
    "armed": true,
    "note": "A4. Every .swift file under Sources is read with whole-line comments dropped and none contains \"Already allowed\". The scan asserts it walked more than 50 files first, so a scan that found nothing because it read nothing is not reported as clean. The value check alone would pass if the sentence had merely moved. Tests/ProctorCoreTests/WalkthroughSettingsLineTests.swift",
    "armedBy": "Same reversion; the retired question reappears in WalkthroughFlow.swift and the scan reddens, exit 1. docs/test-campaign/evidence/pro-0082/arming.json",
    "defects": [
      "DEF-182"
    ],
    "source": {
      "analyzer": "Tests/ProctorCoreTests/WalkthroughSettingsLineTests.swift, theOldWordingIsGoneFromEverySource, reading every .swift file under Sources with whole-line comments dropped and searching each for \"Already allowed\"",
      "examined": 197
    }
  },
  {
    "id": "CASE-0367",
    "req": "REQ-090",
    "surface": "SURF-009",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
    ],
    "armed": true,
    "note": "A4. Removing the question did not remove the information: Copy.restartNote is non-empty and still names the restart, which is the actual answer for the person the question was addressed to — already granted, still looking at an ungranted row. Tests/ProctorCoreTests/WalkthroughSettingsLineTests.swift",
    "defects": [
      "DEF-182"
    ],
    "armedBy": "Rewrote WalkthroughFlow.Copy.restartNote as “Screen Recording takes effect once macOS has applied the change.”, which names neither the restart nor the quit, and watched theRestartNoteStillCarriesTheRealAnswer fail — contains(\"restart\") → false || contains(\"quit\") → false — 1 issue, exit 1. docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
  },
  {
    "id": "CASE-0368",
    "req": "REQ-032",
    "surface": "SURF-008",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/revocation-measurement.md"
    ],
    "armed": true,
    "note": "Item 6, at the layer that owns it. ScreenRecordingProbe is driven with a platform that answers .granted and then .denied — the revocation, with nothing restarted — and the probe reads .granted back, having asked the platform exactly once. Its two-way control is the same probe answering .unconfirmed, which is never cached: there the platform IS asked again and the answer does change, so the freeze is a property of caching a definite answer rather than of the harness. Tests/ProctorAgentTests/StatusWindowDebtWiringTests.swift",
    "armedBy": "The control is the arming and it is inside the suite: `anUnconfirmedAnswerIsReProbed` shows the same instrument reporting the OTHER outcome on the same code path. Without it the freeze test would prove only that a stub returned a stub.",
    "defects": [
      "DEF-180"
    ]
  },
  {
    "id": "CASE-0369",
    "req": "REQ-032",
    "surface": "SURF-008",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/pro-0082/suite-verdict.txt",
      "docs/test-campaign/evidence/pro-0082/revocation-measurement.md",
      "docs/test-campaign/evidence/pro-0082/revocation-before/doctor.json",
      "docs/test-campaign/evidence/pro-0082/revocation-after-attempt/doctor.json",
      "docs/test-campaign/evidence/pro-0082/agent-identity.txt",
      "docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
    ],
    "armed": true,
    "note": "Item 6 end-to-end through Session.doctor(): the Screen Recording grant reads resolvedState .granted and granted true from a report taken AFTER the injected platform flipped to .denied. NOT the glass rung — the brief's own procedure (revoke in System Settings, re-read doctor) was attempted on this machine and could not be completed: macOS 26.6 gates the pane's write behind Touch ID or a password, and the toggle's flip to 0 was a proposal that Cancel reverted. The before-reading and the after-attempt reading are both real MCP responses from the unrestarted agent pid 52976 and are recorded as such rather than as the after-shot the brief asked for. Tests/ProctorAgentTests/StatusWindowDebtWiringTests.swift",
    "defects": [
      "DEF-180"
    ],
    "armedBy": "Removed the freeze: changed ScreenRecordingProbe.state()'s `case .cached(let known): return known` to re-read the platform, and watched doctorReportsTheFrozenValue fail — (row?.resolvedState → .denied) == .granted and (row?.granted → false) == true — 2 issues, exit 1. docs/test-campaign/evidence/pro-0082/arming-gapfix.json"
  },
  {
    "id": "CASE-0370",
    "req": "REQ-091",
    "surface": "SURF-002",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/skill_doc_measure.py",
      "scripts/campaign/skill_doc_arm.py",
      "docs/test-campaign/evidence/PRO-0085/doc-measurement.txt",
      "docs/test-campaign/evidence/PRO-0085/doc-measurement-arming.txt"
    ],
    "armed": true,
    "armedBy": "skill_doc_arm.py mutates a scratch copy of the skill and of ToolCatalogue.swift, one mutation per check, and asserts the named check flips to FAIL. The thirteen checks this case stands on were each watched red: dropping proctor_policy from tools.md reds 'names every shipped tool'; adding proctor_bogus reds 'names no tool the server lacks'; renaming name: \"proctor_zoom\" in the catalogue reds 'catalogue ships 21'; demoting one '## `proctor_wait`' to '###' reds 'specifies 15 sections'; retitling the guest heading reds 'proctor_guest has a specified section'; and deleting `guest` from the full row's adds column reds 'guest listed in full profile adds'. EVERY COUNT EITHER FILE STATES IS NOW READ, because the first instrument read two of six and a corrected site went unwatched: '21 tools' -> '20 tools' reds 'states the count the catalogue ships'; the full row's total 21 -> 20 reds 'full profile row totals the catalogue'; deleting `openai_computer` from that row's adds column reds 'every profile row totals the tools it names', which reads all four row totals against the names they nest over; 'Six more are named' -> 'Five more' reds the specified-plus-named split; 'Fifteen plus six is the 21 above' -> '20 above' reds its restatement; 'rather than all twenty-one' -> 'all twenty' reds SKILL.md's word-form count; 'the 21 tools the server ships' -> '20 tools' reds SKILL.md's depth line; and '`core` is the ten that' -> 'the eleven that' reds the core-profile size both files state. Full output in doc-measurement-arming.txt, 24/24 armed.",
    "note": "PRO-0085, DEF-190 and DEF-192. The deliverable is in another repository (~/Dev/fledgeling-plugins/plugins/proctor/skills/proctor), so this is a hand-run measurement rather than a suite predicate; that limit is stated in the instrument's own docstring and in spec-PRO-0085.md. THREE CHECKS IN THE FIRST VERSION OF THIS INSTRUMENT COULD NOT FAIL and its 17/17 was read as a pass over them: 'all three providers named' tested \"tart\" in tools, which the word 'start' satisfies; 'SKILL.md has a guest lane section' tested for '## The guest lane' anywhere in flattened text and was satisfied by a backticked cross-reference while the heading itself read '## `proctor_ARMING`'; and nothing read the count the page states about itself, so 'The server ships **20 tools**' survived a green run of the very instrument written to catch it. Providers are now matched on a word boundary, headings are anchored to the start of a line, and the stated count is compared to the catalogue.",
    "defects": [
      "DEF-190",
      "DEF-192"
    ]
  },
  {
    "id": "CASE-0371",
    "req": "REQ-092",
    "surface": "SURF-013",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/skill_doc_measure.py",
      "scripts/campaign/skill_doc_arm.py",
      "docs/test-campaign/evidence/PRO-0085/doc-measurement.txt",
      "docs/test-campaign/evidence/PRO-0085/doc-measurement-arming.txt",
      "docs/test-campaign/evidence/PRO-0085/doc-description-anchor-arming.txt"
    ],
    "armed": true,
    "armedBy": "Each of the ten checks this case stands on was watched red under its own mutation on a scratch copy: retitling '## The guest lane' reds the section check; 'Nothing here provisions a guest' -> 'Nothing provisions a guest' reds the both-files check; inserting a `provision` token into tools.md reds 'no provision action documented', which is a negative check and would otherwise never have been seen to fire; 'caps concurrent macOS guests at two' -> 'limits macOS guests to two' reds the cap check; 'How many guests at once? Two.' -> 'How many at once? Two.' reds the scale-question check; 'prlctl' -> 'parallels-cli' reds the provider check; 'both still open' -> 'both open' reds the Tahoe check; and shortening the osVersion sentence reds the honesty check. THE TWO DESCRIPTION CHECKS WERE DEAD and are re-armed: both tested membership against the whole of SKILL.md, so a phrase 800 lines into the body satisfied a claim about the frontmatter description a host reads to route. They now read the `description:` value alone, and each is armed against a mutation that rewrites the phrase in the description while planting it in the body, a file the old predicate still passes on. Watched red in doc-description-anchor-arming.txt, which records the whole-file membership as True beside each FAIL. Full arming output in doc-measurement-arming.txt, 24/24 armed.",
    "note": "PRO-0085, DEF-190 and DEF-191. Checks that SKILL.md routes on isolation rather than throughput: a '## The guest lane' section, the description naming virtual-machine guests and distinguishing the delegated tier, 'Nothing here provisions a guest' with its reason in both files, no provision action documented, and the two-guest cap surviving verbatim under 'How many guests at once? Two.' rather than standing as the file's only word on virtual machines. THE LIVE SKILL WAS FOUND MID-MUTATION: the previous attempt armed three checks by editing the real files and was killed between the mutation and the revert, so every project on this machine spent roughly an hour reading a skill whose guest section was headed '## `proctor_ARMING`', whose guest-lane heading read '## The ARMING lane', and whose cap paragraph was a placeholder. Restored here, and skill_doc_arm.py now mutates scratch copies only so an interrupted run cannot leave the shared skill broken.",
    "defects": [
      "DEF-190",
      "DEF-191"
    ]
  },
  {
    "id": "CASE-0372",
    "req": "REQ-093",
    "surface": "SURF-002",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/AgentVerbBoundaryTests.swift",
      "docs/test-campaign/evidence/PRO-0085/arming-A-hud-in-catalogue.txt",
      "docs/test-campaign/evidence/PRO-0085/baseline.txt",
      "docs/test-campaign/evidence/PRO-0085/full-suite.txt"
    ],
    "armed": true,
    "armedBy": "A ToolSpec named proctor_hud added to ToolCatalogue.all. 'Expectation failed: (ToolCatalogue.spec(named: verb) -> ToolSpec(name: \"proctor_hud\", title: \"arming\", ...)) == nil' at AgentVerbBoundaryTests.swift:40:13, the message naming the spec the shim's gate would have forwarded. Run failed with 2 issues, exit 1; reverted and green. Full output in arming-A-hud-in-catalogue.txt.",
    "note": "PRO-0085. AgentVerbs.swift:20-24 states this boundary in prose and MCPServer.swift:224 is the gate that enforces it; nothing asserted it until here. Adding proctor_hud to the catalogue would hand an MCP client a person's Pause, Resume and Stop while both the comment and the proctor skill kept reading as true. The verbs are read from AgentVerbs rather than copied as strings, so a renamed constant moves the assertion with it.",
    "defects": []
  },
  {
    "id": "CASE-0373",
    "req": "REQ-093",
    "surface": "SURF-002",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/AgentVerbBoundaryTests.swift",
      "docs/test-campaign/evidence/PRO-0085/arming-B-doctor-renamed.txt",
      "docs/test-campaign/evidence/PRO-0085/baseline.txt",
      "docs/test-campaign/evidence/PRO-0085/full-suite.txt"
    ],
    "armed": true,
    "armedBy": "AgentVerbs.doctor renamed so it no longer matches a catalogue entry. 'Expectation failed: (ToolCatalogue.spec(named: AgentVerbs.doctor) -> nil) != nil' at AgentVerbBoundaryTests.swift:50:9. Run failed with 1 issue, exit 1; reverted and green. Full output in arming-B-doctor-renamed.txt.",
    "note": "PRO-0085. proctor_doctor is the one AgentVerbs member that is also a catalogue tool, named there because Proctor's own status window calls it by that name. Asserted positively rather than skipped, so an exclusion rule that swept up a real tool would red here instead of passing quietly.",
    "defects": []
  },
  {
    "id": "CASE-0374",
    "req": "REQ-093",
    "surface": "SURF-002",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/AgentVerbBoundaryTests.swift",
      "docs/test-campaign/evidence/PRO-0085/arming-A-hud-in-catalogue.txt",
      "docs/test-campaign/evidence/PRO-0085/baseline.txt",
      "docs/test-campaign/evidence/PRO-0085/full-suite.txt"
    ],
    "armed": true,
    "armedBy": "The same proctor_hud addition. 'Expectation failed: (names.count -> 22) == 21' at AgentVerbBoundaryTests.swift:58:9. Run failed, exit 1; reverted and green. Full output in arming-A-hud-in-catalogue.txt.",
    "note": "PRO-0085, DEF-192. The count assertion the skill's documented 21 rests on, plus a uniqueness assertion on the names. WHAT THIS DOES NOT COVER, and it is the failure mode this item exists to close: nothing in this suite reads the skill file in the other repository, so references/tools.md drifting from the catalogue again is caught by CASE-0370's hand-run measurement, not by ./scripts/test.sh. The failure message names references/tools.md so the next reader who reds this case knows the document needs the same edit.",
    "defects": [
      "DEF-192"
    ]
  },
  {
    "id": "CASE-0390",
    "req": "REQ-083",
    "surface": "SURF-009",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/WalkthroughFlowTests.swift",
      "design/surfaces/proctor-surfaces.html",
      "docs/test-campaign/evidence/PRO-0100/arming-def162.txt",
      "docs/test-campaign/evidence/PRO-0100/full-suite-run2.txt"
    ],
    "armed": true,
    "armedBy": "The Skip setup BUTTON removed from the design's permissions footer while the pane's caption still names Skip setup in prose. 'Expectation failed: drawsSkipButton' at WalkthroughFlowTests.swift:308 plus the require at 311; 1 test failed with 2 issues, exit 1; reverted and green. Full output in arming-def162.txt.",
    "note": "PRO-0100, DEF-162. The design of record yields, not the build: Skip postdates this design page, WalkthroughFlow.showsSkip gates it and nothing else does, and a verifier proved removing it strands a person while the whole suite stays green. THE FIRST VERSION OF THIS CASE WAS A BLIND PASS AND ARMING IS WHAT FOUND IT: footer(ofState:in:) ended on a marker that matched 2,218 characters later, inside the next pane, so the slice carried this pane's caption and 'Skip setup' in that caption satisfied a claim about a control. The slice is now bounded to the footer's own eight-space close (304 characters, measured) with two guards of its own, and the clause asserts the button element rather than the words.",
    "defects": [
      "DEF-162"
    ],
    "source": {
      "analyzer": "WalkthroughFlowTests.theDesignDrawsSkipOnThePermissionsPane over the permissions pane's wt-foot in design/surfaces/proctor-surfaces.html, sliced to the footer's own eight-space close (304 characters, measured)",
      "examined": 3
    }
  },
  {
    "id": "CASE-0391",
    "req": "REQ-094",
    "surface": "SURF-009",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/WalkthroughFlowTests.swift",
      "Sources/ProctorUI/Walkthrough.swift",
      "docs/test-campaign/evidence/PRO-0100/arming-def163.txt",
      "docs/test-campaign/evidence/PRO-0100/arming-def163-reordered.txt",
      "docs/test-campaign/evidence/PRO-0100/full-suite-run2.txt"
    ],
    "armed": true,
    "armedBy": "Armed TWICE, on purpose. (A) The exact pre-fix shape: PrimaryProminence's application removed and .buttonStyle(.borderedProminent) put straight back after .accessibilityIdentifier. 'Expectation failed: (unbranchedFills -> 1) == 0' at WalkthroughFlowTests.swift:357 and 'takesTheCoreRule' at 387; 2 issues, exit 1. (B) The same unconditional fill with the modifiers RE-ORDERED so the fill sits last: same two failures. (B) exists because the first draft of the clause matched the pre-fix whitespace exactly and (B) survived it, so the clause now reads 'no .borderedProminent anywhere before the branch' rather than one arrangement of modifier lines. Both reverted and green. Output in arming-def163.txt and arming-def163-reordered.txt.",
    "note": "PRO-0100, DEF-163. The build yields here, the opposite direction to CASE-0390, and a referral is what kept the two apart. The fill is chosen by PrimaryProminence from WalkthroughFlow.primaryEnabled, the same call the .disabled reads, so the fill and the refusal cannot nominate different states. The modifier is declared before HeroPermRow because theRowDoesNotDecideItsOwnProminence counts .borderedProminent from that struct to end of file, and it is applied after .disabled and .hint so that theViewDisablesRatherThanHides still finds .disabled( 26 characters after the identifier (measured). All three of those guards pass UNEDITED, which was the constraint that chose this shape over a two-branch if/else.",
    "defects": [
      "DEF-163"
    ],
    "source": {
      "analyzer": "WalkthroughFlowTests.thePrimaryIsProminentOnlyWhenEnabled over Sources/ProctorUI/Walkthrough.swift with comments stripped, counting .borderedProminent and .buttonStyle(.bordered) before and inside the PrimaryProminence span",
      "examined": 6
    }
  },
  {
    "id": "CASE-0392",
    "req": "REQ-094",
    "surface": "SURF-009",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/WalkthroughFlowTests.swift",
      "design/surfaces/proctor-surfaces.html",
      "docs/test-campaign/evidence/PRO-0100/full-suite-run2.txt"
    ],
    "armed": true,
    "armedBy": "PRO-0106, DEF-200. Watched to fail on a scratch basis: the permissions footer's refusing primary redrawn from class=\"btn\" disabled to class=\"btn prominent\" disabled — the accent-fill treatment DEF-163 was about — and WalkthroughFlowTests.theDesignDrawsTheRefusingPrimaryPlain reds at WalkthroughFlowTests.swift:397 with `the design's refusing primary is no longer drawn plain-and-disabled`. The mutation was confirmed landed by re-reading the file before the verdict was read, restored immediately, and the tree checked clean afterwards. docs/test-campaign/evidence/PRO-0106/def200-case-0392-arming.txt.",
    "note": "PRO-0100, DEF-163, the design half. Asserts the record still draws the refusing primary plain-and-disabled (class=\"btn\" disabled) and never in the accent fill. Recorded NOT ARMED at PRO-0100 on the grounds that arming it would mean breaking the design of record; a verifier broke it on a scratch basis anyway and the check red, so the case was armable all along and the registry understated its own strength. The flag stayed down because that probe left no evidence file, and no artifact means no verdict. PRO-0106 ran it again and kept the artifact, which is what closes DEF-200 — the repair was never a change to the check.",
    "defects": [
      "DEF-163"
    ],
    "source": {
      "analyzer": "WalkthroughFlowTests.theDesignDrawsTheRefusingPrimaryPlain over the same 304-character permissions footer slice",
      "examined": 3
    }
  },
  {
    "id": "CASE-0393",
    "req": "REQ-095",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/BrowserLaneWiringTests.swift",
      "docs/test-campaign/evidence/PRO-0100/arming-def165.txt",
      "docs/test-campaign/evidence/PRO-0100/full-suite-run2.txt"
    ],
    "armed": true,
    "armedBy": "accessibilityProbe replaced with a counter returning false on its fifth call, so one of the eight doctor() calls in the matrix sees a different machine. 'Expectation failed: (readies.count -> 2) == 1' at BrowserLaneWiringTests.swift:249 and '(blockerCounts.count -> 2) == 1' at 250; 1 test failed with 2 issues, exit 1; reverted and green. This is the same failure text PRO-0098 measured coming off the real machine, which is what shows the injection reached `ready` rather than being accepted and ignored. Full output in arming-def165.txt.",
    "note": "PRO-0100, DEF-165. The harness injected screenRecordingProbe and left accessibilityProbe and secureInputProbe at Session.init's live defaults, both of which feed blockers, and ready is blockers.isEmpty. Measured before the fix: four full runs of one unchanged tree at exit 0/1/0 with the same 2,028 tests, the red one carrying only these two issues, and this suite alone immediately afterwards green in 0.230 seconds. Verified while fixing that the live set is exactly two: shortcutsProbe stopped feeding blockers at PRO-0082 and Input Monitoring is required:false, so neither reaches ready. The assertion was not touched.",
    "defects": [
      "DEF-165"
    ]
  },
  {
    "id": "CASE-0394",
    "req": "REQ-095",
    "surface": "SURF-012",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/HoldAttributionWiringTests.swift",
      "docs/test-campaign/evidence/PRO-0100/arming-def175.txt",
      "docs/test-campaign/evidence/PRO-0100/full-suite-run2.txt"
    ],
    "armed": true,
    "armedBy": "RunScheduler.hold(run:_:) made a no-op, so the latch still holds the run and the ticket never learns of it. 'Expectation failed: duringHold.active.contains { $0.held != nil }' at HoldAttributionWiringTests.swift:409, once for each of the four endings; 1 test failed with 4 issues after 269.819 seconds, exit 1; reverted and green. Full output in arming-def175.txt.",
    "note": "PRO-0100, DEF-175. A product mutation rather than a test mutation, deliberately: the hazard in adding a wait is that the wait reports success regardless, so what had to be shown is that a hold which genuinely never reaches a ticket still fails. It does, on all four endings, and it takes the full bounded wait to do it. Measured before the fix: five full runs of one unchanged tree at exit 0/0/0/1/0, the red one carrying this as its only issue. The assertion is unchanged; only the accidental claim that the ticket catches up within one scheduling quantum is gone.",
    "defects": [
      "DEF-175"
    ]
  },
  {
    "id": "CASE-0395",
    "req": "REQ-077",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorCoreTests/ConsentSurfaceTests.swift",
      "docs/test-campaign/evidence/PRO-0100/arming-def140.txt",
      "docs/test-campaign/evidence/PRO-0100/arming-def140-control.txt",
      "docs/test-campaign/evidence/PRO-0100/unwrap-census.md",
      "docs/test-campaign/evidence/PRO-0100/full-suite-run2.txt"
    ],
    "armed": true,
    "armedBy": "TWO RUNS, and the contrast between them IS the case. (A) The converted site put back as a bare force-unwrap and fed a nil (a switch id that is not in the catalogue): 'Fatal error: Unexpectedly found nil while unwrapping an Optional value', 'exited with unexpected signal code 5', 'Executed 0 tests, with 0 failures (0 unexpected)' from the XCTest half, and ZERO 'Test run with' verdict lines. exit 1. (B) The SAME nil through the converted try #require: 'Expectation failed: SwitchCatalogue.named(\"PROCTOR_YIELD_INPUT_ARMING\") -> nil' at ConsentSurfaceTests.swift:30, one verdict line reading 'Test run with 1 test in 1 suite failed', exit 1. Output in arming-def140.txt and arming-def140-control.txt.",
    "note": "PRO-0100, DEF-140. Reading the exit code alone would have called these two runs identical, and they are the whole point: one names a failing test and one reports nothing at all while its XCTest line says passed. scripts/test.sh catches (A) only because it refuses an absent verdict. The site chosen is the one DEF-140 names as the class's shape, a catalogue lookup by literal id.",
    "defects": [
      "DEF-140"
    ]
  },
  {
    "id": "CASE-0396",
    "req": "REQ-077",
    "surface": "SURF-022",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0100/unwrap-census.md",
      "docs/test-campaign/evidence/PRO-0100/unwrap-census-raw.txt",
      "docs/test-campaign/evidence/PRO-0100/full-suite-run2.txt"
    ],
    "armed": false,
    "note": "PRO-0100, DEF-140, the census as a count. Three greps, before and after, recorded verbatim at 9f99a0f: try! 86 -> 0; bare ! on a property or subscript 13 lines/14 unwraps -> 4 lines (3 group-1 sites and 1 comment); )! 6 -> 6, DEF-136's own four group-1 sites and two comments, untouched here. 55 test functions gained throws. NOT ARMED: it is a measurement recorded in a document rather than a predicate in the suite, so there is nothing to watch fail. Named as a hand-run count for exactly that reason, the same footing CASE-0370 stands on. The three sites kept carry their input space written out beside them, and one of those reasons was WRONG in the first draft and corrected by the out-of-family plan review, which opened the file and found that :1126's frame comes from FrameCodec.encode rather than from a local append.",
    "defects": [
      "DEF-140"
    ],
    "source": {
      "analyzer": "scripts/campaign greps recorded in docs/test-campaign/evidence/PRO-0100/unwrap-census-raw.txt: `grep -rn 'try!' Tests`, `grep -rnE '[A-Za-z0-9_\\]]!([^=]|$)' Tests` and `grep -rn ')!' Tests`, taken before any edit at 9f99a0f",
      "examined": 105
    }
  },
  {
    "id": "CASE-0397",
    "req": "REQ-096",
    "surface": "SURF-002",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/skill_doc_measure.py",
      "scripts/campaign/skill_doc_arm.py",
      "docs/test-campaign/evidence/PRO-0100/skill-doc-measure.txt",
      "docs/test-campaign/evidence/PRO-0100/skill-doc-arm.txt"
    ],
    "armed": true,
    "armedBy": "gemini.md's count reverted to 'twenty tools' on a scratch copy of the whole skill tree. 'FAIL gemini.md states the catalogue count [[20]]' via skill_doc_arm.py, which reports ARMED for it. Independently, the measurement was run against the UNFIXED live file before the one-word fix and exited 1 with that check and one other red; that run is the arming that came first. Output in skill-doc-arm.txt and skill-doc-measure.txt.",
    "note": "PRO-0100, DEF-193. The catalogue ships 21, measured two ways (21 ToolSpec( entries, 21 distinct proctor_* names). SKILL.md and references/tools.md already read 21 because PRO-0085 corrected them; gemini.md was the fifth stale count and no check opened its file. Committed in the shared repository by explicit path only, at 82ebe7d, with the tree measured clean before and after.",
    "defects": [
      "DEF-193"
    ],
    "source": {
      "analyzer": "scripts/campaign/skill_doc_measure.py, the gemini.md count check, over every count word stated before the word `tools` in the proctor skill",
      "examined": 5
    }
  },
  {
    "id": "CASE-0398",
    "req": "REQ-096",
    "surface": "SURF-002",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/skill_doc_measure.py",
      "scripts/campaign/skill_doc_arm.py",
      "docs/test-campaign/evidence/PRO-0100/skill-doc-arm.txt"
    ],
    "armed": true,
    "armedBy": "A count sentence added to references/methodology.md on a scratch copy ('this page describes nineteen tools') — a file the measurement could not previously open at all. skill_doc_arm.py reports ARMED, and the mutation reds ONLY this check, which is why it is armed in a different file from CASE-0397 rather than sharing that mutation and inheriting its red.",
    "note": "PRO-0100, DEF-193, the denominator half and the actual repair. Every number stated immediately before the word 'tools', in every *.md under the skill, is read and compared to the catalogue. Subset counts are real and are allowlisted WITH their reason rather than silently tolerated: the core profile's ten, the fifteen sections tools.md specifies, and 'Recording is two tools together'. The instrument's own scratch copy now copies the whole tree, because copying the two files a check happened to read was the same mistake one layer down.",
    "defects": [
      "DEF-193"
    ],
    "source": {
      "analyzer": "scripts/campaign/skill_doc_measure.py stated_tool_counts() over every *.md under the proctor skill",
      "examined": 5
    }
  },
  {
    "id": "CASE-0399",
    "req": "REQ-096",
    "surface": "SURF-002",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/skill_doc_measure.py",
      "scripts/campaign/skill_doc_arm.py",
      "docs/test-campaign/evidence/PRO-0100/skill-doc-arm.txt"
    ],
    "armed": true,
    "armedBy": "proctor_zoom renamed to proctor_zoomer in gemini.md on a scratch copy. skill_doc_arm.py reports ARMED and the mutation reds only this check.",
    "note": "PRO-0100, DEF-193, the name half of the same denominator. references/tools.md was checked against the catalogue and the other four *.md files were not, so a renamed tool could leave a stale mention in gemini.md or references/methodology.md with every check green. Measured at PRO-0100: those two files name ten proctor_* identifiers between them and all ten are shipped, so this check starts from a true statement rather than from a backlog. 27 of 27 checks in that instrument are now armed.",
    "defects": [
      "DEF-193"
    ],
    "source": {
      "analyzer": "scripts/campaign/skill_doc_measure.py named_tools_everywhere() over every proctor_* identifier in every *.md under the proctor skill",
      "examined": 119
    }
  },
  {
    "id": "CASE-0400",
    "req": "REQ-091",
    "surface": "SURF-002",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/skill_doc_measure.py",
      "docs/test-campaign/evidence/PRO-0100/skill-doc-measure.txt"
    ],
    "armed": false,
    "note": "PRO-0100. A correction to a row this item did not create, declared as one: skill_doc_measure.py defaulted its catalogue path to .worktrees/PRO-0085/Sources/ProctorCore/ToolCatalogue.swift, and that worktree was removed when PRO-0085 merged, so the measurement could not run at all without --catalogue. Repointed repo-relative, which is how skill_doc_arm.py already resolved it — which is why the arming kept working while the measurement did not. NOT ARMED: a path that does not resolve makes the script exit before any check runs, so there is no predicate here to watch fail, and the evidence is that the script now runs at all (27/27, exit 0).",
    "defects": [],
    "source": {
      "analyzer": "scripts/campaign/skill_doc_measure.py run end to end after repointing its catalogue default, counting the checks that executed at all",
      "examined": 27
    }
  },
  {
    "id": "CASE-0410",
    "req": "REQ-097",
    "surface": "SURF-023",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-D-full-prefix.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "Current selftest.py against reckon.py as it stood at 31697a9: 'a fixed defect is not remaining work FAILED — pre-fix='broken' now='broken' work=True'. arming-D line 1.",
    "note": "The row must come out verified-done with is_work_item False. The pre-fix expression is also restated verbatim in the file (PRE_FIX_DEFECT) and asserted to say 'broken' first, so this stays armed on any registry whose defects all happen to be open. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-210"
    ]
  },
  {
    "id": "CASE-0411",
    "req": "REQ-097",
    "surface": "SURF-023",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-E-controls.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "Not armable by reverting to 31697a9 — pre-fix said broken about everything, so it passed on both sides. Armed by mutation instead: forcing is_work_item False on every defect reds this line. arming-E-controls.txt, first mutation.",
    "note": "The control that stops CASE-0410's repair becoming a blanket retirement. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-210"
    ]
  },
  {
    "id": "CASE-0412",
    "req": "REQ-097",
    "surface": "SURF-023",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-D-full-prefix.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "arming-D: 'an unrecognised defect status stays broken FAILED — 'broken' / 'a defect is a measured negative result...'' — the pre-fix class was right by accident and the reason was wrong, which is what the assertion reads.",
    "note": "Fixture status 'marinated'. Asserting the class alone would pass on pre-fix code that said broken about everything; the assertion requires the word to appear in the row's why, which only reading the status can produce. Guessing done is the one error in this direction nothing recovers from. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-210"
    ]
  },
  {
    "id": "CASE-0413",
    "req": "REQ-097",
    "surface": "SURF-023",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-D-full-prefix.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "arming-D: 'a defect with no status stays broken FAILED'. This line passed against pre-fix code until the assertion was tightened to require 'no status' in the row's why; it was re-armed against 31697a9 after tightening rather than left on the earlier capture.",
    "note": "A registry that does not track defect status is unaffected by this item — it behaves exactly as it did. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-210"
    ]
  },
  {
    "id": "CASE-0414",
    "req": "REQ-097",
    "surface": "SURF-023",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-D-full-prefix.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "arming-D: 'a wontfix defect is a waiver with its reason FAILED'. The assertion also runs R.gate() over a ledger containing the row and requires zero violations.",
    "note": "The first version of this assertion was 'bool(row.get(\"note\") or row.get(\"status\"))', which the fixture made true whatever the code did — a hollow check, found by the out-of-family critic and replaced with one requiring the status word in the row's why plus a clean gate. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-210"
    ]
  },
  {
    "id": "CASE-0415",
    "req": "REQ-097",
    "surface": "SURF-023",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-D-full-prefix.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "arming-D, two lines: 'defect classed against its own status exit=0 FAILED' and 'defect claiming done on an unknown status exit=0 FAILED'. Both wanted exit 1 and got a clean gate.",
    "note": "Two fixtures: an open defect classed verified-done, and a defect with status 'marinated' classed verified-done. The second exists because the first version of DEFECT_LEGAL_CLASS.get() returned None for an unrecognised status and the gate skipped it, leaving the rows with the weakest evidence the only ones the gate could not fire on. Found by the critic. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-210"
    ]
  },
  {
    "id": "CASE-0416",
    "req": "REQ-098",
    "surface": "SURF-023",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-D-full-prefix.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "arming-D: 'a brief nothing joined is unjoined, not unbuilt FAILED — class='unbuilt' kind='product-work''.",
    "note": "The 75-of-91 fault in one assertion. PRE_FIX_BRIEF is restated verbatim in the file so the row keeps discriminating. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-211"
    ]
  },
  {
    "id": "CASE-0417",
    "req": "REQ-098",
    "surface": "SURF-023",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-D-full-prefix.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "arming-D: 'a brief citing ids the registry lacks is unbuilt FAILED — 'broken' / 'the registry records a defect or a failing case against this brief's subject''.",
    "note": "This is what keeps unbuilt a live predicate. Without it every brief would route to unjoined and the partition would carry a class nothing could produce — the eleventh dead predicate this campaign has found, and it would have been introduced by the repair for the tenth. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-211"
    ]
  },
  {
    "id": "CASE-0418",
    "req": "REQ-098",
    "surface": "SURF-023",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-D-full-prefix.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "arming-D: 'an unjoined row carries its nearest candidate FAILED — near_misses: []'.",
    "note": "Without it, unjoined is a second kind of silence: a reader is told the tool could not tell and sent to grep. The candidates come from the same registry_tokens() the join used, so the reader sees what the join saw rather than a second opinion computed differently. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-211"
    ]
  },
  {
    "id": "CASE-0419",
    "req": "REQ-097",
    "surface": "SURF-023",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-D-full-prefix.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "arming-D: 'a brief joined only to a fixed defect is not broken FAILED — 'broken''.",
    "note": "Raised by the gemini design lane rather than found by reading. The assertion names the exact expected class (unmeasured) rather than merely not-broken, after the critic pointed out that '!= broken' would also accept unjoined or unbuilt, which would mean the defect branch had stopped running. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-213"
    ]
  },
  {
    "id": "CASE-0420",
    "req": "REQ-097",
    "surface": "SURF-023",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-E-controls.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "Not armable by reverting to 31697a9 — pre-fix classed it broken for the wrong reason and the line passed. Armed by mutation: forcing hit_defect False reds it. arming-E-controls.txt, second mutation.",
    "note": "The control on CASE-0419. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-213"
    ]
  },
  {
    "id": "CASE-0421",
    "req": "REQ-098",
    "surface": "SURF-023",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-D-full-prefix.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "arming-D: 'unjoined is inside the partition exit=1 FAILED — BRIEF-u has class 'unjoined', which is not one of the partition'.",
    "note": "A new class that the conservation gate rejects would make every real run exit 1. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-211"
    ]
  },
  {
    "id": "CASE-0422",
    "req": "REQ-098",
    "surface": "SURF-023",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-D-full-prefix.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "arming-D: 'with no campaign, a citation is not evidence FAILED'.",
    "note": "Found by the out-of-family critic against the first implementation, which would have classed a citing brief unbuilt in a campaign-less run — a claim that the registry was asked, when there is no registry. references/no-campaign.md states every brief lands in unjoined or waived, and this holds the code to the document rather than the other way round. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-211"
    ]
  },
  {
    "id": "CASE-0423",
    "req": "REQ-098",
    "surface": "SURF-023",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-C-post-critic.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "Armed by mutation, since build_join never produces this combination itself: reverting support to '(cited or my_edges) filtered' reds it. arming-C-post-critic.txt, third mutation.",
    "note": "Defensive against a caller passing hand-built edges; raised by the critic. Recorded honestly as unreachable from build_join today. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-211"
    ]
  },
  {
    "id": "CASE-0424",
    "req": "REQ-098",
    "surface": "SURF-023",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-D-full-prefix.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "arming-D: 'a file number is not a citation FAILED — number-shaped reverse citation still fires: [{'method': 'cited', 'confidence': 1.0}]'. The companion line, 'a project id in a note is still a citation', passes on both sides and is the control that the documented mechanism survived.",
    "note": "Assumption A8's guess-by-number fallback. Measured before the change: the scan contributed 0 of 92 cited edges on this repo, so nothing here is lost. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-214"
    ]
  },
  {
    "id": "CASE-0425",
    "req": "REQ-098",
    "surface": "SURF-023",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-D-full-prefix.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "arming-D: 'the report gives unjoined its own section FAILED'.",
    "note": "The ledger distinguishing the two classes is not enough — the reckoning's own report is what a person reads, and the spec asks for the outcome to be visible there. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-211"
    ]
  },
  {
    "id": "CASE-0426",
    "req": "REQ-098",
    "surface": "SURF-023",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0102/source-denominators.txt",
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-D-full-prefix.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "arming-D: 'every class is produced by a real fixture FAILED ['waived']'.",
    "source": {
      "analyzer": "plugins/reckon/skills/reckon/scripts/selftest.py check 13, 'every class is produced by a real fixture' — two fixtures are put through reckon.classify() and the classes they actually emit are collected, then subtracted from reckon.CLASSES. Denominator taken from a live run on 2026-08-22 against reckon 1.1.0 at fc86fe7, recorded in docs/test-campaign/evidence/PRO-0102/source-denominators.txt: 9 classes judged for reachability (unbuilt, unjoined, broken, unmeasured, unnamed, undecided, retirable, waived, verified-done), 0 unreachable. The population is the classes judged rather than the two fixtures opened, because a class is what this check finds an answer about.",
      "examined": 9
    },
    "note": "This replaces the previous reachability check, which unioned a hand-written set against CLASSES and so would have called unjoined reachable while nothing could produce it — an instrument that could not tell a live predicate from a dead one, which was the only thing it existed to do. Two fixtures are run through classify() and the classes they actually emit are collected. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-211"
    ]
  },
  {
    "id": "CASE-0427",
    "req": "REQ-099",
    "surface": "SURF-023",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0102/source-denominators.txt",
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-B-delivery.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "Armed by forcing plugin.json to 1.0.9 against a marketplace entry at 1.1.0: 'the manifest and the marketplace agree FAILED — version drift: plugin.json 1.0.9, marketplace 1.1.0'. arming-B-delivery.txt, first mutation.",
    "source": {
      "analyzer": "plugins/reckon/skills/reckon/scripts/selftest.py check 14, first assertion 'the manifest and the marketplace agree' — the two version declarations reckon publishes are read and compared. Denominator taken from a live run on 2026-08-22 against reckon 1.1.0 at fc86fe7, recorded in docs/test-campaign/evidence/PRO-0102/source-denominators.txt: 2 declarations judged (plugins/reckon/.claude-plugin/plugin.json:version = 1.1.0 and .claude-plugin/marketplace.json:plugins[reckon].version = 1.1.0), 0 disagreements. The entries marketplace.json carries are deliberately not the denominator: this check reads one of them, and claiming 46 would name a population it never examined.",
      "examined": 2
    },
    "note": "Drift between these two files is a repeat failure in that repository — commit a15febf exists to make two other plugins' marketplace entries catch up with their plugin.json. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-212"
    ]
  },
  {
    "id": "CASE-0428",
    "req": "REQ-099",
    "surface": "SURF-023",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0102/source-denominators.txt",
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-B-delivery.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "Armed by setting both files back to 1.0.0 — the exact state that stranded 81ad488: 'the published version carries these repairs FAILED — published version 1.0.0 predates the behaviour this file asserts'. arming-B-delivery.txt, second mutation.",
    "source": {
      "analyzer": "plugins/reckon/skills/reckon/scripts/selftest.py check 14, second assertion 'the published version carries these repairs' — plugin.json's version is split and compared against the (1, 1, 0) floor. Denominator taken from a live run on 2026-08-22 against reckon 1.1.0 at fc86fe7, recorded in docs/test-campaign/evidence/PRO-0102/source-denominators.txt: 1 published version string judged (plugin.json:version = 1.1.0), 0 below the floor. One is the honest count rather than a convenient one — this assertion reads exactly one value, and CASE-0427 is the row that judges the pair. The 46 checks in the suite this version would strand are what the failure message names, not what this check ranged over.",
      "examined": 1
    },
    "note": "The two files agreeing is not enough; agreeing at 1.0.0 is precisely how a correct repair reached nobody. This is the check the delivery problem would have had to pass. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-212"
    ]
  },
  {
    "id": "CASE-0429",
    "req": "REQ-097",
    "surface": "SURF-023",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "~/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-D-full-prefix.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/arming-C-post-critic.txt",
      "docs/test-campaign/evidence/PRO-0102/after/run.txt",
      "docs/test-campaign/evidence/PRO-0102/arming/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "arming-D, two lines: 'the headline counts what is actually left FAILED' and 'a repaired defect is absent from what remains FAILED'. Also armed against the current source by disabling the headline's unjoined clause, which reds the first line — a first attempt at that mutation only trimmed the sentence's tail, no check rested on it, and it was re-run rather than reported as armed. arming-C-post-critic.txt.",
    "note": "Nothing below classify() was executed by this suite before: cmd_build, the headline, the work arithmetic and both written artifacts could have been deleted and every other row would still have passed. Found by the out-of-family critic. The fixture's true answer is known by construction — one open defect is the only product work, one repaired defect and one unjoinable brief are not — and both defects carry a list-valued evidence field, so this is also the 81ad488 crash exercised on the real build path. The subject is reckon.py in ~/Dev/fledgeling-plugins/plugins/reckon, shipped as 1.1.0 at 224a696. Filed under SURF-023 · Reckoning reconciler, minted for this item because no surface here covered shared tooling outside the app: SURF-022's route is tool://vacuity-check/blind, an in-repo script, so a case about a tool in another repository filed there would name a denominator that instrument never examined.",
    "defects": [
      "DEF-210",
      "DEF-211",
      "DEF-212"
    ]
  },
  {
    "id": "CASE-0441",
    "req": "REQ-102",
    "surface": "SURF-025",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0103/selftest-green.txt",
      "scripts/reckoning/reckoning.py",
      "scripts/reckoning/reckoning_selftest.py"
    ],
    "armed": true,
    "armedBy": "reckoning_selftest.py mutation m1 on a scratch copy: `if dirty_paths:` becomes `if False:`, asserted to substitute exactly once and the copy re-run through --help before use. The same dirty tree then publishes at exit 0 where the unmutated script exits 2. Line 'arming . removing the dirty check publishes the unnamed reading' in selftest-green.txt.",
    "note": "A reading taken over uncommitted inputs cannot be named by a commit, so it is refused before anything is measured. The fixture commits a brief queue and a campaign, then edits cases.json without committing: take exits 2 naming the uncommitted paths. This is the failure the whole item turns on, because the first reckoning here named its tree only in a sentence of prose and nothing machine-readable carried it."
  },
  {
    "id": "CASE-0442",
    "req": "REQ-102",
    "surface": "SURF-025",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0103/selftest-green.txt",
      "scripts/reckoning/reckoning.py"
    ],
    "armed": true,
    "armedBy": "mutation m6: `if not (rec.get('tree') or {}).get('tree_named'):` becomes `if False:`, one substitution asserted; compare then differences the unnamed reading at exit 0. Line 'arming . removing the unnamed check differences it anyway'.",
    "note": "--allow-dirty is an escape hatch that moves the refusal downstream rather than removing it: the reading is taken, run.json records tree_named false, and compare then declines it as a baseline with 'was taken over an unnamed tree - it is a reading, not a baseline'. Two selftest lines cover the pair."
  },
  {
    "id": "CASE-0443",
    "req": "REQ-102",
    "surface": "SURF-025",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/reckoning/2026-08-22-2bdc808/run.json",
      "docs/test-campaign/evidence/PRO-0103/published-take.txt",
      "docs/test-campaign/evidence/PRO-0103/reading-reproduces.txt"
    ],
    "armed": true,
    "armedBy": "Two-way control rather than a mutation: the same command over an uncommitted tree produces no run directory at all (CASE-0441), so the artifact's existence and its commit field cannot both be satisfied by a tree that was never named. Strengthened after the fact: the named commit was checked out into a scratch tree with git archive and rebuilt with the same tool, and the ledger came back byte-identical at sha256 ca5f76ff27daf95d04a4ab6d1329529bb6904143a77fdd77899138c48c7fe25e over 582,690 bytes, so the commit field names the tree that actually produced these numbers rather than merely being present.",
    "note": "The published reading names its own tree. run.json for the 2026-08-22-2bdc808 run records commit 2bdc808eb... on branch ai/pro-0103 with dirty_inputs empty, and the directory name carries the same short sha. Checked against git rev-parse HEAD at the moment of the take."
  },
  {
    "id": "CASE-0444",
    "req": "REQ-103",
    "surface": "SURF-025",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0103/selftest-green.txt",
      "scripts/reckoning/reckoning.py"
    ],
    "armed": true,
    "armedBy": "mutation m1: `if version < MIN_TOOL_VERSION:` becomes `if False:`, one substitution asserted; the 1.0.0 tool then takes a reading at exit 0. Line 'arming . removing the floor lets the old tool through'.",
    "note": "A reckoning copy below the version floor is refused: the fixture builds a scratch plugin declaring 1.0.0 and take exits 2 with 'below the floor ... a delta against it measures the tool'. Below 1.1.0 the tool classes fixed defects as broken and unjoined briefs as unbuilt, which is what produced the first reading's 218."
  },
  {
    "id": "CASE-0445",
    "req": "REQ-103",
    "surface": "SURF-025",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0103/selftest-green.txt",
      "scripts/reckoning/reckoning.py"
    ],
    "armed": true,
    "armedBy": "mutation m2: `missing = REQUIRED_CLASSES - classes` becomes `missing = set()`, one substitution asserted; the mislabelled tool then takes a reading at exit 0. Line 'arming . removing the vocabulary check lets the liar through'.",
    "note": "A declared version is a claim and the class list is the behaviour, so both are read. The fixture builds a copy whose manifest says 1.1.0 and whose CLASSES tuple has had 'unjoined' removed; take exits 2 with 'does not carry the current partition'. This is the failure REQ-099 exists over, arriving from the other side: there the version did not move while the behaviour did."
  },
  {
    "id": "CASE-0446",
    "req": "REQ-103",
    "surface": "SURF-025",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0103/selftest-green.txt",
      "scripts/reckoning/reckoning.py"
    ],
    "armed": true,
    "armedBy": "Two-way control: the same fixture with the ratchet intact takes a reading at exit 0 in the same run ('control . a clean tree and a current tool take a reading'). No removal mutation, because deleting this check leaves the failure to a later AttributeError rather than to a refusal.",
    "note": "A tool exposing no ratchet is refused with 'no ratchet - there is no delta without it', because a comparison that cannot run the ratchet would report a smaller number instead of failing. The fixture renames reckon's ratchet function on a scratch copy and asserts the original name is gone before the run."
  },
  {
    "id": "CASE-0447",
    "req": "REQ-103",
    "surface": "SURF-025",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0103/cache-1.0.0-crash.txt",
      "docs/test-campaign/evidence/PRO-0103/published-take.txt"
    ],
    "armed": true,
    "armedBy": "Two-way control measured in the same minute: the cached 1.0.0 copy exits 1 on this registry and the shared 1.1.0 source exits 0 on the same inputs. Both invocations are in cache-1.0.0-crash.txt with the two plugin.json versions and the cached copy's zero occurrences of 'unjoined'.",
    "note": "The live instance of the same hazard on this machine. The installed plugin cache holds reckon 1.0.0 while the shared source declares 1.1.0, and the cached copy run against this registry exits 1 with AttributeError: 'list' object has no attribute 'lower' at tokens(), because this registry carries evidence as a list of paths. Anyone following reckon's own SKILL.md from the installed plugin gets a crash rather than a reading. Recorded as DEF-216.",
    "defects": [
      "DEF-216"
    ]
  },
  {
    "id": "CASE-0448",
    "req": "REQ-104",
    "surface": "SURF-025",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/reckoning/2026-08-22-2bdc808/delta.json",
      "docs/reckoning/2026-08-22-2bdc808/delta.md",
      "docs/test-campaign/evidence/PRO-0103/metamorphic-decomposition.txt"
    ],
    "armed": true,
    "armedBy": "Two-way control: with both run.json files declaring the same tool version the same pair compares at attribution 'direct' and needs no control ('two-way . with the versions agreeing the same pair compares'). The decomposition is therefore a response to the version difference rather than to the command.",
    "note": "Two readings taken with different tool versions are decomposed rather than differenced. The published pair records attribution 'decomposed': the baseline's own inputs were rebuilt at its own commit 2bd01be with reckon 1.1.0, and the delta reports tool movement -88 work items against project movement +4. Differencing the published ledgers alone would report -84 as progress."
  },
  {
    "id": "CASE-0449",
    "req": "REQ-104",
    "surface": "SURF-025",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0103/metamorphic-decomposition.txt",
      "docs/reckoning/2026-08-22-2bdc808/delta.json"
    ],
    "armed": true,
    "armedBy": "The self-comparison is the arming: an implementation that attributed movement to the wrong column, or that double-counted the control, cannot produce all-zero on identical inputs while producing a decomposition that sums on different ones. Both runs are in metamorphic-decomposition.txt with the assertions that raise on failure.",
    "note": "Two relations over the decomposition, both checked on the published artifacts. Tool movement plus project movement equals net movement for all five counts (work_items -88 +4 = -84, product -172 -1 = -173, evidence +5 +0 = +5, decision +79 +5 = +84, rows +0 +104 = +104). And a reading compared against itself moves nothing: every tool, project and net figure is zero and no row moved, appeared or vanished, which also exercises the same-tool 'direct' path on real data rather than on a fixture."
  },
  {
    "id": "CASE-0450",
    "req": "REQ-104",
    "surface": "SURF-025",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0103/selftest-green.txt",
      "scripts/reckoning/reckoning.py"
    ],
    "armed": true,
    "armedBy": "Two-way control: with the current reading's declared version set back to the tool on disk, the same pair compares at exit 0. Lines 'gate . a control built by a third tool is refused' and 'two-way . with the versions agreeing the same pair compares'.",
    "note": "A control built by a third tool version belongs to neither side of the delta, so compare refuses when the tool on disk is not the one that took the current reading. Found by arming the comparison rather than by design: the first version of this check only compared the two readings to each other."
  },
  {
    "id": "CASE-0451",
    "req": "REQ-104",
    "surface": "SURF-025",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0103/selftest-green.txt",
      "scripts/reckoning/reckoning.py"
    ],
    "armed": true,
    "armedBy": "Two-way control: restoring the real commit on the same run.json makes the identical command produce a decomposed delta at exit 0. Lines 'gate . an unresolvable earlier commit refuses the delta' and 'two-way . a resolvable earlier commit is decomposed instead'.",
    "note": "Where the earlier reading names a commit this repository cannot resolve, the delta is refused rather than attributed: 'the tool cannot be held constant, and a delta across two tool versions would report the tool's repair as the project's progress'. The fixture sets the earlier commit to forty zeroes."
  },
  {
    "id": "CASE-0452",
    "req": "REQ-105",
    "surface": "SURF-025",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0103/selftest-green.txt",
      "scripts/reckoning/reckoning.py"
    ],
    "armed": true,
    "armedBy": "Two-way control: the same pair unedited ratchets clean at exit 0 in the same run ('two-way . the same pair unedited ratchets clean').",
    "note": "The ratchet is the check the second run exists to turn on. The fixture takes two real readings, then edits one row of the later ledger so REQ-2 leaves unmeasured for verified-done while its evidence stays 'reported'; compare exits 3, names REQ-2, and writes 'silent transition' into delta.md as well as printing it. The fixture asserts it moved exactly one row before the run, so a fixture that failed to mutate cannot read as a check that cannot fail."
  },
  {
    "id": "CASE-0453",
    "req": "REQ-105",
    "surface": "SURF-025",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/reckoning/2026-08-22-2bdc808/delta.md",
      "docs/test-campaign/evidence/PRO-0103/published-take.txt"
    ],
    "armed": true,
    "armedBy": "The same pair with one row edited exits 3 (CASE-0452), so a clean verdict here is a measurement rather than the only answer this code can give.",
    "note": "Both ratchet pairs on the published reading came back clean: baseline 2bd01be against 2bdc808, and the rebuilt tool-constant control against 2bdc808. No row left unmeasured by any route, measured rather than assumed - unmeasured is flat at 36 rows with nothing entering and nothing leaving."
  },
  {
    "id": "CASE-0454",
    "req": "REQ-106",
    "surface": "SURF-025",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0103/cadence-and-report-shape.txt",
      "docs/reckoning/2026-08-22-2bdc808/delta.md",
      "docs/test-campaign/evidence/PRO-0103/selftest-green.txt"
    ],
    "armed": true,
    "armedBy": "Two-way control on the predicate: the same regex over a synthetic document headed 'Totals, last' then 'What moved' returns False on the leads-with-movement test. Recorded in cadence-and-report-shape.txt and as a selftest line.",
    "note": "The published delta.md's sections in order are: What moved, Where the movement came from, The ratchet, Rows that changed class, What each side of this comparison carries, Totals, last. The first heading is movement and the last is totals, which is the acceptance clause about leading with what changed rather than with the totals."
  },
  {
    "id": "CASE-0455",
    "req": "REQ-107",
    "surface": "SURF-025",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0103/cadence-and-report-shape.txt",
      "docs/reckoning/CADENCE.md",
      "scripts/reckoning/reckoning.py"
    ],
    "armed": true,
    "armedBy": "Two-way control: the same predicate over a subcommand the script does not expose raises AssertionError, and the third subcommand the script does expose (stamp) is not quoted in the runbook, so the set comparison is not trivially satisfied.",
    "note": "The cadence is written down and its runbook is executable rather than descriptive. CADENCE.md names the trigger ('when a wave closes') and quotes two shell commands; both name scripts/reckoning/reckoning.py and both subcommands, take and compare, are in the set the script's own argparse exposes. The check asserts rather than prints, so a renamed subcommand fails it.",
    "source": {
      "analyzer": "python3 re.findall over CADENCE.md for quoted python3 commands, compared to the subcommand set parsed from reckoning.py --help",
      "examined": 2
    }
  },
  {
    "id": "CASE-0456",
    "req": "REQ-102",
    "surface": "SURF-025",
    "oracle": "effect-witness",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/reckoning/2026-08-22-2bdc808/run.json",
      "docs/test-campaign/evidence/PRO-0103/published-take.txt"
    ],
    "armed": true,
    "armedBy": "Two-way control: the sweep runs on a directory that did not exist before the build, so an empty before-set and a two-file after-set are different readings of the same instrument. A take that wrote nothing would record count 0, and the refusal path (CASE-0441) writes no directory at all. Count corrected from four to two at verification; the two-way control is unaffected, because what it turns on is empty against non-empty.",
    "note": "The take command's filesystem effect, witnessed rather than inferred. run.json records the sweep the command took of its own output directory before and after the reckon build: an empty before-set and an after-set of TWO files, ledger.json and reckoning.md.\n\nCORRECTED AT PRO-0103'S VERIFICATION, and the correction is the point. This note previously said four files — adding delta.json and delta.md — and said each carried its byte count and sha256. run.json holds count 2, two filenames, and no digests at all. The delta files are written after the sweep, so they were never in the after-set; and cmd_take discards the digests sweep() computes, so they were never stored. The case was recording evidence it did not have, and campaign.py counted it witnessed without opening the artifact. The count that stands is 2, which is non-zero and is what the recorder actually saw. DEF-205 holds the repair.",
    "witness": {
      "recorder": "sweep() in scripts/reckoning/reckoning.py: a recursive walk of the run directory taken before and after the reckon build and stored in run.json under effect. It computes a byte count and sha256 per file. When this case was written cmd_take stored only the names and the count, so the recorder was stronger than the record it left — DEF-205, closed by PRO-0106. The record now carries the digest of every file written, the before-and-after pair for any file rewritten between the sweeps, and any file the build stopped writing. The count of two is unchanged and so is this case's two-way control.",
      "effect": "filesystem-write",
      "count": 4
    }
  },
  {
    "id": "CASE-0430",
    "req": "REQ-100",
    "surface": "SURF-024",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/spec_citation_measure.py",
      "scripts/campaign/spec_citation_arm.py",
      "docs/test-campaign/evidence/PRO-0101/provenance-measure.txt",
      "docs/test-campaign/evidence/PRO-0101/provenance-arming.txt"
    ],
    "armed": true,
    "armedBy": "Two mutations on a scratch copy of docs/specs, both watched red. Dropping the **Brief:** line from spec-PRO-0001.md reds it. Rewriting the value to the unbacktick-ed prose '**Brief:** the cua brief' also reds it, which is the arming that matters: a header that looks present and parses to nothing is the failure a presence check written as a substring search would pass.",
    "note": "Presence. All 99 specs in docs/specs carry a citation the grammar can parse: 71 by brief path, 4 declaring no brief, 24 in legacy prose. Before this item 24 carried nothing at all — PRO-0001..0017, 0034, 0045, 0047, 0063, 0075, 0076, 0096. The header form is anchored to the start of a line so a sentence mentioning **Brief:** in the body cannot satisfy it, and a header whose value the grammar cannot parse counts as uncited rather than falling through to the body."
  },
  {
    "id": "CASE-0431",
    "req": "REQ-100",
    "surface": "SURF-024",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/spec_citation_measure.py",
      "scripts/campaign/spec_citation_arm.py",
      "docs/test-campaign/evidence/PRO-0101/provenance-measure.txt",
      "docs/test-campaign/evidence/PRO-0101/provenance-arming.txt"
    ],
    "armed": true,
    "armedBy": "Two mutations, both watched red on scratch copies. Rewriting PRO-0001's citation to docs/features-to-triage/99-no-such-brief.md reds it. Deleting 01-cua-schema-facade.md from the scratch queue while its citation stands also reds it — the citation-names-a-deleted-path case the spec's added clause asks for, arrived at from the queue's side rather than the spec's.",
    "note": "Resolution in the working tree, proved in the same pass as presence. Every path-form and legacy-prose citation names a file that exists in docs/features-to-triage. The lookup runs against whichever brief directory is in play rather than against the repository root, so a scratch queue answers for itself; the first version fell back to the real tree and a deleted scratch brief could not have reddened it."
  },
  {
    "id": "CASE-0432",
    "req": "REQ-100",
    "surface": "SURF-024",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/spec_citation_measure.py",
      "scripts/campaign/spec_citation_arm.py",
      "docs/test-campaign/evidence/PRO-0101/provenance-measure.txt",
      "docs/test-campaign/evidence/PRO-0101/provenance-arming.txt",
      "docs/test-campaign/evidence/PRO-0101/pinned-shas.txt"
    ],
    "armed": true,
    "armedBy": "Pinned to immutable shas, both read with `git cat-file -e` before being written into the instrument, because this campaign has already had an arming resolve its reference with `git merge-base` and report 'caught 0 of the 2 known offenders' once the fix merged. PRO-0001's citation rewritten to `docs/features-to-triage/92-a-spec-says-which-brief-it-came-from.md @ 400808d` reds the check: 400808d is the triage commit that deleted that brief. The same citation `@ 3fb7681` stays PASS: 3fb7681 holds it. Both verdicts were pinned in the arming table and both were observed. Recorded in pinned-shas.txt with 8de8804 (the restore, HOLDS) and 9f99a0f (the tree the spec measured, ABSENT).",
    "note": "The consumed-brief form. A citation written `path @ sha` resolves against git rather than the working tree, so a brief triage deleted is still recoverable from the commit that holds it. No spec on this tree uses the form yet — the shared triage stage will write it when it consumes a brief — so this case is the only thing exercising that code path, and it is armed two-way rather than one."
  },
  {
    "id": "CASE-0433",
    "req": "REQ-100",
    "surface": "SURF-024",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/spec_citation_measure.py",
      "scripts/campaign/spec_citation_arm.py",
      "docs/test-campaign/evidence/PRO-0101/provenance-measure.txt",
      "docs/test-campaign/evidence/PRO-0101/provenance-arming.txt"
    ],
    "armed": true,
    "armedBy": "Replacing the whole of PRO-0075's citation line with '**Brief:** none. See above' reds it. The mutation had to replace the line rather than its opening: a first version replaced only the prefix, the rest of the sentence stayed attached, and the reason it was shortening remained long enough to pass — a NOT ARMED report against a check that worked.",
    "note": "A spec that came from something other than a brief says so, in at least 20 characters that name an artifact a reader can open — a backticked file or tool, a PRD section, or a sha. A length floor alone is cleared by boilerplate: 'none. not applicable here' is 25 characters and names nothing. Four specs declare it: PRO-0063 (the screenshot-encoding research, as a follow-on to PRO-0006), PRO-0075 (the campaign's own report, when test-campaign 0.8.0 brought a plane wave 9 had never run), PRO-0076 (asked for directly, against PRD §9 and §10) and PRO-0096, which already carried the form. Silence and 'unlinked' must not look alike, which is what the bare word 'none' would make them."
  },
  {
    "id": "CASE-0434",
    "req": "REQ-100",
    "surface": "SURF-024",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/spec_citation_measure.py",
      "scripts/campaign/spec_citation_arm.py",
      "docs/test-campaign/evidence/PRO-0101/provenance-measure.txt",
      "docs/test-campaign/evidence/PRO-0101/provenance-arming.txt"
    ],
    "armed": true,
    "armedBy": "Pointing spec-PRO-0002.md's citation at 01-cua-schema-facade.md, which PRO-0001 already claims, reds it. The same mutation also reds the reverse-direction check, because brief 02 becomes unclaimed — collateral reported alongside each mutation rather than judged.",
    "note": "The header citation is one-to-one: no brief is claimed by two specs. Uniqueness is asserted over the header form only, deliberately. 00-WAVE-7-DIRECTION.md is mentioned in the body of eleven specs because it is the direction document all eleven were cut from, and three more briefs are shared the same way, so requiring uniqueness over every path mention would fail on a convention that is correct."
  },
  {
    "id": "CASE-0435",
    "req": "REQ-100",
    "surface": "SURF-024",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/spec_citation_measure.py",
      "scripts/campaign/spec_citation_arm.py",
      "docs/test-campaign/evidence/PRO-0101/provenance-measure.txt",
      "docs/test-campaign/evidence/PRO-0101/provenance-arming.txt"
    ],
    "armed": true,
    "armedBy": "Three mutations, each watched red. Adding 97-arming.md to the scratch queue reds the claimed-or-registered check. Adding a register row for 98-not-a-brief.md, which the queue does not hold, reds the stale-register check. Blanking the reason column of brief 96's row reds the reason check — without that one the register would have decayed into a list of exemptions nobody justified, which is the absence it exists to replace.",
    "note": "The reverse direction is total: each of the 96 briefs in the queue is claimed by a spec (92) or carries a row in docs/feature-specs/UNCLAIMED-BRIEFS.md with a reason (4). The four registered are brief 23 and brief 40, whose ledger rows PRO-0022 and PRO-0039 have no spec file at all (DEF-215); brief 85, which is PRO-0092, held for a quiet machine and unspecced; and brief 96, filed 2026-08-22 and untriaged. A register row is only a row if it carries 20 characters of reason.",
    "defects": [
      "DEF-215"
    ]
  },
  {
    "id": "CASE-0436",
    "req": "REQ-101",
    "surface": "SURF-024",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/spec_citation_measure.py",
      "scripts/campaign/spec_citation_arm.py",
      "docs/test-campaign/evidence/PRO-0101/provenance-measure.txt",
      "docs/test-campaign/evidence/PRO-0101/provenance-arming.txt"
    ],
    "armed": true,
    "armedBy": "Two mutations to the regex on a scratch copy of reckon.py, one per direction. Loosening it to the pre-fix shape \\A([^-]+-[^-]+)\\b makes '03-menu' score as an id and reds the refusal check alone. Tightening it to \\A([A-Za-z]{4,}-\\d{2,})\\b makes SCR-0075 unreadable and reds the reads-a-real-id check alone. Neither mutation reds the other check, so the two directions are separately watched rather than one predicate reported twice.",
    "note": "The shared join refuses to turn a brief's position in a directory listing into a citation. project_id_in in the reckon tool is executed rather than read: '03-menu-bar-key-equivalents.md' must score no id, and 'SCR-0075-dead-credential.md' must still score SCR-0075. PRO-0102 had already removed the fallback at 224a696, so this case verifies rather than re-edits, per spec-PRO-0101's assumption that whichever item reaches it second does not edit it twice. The counterexample is in this repo: brief 35 is PRO-0034 and its own retirement banner says so, while the number points at PRO-0035, 'The browser catalogue stops guessing'."
  },
  {
    "id": "CASE-0437",
    "req": "REQ-101",
    "surface": "SURF-024",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/spec_citation_measure.py",
      "scripts/campaign/spec_citation_arm.py",
      "docs/test-campaign/evidence/PRO-0101/provenance-measure.txt",
      "docs/test-campaign/evidence/PRO-0101/provenance-arming.txt"
    ],
    "armed": true,
    "armedBy": "Four mutations on a scratch copy of the skill, never the live files. Dropping the scaffold's **Brief:** line reds the scaffold check; rewriting its value to '<the brief>' reds the path-form check; rewording 'records the commit that still holds it' reds the consumed-form check; retitling the table row 'The feature was never a brief' reds the no-brief check; and rewording SKILL.md's 'write the citation into the spec's' reds the procedure check. The arming runs on copies because a predecessor on another item mutated the live shared skill, was killed between mutation and revert, and left every project on this machine reading a skill with an arming mutation in its heading for about an hour.",
    "note": "The shipyard triage stage records the citation at id allocation. Four checks over the shared skill: the spec scaffold carries a **Brief:** line, that line's example is a docs/features-to-triage path, references/spec-format.md documents the consumed form and the no-brief form, and SKILL.md step 1 tells triage to write it. Read from the source tree at ~/Dev/fledgeling-plugins rather than the installed plugin cache, because the cache lags a version — wave 16 found reckon 1.0.0 still carrying a fault fixed in source at 81ad488."
  },
  {
    "id": "CASE-0438",
    "req": "REQ-100",
    "surface": "SURF-024",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/spec_citation_measure.py",
      "scripts/campaign/spec_citation_arm.py",
      "docs/test-campaign/evidence/PRO-0101/provenance-measure.txt",
      "docs/test-campaign/evidence/PRO-0101/provenance-arming.txt",
      "docs/test-campaign/evidence/PRO-0101/pinned-shas.txt"
    ],
    "armed": true,
    "armedBy": "spec_citation_arm.py itself. It refuses to report anything unless an unmutated scratch copy is green first, it asserts that each mutation matched something before measuring, it pins the expected verdict per mutation rather than assuming FAIL, and it exits 1 on any check no mutation covers. Three of its own mutations reported NOT ARMED against checks that worked and each was the mutation's fault — a partial line replacement, two regexes emitting a doubled backslash into the mutated source, and a scaffold quoted with newlines that broke the verdict parser and read as MISSING.",
    "note": "Every check in spec_citation_measure.py has been watched to fail. 23 mutations, each pinned in the table to the verdict it must produce, 15 of 15 checks covered, exit 0. This case exists because two instruments in this campaign have reported a clean zero they could not have earned: one detector's regex could not cross a parenthesis and missed the line it was written for, and one census resolved its arming with git merge-base and read the fixed tree."
  },
  {
    "id": "CASE-0439",
    "req": "REQ-100",
    "surface": "SURF-024",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/spec_citation_measure.py",
      "scripts/campaign/spec_citation_arm.py",
      "docs/test-campaign/evidence/PRO-0101/provenance-measure.txt",
      "docs/test-campaign/evidence/PRO-0101/provenance-arming.txt",
      "docs/test-campaign/evidence/PRO-0101/gemini-review.md"
    ],
    "armed": true,
    "armedBy": "Changing 57-vm-targets.md's count in the register from 7 specs to 6 reds it. The mutation is the count rather than the row, because a check that only asked whether the brief was listed would pass while the number it publishes was wrong.",
    "note": "Uniqueness holds over the header form only, and the four briefs that make that necessary are now enumerated with their counts instead of being silently exempt: 00-WAVE-7-DIRECTION.md over 11 specs, 57-vm-targets.md over 7, 55-three-tests-still-redden-the-gate.md and 58-swiftui-conversion-direction.md over 2 each. Each is a direction document several items were cut from at once. Raised by the out-of-family review, which was right that a prose-claimed brief sat outside the uniqueness check. Its other proposal — normalise all 24 prose citations to headers — was measured against this tree and rejected: it would put brief 55 on two headers and brief 57 on seven, breaking the invariant it was meant to strengthen. A brief one spec owns by header is not a shared parent whatever else mentions it, which is why brief 35 (PRO-0034's, referred to by PRO-0051) is not in the table."
  },
  {
    "id": "CASE-0440",
    "req": "REQ-100",
    "surface": "SURF-024",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/spec_citation_measure.py",
      "scripts/campaign/spec_citation_arm.py",
      "docs/test-campaign/evidence/PRO-0101/provenance-measure.txt",
      "docs/test-campaign/evidence/PRO-0101/provenance-arming.txt",
      "docs/test-campaign/evidence/PRO-0101/gemini-review.md"
    ],
    "armed": true,
    "armedBy": "Three shapes against a purpose-built git fixture the arming builds and throws away, because the real history holds no commit where a cited brief path is a tree or an empty file, and a branch no mutation can reach is a branch nobody has watched. A citation naming a blob with bytes PASSes; one naming a zero-byte blob FAILs; one naming a path that is a directory FAILs. All three verdicts were pinned in the table before the run and all three were observed.",
    "note": "A commit-form citation asserts the brief is readable at that commit, and `git cat-file -e` does not say that: it is satisfied by a tree entry and by a zero-byte blob. The check now reads the object type and its size. Raised by the out-of-family review of this item, which named the same two shapes."
  },
  {
    "id": "CASE-0457",
    "req": "REQ-047",
    "surface": "SURF-002",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/MutationSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0092/seam-arming.json",
      "scripts/campaign/mutation_seam_arm.py"
    ],
    "armed": true,
    "armedBy": "The mutant re-applied at the site the extraction moved it to, the mutation confirmed to have landed by re-reading the file, and `listAppsHonoursIncludeWindowless` watched going red: `guard includeWindowless || app.isRegular else { return nil }  ->  guard includeWindowless && app.isRegular else { return nil }`, exit 1, Test run with 1 test in 1 suite failed after 0.001 seconds with 3 issues.. Reverted, and the tree measured clean after.",
    "note": "Survivor 1 of PRO-0080's sample. `AXEngineImpl.listApps` read `NSWorkspace.shared.runningApplications` directly, so no test could state a population and the `||` that reads `includeWindowless` was unwatched. The seam is a parameter with the live reader as its default, in `GuestProvider.init(executable:timeoutMs:run:)`'s shape. With `&&` the flag inverts: asking for windowless apps returns only the ones with a Dock icon and asking for the ordinary list returns nothing at all. Three clauses, and the accessory app is named directly rather than left to a count."
  },
  {
    "id": "CASE-0458",
    "req": "REQ-108",
    "surface": "SURF-002",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/MutationSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0092/seam-arming.json",
      "scripts/campaign/mutation_seam_arm.py"
    ],
    "armed": true,
    "armedBy": "The mutant re-applied at the site the extraction moved it to, the mutation confirmed to have landed by re-reading the file, and `dispatchDefaultsAgreeWithTheCatalogue` watched going red: `args.bool(\"includeTiles\", false)  ->  args.bool(\"includeTiles\", true)`, exit 1, Test run with 1 test in 1 suite failed after 0.004 seconds with 1 issue.. Reverted, and the tree measured clean after.",
    "note": "Survivor 2. `includeTiles` decoded to `false` and the mutant made it `true`, so every stability run would compare pixel tiles nobody asked for. The oracle is the published schema rather than the source: `Dispatch.swift`'s own header says ToolCatalogue is the source of truth for names and defaults, and this checks the second copy against the first. Sixteen (tool, argument) pairs are comparable and the join is floored at twelve, so a parser that stopped finding sites cannot pass by comparing nothing.",
    "source": {
      "analyzer": "NSRegularExpression over Sources/ProctorAgent/Dispatch.swift, sectioned by `// MARK: - proctor_<tool>`, joined per (tool, argument) against the prose default parsed from each boolean property's description in ToolCatalogue.all",
      "examined": 34
    }
  },
  {
    "id": "CASE-0459",
    "req": "REQ-108",
    "surface": "SURF-002",
    "oracle": "source-analysis",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/MutationSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0092/seam-arming.json",
      "scripts/campaign/mutation_seam_arm.py"
    ],
    "armed": true,
    "armedBy": "The mutant re-applied at the site the extraction moved it to, the mutation confirmed to have landed by re-reading the file, and `dispatchDefaultsAgreeWithTheCatalogue` watched going red: `args.bool(\"presentation\", true)  ->  args.bool(\"presentation\", false)`, exit 1, Test run with 1 test in 1 suite failed after 0.004 seconds with 1 issue.. Reverted, and the tree measured clean after.",
    "note": "Survivor 8, the same check from the other direction. `presentation` decoded to `true` and the mutant made it `false`, so `proctor_inspect` would stop returning the CALayer presentation values its schema says it returns by default. Armed separately from CASE-0458 because one test covering two mutants proves nothing about the second unless the second is watched.",
    "source": {
      "analyzer": "as CASE-0458",
      "examined": 34
    }
  },
  {
    "id": "CASE-0460",
    "req": "REQ-108",
    "surface": "SURF-002",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/MutationSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0092/seam-arming.json",
      "scripts/campaign/mutation_seam_arm.py"
    ],
    "armed": true,
    "armedBy": "The mutant re-applied at the site the extraction moved it to, the mutation confirmed to have landed by re-reading the file, and `snapshotOptionDefaultsMatchTheSchema` watched going red: `var includeInvisible: Bool = false  ->  var includeInvisible: Bool = true`, exit 1, Test run with 1 test in 1 suite failed after 0.001 seconds with 1 issue.. Reverted, and the tree measured clean after.",
    "note": "Survivor 4. `SnapshotOptions.includeInvisible` defaulted to `false` and the mutant made it `true`, so an unasked-for snapshot would carry zero-area and offscreen nodes. Never `no seam` — the struct was always constructible — only untested. The oracle is `proctor_snapshot`'s own schema prose rather than the literal, so the test does not restate the value it checks."
  },
  {
    "id": "CASE-0461",
    "req": "REQ-047",
    "surface": "SURF-002",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/MutationSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0092/seam-arming.json",
      "docs/test-campaign/evidence/PRO-0092/correlate-crash-arming.txt"
    ],
    "armed": true,
    "armedBy": "The mutant re-applied at the site the extraction moved it to, the mutation confirmed to have landed by re-reading the file, and `correlateReturnsTheMatchingWindowsNumber` watched going red: `return matches.count == 1 ? matches[0].number : nil  ->  return matches.count == 1 ? matches[1].number : nil`, exit 1. Reverted, and the tree measured clean after. THE KILL ARRIVES AS AN ABORT RATHER THAN A FAILING ASSERTION, and that is a property of the site rather than of the test: `matches[1]` on a one-element array traps, so the run printed `Fatal error: Index out of range`, `Executed 0 tests, with 0 failures`, ZERO verdict lines and `FAIL: no swift-testing verdict line`, at signal 5. That is DEF-140's class arriving from production rather than from a test. The subscript is total under its own `count == 1` guard, so it is recorded rather than converted. Full output in correlate-crash-arming.txt.",
    "note": "Survivor 10. `CGWindowIndex.correlate(frame:title:in:)` already took its records as a parameter and nothing had ever called it. A wrong window number captures the wrong window and nothing downstream can tell, which is why the function refuses to guess when more than one candidate fits — and that refusal was unwatched too. Four clauses: the number, order-independence, two fitting candidates reported absent, and a non-zero-layer window not a candidate at all."
  },
  {
    "id": "CASE-0462",
    "req": "REQ-047",
    "surface": "SURF-002",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/MutationSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0092/seam-arming.json",
      "scripts/campaign/mutation_seam_arm.py"
    ],
    "armed": true,
    "armedBy": "The mutant re-applied at the site the extraction moved it to, the mutation confirmed to have landed by re-reading the file, and `barePidIsSynthesisedExactlyOnce` watched going red: `!candidates.contains(where: { $0.pid == pid }) else { return candidates }  ->  !candidates.contains(where: { $0.pid != pid }) else { return candidates }`, exit 1, Test run with 1 test in 1 suite failed after 0.001 seconds with 2 issues.. Reverted, and the tree measured clean after.",
    "note": "Survivor 7. The bare-pid rule was inline in `killProcesses`, which enumerates `NSWorkspace` first, so the rule could not be stated on its own. `KillCandidates.includingBarePid` is that rule as a pure function. With `!=` it appends nothing whenever any other process is running and duplicates the target whenever none is, so `proctor_kill` on a bare pid that is not a GUI application would report nothing matched. Both directions are asserted, and the synthesised candidate's absent bundle id is asserted with them because that is the fail-closed half."
  },
  {
    "id": "CASE-0463",
    "req": "REQ-047",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/MutationSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0092/seam-arming.json",
      "scripts/campaign/mutation_seam_arm.py"
    ],
    "armed": true,
    "armedBy": "The mutant re-applied at the site the extraction moved it to, the mutation confirmed to have landed by re-reading the file, and `hudPanelTakesKeyAndRefusesMain` watched going red: `override var canBecomeMain: Bool { false }  ->  override var canBecomeMain: Bool { true }`, exit 1, Test run with 1 test in 1 suite failed after 0.967 seconds with 1 issue.. Reverted, and the tree measured clean after.",
    "note": "Survivor 9. `HUDPanel` was `private`, so nothing could ask it either of the two answers the class exists for. Made internal, both overrides unchanged. A HUD that can become main takes the main window from the application the run is driving while still taking key so its Stop control stays clickable, and the pair is what the test asserts."
  },
  {
    "id": "CASE-0464",
    "req": "REQ-047",
    "surface": "SURF-004",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/MutationSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0092/seam-arming.json",
      "scripts/campaign/mutation_seam_arm.py"
    ],
    "armed": true,
    "armedBy": "The mutant re-applied at the site the extraction moved it to, the mutation confirmed to have landed by re-reading the file, and `lightPaletteTonesShareOneBase` watched going red: `ink3: hex(17, 18, 21, 0.36), ink4: hex(17, 18, 21, 0.16),  ->  ink3: hex(18, 18, 21, 0.36), ink4: hex(17, 18, 21, 0.16),`, exit 1, Test run with 1 test in 1 suite failed after 0.001 seconds with 1 issue.. Reverted, and the tree measured clean after.",
    "note": "Survivor 11. The oracle is not the number. The light palette's ink and fill tones are one graphite at several opacities, and `hex(17, 18, 21, …)` drifting to `hex(18, …)` in one tone is a different colour wearing the same name. The invariant holds whatever the base is, so the check does not restate the value it checks. The opacity floor is five distinct values of seven tones, measured: `fill2` and `separator` are both 0.09, which is the palette agreeing with itself."
  },
  {
    "id": "CASE-0465",
    "req": "REQ-047",
    "surface": "SURF-005",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/MutationSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0092/seam-arming.json",
      "scripts/campaign/mutation_seam_arm.py"
    ],
    "armed": true,
    "armedBy": "The mutant re-applied at the site the extraction moved it to, the mutation confirmed to have landed by re-reading the file, and `tapDisableNoticesAreTheOnlyDisableNotices` watched going red: `type == .tapDisabledByTimeout || type == .tapDisabledByUserInput  ->  type != .tapDisabledByTimeout || type == .tapDisabledByUserInput`, exit 1, Test run with 1 test in 1 suite failed after 0.001 seconds with 2 issues.. Reverted, and the tree measured clean after.",
    "note": "Survivor 12. The predicate was inline in a handler that cannot be called from a test — it needs a live `CGEvent` and a tap this process has been granted — while the question it asks first is pure. With `!=` every ordinary keystroke reads as macOS switching the tap off, so the second key a person pressed would release the hold, and a real disable notice would fall through as if it were input. Nine ordinary event types are checked alongside the two notices."
  },
  {
    "id": "CASE-0466",
    "req": "REQ-047",
    "surface": "SURF-012",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/MutationSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0092/seam-arming.json",
      "scripts/campaign/mutation_seam_arm.py"
    ],
    "armed": true,
    "armedBy": "The mutant re-applied at the site the extraction moved it to, the mutation confirmed to have landed by re-reading the file, and `publicKeyURLNamesAFileThatRoundTrips` watched going red: `directory.appendingPathComponent(\"audit.pub\", isDirectory: false)  ->  directory.appendingPathComponent(\"audit.pub\", isDirectory: true)`, exit 1, Test run with 1 test in 1 suite failed after 0.002 seconds with 1 issue.. Reverted, and the tree measured clean after.",
    "note": "Survivor 13. `isDirectory:` is load-bearing rather than cosmetic: with `true` the URL carries a trailing slash and claims to be a directory, and the cached public key is what keeps the audit write path out of the Keychain entirely — no unlock and no prompt while the agent works alone. The seam is a function of the directory. Both halves are asserted: the URL does not claim to be a directory, and bytes written at it come back through it as a regular file."
  },
  {
    "id": "CASE-0467",
    "req": "REQ-109",
    "surface": "SURF-026",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/mutate_swift.py",
      "scripts/campaign/mutation_timeout_arm.py",
      "docs/test-campaign/evidence/PRO-0092/timeout-arming.json"
    ],
    "armed": true,
    "armedBy": "Both runners driven from one fixture, which is the arming rather than a description of it. `main`'s runner and this one were each given a suite runner returning green for the baseline and a timeout for the mutant, over the same real tracked file. Before: `verdict=killed scored=1 survivalRate=0.0`. After: `verdict=TIMEOUT scored=0 timedOut=1 survivalRate=None`. A genuine failure is unchanged in both, so the change is not simply relabelling every red run. The pre-change source is matched by a marker string before it is trusted, and both runs left the tree clean.",
    "note": "The instrument fault DEF-033 was measured through. A run that reached the bound was scored KILLED, which is the direction that flatters the suite: starvation can turn a survivor into a false kill and can never turn a kill into a false survivor. PRO-0080's first ProctorAgent sample carried two at exactly 600.0s under a load average of 271, and the reported rate was 79.2% where the honest one was 86.4% — a reader of the summary alone had no way to tell them from the three real kills. TIMEOUT is now its own verdict, counted apart and left out of the survival-rate denominator, and every scored mutant within 2% of the bound is named in the summary."
  },
  {
    "id": "CASE-0468",
    "req": "REQ-110",
    "surface": "SURF-026",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0092/mutation-agent-pro0092.json",
      "docs/test-campaign/evidence/PRO-0092/mutation-agent-pro0092.txt",
      "docs/test-campaign/evidence/PRO-0092/machine-readings.json"
    ],
    "armed": true,
    "armedBy": "The runner's own three safety rules are the arming for the measurement's integrity rather than for its value: it refused to start until `git status` was clean, ran a green baseline before mutating anything, and reported `tree clean after: True` at the end. The instrument's new verdict is armed separately at CASE-0467. What makes THIS reading trustworthy in the direction that matters is that no mutant reached the bound and none came near it — the longest was 36.0s of a 600s bound (SessionAssert.swift:63; corrected at verification from 31.9s, the third-longest), `nearBound` is empty, and `timedOut` is 0 — so no verdict here is a starved run wearing a kill.",
    "note": "The re-measurement DEF-033 asked for. 24 mutants over 3,158 sites across the same 84 ProctorAgent files, seed 20260823 against PRO-0080's 20260821, so the two mutant sets are disjoint and this measures the package rather than the tests written for the first sample. 20 SURVIVED, 4 killed, 0 unbuildable, 0 TIMEOUT. Survival 20/24 = 83.3% against DEF-033's 19/22 = 86.4%.\n\nTHE TWO ARE WITHIN ONE MUTANT OF EACH OTHER. One mutant is 4.2 points at n=24, so a 3.1-point move is smaller than the resolution of either sample, and the package-level rate has not been shown to move. That is the arithmetic rather than a hedge: ten survivors killed out of a pool of 3,158 sites is 0.3% of the surface, and killing ten named sites cannot move a rate sampled at random from the other 3,148.\n\nThe pool moved from 3,189 to 3,158 sites over the same 84 files, which is the package changing between the two waves rather than a different target set — the file lists are identical.\n\nSurvivors spread over 17 distinct files with no file carrying more than three, so 'no seam' is a property of the package rather than of the seven files PRO-0080's seed happened to hit.\n\nMACHINE, both ends and every 45 seconds between. Before: load per core 4.681, 3.373, 2.566 with pressure critical then tight then tight, and thermal limited (held 1,611s and 1,643s) then not_limited (held 0s). After: 0.677, 0.819, 1.136, pressure healthy then busy, thermal limited throughout. During: 14 samples, load per core 0.542 to 1.894, thermal flipping five times — limited, not_limited, limited, not_limited, limited — while load never moved more than 1.4 per core. Every one of the 24 was scored inside that window, and none of them near the bound, so no mutant needs reporting apart."
  },
  {
    "id": "CASE-0469",
    "req": "REQ-108",
    "surface": "SURF-002",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0092/fresh-sample-attribution.txt",
      "docs/test-campaign/evidence/PRO-0092/mutation-agent-pro0092.json"
    ],
    "armed": true,
    "armedBy": "The fresh seed armed it rather than this item. `Dispatch.swift:244`, `args.bool(\"normalize\", true)` mutated to `false`, was chosen by seed 20260823 out of 3,158 sites and scored `killed` in the sample. Re-applied afterwards to attribute the kill: `dispatchDefaultsAgreeWithTheCatalogue` exits 1 with `[\"proctor_capture.normalize decodes false, the schema says true\"]`, and the mutation was confirmed landed before the verdict was read.",
    "note": "The case for closing a class rather than nine instances. The agreement check was written for two named survivors of the FIRST sample and it killed a third mutant in the SECOND, at a decode site nobody had looked at, under a seed the item never saw. 34 decode sites in Dispatch.swift are now defended by one check where a per-site test would have defended two.\n\nWhat it does not say: 34 of 3,158 sites is 1.1% of the pool, so this is a class closed rather than the package covered."
  },
  {
    "id": "CASE-0470",
    "req": "REQ-108",
    "surface": "SURF-002",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/MutationSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0092/seam-arming.json",
      "Sources/ProctorAgent/Dispatch.swift"
    ],
    "armed": true,
    "armedBy": "The mutant applied inside the decoder the extraction created, confirmed landed by re-reading the file, and `decodedArgumentsResolveToThePublishedDefaults` watched going red: `includeTiles = args.bool(\"includeTiles\", false)  ->  includeTiles = args.bool(\"includeTiles\", true)`, exit 1, Test run with 1 test in 1 suite failed after 0.001 seconds with 1 issue.. Reverted, tree clean after.",
    "note": "Survivor 2, asked of the decoder rather than of the source. CASE-0458 kills the same mutant by reading Dispatch.swift as text, and this item's out-of-family review named that twice as a kill registered without ever asking what an omitted argument resolves to. `Dispatcher.StabilityArguments` is the seam: an `Args` with no keys decodes to the defaults the schema publishes, and an `Args` carrying all three decodes to the supplied values, so a decoder returning a constant satisfies neither. The two decoded values are compared for inequality as well, which is what stops both clauses being satisfied by one answer.\n\nBoth cases are kept. This one is the behavioural kill; CASE-0458 is the class check over the other 32 decode sites, and it is registered as source-analysis because that is what it is."
  },
  {
    "id": "CASE-0471",
    "req": "REQ-108",
    "surface": "SURF-002",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/MutationSeamTests.swift",
      "docs/test-campaign/evidence/PRO-0092/seam-arming.json",
      "Sources/ProctorAgent/Dispatch.swift"
    ],
    "armed": true,
    "armedBy": "The mutant applied inside the decoder the extraction created, confirmed landed by re-reading the file, and `decodedArgumentsResolveToThePublishedDefaults` watched going red: `presentation = args.bool(\"presentation\", true)  ->  presentation = args.bool(\"presentation\", false)`, exit 1, Test run with 1 test in 1 suite failed after 0.001 seconds with 1 issue.. Reverted, tree clean after.",
    "note": "Survivor 8, the same at runtime through `Dispatcher.InspectArguments`. Armed separately from CASE-0470 because one test covering two mutants proves nothing about the second unless the second is watched.\n\nMoving the decode found a fault in the class check on the way: `InspectArguments` was first declared in the stability section, so the source parser attributed `presentation` to proctor_stability and the (tool, argument) join went nil. Each struct now sits under its own tool MARK. A parser sectioned by comment markers is only as right as where the code sits relative to them, and that failure was visible because CASE-0458 asserts the two pairs it must find by name."
  },
  {
    "id": "CASE-0500",
    "req": "REQ-121",
    "surface": "SURF-023",
    "state": "classifying",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/reckon-selftest.txt"
    ],
    "armed": true,
    "armedBy": "selftest.py section 17d, `fenced`: PRE_FIX_SCAN over the same document returns ['DEF-0404'] while scan_ids returns cited [] and shown {'fenced': ['DEF-0404']}. Two-way control in the same fixture: the identical id written into prose instead returns cited ['DEF-0404'] and no shown region, so the exclusion is distinguishable from a scanner that stopped finding ids.",
    "note": "One exclusion kind, on its own. A fixture carrying a fence, a comment and a strikethrough together proves whichever region is stripped first and nothing about the other two. The pre-fix expression is restated verbatim in the fixture and asserted to give the wrong answer first, so a green here cannot come from an input that never exercised the defect."
  },
  {
    "id": "CASE-0501",
    "req": "REQ-121",
    "surface": "SURF-023",
    "state": "classifying",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/reckon-selftest.txt"
    ],
    "armed": true,
    "armedBy": "selftest.py section 17d, `comment`: `<!-- DEF-0404 was the example -->` yields cited [] and shown {'comment': ['DEF-0404']}, against PRE_FIX_SCAN's ['DEF-0404']; the prose control in the same fixture still cites it.",
    "note": "The second of the three exclusions, armed separately from CASE-0500. The pre-fix expression is restated verbatim in the fixture and asserted to give the wrong answer first, so a green here cannot come from an input that never exercised the defect."
  },
  {
    "id": "CASE-0502",
    "req": "REQ-121",
    "surface": "SURF-023",
    "state": "classifying",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/reckon-selftest.txt"
    ],
    "armed": true,
    "armedBy": "selftest.py section 17d, `struck`: `~~DEF-0404~~ was the example.` yields cited [] and shown {'struck': ['DEF-0404']}, against PRE_FIX_SCAN's ['DEF-0404']; the prose control still cites.",
    "note": "The third exclusion, armed separately. The pre-fix expression is restated verbatim in the fixture and asserted to give the wrong answer first, so a green here cannot come from an input that never exercised the defect."
  },
  {
    "id": "CASE-0503",
    "req": "REQ-121",
    "surface": "SURF-023",
    "state": "classifying",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/reckon-selftest.txt"
    ],
    "armed": true,
    "armedBy": "selftest.py section 17e control: a brief reading 'This is about REQ-1 and nothing else.' still produces a cited edge to REQ-1 at confidence 1.0 after the placeholder rule lands.",
    "note": "The two-way control on the placeholder rule rather than on the exclusions. A rule that refused every id would pass CASE-0504 alone; this is the half that says it did not."
  },
  {
    "id": "CASE-0504",
    "req": "REQ-121",
    "surface": "SURF-023",
    "state": "classifying",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/reckon-selftest.txt"
    ],
    "armed": true,
    "armedBy": "selftest.py section 17e: `CASE-9999` in plain prose builds no cited edge, the brief row comes out `unjoined` rather than `unbuilt`, the row carries unclassifiable_ids ['CASE-9999'], and unclassified_inputs returns exactly [('id-shaped token', 'CASE-9999', 1)].",
    "note": "The third outcome rather than a second exclusion. Dropping a placeholder silently would be the same guess in the other direction, so it is named with its count and cannot carry `unbuilt`. The pre-fix expression is restated verbatim in the fixture and asserted to give the wrong answer first, so a green here cannot come from an input that never exercised the defect."
  },
  {
    "id": "CASE-0505",
    "req": "REQ-120",
    "surface": "SURF-023",
    "state": "classifying",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/reckon-selftest.txt"
    ],
    "armed": true,
    "armedBy": "selftest.py section 17a asserts each of `by design`, `invalid`, `obsolete`, `superseded`, `cannot reproduce`, `not-a-defect` and `vacuous` on its own: class `waived`, is_work_item False, and the word named in the row's `why`. PRE_FIX_WORD is restated and returns `broken` for every one of them. A loop that stopped at the first word would leave six unmeasured, so each prints its own line.",
    "note": "Seven words that mean the row owes nothing without meaning anybody repaired it. `resolved` is asserted separately and stays `verified-done` — it is the one word in DEF-202's sentence that does record a repair."
  },
  {
    "id": "CASE-0506",
    "req": "REQ-123",
    "surface": "SURF-023",
    "state": "classifying",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/reckon-selftest.txt"
    ],
    "armed": true,
    "armedBy": "selftest.py section 17b: `partially-fixed` and `partially fixed` both class `broken` with is_work_item True, and the row's `why` names the word and the word 'owes'.",
    "note": "The deliberate exception, and the one place in reckon where the fail-closed direction is chosen against a word that sounds like a closure. Retiring it would make the tool under-report for the first time."
  },
  {
    "id": "CASE-0507",
    "req": "REQ-120",
    "surface": "SURF-023",
    "state": "reporting",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/reckon-selftest.txt",
      "docs/test-campaign/evidence/PRO-0104/reckon-check-two-way.txt"
    ],
    "armed": true,
    "armedBy": "selftest.py section 17c: a defect row at `characterised` produces a `vocabulary` violation reading \"defect status 'characterised' is not a word this tool classifies\" and R.verdict returns 4.",
    "note": "The row is still placed, because the partition has to be total. What the gate refuses is a placement this tool chose being presented as a reading of the registry."
  },
  {
    "id": "CASE-0508",
    "req": "REQ-120",
    "surface": "SURF-023",
    "state": "reporting",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/reckon-selftest.txt"
    ],
    "armed": true,
    "armedBy": "selftest.py section 17c: a case row at `marinated` produces a `vocabulary` violation naming the word, at exit 4, and the classifier's own `why` for it says the word is not one this tool classifies rather than quoting a legality rule that does not exist.",
    "note": "The case vocabulary fails in the safe direction — an unknown case status is held `unmeasured` — and it was still a silent default before this."
  },
  {
    "id": "CASE-0509",
    "req": "REQ-120",
    "surface": "SURF-023",
    "state": "reporting",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/reckon-selftest.txt",
      "docs/test-campaign/evidence/PRO-0104/vocabulary-delta.json"
    ],
    "armed": true,
    "armedBy": "selftest.py section 17c: a requirement at `inconclusive` produces a `vocabulary` violation at exit 4, and the classified row's `why` contains 'not a word this tool classifies' and does NOT contain 'own account of itself'.",
    "note": "REQ-072 in this registry is the real instance, and the second assertion is the one that matters: the pre-fix branch classed it correctly by accident and explained it to every reader as a self-report. DEF-230 records what is left, which is a decision about the row."
  },
  {
    "id": "CASE-0510",
    "req": "REQ-120",
    "surface": "SURF-023",
    "state": "reporting",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/reckon-selftest.txt"
    ],
    "armed": true,
    "armedBy": "selftest.py section 17c: three defect rows carrying the same unknown word return one finding with count 3 and ids ['DEF-X1', 'DEF-X2', 'DEF-X3'], not three findings and not one naming one row.",
    "note": "The count is half of what makes it a finding. 'a word I do not know' sends nobody anywhere; 'this word, on these three rows' is a work order."
  },
  {
    "id": "CASE-0511",
    "req": "REQ-120",
    "surface": "SURF-023",
    "state": "reporting",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/reckon-selftest.txt"
    ],
    "armed": true,
    "armedBy": "selftest.py section 17c: a ledger carrying both a duplicated id and an unknown case status reports both kinds and R.verdict returns 1, not 4.",
    "note": "Exit 4 must not mask a structural fault. The ordering is asserted rather than assumed, because a new code inserted at the top of verdict() would swallow conservation violations while every other row here stayed green."
  },
  {
    "id": "CASE-0512",
    "req": "REQ-120",
    "surface": "SURF-023",
    "state": "reporting",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/reckon-selftest.txt"
    ],
    "armed": true,
    "armedBy": "selftest.py section 17c executes R.render over a ledger holding an unclassifiable defect status and asserts the output carries 'could not classify', the word 'characterised' and the row id DEF-R.",
    "note": "The exit code says only that something is wrong. A reader acts on the report, so the finding has to appear there, and render is executed rather than inspected."
  },
  {
    "id": "CASE-0513",
    "req": "REQ-123",
    "surface": "SURF-023",
    "state": "classifying",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/reckon-selftest.txt"
    ],
    "armed": true,
    "armedBy": "selftest.py section 17b: a defect row at `partially-fixed` classed `verified-done` is a placement violation reading 'may only be broken' at exit 1, and a row at `invalid` classed `broken` is a placement violation reading 'may only be waived'.",
    "note": "Legality rather than preference, in both directions: the two new sets are in DEFECT_LEGAL_CLASS, so a ledger whose class contradicts its own status is refused the same way cases have been since the first version."
  },
  {
    "id": "CASE-0514",
    "req": "REQ-121",
    "surface": "SURF-023",
    "state": "classifying",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/reckon-selftest.txt"
    ],
    "armed": true,
    "armedBy": "selftest.py section 17d, the reproduction DEF-201 was filed on: a brief whose only id-shaped token is `CASE-9999` inside a fenced example command builds no cited edge and classes `unjoined`, with the token and its count named in the row's `why`. Before the repair the same brief produced a confidence-1.0 citation and classed `unbuilt` — product work, on a token nobody cited.",
    "note": "`unbuilt` is the one class PRO-0102 narrowed to a positive claim: the registry was asked and said no. A fenced example must not be able to reach it."
  },
  {
    "id": "CASE-0515",
    "req": "REQ-120",
    "surface": "SURF-023",
    "state": "reporting",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/reckon-check-two-way.txt"
    ],
    "armed": true,
    "armedBy": "The two-way control on the gate itself, run through the CLI rather than the module: the same two-row ledger with defect status `open` gives 'gate: clean', exit 0; with `characterised` it gives the vocabulary violation naming the word and DEF-1, 'gate: FAILED (exit 4)', exit 4. Both readings are in the artifact.",
    "note": "One command, one field changed, two exits. It separates 'this gate fires' from 'this command always refuses', which a one-sided arming cannot."
  },
  {
    "id": "CASE-0516",
    "req": "REQ-120",
    "surface": "SURF-023",
    "state": "classifying",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/vocabulary-delta.json"
    ],
    "armed": true,
    "armedBy": "reckon 1.1.0 and 1.2.0 built over the identical tree (100 briefs, 335 cases, 129 defect rows, 105 requirements): class counts identical, work_items 147 both, rows reclassified 0, one cited edge removed (BRIEF-97 -> CASE-9999), join 21.0% both, build exit 0 -> 4 on two findings.",
    "note": "The blast radius, measured rather than asserted. The repair changes no classification on this corpus; what it changes is that two inputs previously resolved by a default are now named. The zero here is a fact about this repository's idiom — all 24 legacy citations sit in real prose at fence depth 0 — and not about the risk."
  },
  {
    "id": "CASE-0517",
    "req": "REQ-121",
    "surface": "SURF-024",
    "state": "cited by path",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/spec-citation-arm.txt",
      "scripts/campaign/spec_citation_measure.py"
    ],
    "armed": true,
    "armedBy": "spec_citation_arm.py mutation: PRO-0001's `**Brief:**` line wrapped in a ```text fence. `every spec carries a parseable citation` moves PASS -> FAIL, and the run records the reds it moved. Baseline on the unmutated scratch copy is 19/19 PASS.",
    "note": "A `**Brief:**` header inside a fence in the first 20 lines used to satisfy the check, because the header was read out of the raw text. One exclusion kind per mutation."
  },
  {
    "id": "CASE-0518",
    "req": "REQ-121",
    "surface": "SURF-024",
    "state": "cited by path",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/spec-citation-arm.txt",
      "scripts/campaign/spec_citation_measure.py"
    ],
    "armed": true,
    "armedBy": "spec_citation_arm.py mutation: the same citation wrapped in `<!-- ... -->`. Same check moves PASS -> FAIL, armed separately from CASE-0517.",
    "note": "The second kind. Proving the fence case and the comment case together would prove neither individually."
  },
  {
    "id": "CASE-0519",
    "req": "REQ-121",
    "surface": "SURF-024",
    "state": "cited by path",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/spec-citation-arm.txt",
      "scripts/campaign/spec_citation_measure.py"
    ],
    "armed": true,
    "armedBy": "spec_citation_arm.py mutation: the same citation wrapped in `~~ ... ~~`. Same check moves PASS -> FAIL, armed separately.",
    "note": "The third kind."
  },
  {
    "id": "CASE-0520",
    "req": "REQ-121",
    "surface": "SURF-024",
    "state": "registered as unclaimed",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/spec-citation-arm.txt",
      "scripts/campaign/spec_citation_arm.py"
    ],
    "armed": true,
    "armedBy": "spec_citation_arm.py mutation `add_brief_shown_as(FENCED)`: a new brief `97-arming.md` whose only trace anywhere is a fenced mention in spec-PRO-0003. `no brief's only trace is a shown-not-used mention` moves PASS -> FAIL and names the brief with its count.",
    "note": "Before the repair the same mutation left the check green: any body path counted as a claim, so a brief nobody built from read as claimed."
  },
  {
    "id": "CASE-0521",
    "req": "REQ-121",
    "surface": "SURF-024",
    "state": "registered as unclaimed",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/spec-citation-arm.txt",
      "scripts/campaign/spec_citation_arm.py"
    ],
    "armed": true,
    "armedBy": "The same mutation with `COMMENTED`: the only trace is inside an HTML comment. Same check moves PASS -> FAIL, armed separately.",
    "note": "One fixture per kind, in the reverse-totality direction as well as the citation-of-record one."
  },
  {
    "id": "CASE-0522",
    "req": "REQ-121",
    "surface": "SURF-024",
    "state": "registered as unclaimed",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/spec-citation-arm.txt",
      "scripts/campaign/spec_citation_arm.py"
    ],
    "armed": true,
    "armedBy": "The same mutation with `STRUCK`. Same check moves PASS -> FAIL, armed separately.",
    "note": "The third kind in this direction."
  },
  {
    "id": "CASE-0523",
    "req": "REQ-122",
    "surface": "SURF-024",
    "state": "registered as unclaimed",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/spec-citation-arm.txt",
      "scripts/campaign/spec_citation_measure.py"
    ],
    "armed": true,
    "armedBy": "spec_citation_arm.py mutation `add_brief_shown_as(PROSE)`: the brief is mentioned in plain body prose and claimed nowhere. `no brief is claimed only by an incidental mention` moves PASS -> FAIL, naming the brief, the count and the specs mentioning it.",
    "note": "DEF-203's third clause. A mention is a reference; an account is a decision somebody wrote down."
  },
  {
    "id": "CASE-0524",
    "req": "REQ-122",
    "surface": "SURF-024",
    "state": "cited by path",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/spec-citation-arm.txt",
      "scripts/campaign/spec_citation_measure.py"
    ],
    "armed": true,
    "armedBy": "spec_citation_arm.py appends `**Cut from:** `docs/features-to-triage/01-cua-schema-facade.md`` to a spec. `no brief is accounted for by an unrecognised relation` moves PASS -> FAIL naming the spec, the relation and the brief.",
    "note": "The vocabulary half of the same shape, in the citation instrument rather than the reckoning one: a header relation this check has no rule for is a finding rather than read either way. The three accepted relations are derived from the corpus — Brief 76, Direction 12, Supersedes 1 across 104 specs."
  },
  {
    "id": "CASE-0525",
    "req": "REQ-122",
    "surface": "SURF-024",
    "state": "declared no brief",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/spec-citation-arm.txt",
      "scripts/campaign/spec_citation_measure.py"
    ],
    "armed": true,
    "armedBy": "Three mutations on PRO-0075's `none.` citation, each moving `every `none` citation resolves to something here` PASS -> FAIL: a path that does not resolve (`docs/nowhere/imaginary-source.md`), a PRD section that does not exist (§99), and a backtick pair padded past the twenty-character floor (`whatever.md`).",
    "note": "The third is why a longer floor is not the repair: it clears the length floor and the names-an-artifact floor and resolves nothing. A name never satisfies the requirement, and it never breaks a reason that also carries something resolvable — naming the tool a finding came off, beside an artifact a reader can open, is honest description."
  },
  {
    "id": "CASE-0526",
    "req": "REQ-122",
    "surface": "SURF-024",
    "state": "registered as unclaimed",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/spec-citation-measure.txt",
      "scripts/campaign/spec_citation_measure.py"
    ],
    "armed": true,
    "armedBy": "spec_citation_measure.py on this tree after the strict rule: 19/19 checks, specs 104 (legacy 24, none 4, path 76), briefs 100, claimed 96, registered 4, unclaimed 0, and the new disclosure line 'none-citations 4: 0 resolving nothing · 1 naming something this check cannot resolve (spec-PRO-0096.md `campaign.py`)'.",
    "note": "Reverse totality holds at 0 under the tighter rule, which was the risk in tightening it. Brief 32 keeps its account through spec-PRO-0050's `**Supersedes:**` line — retired, not built, both halves absorbed there — which is why the account grammar has three relations rather than one. spec-PRO-0096 was the one real red and its citation now names `docs/test-campaign`."
  },
  {
    "id": "CASE-0527",
    "req": "REQ-121",
    "surface": "SURF-023",
    "state": "classifying",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/reckon-selftest.txt",
      "docs/test-campaign/evidence/PRO-0104/gemini-review.md"
    ],
    "armed": true,
    "armedBy": "selftest.py section 17e: seven tokens asserted against PLACEHOLDER_ID_RE one at a time — REQ-9, DEF-0 and CASE-99 are not placeholder shapes, CASE-999, CASE-9999, REQ-000 and SURF-0000 are — and a scan over 'about REQ-9 and DEF-0, and CASE-9999 is the example' returns cited ['DEF-0', 'REQ-9'] with unclassifiable ['CASE-9999']. The pre-fix pattern `(?:9+|0+)` matched REQ-9, which the assertions catch.",
    "note": "Found by the gemini out-of-family review of this item's own diff, not by a fixture. A repository numbering its brief queue without padding has a real ninth item, and the first version of the placeholder rule would have reported a citation to it as a worked example. Three digits is now the floor, and a repository large enough to hold a real CASE-999 gets a finding rather than a lost citation, which is the direction this mechanism fails in on purpose."
  },
  {
    "id": "CASE-0528",
    "req": "REQ-122",
    "surface": "SURF-024",
    "state": "declared no brief",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0104/spec-citation-arm.txt",
      "docs/test-campaign/evidence/PRO-0104/gemini-review.md"
    ],
    "armed": true,
    "armedBy": "Two mutations on PRO-0075's `none.` citation, each moving `every `none` citation resolves to something here` PASS -> FAIL: a reason naming `/bin/sh` as its origin, and one naming `../../../etc/passwd`. Before `inside_repo`, `REPO / '/bin/sh'` is `/bin/sh` in pathlib, so the first resolved against the machine and passed as a repository path.",
    "note": "Found by the gemini out-of-family review of this item's own diff. The same helper also makes a backticked path holding a space resolve as itself rather than as its first word, since the whole token is tried before the split."
  },
  {
    "id": "CASE-0472",
    "req": "REQ-111",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0107/capture-lineage-before.txt",
      "docs/test-campaign/evidence/PRO-0107/capture-lineage-after.txt",
      "docs/test-campaign/evidence/shots/captures.json"
    ],
    "armed": true,
    "armedBy": "The unpublishedReason was removed from evidence/shots/sweepL-status-agent-down.png's entry, the removal confirmed landed by re-reading the file before any verdict was read, and the gate watched going red: exit 2, `evidence/shots/sweepL-status-agent-down.png: the manifest names it and no subject publishes it, and it records no unpublishedReason`, 1 hard failure. Restored, and the restored reading diffs identically against the pre-arming one. docs/test-campaign/evidence/PRO-0107/capture-lineage-armed.txt.",
    "note": "`capture-lineage.py docs/test-campaign --gate` exits 0 BECAUSE THE POPULATION IS ACCOUNTED FOR, not because it shrank: 43 files in the shots directory before and 43 after, 8 published before and 8 after, 0 deleted, 35 moved from UNACCOUNTED to `COUNTED APART (35) — admissible, not judged`. Judged 6 of 8 and ratchet 6, both unchanged, which is the honest reading — no capture became judgeable, so the change earned no raise."
  },
  {
    "id": "CASE-0473",
    "req": "REQ-112",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0107/shot-audit.json",
      "scripts/build_test_campaign.py",
      "design/icon/icon-proctor-1024.png"
    ],
    "armed": false,
    "note": "Nine of nine files named for engine surfaces are app-icon renders, established by opening every one: surf-001, surf-002, surf-003, surf-011, surf-012, surf-013, surf-014, surf-015 and surf-016 all show the Proctor icon's dark window dissolving into orange squares at exactly 1024x1024. The mechanism is in the repository rather than inferred — scripts/build_test_campaign.py:282-296 copies design/icon audit renders and icon-run candidates into evidence/shots/ under surface-shaped names — and surf-016-install-notarize.png is byte-identical to design/icon/icon-proctor-1024.png (sha256 a67cf67af07e…), which is the machine-checkable half. Unarmed deliberately: it is a census with a denominator of 9, re-derived by shot_disposition.py on every run, and there is no assertion here to watch fail. DEF-218."
  },
  {
    "id": "CASE-0474",
    "req": "REQ-112",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/shot_disposition.py",
      "docs/test-campaign/evidence/PRO-0107/shot-disposition-verify.txt",
      "docs/test-campaign/evidence/PRO-0107/shot-audit.json"
    ],
    "armed": true,
    "armedBy": "surf-016-install-notarize.png's sha256 in shot-audit.json was replaced with 64 zeroes, the replacement confirmed landed by re-reading the file, and the check watched going red: exit 1, `surf-016-install-notarize.png: bytes changed under a disposition written for the old ones (000000000000 → a67cf67af07e)`. Restored from the pre-mutation copy and exit 0 reproduced. docs/test-campaign/evidence/PRO-0107/shot-disposition-armed.txt.",
    "note": "43 of 43 images carry a disposition — 8 published under a subject, 35 declared unpublished with a reason — and `shot_disposition.py` with no flags re-measures every file and fails on three conditions: an image with no disposition, a disposition whose file is gone, and a file whose bytes moved under a disposition written for the old ones. That last one is what keeps 35 sentences about pictures from becoming a stale opinion about a directory."
  },
  {
    "id": "CASE-0475",
    "req": "REQ-113",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0107/shot-audit.json"
    ],
    "armed": false,
    "note": "Four byte-identical groups among the 43 files, six redundant files: the three overlay-exclusion frames (ee7807db110e), sweepK-theme-before with sweepL-wedged-t1 and sweepL-wedged-recovered (670a93988315), sweepL-status-t0.6 with t3.5 (4f8c11912bbd), and sweepL-wedged-t7 with t14 (5c7d4535e70c). NOTHING WAS DELETED, and the two tests that would have licensed it were applied and came back empty: no file in the directory is zero bytes (the smallest is 10,680), and no byte-identical group contains a published file, so none of the eleven members is an exact duplicate of something already shown. The overlay trio's identity is the measurement rather than a fault — evidence/overlay-exclusion.json carries a positive control proving the capture path was live — and the other three groups are the evidence for DEF-221 and DEF-222, so deleting a member would delete the finding. Unarmed: a census, denominator 43."
  },
  {
    "id": "CASE-0476",
    "req": "REQ-113",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0107/shot-audit.json",
      "docs/test-campaign/evidence/sweeps-kl.json",
      "docs/test-campaign/evidence/sweepL-halfopen.json"
    ],
    "armed": false,
    "note": "sweepL-status-agent-down.png depicts a READY status window — green Ready pill, both grants Granted, `Ready. Every permission Proctor needs is granted.` — and no agent-down state, while sweepL-status-t0.6.png and sweepL-status-t3.5.png show `Agent down` and are byte-identical to each other. evidence/sweeps-kl.json accounts for the picture and not the name: `Agent down at t+0.71s and t+3.63s … The earlier Ready frame was captured inside one 2s doctor tick`. One step further, sweepL-wedged-recovered.png is byte-identical to sweepL-wedged-t1.png, so a recovery the sweep records as true is shown by the same bytes as the frame before it. DEF-221, DEF-222. Unarmed: the finding is a reading of files that exist, not an assertion."
  },
  {
    "id": "CASE-0477",
    "req": "REQ-112",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0107/shot-audit.json",
      "docs/test-campaign/evidence/hud-capture-channels.json"
    ],
    "armed": false,
    "note": "The three files named for the takeover shield contain no image: 0 non-transparent pixels of 14,745,600 (external), 7,720,704 (laptop) and 677,888 (window), and exactly 1 distinct RGBA value each, measured off the files with Pillow rather than read back from a capture reply. evidence/hud-capture-channels.json already lists all three under `framesThatWereNotEvidence` — SCFrameStatus `complete` over an entirely transparent frame, the same instrument fault CASE-0059 carries — so the files were deliberately kept and what was missing was the declaration. DEF-219. Unarmed: a measurement over a fixed denominator of 3."
  },
  {
    "id": "CASE-0478",
    "req": "REQ-112",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0107/shot-audit.json",
      "docs/test-campaign/evidence/sweepK-scaling.json"
    ],
    "armed": false,
    "note": "evidence/sweepK-scaling.json names four files in `shots` and records four modes whose capture sizes are 970x773, 820x654, 970x773 and 781x961. Two tie: sweepK-scale-small.png is 970x773, sweepK-scale-1x.png is 820x654. Two do not: sweepK-external.png and sweepK-laptop.png are both 900x833, a size no mode in the table captured, and no file in the directory is 781x961, so the sweep's baseline frame is absent while two frames it lists came from outside its measurements. The sweep's own verdict (examined=4 failures=0) rests on the mode table and is unaffected. DEF-220. Unarmed: a dimension comparison over a denominator of 4."
  },
  {
    "id": "CASE-0479",
    "req": "REQ-111",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/cases.json",
      "docs/test-campaign/evidence/shots/captures.json",
      "docs/test-campaign/evidence/PRO-0107/gemini-review.md"
    ],
    "armed": false,
    "note": "The two registries disagree about five files, measured by joining every `evidence/shots/*.png` path in cases.json against the published set and the unpublished declarations. Three raster-visual passes cite captures the manifest declares unpublished — CASE-0008, CASE-0010, CASE-0011 — and each of the three does carry a case-level capture method, so what is missing is the shutter-recorded target rather than the channel. Two outcome passes, CASE-0028 and CASE-0029, cite frames DEF-222 and DEF-221 show to be misnamed or byte-duplicated; their verdicts rest on their sweep JSON, so the frames are what is wrong. Denominator: 17 shots cited across cases.json. DEF-224. NOT ARMED, and recorded so rather than claimed: this case asserts a property of the disposition record itself, and the only mutation that would red it is editing the record the case reads, which tests the edit rather than the instrument. The five other unarmed cases in this item carry the same declaration; CASE-0479 and CASE-0480 were missing it, found at out-of-family adjudication."
  },
  {
    "id": "CASE-0480",
    "req": "REQ-111",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/cases.json",
      "docs/test-campaign/evidence/PRO-0107/shot-audit.json"
    ],
    "armed": false,
    "note": "Of the 17 `evidence/shots/*.png` paths cited across cases.json, one does not resolve: CASE-0100 names `evidence/shots/a3-walkthrough-permissions-disabled.png` and no such file exists. It is invisible to both instruments — campaign.py resolves an evidence path only on the raster rungs and CASE-0100 stands at effect-witness, and capture-lineage never reads cases.json. DEF-225. NOT ARMED, and recorded so rather than claimed: same reason as CASE-0479 — the assertion is over the disposition record, so the available mutation edits the thing being read. Declaration added at out-of-family adjudication, where its absence was the finding."
  },
  {
    "id": "CASE-0540",
    "req": "REQ-131",
    "surface": "SURF-026",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "docs/test-campaign/evidence/PRO-0106/def207-def208-arming.txt"
    ],
    "armed": true,
    "armedBy": "Three mutations, each alone and each confirmed landed by re-reading the file before the verdict was read. Removing the offset assertion reds 9 checks; removing the read-back reds 1; removing the inconclusive branch reds 6. All restored, control green at 106 before and after.",
    "note": "DEF-207. apply() refuses a splice whose recorded offsets no longer hold the recorded before-text, and leaves the file exactly as it was. The fixture is the drift the offsets are actually exposed to — candidates() reads the file, and between that read and the write a line can be inserted above. The pre-repair form is reproduced inline on the same fixture and shown mangling the declaration to `fun!=f()` while `a == b`, the site it meant to mutate, stands untouched. That is the damage nothing in the log would have mentioned.",
    "defects": [
      "DEF-207"
    ]
  },
  {
    "id": "CASE-0541",
    "req": "REQ-131",
    "surface": "SURF-026",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "docs/test-campaign/evidence/PRO-0106/def207-def208-arming.txt"
    ],
    "armed": true,
    "armedBy": "Removing the `if not landed` branch from main() reds 6 checks, including the verdict map, the denominator and the count of suite runs.",
    "note": "DEF-207, the verdict wiring rather than the check. main() is driven end to end over two mutants, one landing and one drifted: the drifted one is scored `inconclusive`, kept out of the survival-rate denominator, and the suite is not run for it at all — running it would have produced a verdict about pristine code. The summary line telling the reader an unproved mutant was set aside is asserted too, because a verdict nobody is told about is a verdict nobody can act on.",
    "defects": [
      "DEF-207"
    ]
  },
  {
    "id": "CASE-0542",
    "req": "REQ-132",
    "surface": "SURF-026",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "docs/test-campaign/evidence/PRO-0092/correlate-crash-arming.txt",
      "docs/test-campaign/evidence/PRO-0106/def207-def208-arming.txt"
    ],
    "armed": true,
    "armedBy": "Reverting score_arming to `armed = code != 0` for a run with no verdict line reds 2 checks; removing the verdict-line requirement reds 5.",
    "note": "DEF-208. The fixture is CASE-0461's own recorded trapping run — signal 5, zero verdict lines, the suite's own `FAIL: no swift-testing verdict line` — rather than a hand-written imitation. It scores ARMED because the log shows `Test \"A single fitting window is correlated to its own number\" started.` with no completion beside it, and that display string is read out of MutationSeamTests.swift rather than copied into the table. The same log with the started line removed is the setup death and scores INCONCLUSIVE, and the two differ in no way the pre-repair rule could see: both exit 1.",
    "defects": [
      "DEF-208"
    ]
  },
  {
    "id": "CASE-0543",
    "req": "REQ-132",
    "surface": "SURF-026",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "docs/test-campaign/evidence/PRO-0106/def207-def208-arming.txt"
    ],
    "armed": true,
    "armedBy": "Removing the ambiguity refusal reds 2 checks — but only after the check was given a fixture. On the first arming pass that mutation changed nothing at all, because no function under this repository's Tests/ resolves two ways, so the branch could not be made to fire against the real tree. display_name now takes a tests_root and the fixture is two files declaring one function under different @Test names. A check nobody has watched fire is the shape this whole item is about, and it turned up in this item's own work.",
    "note": "DEF-208, the lookup the trap evidence rests on. Two declarations of one function under different display names are refused rather than picked between, a function with no declaration is refused, and a trap the instrument cannot attribute is INCONCLUSIVE rather than armed. Removing the second declaration resolves it again, which is the pass direction.",
    "defects": [
      "DEF-208"
    ]
  },
  {
    "id": "CASE-0544",
    "req": "REQ-132",
    "surface": "SURF-026",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "docs/test-campaign/evidence/PRO-0106/def240-arming.txt"
    ],
    "armed": true,
    "armedBy": "Two mutations, each alone and each confirmed landed. Removing the verdict-line test from run_suite reds 2 checks; removing the `no-verdict-line` branch from score() reds 1. Control green at 106 before and after.",
    "note": "DEF-240. Four suite outputs are driven through run_suite and score together: a reported failure scores killed, a run that died after `Build complete` with no verdict line scores inconclusive, a pass scores SURVIVED, and a compile error scores unbuildable. The (passed, why) contract is asserted to still match the substitute mutation_timeout_arm.py installs, because widening it would break the arming that watches this file.",
    "defects": [
      "DEF-240"
    ]
  },
  {
    "id": "CASE-0545",
    "req": "REQ-134",
    "surface": "SURF-022",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/reckoning/reckoning_selftest.py",
      "docs/test-campaign/evidence/PRO-0106/reckoning-selftest.txt"
    ],
    "armed": true,
    "armedBy": "The mutation restores the pre-repair `git()`-then-`[3:]` pair verbatim on a scratch copy, and the phantom `ocs/test-campaign/cases.json` comes back in the refusal. This is a comparison across the repair rather than a check that merely passes now.",
    "note": "DEF-206. The fixture asserts first that the status line is the one that fires it — a leading space in the status column, which is what git() stripped — then that the refusal names a path that exists on disk, and that --allow-dirty records a path the reader can open rather than a phantom.",
    "defects": [
      "DEF-206"
    ]
  },
  {
    "id": "CASE-0546",
    "req": "REQ-133",
    "surface": "SURF-022",
    "oracle": "effect-witness",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/reckoning/reckoning_selftest.py",
      "docs/test-campaign/evidence/PRO-0106/reckoning-selftest.txt"
    ],
    "armed": true,
    "armedBy": "Mutating the effect block to write an empty `written` map leaves the witness naming files only, and the check reds.",
    "witness": {
      "recorder": "sweep() in scripts/reckoning/reckoning.py, taken by cmd_take before and after the reckon build and stored in run.json under effect. It walks the run directory and computes a byte count and sha256 per file, and since PRO-0106 the record keeps all of it: `written` per new file, `rewritten` for a file whose digest moved between the sweeps, and `removed` for one the build stopped writing.",
      "effect": "filesystem-write",
      "count": 2
    },
    "note": "DEF-205, the record half. Every file the take wrote carries a byte count and a sha256 in run.json, each re-derived from the file on disk by the selftest rather than read back from the record that is under test, and the digest map's keys are checked against files_written so the two halves cannot drift apart.",
    "defects": [
      "DEF-205"
    ]
  },
  {
    "id": "CASE-0547",
    "req": "REQ-133",
    "surface": "SURF-022",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/reckoning/reckoning_selftest.py",
      "docs/test-campaign/evidence/PRO-0106/reckoning-selftest.txt"
    ],
    "armed": true,
    "armedBy": "The two-way control is the arming: the same rewrite is invisible to the name diff and named by the digest diff, in one run, so a check that cannot fire is distinguishable from a check that found nothing.",
    "note": "DEF-205, the half a name-only witness could not see at all. The subject's own sweep() is imported and run over a file rewritten to different content of equal length: `set(after) - set(before)` is empty, and the digest comparison names it. A reimplementation of sweep() here would have armed a copy rather than the tool.",
    "defects": [
      "DEF-205"
    ]
  },
  {
    "id": "CASE-0548",
    "req": "REQ-135",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0106/def226-adoption-arming.txt",
      "scripts/campaign/shot_disposition.py"
    ],
    "armed": true,
    "armedBy": "The defect's own fixture, watched in both directions in one run: a flat magenta frame written over surf-007-zoom.png takes the check to exit 1, `--write` now stays at 1 with the audit byte-identical on disk, and `--write --adopt surf-007-zoom.png` writes it. Before the repair that middle step returned 0 with the row still recording publishedAs SURF-007 and distinctRGBA 1.",
    "note": "DEF-226. The frame and the audit are both restored from bytes held in memory and the gate is re-run afterwards, so the fixture leaves nothing behind. Adoption is per file and there is no blanket flag, because the point is that somebody looked at the picture.",
    "defects": [
      "DEF-226"
    ]
  },
  {
    "id": "CASE-0549",
    "req": "REQ-136",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0106/def227-citation-arming.txt",
      "scripts/campaign/shot_disposition.py"
    ],
    "armed": true,
    "armedBy": "Its own fixture, separate from DEF-226's — a fixture covering two repairs together proves neither individually. One spliced case citing a file that is not on disk and one citing a picture no subject publishes each take the gate to exit 1 naming the case; the merge-base instrument is run against the same two fixtures and exits 0 without mentioning either, which is the class it could not see. A third fixture citing a published capture clears.",
    "note": "DEF-227. Each fixture is spliced into cases.json as text and restored with git checkout, with json.load run after every edit and the tree checked clean at the end. The nine rows DEF-224 and DEF-225 already record are printed as notes rather than failures, so this item does not claim repairs that belong to those rows.",
    "defects": [
      "DEF-227"
    ]
  },
  {
    "id": "CASE-0550",
    "req": "REQ-134",
    "surface": "SURF-022",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/reckoning/reckoning_selftest.py",
      "docs/test-campaign/evidence/PRO-0106/reckoning-selftest.txt"
    ],
    "armed": true,
    "armedBy": "The pre-repair slice is reintroduced against each of the six kinds in the same run, and the check asserts which four it gets wrong rather than that it gets any wrong. A control that changes on every input measures the harness.",
    "note": "DEF-206, driven over every kind of status entry rather than the one the fixture happened to run — the failure mode this item was warned about by name. Modified-unstaged, untracked, staged addition, deleted, renamed and a quoted non-ASCII path all resolve to the path on disk. The pre-repair form is wrong on four of the six: an untracked entry and a staged addition survive a strip intact, which is why the defect went unnoticed and why the fixture had to be a modified tracked file.",
    "defects": [
      "DEF-206"
    ]
  },
  {
    "id": "CASE-0551",
    "req": "REQ-131",
    "surface": "SURF-026",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py"
    ],
    "armed": true,
    "armedBy": "Both halves driven to refuse in one run: an anchor occurring twice, an anchor occurring not at all, and a write the disk does not take. The passing direction is driven in the same run, so a check that cannot fire is distinguishable from a check that found nothing.",
    "note": "The other write path. PRO-0106 rewrote mutate_swift.apply() and mutation_seam_arm.py has its own apply() with its own occurrence-count and read-back check, which nothing was driving. A repair proved on the exercised path while another stays undriven is this item's named failure mode, so the original defect is reintroduced on that path too. Each refusal leaves the file exactly as it was, which is asserted rather than assumed.",
    "defects": [
      "DEF-207"
    ]
  },
  {
    "id": "CASE-0552",
    "req": "REQ-130",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "scripts/campaign/shot_disposition.py"
    ],
    "armed": true,
    "armedBy": "The count assertion is what found the defect: the test asserted eleven files from the prose and the instrument computed ten. The grouping check compares the recomputed groups against the audit's, so a mutation that stops grouping reds it rather than printing zero.",
    "note": "DEF-241, and the open question main recorded. The instrument detects byte-identity and that identity carries DEF-221 and DEF-222, but nothing would have failed if it silently stopped grouping — the count was printed and never checked. verify() now fails on a change to the grouping. And the sentence the instrument publishes about its own directory said eleven files where four groups cover ten, with the smallest-file byte count a literal beside it; both are derived from the rows now, and a check asserts the published sentence states the count that run computed.",
    "defects": [
      "DEF-241"
    ]
  },
  {
    "id": "CASE-0553",
    "req": "REQ-135",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "scripts/campaign/shot_disposition.py"
    ],
    "armed": true,
    "armedBy": "Every sha256 and byte count in the emitted manifest is re-derived from the file on disk by the check rather than read back from the record under test, so a manifest carrying stale bytes fails.",
    "note": "The second output path, undriven until now. --manifest emits one entry per disposed-and-unpublished image with a digest and a byte count, and nothing was checking that those are the bytes on disk. 35 entries, all re-derived.",
    "defects": [
      "DEF-226"
    ]
  },
  {
    "id": "CASE-0554",
    "req": "REQ-130",
    "surface": "SURF-026",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0106/timeout-arming.json",
      "scripts/campaign/mutation_timeout_arm.py"
    ],
    "armed": true,
    "armedBy": "The gate is its own two-way control and it was found failing, not passing: on a clean detached worktree of main it exits 1 with before=TIMEOUT after=TIMEOUT, and with the baseline pinned it exits 0 with before=killed after=TIMEOUT. Both readings taken in the same session.",
    "note": "DEF-242. The gate's --baseline-ref defaulted to `main`, which absorbed the fix the gate exists to demonstrate, so it loaded the same file twice and had been red since fc1b9a4 with nothing reading it. Pinned to fc1b9a4~1. A baseline ref that will one day contain the change cannot hold the state before it.",
    "defects": [
      "DEF-242"
    ]
  },
  {
    "id": "CASE-0555",
    "req": "REQ-131",
    "surface": "SURF-026",
    "oracle": "effect-witness",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "docs/test-campaign/evidence/PRO-0106/def207-def208-arming.txt"
    ],
    "armed": true,
    "witness": {
      "recorder": "The test re-reads the mutated file off disk with Path.read_text and compares it against the text the splice should have produced, byte for byte — the same read-back apply() itself performs, done independently by the caller so the instrument's own claim is not the evidence for it. The refusal paths are witnessed the same way: the file is read back and compared against the bytes it held before the call.",
      "effect": "filesystem-write",
      "count": 1
    },
    "armedBy": "Two-way in one run: the landing splice is witnessed changing the file, and each refusal is witnessed leaving it byte-identical. Removing the offset check reds 9 checks and removing the read-back reds 1, both watched.",
    "note": "DEF-207's effect, witnessed rather than inferred. apply() writes exactly one file per mutant and the point of the repair is that the write is established rather than assumed, so the effect and its absence are both read off the disk. A write the disk does not take is driven too, by intercepting Path.write_text so the file keeps its old bytes: the instrument refuses on the re-read, which is the case an offset check alone cannot cover.",
    "defects": [
      "DEF-207"
    ]
  },
  {
    "id": "CASE-0556",
    "req": "REQ-132",
    "surface": "SURF-026",
    "oracle": "effect-witness",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0106/seam-arming.json"
    ],
    "armed": true,
    "witness": {
      "recorder": "subprocess.run(['./scripts/test.sh', '--filter', <function>]) with stdout and stderr captured, one process per case, and the captured output parsed for swift-testing's own verdict, started and finished lines. seam-arming.json records the exit code, the verdict line, the tests started and finished, and the elapsed seconds for every one.",
      "effect": "subprocess",
      "count": 12
    },
    "armedBy": "The population is its own control and it is not uniform: 11 of the 12 processes printed a verdict line and one did not. CASE-0461's trapping mutant produced zero verdict lines and is armed on its started line instead, which is the distinction the repair exists to make — under the pre-repair rule all 12 were one undifferentiated `exit != 0`.",
    "note": "DEF-208's effect. Twelve filtered suite runs across 87.1 seconds, each spawned against a mutated tree and each reverted after. What the record carries is what the process said rather than only what it returned: exit code, verdict line, and the list of tests swift-testing reported starting and finishing.",
    "defects": [
      "DEF-208"
    ]
  },
  {
    "id": "CASE-0557",
    "req": "REQ-135",
    "surface": "SURF-027",
    "oracle": "effect-witness",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0106/def226-adoption-arming.txt",
      "scripts/campaign/shot_disposition.py"
    ],
    "armed": true,
    "witness": {
      "recorder": "The audit file's bytes are held in memory before the run and compared against the file on disk after it, so a refused --write is witnessed writing nothing rather than reported as having written nothing. The adopted write is witnessed by reading the row back and finding the new sha256 and distinctRGBA in it.",
      "effect": "filesystem-write",
      "count": 1
    },
    "armedBy": "Three writes driven in one run and the middle one is the arming: --write over an unchanged directory writes, --write over a replaced frame is refused and the audit on disk is byte-identical to before, and --write --adopt writes. Before the repair the middle step wrote and returned 0.",
    "note": "DEF-226's effect. The instrument's one filesystem write is the audit, and the defect was that it performed that write while treating its own re-measurement as the standard. Both the refusal and the write are read off the disk rather than taken from the exit code.",
    "defects": [
      "DEF-226"
    ]
  },
  {
    "id": "CASE-0558",
    "req": "REQ-137",
    "surface": "SURF-026",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "docs/test-campaign/evidence/PRO-0106/zero-tests-arming.txt"
    ],
    "armed": true,
    "armedBy": "The pre-repair rule `armed = code != 0` is reintroduced against this exact fixture and returns ARMED, which is the verdict the verifier drove end to end on CASE-0461. The same fixture at exit 0 is scored NOT ARMED by that rule, so the two-way control shows one non-measurement filed as a pass in one direction and a fail in the other, and inconclusive both ways after the repair. A verdict line over one test that failed still arms, so the guard is watched clearing as well as firing.",
    "note": "DEF-244. The fixture is `scripts/test.sh --filter thisFunctionDoesNotExistAnywhere` recorded rather than a string written in the test: a well-formed `Test run with 0 tests in 0 suites passed`, scripts/test.sh's own zero-test refusal, exit 1, and no started line. The count is read back out of the published verdict line rather than carried beside it.",
    "defects": [
      "DEF-244"
    ]
  },
  {
    "id": "CASE-0559",
    "req": "REQ-137",
    "surface": "SURF-026",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "docs/test-campaign/evidence/PRO-0106/instrument-checks.txt"
    ],
    "armed": true,
    "armedBy": "`armed = code != 0` is reintroduced against every one of the eleven routes and the check asserts WHICH three it agrees on rather than that it disagrees somewhere — a control that moves on every input is measuring the harness rather than the repair. The three it agrees on are a reported failure, a reported pass, and the trap; it differs on the other eight, both zero-test routes among them.",
    "note": "DEF-244's own shape, refused: every route through score_arming enumerated from the code rather than from the tests, one fixture each, none standing for two. The zero-test route survived the first repair because the fixtures were written from the paths the harness happened to drive.",
    "defects": [
      "DEF-244"
    ]
  },
  {
    "id": "CASE-0560",
    "req": "REQ-134",
    "surface": "SURF-022",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/reckoning/reckoning_selftest.py",
      "docs/test-campaign/evidence/PRO-0106/reckoning-selftest.txt"
    ],
    "armed": true,
    "armedBy": "Three parses are computed against all thirteen kinds in one run: the pre-repair slice, the rsplit repair, and the code as it stands. The slice is wrong on nine and the rsplit on exactly the two whose last ` -> ` sits inside a quoted destination, and both sets are asserted by membership. End to end, a scratch copy carrying the rsplit writes `stage-2.md\"` into run.json.dirty_inputs under --allow-dirty and the real subject writes the path that opens.",
    "note": "DEF-245. CASE-0550 drove six kinds with the tree carrying all six at once and an arrow only ever inside a ` M` path, so a rename DESTINATION holding one was undriven — and splitting from the right, the repair CASE-0550 was checking, is wrong on exactly that. Thirteen kinds are enumerated from porcelain_paths' branches and each is driven ALONE in a reset tree. Also driven: an unstaged rename arrives as ` D` plus `??` rather than the ` R` the out-of-family review claimed, so the guard that reads both status columns rests on a measurement rather than on the claim.",
    "defects": [
      "DEF-245",
      "DEF-206"
    ]
  },
  {
    "id": "CASE-0561",
    "req": "REQ-136",
    "surface": "SURF-027",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "docs/test-campaign/evidence/PRO-0106/instrument-checks.txt"
    ],
    "armed": true,
    "armedBy": "The population is moved rather than the branch: at a `.png` population the cited .mov is a notice and at a `.mov` population the same citation fails as absent while the .png becomes the notice, in the same run. So the branch is shown to read the declared population and to be able to fire in both directions, which a check pinned to the literal `.png` could not show.",
    "note": "DEF-246. Reproduced before the repair by citing a real .mov under evidence/shots: `CASE-0001 cites evidence/shots/probe-clip.mov: under evidence/shots and absent from this audit`. audit() measures PNG rasters — an IHDR read out of bytes 16..24 and PIL for the pixel counts — so a .mov has no row there to be absent from, and the population is declared once and read by the citation branch.",
    "defects": [
      "DEF-246"
    ]
  },
  {
    "id": "CASE-0562",
    "req": "REQ-136",
    "surface": "SURF-027",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "docs/test-campaign/evidence/PRO-0106/instrument-checks.txt"
    ],
    "armed": true,
    "armedBy": "The pre-repair walk is reimplemented verbatim beside the repaired one and run over the same two fixtures: it finds nothing under a dict key and nothing in a spaced path, while the repaired walk finds both. The spaced path is then deleted and the same string stops being a citation, which is the residual stated rather than hidden.",
    "note": "DEF-247. Two places a citation could hide from cite_paths, each on its own fixture: a path used as a dict key was not walked at all, and any string holding a space was disqualified outright — a proxy for prose that also disqualifies a real capture whose name has a space in it. A spaced string that resolves on disk is a citation now; one that does not stays prose, because a sentence naming a file and a citation of a missing file with a space in its name are not separable by any rule this has.",
    "defects": [
      "DEF-247"
    ]
  },
  {
    "id": "CASE-0563",
    "req": "REQ-134",
    "surface": "SURF-022",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/reckoning/reckoning_selftest.py",
      "docs/test-campaign/evidence/PRO-0106/reckoning-selftest.txt"
    ],
    "armed": true,
    "armedBy": "The pre-repair `unicode_escape` round trip is reintroduced against both forms in one run: it decodes the octal one and returns the literal one with its quotes still attached, which is the unopenable name. End to end, the fixture repo is set to core.quotePath=false and porcelain_paths resolves a file whose name carries both a non-ASCII byte and a space to a path that opens.",
    "note": "DEF-248. Not a filename this time but a git CONFIG: core.quotePath decides whether git escapes non-ASCII in octal or writes the UTF-8 through literally, and it is a setting a user commonly carries in their own gitconfig. Both modes were measured against git 2.50.1 and both are driven. The unquote works byte by byte now, and a sequence that is not UTF-8 decodes with surrogateescape so an undecodable name stays openable rather than being mangled into one that is not. A quote, a backslash and a tab escape are each driven back to one character.",
    "defects": [
      "DEF-248"
    ]
  },
  {
    "id": "CASE-0564",
    "req": "REQ-134",
    "surface": "SURF-026",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "docs/test-campaign/evidence/PRO-0106/instrument-checks.txt"
    ],
    "armed": true,
    "armedBy": "Six porcelain lines are driven, and the pre-repair filter is run over the same set. It errs in both directions and the check asserts which: it refuses over three quoted docs paths it should tolerate, and it silently tolerates `R  docs/x.md -> Sources/y.swift` because the whole entry starts with `docs/` when nothing splits the rename.",
    "note": "DEF-249. The same `line[3:]` slice DEF-206 was about, sitting in mutation_seam_arm.py — the file DEF-206's second round repaired. tree_dirty filtered on `line[3:].strip().startswith('docs/')`, so a quoted entry arrived as `\"docs/...` and this arm refused at exit 2 over a change it is meant to tolerate. Fail closed rather than a wrong verdict, and still the original defect on a sibling path. A rename is tolerated now only when BOTH names it carries are under docs/.",
    "defects": [
      "DEF-249"
    ]
  },
  {
    "id": "CASE-0565",
    "req": "REQ-136",
    "surface": "SURF-027",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "docs/test-campaign/evidence/PRO-0106/instrument-checks.txt"
    ],
    "armed": true,
    "armedBy": "The same file is cited both ways in the same run against the same audit: campaign-relative and repo-relative reach the same branch and produce the same notice. Before the repair the repo-relative form produced neither a row nor a prefix match, so it resolved on disk and returned no reasons at all.",
    "note": "DEF-250. cases.json may write a citation either way. `rows` is keyed campaign-relative and the branch tested `path.startswith('evidence/shots/')`, so a repo-relative citation missed both, resolved on disk, and skipped the disposition and publishing checks entirely — DEF-227's own class reached by writing the path the other way round. The prefix is derived from CAMPAIGN.relative_to(REPO) rather than written a second time.",
    "defects": [
      "DEF-250"
    ]
  },
  {
    "id": "CASE-0566",
    "req": "REQ-130",
    "surface": "SURF-027",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "docs/test-campaign/evidence/PRO-0106/instrument-checks.txt"
    ],
    "armed": true,
    "armedBy": "verify() is called twice in one session: once over an audit dict whose byteIdenticalGroups have been emptied, where it exits 1 and prints `the byte-identical grouping moved`, and once over the grouping this run computes, where it exits 0 and does not. That is the branch watched firing and clearing, which reading the source for the sentence could not do.",
    "note": "DEF-251. The check that the grouping guard exists read the instrument's SOURCE for the string literal it would print: `check(\"the byte-identical grouping moved\" in text, ...)`. It passes whether or not the branch runs, and it never called verify(). A check nobody has watched fire is indistinguishable from a check that cannot — the sentence this item opens with, in this item's own fixture file, found by an out-of-family reviewer rather than by the item.",
    "defects": [
      "DEF-251"
    ]
  },
  {
    "id": "CASE-0570",
    "req": "REQ-140",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0107/shot-audit.json"
    ],
    "armed": false,
    "note": "DEF-218: Nine app-icon renders in shots directory renamed to icon-audit-* and icon-run-* matching what they depict rather than engine surface names. Unarmed: exact filename audit."
  },
  {
    "id": "CASE-0571",
    "req": "REQ-140",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0107/shot-audit.json"
    ],
    "armed": false,
    "note": "DEF-219: Three takeover shield frames replaced with real captures having 100% opaque pixels. Unarmed: pixel count measurement."
  },
  {
    "id": "CASE-0572",
    "req": "REQ-140",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/sweepK-scaling.json"
    ],
    "armed": false,
    "note": "DEF-220: sweepK-scaling.json reconciled with disk, listing only the two frames produced by the sweep scaling modes. Unarmed: JSON schema match."
  },
  {
    "id": "CASE-0573",
    "req": "REQ-140",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0107/shot-audit.json"
    ],
    "armed": false,
    "note": "DEF-221: sweepL-wedged-recovered.png given a genuine re-take (1,046-pixel delta against sweepL-wedged-t1.png), witnessing the recovery transition. Unarmed: pixel difference verification."
  },
  {
    "id": "CASE-0574",
    "req": "REQ-140",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0107/shot-audit.json"
    ],
    "armed": false,
    "note": "DEF-222: sweepL-status-agent-down.png replaced with frame showing the Agent-down pill matching sweepL-status-t0.6.png. Unarmed: image content match."
  },
  {
    "id": "CASE-0575",
    "req": "REQ-140",
    "surface": "SURF-028",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/evidence/PRO-0107/shot-audit.json"
    ],
    "armed": false,
    "note": "DEF-223: Surface prefixes corrected for drawn pointer (surf-003) and About panel (surf-028). Unarmed: prefix-to-surface match."
  },
  {
    "id": "CASE-0576",
    "req": "REQ-140",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/cases.json"
    ],
    "armed": false,
    "note": "DEF-224: Case evidence citations in cases.json reconciled to point only to published captures or sweep records. Unarmed: lineage cross-check."
  },
  {
    "id": "CASE-0577",
    "req": "REQ-140",
    "surface": "SURF-027",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "docs/test-campaign/cases.json"
    ],
    "armed": false,
    "note": "DEF-225: CASE-0100 citation pointed to published step-a3-walkthrough-primary-disabled.png. Unarmed: path resolution."
  },
  {
    "id": "CASE-0590",
    "req": "REQ-145",
    "surface": "SURF-029",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/ledger_gate.py",
      "docs/feature-specs/LEDGER.md"
    ],
    "armed": true,
    "armedBy": "ledger_gate.audit_ledger() run against this repository's LEDGER.md and docs/specs/ passes with zero failures across all 110 rows and 107 specs.",
    "note": "DEF-228: standing gate verifying LEDGER.md status against git merges and specs on disk.",
    "defects": [
      "DEF-228"
    ]
  },
  {
    "id": "CASE-0591",
    "req": "REQ-145",
    "surface": "SURF-029",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "scripts/campaign/ledger_gate.py"
    ],
    "armed": true,
    "armedBy": "test_ledger_gate_mutation_checks creates a scratch spec with no ledger row and verifies ledger_gate fails with spec on disk with no ledger row.",
    "note": "DEF-228: ledger_gate fails when a spec on disk is missing from LEDGER.md.",
    "defects": [
      "DEF-228"
    ]
  },
  {
    "id": "CASE-0592",
    "req": "REQ-145",
    "surface": "SURF-029",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "scripts/campaign/ledger_gate.py"
    ],
    "armed": true,
    "armedBy": "test_ledger_gate_mutation_checks creates an undeclared ledger row without a spec file and verifies ledger_gate fails.",
    "note": "DEF-228: ledger_gate fails when a ledger row lacks a spec file and is not declared in Rows with no spec file.",
    "defects": [
      "DEF-228"
    ]
  },
  {
    "id": "CASE-0593",
    "req": "REQ-145",
    "surface": "SURF-029",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "scripts/campaign/ledger_gate.py"
    ],
    "armed": true,
    "armedBy": "test_ledger_gate_mutation_checks mutates a declared reason to under 20 characters and verifies ledger_gate fails on thin reason.",
    "note": "DEF-228: ledger_gate fails on declared no-spec rows with thin reasons.",
    "defects": [
      "DEF-228"
    ]
  },
  {
    "id": "CASE-0594",
    "req": "REQ-145",
    "surface": "SURF-029",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "scripts/campaign/ledger_gate.py"
    ],
    "armed": true,
    "armedBy": "test_ledger_gate_mutation_checks writes a spec file for a declared no-spec row and verifies ledger_gate fails on stale declaration.",
    "note": "DEF-228: ledger_gate fails when a declared no-spec row actually exists on disk.",
    "defects": [
      "DEF-228"
    ]
  },
  {
    "id": "CASE-0595",
    "req": "REQ-147",
    "surface": "SURF-029",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/shot_disposition.py",
      "docs/test-campaign/evidence/PRO-0107/shot-audit.json"
    ],
    "armed": true,
    "armedBy": "shot_disposition.audit() audits all 4 mock PNG files under evidence/shots/mock/ with explicit dispositions in DISPOSITIONS.",
    "note": "DEF-243: mock files under evidence/shots/mock/ are audited with explicit dispositions and byte measures.",
    "defects": [
      "DEF-243"
    ]
  },
  {
    "id": "CASE-0596",
    "req": "REQ-147",
    "surface": "SURF-029",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py",
      "scripts/campaign/shot_disposition.py"
    ],
    "armed": true,
    "armedBy": "test_shot_disposition_mock_lane_accounted_and_guarded zeros a mock file sha256 and verifies verify() fails with exit 1.",
    "note": "DEF-243: verify() fails when a mock file sha256 drifts or is modified.",
    "defects": [
      "DEF-243"
    ]
  },
  {
    "id": "CASE-0597",
    "req": "REQ-146",
    "surface": "SURF-029",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/shot_disposition.py",
      "scripts/campaign/test_instruments.py"
    ],
    "armed": true,
    "armedBy": "shot_disposition.citations() cross-checks all cases.json image citations against captures.json and disk, reporting 0 unrecorded citation failures.",
    "note": "DEF-229: cases citing unpublished, misnamed, or absent images are cross-checked and gated.",
    "defects": [
      "DEF-229"
    ]
  },
  {
    "id": "CASE-0600",
    "req": "REQ-150",
    "surface": "SURF-030",
    "oracle": "metamorphic",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/reckoning/reckoning.py",
      "scripts/reckoning/reckoning_selftest.py"
    ],
    "armed": true,
    "armedBy": "reckoning_selftest.py section 7c proves altered code at matching version is refused at exit 2 and allowed with --allow-differing-tool, with removal mutation confirming arming.",
    "note": "DEF-204: compare identifies tools by content hash and source_commit, refusing altered code at matching version.",
    "defects": [
      "DEF-204",
      "DEF-270"
    ]
  },
  {
    "id": "CASE-0601",
    "req": "REQ-151",
    "surface": "SURF-030",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py"
    ],
    "armed": true,
    "armedBy": "test_plugin_cache_content_check in test_instruments.py tests 0 vs 14 unjoined count disparity and sha256 mismatch detection on simulated stale plugin cache.",
    "note": "DEF-216: standing checks verify plugin content disparity is explicitly detected and reported.",
    "defects": [
      "DEF-216",
      "DEF-270"
    ]
  },
  {
    "id": "CASE-0602",
    "req": "REQ-150",
    "surface": "SURF-030",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py"
    ],
    "armed": true,
    "armedBy": "test_tool_identity_content_over_version in test_instruments.py tests compare exit 2 refusal and --allow-differing-tool override.",
    "note": "DEF-204: standing check in test_instruments.py for tool identity content gate.",
    "defects": [
      "DEF-204"
    ]
  },
  {
    "id": "CASE-0620",
    "req": "REQ-155",
    "surface": "SURF-031",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "/Users/lukerhodes/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py"
    ],
    "armed": true,
    "armedBy": "selftest.py check 18a asserts headline carries denominators beside percentages for both cases and requirements",
    "note": "DEF-280: headline output carries explicit cases and requirements denominators (X/Y (Z%)).",
    "defects": [
      "DEF-280"
    ]
  },
  {
    "id": "CASE-0621",
    "req": "REQ-155",
    "surface": "SURF-031",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "/Users/lukerhodes/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py"
    ],
    "armed": true,
    "armedBy": "selftest.py check 18c asserts weak-join warning carries joined/total denominator beside percentage",
    "note": "DEF-280: weak-join warning output carries explicit denominator (only N/D (pct%) of briefs).",
    "defects": [
      "DEF-280"
    ]
  },
  {
    "id": "CASE-0622",
    "req": "REQ-155",
    "surface": "SURF-031",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "/Users/lukerhodes/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py"
    ],
    "armed": true,
    "armedBy": "selftest.py check 18b asserts blocker table in markdown report carries denominator in coverage returned",
    "note": "DEF-280: blocker table Coverage returned column prints denominator (+X/Y (+Z.Z pts)).",
    "defects": [
      "DEF-280"
    ]
  },
  {
    "id": "CASE-0623",
    "req": "REQ-155",
    "surface": "SURF-031",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "/Users/lukerhodes/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py"
    ],
    "armed": true,
    "armedBy": "selftest.py check clean ledger passes and render produces Measured and Of columns next to percentage",
    "note": "DEF-280: reckon markdown report What it can speak for table includes Measured and Of denominator columns.",
    "defects": [
      "DEF-280"
    ]
  },
  {
    "id": "CASE-0624",
    "req": "REQ-157",
    "surface": "SURF-031",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "/Users/lukerhodes/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py"
    ],
    "armed": true,
    "armedBy": "selftest.py check 19a asserts brief citing only via source joins at confidence 1.0 cited edge",
    "note": "DEF-281: read source to join creates cited edges at confidence 1.0 for brief frontmatter and registry citations.",
    "defects": [
      "DEF-281"
    ]
  },
  {
    "id": "CASE-0625",
    "req": "REQ-156",
    "surface": "SURF-031",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "/Users/lukerhodes/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py"
    ],
    "armed": true,
    "armedBy": "selftest.py check 19b asserts circular source evidence stays unmeasured with circular why and zero unclassified inputs",
    "note": "DEF-281: refuse to let source grade classes circular source evidence as unmeasured with circular why rather than unclassified finding.",
    "defects": [
      "DEF-281"
    ]
  },
  {
    "id": "CASE-0626",
    "req": "REQ-156",
    "surface": "SURF-031",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "/Users/lukerhodes/Dev/fledgeling-plugins/plugins/reckon/skills/reckon/scripts/selftest.py"
    ],
    "armed": true,
    "armedBy": "selftest.py check 19b expect circular source evidence classed done fails gate with placement violation exit 1",
    "note": "DEF-281: gate() placement check rejects requirement with circular source evidence classed verified-done.",
    "defects": [
      "DEF-281"
    ]
  },
  {
    "id": "CASE-0630",
    "req": "REQ-160",
    "surface": "SURF-031",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/ReadIsolationPolicyTests.swift"
    ],
    "armed": true,
    "armedBy": "ReadIsolationPolicyTests asserts PolicyStore and FlowStore load strictly from designated sandbox directories",
    "note": "PRO-0111 / DEF-141: PolicyStore and FlowStore read-isolation in test processes vs injected roots.",
    "defects": [
      "DEF-141"
    ]
  },
  {
    "id": "CASE-0631",
    "req": "REQ-160",
    "surface": "SURF-031",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/ReadIsolationPolicyTests.swift"
    ],
    "armed": true,
    "armedBy": "ReadIsolationPolicyTests asserts CaptureEngineImpl and Session zoom paths resolve within temporary test roots",
    "note": "PRO-0111 / DEF-141: CaptureEngine and Session default capture directories resolve to isolated temporary roots.",
    "defects": [
      "DEF-141"
    ]
  },
  {
    "id": "CASE-0632",
    "req": "REQ-160",
    "surface": "SURF-031",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "Tests/ProctorAgentTests/ReadIsolationPolicyTests.swift"
    ],
    "armed": true,
    "armedBy": "ReadIsolationPolicyTests asserts unconfigured Session policyStore does not point to operatorDirectory",
    "note": "PRO-0111 / DEF-141: Unconfigured Session state routes policy checks through isolated store rather than operator policy.",
    "defects": [
      "DEF-141"
    ]
  },
  {
    "id": "CASE-0633",
    "req": "REQ-161",
    "surface": "SURF-031",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/hardware_yield_probe.py",
      "Tests/ProctorCoreTests/HardwareInputBoundaryTests.swift"
    ],
    "armed": true,
    "armedBy": "HardwareInputBoundaryTests and hardware_yield_probe.py assert PersonInput truth table for hardware vs synthetic event source PIDs",
    "note": "PRO-0111 / DEF-151: PersonInput accepts kernel/hardware events (sourcePid == 0) and rejects userspace synthetic posts.",
    "defects": [
      "DEF-151",
      "DEF-285"
    ]
  },
  {
    "id": "CASE-0634",
    "req": "REQ-161",
    "surface": "SURF-031",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/hardware_yield_probe.py",
      "Tests/ProctorCoreTests/HardwareInputBoundaryTests.swift"
    ],
    "armed": true,
    "armedBy": "HardwareInputBoundaryTests asserts PersonInput grace boundary filter before and after 0.25s",
    "note": "PRO-0111 / DEF-151: PersonInput suppresses events within post-synthetic grace window and admits events after.",
    "defects": [
      "DEF-151",
      "DEF-285"
    ]
  },
  {
    "id": "CASE-0635",
    "req": "REQ-161",
    "surface": "SURF-031",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/hardware_yield_probe.py",
      "Tests/ProctorCoreTests/HardwareInputBoundaryTests.swift"
    ],
    "armed": true,
    "armedBy": "HardwareInputBoundaryTests and hardware_yield_probe.py assert InputBlock classification of ourPid, delegatedPid, and untagged hardware events",
    "note": "PRO-0111 / DEF-151: InputBlock.isOurs correctly classifies automation, delegated, and human events.",
    "defects": [
      "DEF-151",
      "DEF-285"
    ]
  },
  {
    "id": "CASE-0636",
    "req": "REQ-162",
    "surface": "SURF-031",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/dynamic_grant_probe.py",
      "Tests/ProctorCoreTests/DynamicGrantProbeTests.swift"
    ],
    "armed": true,
    "armedBy": "DynamicGrantProbeTests and dynamic_grant_probe.py assert GrantProbeKeeper definite result caching across consecutive claims",
    "note": "PRO-0111 / DEF-180: GrantProbeKeeper caches definite outcomes (.granted / .denied) for process lifecycle.",
    "defects": [
      "DEF-180"
    ]
  },
  {
    "id": "CASE-0637",
    "req": "REQ-162",
    "surface": "SURF-031",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/dynamic_grant_probe.py",
      "Tests/ProctorCoreTests/DynamicGrantProbeTests.swift"
    ],
    "armed": true,
    "armedBy": "DynamicGrantProbeTests and dynamic_grant_probe.py assert invalidateDefinite clears cached state and allows fresh probe",
    "note": "PRO-0111 / DEF-180: invalidateDefinite clears cached state and allows dynamic re-probing on demand.",
    "defects": [
      "DEF-180"
    ]
  },
  {
    "id": "CASE-0638",
    "req": "REQ-162",
    "surface": "SURF-031",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/dynamic_grant_probe.py",
      "Tests/ProctorCoreTests/DynamicGrantProbeTests.swift"
    ],
    "armed": true,
    "armedBy": "DynamicGrantProbeTests and dynamic_grant_probe.py assert in-flight call join and backoff retry schedule",
    "note": "PRO-0111 / DEF-180: In-flight calls join running probe and timeout triggers backoff recovery.",
    "defects": [
      "DEF-180"
    ]
  },
  {
    "id": "CASE-0650",
    "req": "REQ-165",
    "surface": "SURF-032",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py"
    ],
    "armed": true,
    "armedBy": "test_instruments.py test_warrant_charter_integrity asserts charter_validate and export-warrant pass cleanly",
    "note": "DEF-290: .warrant/warrant.toml validates against charter_validate.py and export-warrant without charter-absent blocks.",
    "defects": [
      "DEF-290"
    ]
  },
  {
    "id": "CASE-0651",
    "req": "REQ-165",
    "surface": "SURF-032",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py"
    ],
    "armed": true,
    "armedBy": "test_instruments.py test_warrant_census_classes_and_surface_coverage asserts all 31 surfaces match charter class globs",
    "note": "DEF-290: Campaign export-warrant exports suite-health and oracle-coverage with all 7 classes rolled up across all 31 surfaces.",
    "defects": [
      "DEF-290"
    ]
  },
  {
    "id": "CASE-0652",
    "req": "REQ-166",
    "surface": "SURF-032",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py"
    ],
    "armed": true,
    "armedBy": "test_instruments.py test_warrant_census_classes_and_surface_coverage asserts census classes capped at tier 0 and 100% sampled",
    "note": "DEF-290: Census classes (evidence-integrity, operator-state, inconclusive) verified capped at tier 0 and sampled at 100%.",
    "defects": [
      "DEF-290"
    ]
  },
  {
    "id": "CASE-0653",
    "req": "REQ-167",
    "surface": "SURF-032",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py"
    ],
    "armed": true,
    "armedBy": "test_instruments.py test_warrant_release_gate_and_export_verification verifies release gate assertions and mutation checks",
    "note": "DEF-290: Release boundary gate verification asserts charter integrity and prevents unearned tier escalation.",
    "defects": [
      "DEF-290"
    ]
  },
  {
    "id": "CASE-0670",
    "req": "REQ-170",
    "surface": "SURF-033",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py"
    ],
    "armed": true,
    "armedBy": "test_instruments.py test_reckon_brief_join_rate_and_retirement_ladder asserts structured frontmatter citations across briefs",
    "note": "DEF-295: Brief frontmatter structured source declarations resolve at confidence 1.0 across feature briefs.",
    "defects": [
      "DEF-295"
    ]
  },
  {
    "id": "CASE-0671",
    "req": "REQ-171",
    "surface": "SURF-033",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py"
    ],
    "armed": true,
    "armedBy": "test_instruments.py test_reckon_brief_join_rate_and_retirement_ladder asserts requirement source bidirectional citations",
    "note": "DEF-295: Requirement source fields in inventory.json establish bidirectional links to feature briefs.",
    "defects": [
      "DEF-295"
    ]
  },
  {
    "id": "CASE-0672",
    "req": "REQ-172",
    "surface": "SURF-033",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "evidence": [
      "scripts/campaign/test_instruments.py"
    ],
    "armed": true,
    "armedBy": "test_instruments.py test_reckon_brief_join_rate_and_retirement_ladder asserts join rate >= 50% and retirement ladder unblocking",
    "note": "DEF-295: Reckon build join rate climbs past 50% threshold without token guessing, enabling retirement verification.",
    "defects": [
      "DEF-295"
    ]
  },
  {
    "id": "CASE-0700",
    "req": "REQ-180",
    "surface": "SURF-035",
    "oracle": "effect-witness",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "test_instruments.py test_subprocess_witness_characterization asserts process spawn, argument passing, and clean exit",
    "evidence": [
      "Tests/ProctorAgentTests/CuaSubprocessWitnessTests.swift",
      "scripts/campaign/subprocess_witness.py"
    ],
    "witness": {
      "recorder": "independent process table inspection and sentinel files on disk written by child process with $$",
      "effect": "subprocess",
      "count": 3
    },
    "note": "DEF-305 / REQ-180: Process-table witness observes cua-driver endpoint spawn with arguments ['serve', '--stdio'], stdio exchange, and clean exit.",
    "defects": [
      "DEF-305"
    ]
  },
  {
    "id": "CASE-0701",
    "req": "REQ-180",
    "surface": "SURF-035",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "CuaSubprocessWitnessTests oneShotTransportLifecycleAndStderrCapture asserts exit code 2 and stderr capture in AgentError",
    "evidence": [
      "Tests/ProctorAgentTests/CuaSubprocessWitnessTests.swift",
      "scripts/campaign/subprocess_witness.py"
    ],
    "note": "DEF-305 / REQ-180: Subprocess failure paths capture nonzero exit codes and stderr messages, propagating into AgentError.",
    "defects": [
      "DEF-305"
    ]
  },
  {
    "id": "CASE-0702",
    "req": "REQ-180",
    "surface": "SURF-035",
    "oracle": "effect-witness",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "CuaSubprocessWitnessTests endpointTransportLifecycleAndShutdownInterlock verifies child process terminated and dead in process table",
    "evidence": [
      "Tests/ProctorAgentTests/CuaSubprocessWitnessTests.swift",
      "scripts/campaign/subprocess_witness.py"
    ],
    "witness": {
      "recorder": "process table inspection via kill(pid, 0) and Darwin process table audit verifying process absence after stop",
      "effect": "subprocess",
      "count": 1
    },
    "note": "DEF-305 / REQ-180: Daemon shutdown interlock ensures child driver process is terminated on stop/deinit without orphaned daemon processes.",
    "defects": [
      "DEF-305"
    ]
  },
  {
    "id": "CASE-0750",
    "req": "REQ-195",
    "surface": "SURF-038",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "GuestMultiSessionQueueWitnessTests threeConcurrentRequestsAgainstTwoSlotLane asserts 2 acquire, 3rd waits, and acquires on release",
    "evidence": [
      "Tests/ProctorAgentTests/GuestMultiSessionQueueWitnessTests.swift"
    ],
    "note": "DEF-320 / REQ-195: Multi-session queue testing harness simulates 3 concurrent requests against 2-slot lane, verifying fair acquisition and clean waiting.",
    "defects": [
      "DEF-320"
    ]
  },
  {
    "id": "CASE-0751",
    "req": "REQ-038",
    "surface": "SURF-038",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "GuestMultiSessionQueueWitnessTests threeConcurrentRequestsAgainstTwoSlotLane asserts 2 acquire, 3rd waits, and acquires on release",
    "evidence": [
      "Tests/ProctorAgentTests/GuestMultiSessionQueueWitnessTests.swift"
    ],
    "note": "DEF-320 / REQ-038: 2-slot macOS guest lane contention correctly admits first two sessions and queues third without refusal.",
    "defects": [
      "DEF-320"
    ]
  },
  {
    "id": "CASE-0752",
    "req": "REQ-039",
    "surface": "SURF-038",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "GuestMultiSessionQueueWitnessTests neverEvictingPolicyGuaranteesIntegrity asserts 0 provider stop calls during contention",
    "evidence": [
      "Tests/ProctorAgentTests/GuestMultiSessionQueueWitnessTests.swift"
    ],
    "note": "DEF-320 / REQ-039: Never-evicting queue policy guarantees running VMs are never stopped to free slots under contention.",
    "defects": [
      "DEF-320"
    ]
  },
  {
    "id": "CASE-0753",
    "req": "REQ-040",
    "surface": "SURF-038",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "GuestMultiSessionQueueWitnessTests darwinProviderAssignsNativeWitnessTier asserts darwin yields native tier and evaluates tree assertions",
    "evidence": [
      "Tests/ProctorAgentTests/GuestMultiSessionQueueWitnessTests.swift"
    ],
    "note": "DEF-320 / REQ-040: Provider reporting darwin platform assigns macOS native witness tier, evaluating AX tree assertions without skipping.",
    "defects": [
      "DEF-320"
    ]
  },
  {
    "id": "CASE-0754",
    "req": "REQ-037",
    "surface": "SURF-038",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "GuestMultiSessionQueueWitnessTests multiSessionSocketForwardingIsolation asserts guest socket forwarding with 0 host actuations",
    "evidence": [
      "Tests/ProctorAgentTests/GuestMultiSessionQueueWitnessTests.swift"
    ],
    "note": "DEF-320 / REQ-037: Multi-session socket forwarding executes actions inside attached guests with host actuator untouched.",
    "defects": [
      "DEF-320"
    ]
  },
  {
    "id": "CASE-0730",
    "req": "REQ-190",
    "surface": "SURF-037",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "NativeOCRZoomTests nativeVisionOCRTextRecognition verifies VNRecognizeTextRequest extracts text strings and bounding boxes from rendered text",
    "evidence": [
      "Tests/ProctorAgentTests/NativeOCRZoomTests.swift",
      "Sources/ProctorAgent/Capture/VisionOCR.swift"
    ],
    "note": "DEF-315 / REQ-190: proctor_zoom with recognize_text: true extracts text strings, confidence scores, and pixel/point bounding boxes via VNRecognizeTextRequest.",
    "defects": [
      "DEF-315"
    ]
  },
  {
    "id": "CASE-0731",
    "req": "REQ-190",
    "surface": "SURF-037",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "NativeOCRZoomTests highDPIRetinaScalePreservation verifies scale factor 2.0 and pointBox/boundingBox coordinate transformation",
    "evidence": [
      "Tests/ProctorAgentTests/NativeOCRZoomTests.swift",
      "Sources/ProctorAgent/Capture/VisionOCR.swift"
    ],
    "note": "DEF-315 / REQ-190: High-DPI Retina scale factor is preserved in zoom OCR results and maps pixel coordinates to window point geometry.",
    "defects": [
      "DEF-315"
    ]
  },
  {
    "id": "CASE-0732",
    "req": "REQ-190",
    "surface": "SURF-037",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "NativeOCRZoomTests textContrastVerification verifies WCAG contrast ratio measurement and pass/fail determination across themes",
    "evidence": [
      "Tests/ProctorAgentTests/NativeOCRZoomTests.swift",
      "Sources/ProctorAgent/Capture/VisionOCR.swift"
    ],
    "note": "DEF-315 / REQ-190: High-DPI contrast checking measures dominant background and foreground text colors and computes WCAG contrast ratios.",
    "defects": [
      "DEF-315"
    ]
  },
  {
    "id": "CASE-0733",
    "req": "REQ-190",
    "surface": "SURF-037",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "NativeOCRZoomTests boundedExecutionTimeout verifies OCR completion duration and timeout enforcement within 500ms",
    "evidence": [
      "Tests/ProctorAgentTests/NativeOCRZoomTests.swift",
      "Sources/ProctorAgent/Capture/VisionOCR.swift"
    ],
    "note": "DEF-315 / REQ-190: Vision OCR execution duration is measured and bounded to < 500ms timeouts for region crops.",
    "defects": [
      "DEF-315"
    ]
  },
  {
    "id": "CASE-0734",
    "req": "REQ-190",
    "surface": "SURF-037",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "NativeOCRZoomTests zoomToolDispatchAndGracefulDegradation verifies tool argument parsing and nil OCR when recognize_text is false",
    "evidence": [
      "Tests/ProctorAgentTests/NativeOCRZoomTests.swift",
      "Sources/ProctorAgent/Dispatch.swift"
    ],
    "note": "DEF-315 / REQ-190: proctor_zoom dispatch supports recognize_text and recognizeText parameters with graceful degradation when disabled.",
    "defects": [
      "DEF-315"
    ]
  },
  {
    "id": "CASE-0710",
    "req": "REQ-030",
    "surface": "SURF-036",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "test_instruments.py test_supervision_tui_pty_probe_characterization asserts 80x24 floor rendering across all 5 panes with 0 line overflow",
    "evidence": [
      "Tests/ProctorAgentTests/SupervisionTUIPtyWitnessTests.swift",
      "scripts/campaign/supervision_tui_pty_probe.py"
    ],
    "note": "DEF-310 / REQ-030: Headless pty probe sets TIOCSWINSZ to 80x24 floor and renders all 5 panes (run, queue, readiness, history, switches), verifying 0 horizontal overflow or line tearing.",
    "defects": [
      "DEF-310"
    ]
  },
  {
    "id": "CASE-0711",
    "req": "REQ-030",
    "surface": "SURF-036",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "test_instruments.py test_supervision_tui_pty_probe_characterization asserts 100x30 target rendering across all 5 panes with full layout fidelity",
    "evidence": [
      "Tests/ProctorAgentTests/SupervisionTUIPtyWitnessTests.swift",
      "scripts/campaign/supervision_tui_pty_probe.py"
    ],
    "note": "DEF-310 / REQ-030: Headless pty probe sets TIOCSWINSZ to 100x30 target geometry and renders all 5 panes, verifying complete keybars, tab headers, and shelf metadata.",
    "defects": [
      "DEF-310"
    ]
  },
  {
    "id": "CASE-0712",
    "req": "REQ-030",
    "surface": "SURF-036",
    "oracle": "effect-witness",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "SupervisionTUIPtyWitnessTests tuiKeypressUpdatesSharedLatchState asserts pause and stop keypresses mutate shared latch",
    "evidence": [
      "Tests/ProctorAgentTests/SupervisionTUIPtyWitnessTests.swift",
      "scripts/campaign/supervision_tui_pty_probe.py"
    ],
    "witness": {
      "recorder": "pty keystroke injection and RunControl / SupervisionControl latch state observation",
      "effect": "ipc",
      "count": 2
    },
    "note": "DEF-310 / REQ-030: TUI interactive keypress events ('p' for pause/resume, 's' for stop) mutate shared RunControl state and dispatch proctor.control requests.",
    "defects": [
      "DEF-310"
    ]
  },
  {
    "id": "CASE-0713",
    "req": "REQ-031",
    "surface": "SURF-036",
    "oracle": "effect-witness",
    "lane": "macos",
    "status": "pass",
    "armed": true,
    "armedBy": "MenuBarHierarchyWitnessTests menuBarHierarchyCompletenessAndToolCatalogueWitness asserts all 20 menu commands and 21 tools in NSMenu hierarchy",
    "evidence": [
      "Tests/ProctorAgentTests/MenuBarHierarchyWitnessTests.swift"
    ],
    "witness": {
      "recorder": "AppKit NSMenu extra item traversal and CommandSurface / ToolCatalogue bidirectional validation",
      "effect": "ipc",
      "count": 21
    },
    "note": "DEF-310 / REQ-031: Live AppKit NSMenu extra hierarchy traversal validates that all 20 declared commands and 21 catalogue tools are populated with exact titles, shortcuts, and dynamic enablement.",
    "defects": [
      "DEF-310"
    ]
  },
  {
    "id": "CASE-0714",
    "req": "REQ-185",
    "surface": "SURF-036",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "test_instruments.py test_supervision_tui_pty_probe_characterization verifies 5-scenario truth table passes",
    "evidence": [
      "scripts/campaign/supervision_tui_pty_probe.py"
    ],
    "note": "DEF-310 / REQ-185: Characterization harness executes full 5-scenario truth table for headless pty TUI rendering, key navigation, latch mutation, and clean teardown.",
    "defects": [
      "DEF-310"
    ]
  },
  {
    "id": "CASE-0715",
    "req": "REQ-033",
    "surface": "SURF-036",
    "oracle": "effect-witness",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "SupervisionTUIPtyWitnessTests tuiReadinessSwitchesAndHistoryProjectionWitness asserts proctor_doctor and proctor_history projection into TUISurface model",
    "evidence": [
      "Tests/ProctorAgentTests/SupervisionTUIPtyWitnessTests.swift",
      "scripts/campaign/supervision_tui_pty_probe.py"
    ],
    "witness": {
      "recorder": "proctor_doctor and proctor_history IPC query and TUISurface projection state validation",
      "effect": "ipc",
      "count": 2
    },
    "note": "DEF-310 / REQ-033: Supervision client queries proctor_doctor and proctor_history across the socket, projecting machine readiness grants, switch states, and historical audit entries into the TUI panes.",
    "defects": [
      "DEF-310"
    ]
  },
  {
    "id": "CASE-0760",
    "req": "REQ-043",
    "surface": "SURF-039",
    "oracle": "effect-witness",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "scripts/campaign/window_occlusion_witness.py Scenario 2: target point occluded by overlapping front window suppresses drawn pointer and records hold reason.",
    "note": "WindowServer occlusion evaluation suppresses pointer rendering over occluded target windows. PRO-0118.",
    "witness": {
      "recorder": "scripts/campaign/window_occlusion_witness.py: window occlusion truth table evaluation",
      "effect": "none"
    }
  },
  {
    "id": "CASE-0761",
    "req": "REQ-200",
    "surface": "SURF-039",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "CoveredTargetOcclusionWitnessTests.targetOcclusionSuppressesPointer: verifies PointerPlane.evaluateOcclusion returns .pointOccluded.",
    "note": "PointerPlane evaluates window levels, bounds, and occlusion geometry across CGWindowList entries. PRO-0118."
  },
  {
    "id": "CASE-0762",
    "req": "REQ-200",
    "surface": "SURF-039",
    "oracle": "outcome",
    "lane": "headless",
    "status": "pass",
    "armed": true,
    "armedBy": "CoveredTargetOcclusionWitnessTests.selfOverlayPanelsIgnored: verifies Proctor overlay windows do not cause self-occlusion.",
    "note": "Proctor overlays (Run HUD, Takeover Shield, Pointer Panel) are filtered from occlusion candidates. PRO-0118."
  }
]
