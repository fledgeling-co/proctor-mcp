campaign.py check, after — and it still exits 1
===============================================

Run: `python3 campaign.py check docs/test-campaign` from the worktree root.
EXIT CODE: 1, and stating that first is the point of this file. Four of the six
blockers `campaign-check-before.txt` carries are gone; two are not, and both are
recorded rather than closed.

Gone, each with the before/after number that says so:

  raster-visual claims with no usable capture   2 -> 0   (CASE-0124, CASE-0125)
  pixel claims with no capture method           2 -> 0   (the same two)
  source-analysis with no analyzer/denominator  4 -> 0   (CASE-0126, CASE-0127)
  effect-witness with no recorder or count      6 -> 0   (CASE-0128, CASE-0129)

  raster-visual oracle count                   11 -> 9   — DOWN, which is what
  distinguishes correcting a mislabel from reclassifying to clear a gate.
  source-analysis                               7 -> 10
  Armed                                   140/141 -> 157/157

THE ARMED RATIO, WITH ITS ARITHMETIC, because 157/157 read alone looks like a
number that has never been anything else. Immediately before this item's last
change it read 155/156: one passing case nobody had watched bite. That case is
CASE-0113, armed here by mutation. The denominators differ because this item
also adds cases: 141 passing at the before file, 156 once this item's twelve
new cases and CASE-0139's flip landed, 157 with CASE-0224 — which is the case
that records this ratio, and is armed by the 155/156 the gate printed an hour
earlier. A ratio that had only ever read N/N would prove nothing about whether
it can count short. This one was seen short, on this registry, this session.

Remaining, and neither is this item's to close:

  · 2 inconclusive cases. REQ-007's is the human-input path, unreachable from
    any instrument on this lane; it carries its reason in the status field.
  · 3 requirements claiming an external effect with no effect-witness case:
    REQ-007 (the same one), REQ-055 and REQ-063. The last two are DEF-110 and
    DEF-111, findings this item states and does not claim to have closed. The
    before file lists four here; REQ-024 came off it by being recorded
    `vacuous`, which is the honest disposition for a capability that never runs
    and was one of the fourteen values a merge had dropped.

An item that left this at exit 0 would have had to build two effect witnesses
it did not scope or reclassify two requirements to make a gate quiet. Reporting
the 1 with its reasons is the alternative.

---------------------------------------------------------------------------
proctor-mcp · lanes: macos, mcp-stdio, cli
Requirements: 62 inventoried, 0 with no case
Surfaces:   22 enumerated, 0 with no case
External effects: examined=27 witnessed=23 vacuous=1 unwitnessed=0 — armed proves the suite bites, and only a witness proves the product acted.
  Vacuous (the capability the claim constrains never runs): REQ-024
Cases:      157 pass · 0 fail · 0 skip · 0 n/a · 0 carried · 2 inconclusive · 0 unoracled · 0 blocked · 0 open  (of 159)
Observed:   157/159 cases produced a measurement · 2 could not be measured · 0 not attempted
On glass:   guest-glass → .build/Proctor.app (Guest proctor-guest (lume, macOS 26.6.2 Tahoe), provisioned with lume create --unattended so it holds a real Aqua session: stat -f %Su /dev/console reads lume, not root, which is what the earlier proctor-mac-node clone could not offer. launchctl print gui/501/app.fledgeling.procter.agent reads state = running, pid = 1322. That agent's own proctor_doctor answers ready true with Accessibility and Screen Recording both granted, and lists attachedApps app:1085:1 Calculator windows 1 — the guest's accessibility API reading a window the guest's own window server owns. Reached from this Mac over an SSH StreamLocal forward between the two agents' unix sockets, with the host agent relaying.)
On glass:   macos-glass → .build/Proctor.app (pid 88463 owns CG windows 43384 'History' and 43385 'Proctor' (640x572); glass_probe read it from CGWindowListCopyWindowInfo, the window server's own list. Launched with open -n because the installed build holds the same bundle id and LaunchServices activates it instead of starting a second instance — which is also why every capture names a CG window id rather than a title.)
Oracles:    outcome 102 · metamorphic 11 · effect-witness 26 · raster-visual 9 · interactive-glass 1
Off-ladder: source-analysis 10 — reads source, runs nothing, buys no effect credit
Armed:      157/157 passing cases have been watched to fail
Scope:      FULL — every case in the campaign was run
Sample:     full multi-witness coverage across all 21 MCP tools, status/walkthrough/overlay UI surfaces, and guest/takeover controls

Not ready to report:
  · 2 case(s) inconclusive — attempted, and the instrument could not measure. Not a pass and not an n/a: 'we do not know' is a weaker claim than 'no difference found' and a different one from 'does not apply'
  · 3 requirement(s) claiming an effect outside the product and recorded `observed` with no effect-witness case behind them — the shape that passed 230 cases over a product that performed no external I/O

  External-effect claims with no witness (23 of 27 witnessed):
    · REQ-007 claims a device effect and is recorded `observed`, and no case backing it stands at effect-witness
    · REQ-055 claims a filesystem-write effect and is recorded `observed`, and no case backing it stands at effect-witness
    · REQ-063 claims a filesystem-write effect and is recorded `observed`, and no case backing it stands at effect-witness
    (showing 3 of 3)
  Drive the effect from a production entry point with a recorder attached and
  record it:
    campaign.py set <dir> --case CASE-0001 --status pass --oracle effect-witness \
      --recorder 'strace -f -e trace=network' --effect-class outbound-socket \
      --effect-count 3
  Where the product does not perform the effect at all, the requirement is
  `vacuous`, not `observed` — a guarantee that holds because nothing does the
  thing is a finding, and recording it clears this blocker honestly.
Wall:       8 published shot(s) · 8 distinct image(s)

Finish them, resolve each to skip:/n-a: with a reason, or declare the stop with its resume point — in the reply, the verdict line and the report. Never silently.

EXIT=1
