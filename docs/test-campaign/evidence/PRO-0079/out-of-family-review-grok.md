🫥
The six shapes hold. The five vocabulary examples are not blind: each observes an effect of the mutation after it happens (`recognisedPids` after `begin`, `showRefusal` after `setCanShow`, a file probe after `setAttributes`, a derived message after `pause`, a spy log after `cancel`). Those are read-backs the reader list does not know. They are unverified only if “reader” means this exact verb list, not if it means the test checked something about what it did. False-mutator, return-value, teardown, not-a-test, and body-bleed are scanner errors or the last-mutator rule, not missing observations.

“At most 3 genuine in 78 at 95%” is a fair engineering bound, not a tight proof. The 32-finding tail is a census at zero, so it adds nothing to the upper bound. In the remaining 46, zero genuine in 25 draws rejects four or more under simple random sampling (P = 0.037) and does not reject three (P ≈ 0.088). The 25 were five per verb-bucket, not a simple random sample, so the hypergeometric is an approximation; classification error on the 57 sits outside that model too. “At most three” is the right 95% upper bound under those assumptions. It is not a claim that three exist.

Refuse `.calls` and `.recorded` as well. They are a real spy-log idiom, not `#expect`-like plumbing, and they are not a one-off, so they pass the rule you used to reject `.contains` and `.recognisedPids`. They still only move 78 to 76. The pass is already all false positives in 57 reads, and the mass of those is the func-line prefix, teardown’s last-mutator rule, return values, and body-bleed. Two spy spellings do not make the list usable, and they start the accretion you correctly refused elsewhere. Leave the reader list as it is until the matcher is looking at tests.
