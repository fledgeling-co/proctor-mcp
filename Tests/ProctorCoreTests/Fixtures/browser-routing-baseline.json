{
  "com.apple.Safari|<nil>|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"AXURL was absent or empty on the web area.\"}",
  "com.apple.Safari|<nil>|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"AXURL was absent or empty on the web area.\"}",
  "com.apple.Safari|<nil>|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"AXURL was absent or empty on the web area.\"}",
  "com.apple.Safari|<nil>|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"AXURL was absent or empty on the web area.\"}",
  "com.apple.Safari|<nil>|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"AXURL was absent or empty on the web area.\"}",
  "com.apple.Safari|<nil>|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"AXURL was absent or empty on the web area.\"}",
  "com.apple.Safari|<nil>|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"AXURL was absent or empty on the web area.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|<nil>|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"AXURL was absent or empty on the web area.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|<nil>|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"AXURL was absent or empty on the web area.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|<nil>|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"AXURL was absent or empty on the web area.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|<nil>|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"AXURL was absent or empty on the web area.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|<nil>|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"AXURL was absent or empty on the web area.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|<no probe>|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"No window was named, so no page URL was read.\"}",
  "com.apple.Safari|<no probe>|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"No window was named, so no page URL was read.\"}",
  "com.apple.Safari|<no probe>|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"No window was named, so no page URL was read.\"}",
  "com.apple.Safari|<no probe>|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"No window was named, so no page URL was read.\"}",
  "com.apple.Safari|<no probe>|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"No window was named, so no page URL was read.\"}",
  "com.apple.Safari|<no probe>|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"No window was named, so no page URL was read.\"}",
  "com.apple.Safari|<no probe>|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"No window was named, so no page URL was read.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|<no probe>|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"No window was named, so no page URL was read.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|<no probe>|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"No window was named, so no page URL was read.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|<no probe>|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"No window was named, so no page URL was read.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|<no probe>|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"No window was named, so no page URL was read.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|<no probe>|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"No window was named, so no page URL was read.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|about:blank|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|about:blank|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|about:blank|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|about:blank|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|about:blank|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|about:blank|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|about:blank|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|about:blank|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|about:blank|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|about:blank|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|about:blank|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|about:blank|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome-extension://abcdefghijklmnop/options.html|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome-extension://abcdefghijklmnop/options.html|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome-extension://abcdefghijklmnop/options.html|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome-extension://abcdefghijklmnop/options.html|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome-extension://abcdefghijklmnop/options.html|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome-extension://abcdefghijklmnop/options.html|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome-extension://abcdefghijklmnop/options.html|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome-extension://abcdefghijklmnop/options.html|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome-extension://abcdefghijklmnop/options.html|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome-extension://abcdefghijklmnop/options.html|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome-extension://abcdefghijklmnop/options.html|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome-extension://abcdefghijklmnop/options.html|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://newtab|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://newtab|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://newtab|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://newtab|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://newtab|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://newtab|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://newtab|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://newtab|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://newtab|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://newtab|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://newtab|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://newtab|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://settings|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://settings|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://settings|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://settings|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://settings|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://settings|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://settings|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://settings|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://settings|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://settings|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://settings|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|chrome://settings|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|data:text/html,<p>hi</p>|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|data:text/html,<p>hi</p>|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|data:text/html,<p>hi</p>|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|data:text/html,<p>hi</p>|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|data:text/html,<p>hi</p>|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|data:text/html,<p>hi</p>|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|data:text/html,<p>hi</p>|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|data:text/html,<p>hi</p>|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|data:text/html,<p>hi</p>|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|data:text/html,<p>hi</p>|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|data:text/html,<p>hi</p>|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|data:text/html,<p>hi</p>|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|devtools://devtools/bundled/inspector.html|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|devtools://devtools/bundled/inspector.html|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|devtools://devtools/bundled/inspector.html|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|devtools://devtools/bundled/inspector.html|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|devtools://devtools/bundled/inspector.html|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|devtools://devtools/bundled/inspector.html|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|devtools://devtools/bundled/inspector.html|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|devtools://devtools/bundled/inspector.html|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|devtools://devtools/bundled/inspector.html|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|devtools://devtools/bundled/inspector.html|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|devtools://devtools/bundled/inspector.html|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|devtools://devtools/bundled/inspector.html|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.apple.Safari|file:///Users/x/page.html|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|file:///Users/x/page.html|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|file:///Users/x/page.html|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|file:///Users/x/page.html|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|file:///Users/x/page.html|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|file:///Users/x/page.html|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|file:///Users/x/page.html|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|file:///Users/x/page.html|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|file:///Users/x/page.html|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|file:///Users/x/page.html|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|file:///Users/x/page.html|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|file:///Users/x/page.html|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.apple.Safari|http://192.168.1.4:3000/report.pdf?t=1|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\"}",
  "com.apple.Safari|http://192.168.1.4:3000/report.pdf?t=1|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\"}",
  "com.apple.Safari|http://192.168.1.4:3000/report.pdf?t=1|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\"}",
  "com.apple.Safari|http://192.168.1.4:3000/report.pdf?t=1|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\"}",
  "com.apple.Safari|http://192.168.1.4:3000/report.pdf?t=1|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\"}",
  "com.apple.Safari|http://192.168.1.4:3000/report.pdf?t=1|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\"}",
  "com.apple.Safari|http://192.168.1.4:3000/report.pdf?t=1|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"notes\":[\"This is a private-network address, so it needs --allow-private-network; without it Obscura blocks the fetch and the failure reads like a network error.\",\"This URL is a PDF. Neither browser tool reads a PDF's structure — rendering one measures the viewer rather than the document — so fetch the file and parse it instead.\"],\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|http://192.168.1.4:3000/report.pdf?t=1|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"notes\":[\"This is a private-network address, so it needs --allow-private-network; without it Obscura blocks the fetch and the failure reads like a network error.\",\"This URL is a PDF. Neither browser tool reads a PDF's structure — rendering one measures the viewer rather than the document — so fetch the file and parse it instead.\"],\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|http://192.168.1.4:3000/report.pdf?t=1|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"notes\":[\"This is a private-network address, so it needs --allow-private-network; without it Obscura blocks the fetch and the failure reads like a network error.\",\"This URL is a PDF. Neither browser tool reads a PDF's structure — rendering one measures the viewer rather than the document — so fetch the file and parse it instead.\"],\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|http://192.168.1.4:3000/report.pdf?t=1|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"notes\":[\"This is a private-network address, so it needs --allow-private-network; without it Obscura blocks the fetch and the failure reads like a network error.\",\"This URL is a PDF. Neither browser tool reads a PDF's structure — rendering one measures the viewer rather than the document — so fetch the file and parse it instead.\"],\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|http://192.168.1.4:3000/report.pdf?t=1|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"notes\":[\"This is a private-network address, so it needs --allow-private-network; without it Obscura blocks the fetch and the failure reads like a network error.\",\"This URL is a PDF. Neither browser tool reads a PDF's structure — rendering one measures the viewer rather than the document — so fetch the file and parse it instead.\"],\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|http://192.168.1.4:3000/report.pdf?t=1|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"notes\":[\"This is a private-network address, so it needs --allow-private-network; without it Obscura blocks the fetch and the failure reads like a network error.\",\"This URL is a PDF. Neither browser tool reads a PDF's structure — rendering one measures the viewer rather than the document — so fetch the file and parse it instead.\"],\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|https://example.com/dashboard|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"https://example.com/dashboard\"}",
  "com.apple.Safari|https://example.com/dashboard|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"https://example.com/dashboard\"}",
  "com.apple.Safari|https://example.com/dashboard|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"https://example.com/dashboard\"}",
  "com.apple.Safari|https://example.com/dashboard|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"https://example.com/dashboard\"}",
  "com.apple.Safari|https://example.com/dashboard|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"https://example.com/dashboard\"}",
  "com.apple.Safari|https://example.com/dashboard|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"https://example.com/dashboard\"}",
  "com.apple.Safari|https://example.com/dashboard|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"url\":\"https://example.com/dashboard\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|https://example.com/dashboard|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"url\":\"https://example.com/dashboard\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|https://example.com/dashboard|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"url\":\"https://example.com/dashboard\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|https://example.com/dashboard|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"url\":\"https://example.com/dashboard\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|https://example.com/dashboard|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"url\":\"https://example.com/dashboard\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.apple.Safari|https://example.com/dashboard|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Safari\",\"bundleId\":\"com.apple.Safari\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"url\":\"https://example.com/dashboard\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|<nil>|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"AXURL was absent or empty on the web area.\"}",
  "com.google.Chrome|<nil>|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"AXURL was absent or empty on the web area.\"}",
  "com.google.Chrome|<nil>|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"AXURL was absent or empty on the web area.\"}",
  "com.google.Chrome|<nil>|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"AXURL was absent or empty on the web area.\"}",
  "com.google.Chrome|<nil>|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"AXURL was absent or empty on the web area.\"}",
  "com.google.Chrome|<nil>|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"AXURL was absent or empty on the web area.\"}",
  "com.google.Chrome|<nil>|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"AXURL was absent or empty on the web area.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|<nil>|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"AXURL was absent or empty on the web area.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|<nil>|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"AXURL was absent or empty on the web area.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|<nil>|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"AXURL was absent or empty on the web area.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|<nil>|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"AXURL was absent or empty on the web area.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|<nil>|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"AXURL was absent or empty on the web area.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|<no probe>|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"No window was named, so no page URL was read.\"}",
  "com.google.Chrome|<no probe>|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"No window was named, so no page URL was read.\"}",
  "com.google.Chrome|<no probe>|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"No window was named, so no page URL was read.\"}",
  "com.google.Chrome|<no probe>|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"No window was named, so no page URL was read.\"}",
  "com.google.Chrome|<no probe>|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"No window was named, so no page URL was read.\"}",
  "com.google.Chrome|<no probe>|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"No window was named, so no page URL was read.\"}",
  "com.google.Chrome|<no probe>|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"No window was named, so no page URL was read.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|<no probe>|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"No window was named, so no page URL was read.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|<no probe>|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"No window was named, so no page URL was read.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|<no probe>|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"No window was named, so no page URL was read.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|<no probe>|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"No window was named, so no page URL was read.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|<no probe>|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"No window was named, so no page URL was read.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|about:blank|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|about:blank|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|about:blank|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|about:blank|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|about:blank|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|about:blank|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|about:blank|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|about:blank|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|about:blank|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|about:blank|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|about:blank|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|about:blank|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome-extension://abcdefghijklmnop/options.html|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to browser-use, which is not a command that does one thing: it is an autonomous agent that decides its own steps in a real browser. The native chrome around the page — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"url\":\"chrome-extension://abcdefghijklmnop/options.html\",\"use\":\"browser-use\",\"why\":\"This page lives inside the browser itself and has no equivalent in Obscura's engine, so Obscura cannot open it at all. browser-use drives a real Chromium, which is where such a page exists. That it is reachable there is not a promise that it drives: several browser-internal pages are shadow-DOM interfaces that resist automation, and an extension page needs that extension loaded in the profile it opens.\"}",
  "com.google.Chrome|chrome-extension://abcdefghijklmnop/options.html|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to browser-use, which is not a command that does one thing: it is an autonomous agent that decides its own steps in a real browser. The native chrome around the page — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Attaching to a browser already running needs its remote-debugging prompt approved, and that is a latch rather than a one-off: the port stays open to every local process until that browser quits.\",\"It chooses its own browser and that may not be the window Proctor is attached to, so a job started here can land in a different browser with a different session.\",\"It needs a model credential, and a run costs whatever that model costs.\",\"Run it from an installed entry point rather than an installer that runs it, and give it an isolated browser profile unless continuing this person's session is the point of the job.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"url\":\"chrome-extension://abcdefghijklmnop/options.html\",\"use\":\"browser-use\",\"why\":\"This page lives inside the browser itself and has no equivalent in Obscura's engine, so Obscura cannot open it at all. browser-use drives a real Chromium, which is where such a page exists. That it is reachable there is not a promise that it drives: several browser-internal pages are shadow-DOM interfaces that resist automation, and an extension page needs that extension loaded in the profile it opens.\"}",
  "com.google.Chrome|chrome-extension://abcdefghijklmnop/options.html|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome-extension://abcdefghijklmnop/options.html|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome-extension://abcdefghijklmnop/options.html|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor does not install this and has no opinion about whether it should be here. The person driving you enabled the lane, so tell them it is not on the machine.\",\"docs\":\"https://docs.browser-use.com\",\"missing\":\"The second browser lane is enabled but browser-use is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH, and a project-local virtualenv is invisible to it; proctor_doctor reports every path it checked.\",\"tool\":\"browser-use\"},\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome-extension://abcdefghijklmnop/options.html|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor does not install this and has no opinion about whether it should be here. The person driving you enabled the lane, so tell them it is not on the machine.\",\"docs\":\"https://docs.browser-use.com\",\"missing\":\"The second browser lane is enabled but browser-use is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH, and a project-local virtualenv is invisible to it; proctor_doctor reports every path it checked.\",\"tool\":\"browser-use\"},\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome-extension://abcdefghijklmnop/options.html|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to browser-use, which is not a command that does one thing: it is an autonomous agent that decides its own steps in a real browser. The native chrome around the page — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"url\":\"chrome-extension://abcdefghijklmnop/options.html\",\"use\":\"browser-use\",\"why\":\"This page lives inside the browser itself and has no equivalent in Obscura's engine, so Obscura cannot open it at all. browser-use drives a real Chromium, which is where such a page exists. That it is reachable there is not a promise that it drives: several browser-internal pages are shadow-DOM interfaces that resist automation, and an extension page needs that extension loaded in the profile it opens.\"}",
  "com.google.Chrome|chrome-extension://abcdefghijklmnop/options.html|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to browser-use, which is not a command that does one thing: it is an autonomous agent that decides its own steps in a real browser. The native chrome around the page — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Attaching to a browser already running needs its remote-debugging prompt approved, and that is a latch rather than a one-off: the port stays open to every local process until that browser quits.\",\"It chooses its own browser and that may not be the window Proctor is attached to, so a job started here can land in a different browser with a different session.\",\"It needs a model credential, and a run costs whatever that model costs.\",\"Run it from an installed entry point rather than an installer that runs it, and give it an isolated browser profile unless continuing this person's session is the point of the job.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"url\":\"chrome-extension://abcdefghijklmnop/options.html\",\"use\":\"browser-use\",\"why\":\"This page lives inside the browser itself and has no equivalent in Obscura's engine, so Obscura cannot open it at all. browser-use drives a real Chromium, which is where such a page exists. That it is reachable there is not a promise that it drives: several browser-internal pages are shadow-DOM interfaces that resist automation, and an extension page needs that extension loaded in the profile it opens.\"}",
  "com.google.Chrome|chrome-extension://abcdefghijklmnop/options.html|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome-extension://abcdefghijklmnop/options.html|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome-extension://abcdefghijklmnop/options.html|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor does not install this and has no opinion about whether it should be here. The person driving you enabled the lane, so tell them it is not on the machine.\",\"docs\":\"https://docs.browser-use.com\",\"missing\":\"The second browser lane is enabled but browser-use is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH, and a project-local virtualenv is invisible to it; proctor_doctor reports every path it checked.\",\"tool\":\"browser-use\"},\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome-extension://abcdefghijklmnop/options.html|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor does not install this and has no opinion about whether it should be here. The person driving you enabled the lane, so tell them it is not on the machine.\",\"docs\":\"https://docs.browser-use.com\",\"missing\":\"The second browser lane is enabled but browser-use is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH, and a project-local virtualenv is invisible to it; proctor_doctor reports every path it checked.\",\"tool\":\"browser-use\"},\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome://newtab|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to browser-use, which is not a command that does one thing: it is an autonomous agent that decides its own steps in a real browser. The native chrome around the page — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"url\":\"chrome://newtab\",\"use\":\"browser-use\",\"why\":\"This page lives inside the browser itself and has no equivalent in Obscura's engine, so Obscura cannot open it at all. browser-use drives a real Chromium, which is where such a page exists. That it is reachable there is not a promise that it drives: several browser-internal pages are shadow-DOM interfaces that resist automation, and an extension page needs that extension loaded in the profile it opens.\"}",
  "com.google.Chrome|chrome://newtab|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to browser-use, which is not a command that does one thing: it is an autonomous agent that decides its own steps in a real browser. The native chrome around the page — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Attaching to a browser already running needs its remote-debugging prompt approved, and that is a latch rather than a one-off: the port stays open to every local process until that browser quits.\",\"It chooses its own browser and that may not be the window Proctor is attached to, so a job started here can land in a different browser with a different session.\",\"It needs a model credential, and a run costs whatever that model costs.\",\"Run it from an installed entry point rather than an installer that runs it, and give it an isolated browser profile unless continuing this person's session is the point of the job.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"url\":\"chrome://newtab\",\"use\":\"browser-use\",\"why\":\"This page lives inside the browser itself and has no equivalent in Obscura's engine, so Obscura cannot open it at all. browser-use drives a real Chromium, which is where such a page exists. That it is reachable there is not a promise that it drives: several browser-internal pages are shadow-DOM interfaces that resist automation, and an extension page needs that extension loaded in the profile it opens.\"}",
  "com.google.Chrome|chrome://newtab|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome://newtab|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome://newtab|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor does not install this and has no opinion about whether it should be here. The person driving you enabled the lane, so tell them it is not on the machine.\",\"docs\":\"https://docs.browser-use.com\",\"missing\":\"The second browser lane is enabled but browser-use is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH, and a project-local virtualenv is invisible to it; proctor_doctor reports every path it checked.\",\"tool\":\"browser-use\"},\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome://newtab|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor does not install this and has no opinion about whether it should be here. The person driving you enabled the lane, so tell them it is not on the machine.\",\"docs\":\"https://docs.browser-use.com\",\"missing\":\"The second browser lane is enabled but browser-use is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH, and a project-local virtualenv is invisible to it; proctor_doctor reports every path it checked.\",\"tool\":\"browser-use\"},\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome://newtab|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to browser-use, which is not a command that does one thing: it is an autonomous agent that decides its own steps in a real browser. The native chrome around the page — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"url\":\"chrome://newtab\",\"use\":\"browser-use\",\"why\":\"This page lives inside the browser itself and has no equivalent in Obscura's engine, so Obscura cannot open it at all. browser-use drives a real Chromium, which is where such a page exists. That it is reachable there is not a promise that it drives: several browser-internal pages are shadow-DOM interfaces that resist automation, and an extension page needs that extension loaded in the profile it opens.\"}",
  "com.google.Chrome|chrome://newtab|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to browser-use, which is not a command that does one thing: it is an autonomous agent that decides its own steps in a real browser. The native chrome around the page — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Attaching to a browser already running needs its remote-debugging prompt approved, and that is a latch rather than a one-off: the port stays open to every local process until that browser quits.\",\"It chooses its own browser and that may not be the window Proctor is attached to, so a job started here can land in a different browser with a different session.\",\"It needs a model credential, and a run costs whatever that model costs.\",\"Run it from an installed entry point rather than an installer that runs it, and give it an isolated browser profile unless continuing this person's session is the point of the job.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"url\":\"chrome://newtab\",\"use\":\"browser-use\",\"why\":\"This page lives inside the browser itself and has no equivalent in Obscura's engine, so Obscura cannot open it at all. browser-use drives a real Chromium, which is where such a page exists. That it is reachable there is not a promise that it drives: several browser-internal pages are shadow-DOM interfaces that resist automation, and an extension page needs that extension loaded in the profile it opens.\"}",
  "com.google.Chrome|chrome://newtab|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome://newtab|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome://newtab|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor does not install this and has no opinion about whether it should be here. The person driving you enabled the lane, so tell them it is not on the machine.\",\"docs\":\"https://docs.browser-use.com\",\"missing\":\"The second browser lane is enabled but browser-use is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH, and a project-local virtualenv is invisible to it; proctor_doctor reports every path it checked.\",\"tool\":\"browser-use\"},\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome://newtab|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor does not install this and has no opinion about whether it should be here. The person driving you enabled the lane, so tell them it is not on the machine.\",\"docs\":\"https://docs.browser-use.com\",\"missing\":\"The second browser lane is enabled but browser-use is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH, and a project-local virtualenv is invisible to it; proctor_doctor reports every path it checked.\",\"tool\":\"browser-use\"},\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome://settings|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"This is one of the browser's own configuration, credential or history pages. Obscura cannot open it, and Proctor does not point the other lane at it either: that lane is an autonomous agent acting as the person sitting here, and this page is where their saved passwords, extensions and history live. Drive it by hand, or not at all.\"}",
  "com.google.Chrome|chrome://settings|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This is one of the browser's own configuration, credential or history pages. Obscura cannot open it, and Proctor does not point the other lane at it either: that lane is an autonomous agent acting as the person sitting here, and this page is where their saved passwords, extensions and history live. Drive it by hand, or not at all.\"}",
  "com.google.Chrome|chrome://settings|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome://settings|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome://settings|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor does not install this and has no opinion about whether it should be here. The person driving you enabled the lane, so tell them it is not on the machine.\",\"docs\":\"https://docs.browser-use.com\",\"missing\":\"The second browser lane is enabled but browser-use is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH, and a project-local virtualenv is invisible to it; proctor_doctor reports every path it checked.\",\"tool\":\"browser-use\"},\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome://settings|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor does not install this and has no opinion about whether it should be here. The person driving you enabled the lane, so tell them it is not on the machine.\",\"docs\":\"https://docs.browser-use.com\",\"missing\":\"The second browser lane is enabled but browser-use is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH, and a project-local virtualenv is invisible to it; proctor_doctor reports every path it checked.\",\"tool\":\"browser-use\"},\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome://settings|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"This is one of the browser's own configuration, credential or history pages. Obscura cannot open it, and Proctor does not point the other lane at it either: that lane is an autonomous agent acting as the person sitting here, and this page is where their saved passwords, extensions and history live. Drive it by hand, or not at all.\"}",
  "com.google.Chrome|chrome://settings|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This is one of the browser's own configuration, credential or history pages. Obscura cannot open it, and Proctor does not point the other lane at it either: that lane is an autonomous agent acting as the person sitting here, and this page is where their saved passwords, extensions and history live. Drive it by hand, or not at all.\"}",
  "com.google.Chrome|chrome://settings|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome://settings|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome://settings|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor does not install this and has no opinion about whether it should be here. The person driving you enabled the lane, so tell them it is not on the machine.\",\"docs\":\"https://docs.browser-use.com\",\"missing\":\"The second browser lane is enabled but browser-use is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH, and a project-local virtualenv is invisible to it; proctor_doctor reports every path it checked.\",\"tool\":\"browser-use\"},\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|chrome://settings|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor does not install this and has no opinion about whether it should be here. The person driving you enabled the lane, so tell them it is not on the machine.\",\"docs\":\"https://docs.browser-use.com\",\"missing\":\"The second browser lane is enabled but browser-use is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH, and a project-local virtualenv is invisible to it; proctor_doctor reports every path it checked.\",\"tool\":\"browser-use\"},\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|data:text/html,<p>hi</p>|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|data:text/html,<p>hi</p>|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|data:text/html,<p>hi</p>|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|data:text/html,<p>hi</p>|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|data:text/html,<p>hi</p>|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|data:text/html,<p>hi</p>|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|data:text/html,<p>hi</p>|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|data:text/html,<p>hi</p>|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|data:text/html,<p>hi</p>|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|data:text/html,<p>hi</p>|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|data:text/html,<p>hi</p>|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|data:text/html,<p>hi</p>|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|devtools://devtools/bundled/inspector.html|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"This is one of the browser's own configuration, credential or history pages. Obscura cannot open it, and Proctor does not point the other lane at it either: that lane is an autonomous agent acting as the person sitting here, and this page is where their saved passwords, extensions and history live. Drive it by hand, or not at all.\"}",
  "com.google.Chrome|devtools://devtools/bundled/inspector.html|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This is one of the browser's own configuration, credential or history pages. Obscura cannot open it, and Proctor does not point the other lane at it either: that lane is an autonomous agent acting as the person sitting here, and this page is where their saved passwords, extensions and history live. Drive it by hand, or not at all.\"}",
  "com.google.Chrome|devtools://devtools/bundled/inspector.html|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|devtools://devtools/bundled/inspector.html|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|devtools://devtools/bundled/inspector.html|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor does not install this and has no opinion about whether it should be here. The person driving you enabled the lane, so tell them it is not on the machine.\",\"docs\":\"https://docs.browser-use.com\",\"missing\":\"The second browser lane is enabled but browser-use is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH, and a project-local virtualenv is invisible to it; proctor_doctor reports every path it checked.\",\"tool\":\"browser-use\"},\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|devtools://devtools/bundled/inspector.html|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor does not install this and has no opinion about whether it should be here. The person driving you enabled the lane, so tell them it is not on the machine.\",\"docs\":\"https://docs.browser-use.com\",\"missing\":\"The second browser lane is enabled but browser-use is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH, and a project-local virtualenv is invisible to it; proctor_doctor reports every path it checked.\",\"tool\":\"browser-use\"},\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|devtools://devtools/bundled/inspector.html|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"This is one of the browser's own configuration, credential or history pages. Obscura cannot open it, and Proctor does not point the other lane at it either: that lane is an autonomous agent acting as the person sitting here, and this page is where their saved passwords, extensions and history live. Drive it by hand, or not at all.\"}",
  "com.google.Chrome|devtools://devtools/bundled/inspector.html|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This is one of the browser's own configuration, credential or history pages. Obscura cannot open it, and Proctor does not point the other lane at it either: that lane is an autonomous agent acting as the person sitting here, and this page is where their saved passwords, extensions and history live. Drive it by hand, or not at all.\"}",
  "com.google.Chrome|devtools://devtools/bundled/inspector.html|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|devtools://devtools/bundled/inspector.html|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|devtools://devtools/bundled/inspector.html|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor does not install this and has no opinion about whether it should be here. The person driving you enabled the lane, so tell them it is not on the machine.\",\"docs\":\"https://docs.browser-use.com\",\"missing\":\"The second browser lane is enabled but browser-use is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH, and a project-local virtualenv is invisible to it; proctor_doctor reports every path it checked.\",\"tool\":\"browser-use\"},\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|devtools://devtools/bundled/inspector.html|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor does not install this and has no opinion about whether it should be here. The person driving you enabled the lane, so tell them it is not on the machine.\",\"docs\":\"https://docs.browser-use.com\",\"missing\":\"The second browser lane is enabled but browser-use is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH, and a project-local virtualenv is invisible to it; proctor_doctor reports every path it checked.\",\"tool\":\"browser-use\"},\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "com.google.Chrome|file:///Users/x/page.html|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|file:///Users/x/page.html|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|file:///Users/x/page.html|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|file:///Users/x/page.html|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|file:///Users/x/page.html|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|file:///Users/x/page.html|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|file:///Users/x/page.html|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|file:///Users/x/page.html|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|file:///Users/x/page.html|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|file:///Users/x/page.html|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|file:///Users/x/page.html|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|file:///Users/x/page.html|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "com.google.Chrome|http://192.168.1.4:3000/report.pdf?t=1|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\"}",
  "com.google.Chrome|http://192.168.1.4:3000/report.pdf?t=1|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\"}",
  "com.google.Chrome|http://192.168.1.4:3000/report.pdf?t=1|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\"}",
  "com.google.Chrome|http://192.168.1.4:3000/report.pdf?t=1|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\"}",
  "com.google.Chrome|http://192.168.1.4:3000/report.pdf?t=1|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\"}",
  "com.google.Chrome|http://192.168.1.4:3000/report.pdf?t=1|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\"}",
  "com.google.Chrome|http://192.168.1.4:3000/report.pdf?t=1|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"notes\":[\"This is a private-network address, so it needs --allow-private-network; without it Obscura blocks the fetch and the failure reads like a network error.\",\"This URL is a PDF. Neither browser tool reads a PDF's structure — rendering one measures the viewer rather than the document — so fetch the file and parse it instead.\"],\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|http://192.168.1.4:3000/report.pdf?t=1|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"notes\":[\"This is a private-network address, so it needs --allow-private-network; without it Obscura blocks the fetch and the failure reads like a network error.\",\"This URL is a PDF. Neither browser tool reads a PDF's structure — rendering one measures the viewer rather than the document — so fetch the file and parse it instead.\"],\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|http://192.168.1.4:3000/report.pdf?t=1|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"notes\":[\"This is a private-network address, so it needs --allow-private-network; without it Obscura blocks the fetch and the failure reads like a network error.\",\"This URL is a PDF. Neither browser tool reads a PDF's structure — rendering one measures the viewer rather than the document — so fetch the file and parse it instead.\"],\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|http://192.168.1.4:3000/report.pdf?t=1|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"notes\":[\"This is a private-network address, so it needs --allow-private-network; without it Obscura blocks the fetch and the failure reads like a network error.\",\"This URL is a PDF. Neither browser tool reads a PDF's structure — rendering one measures the viewer rather than the document — so fetch the file and parse it instead.\"],\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|http://192.168.1.4:3000/report.pdf?t=1|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"notes\":[\"This is a private-network address, so it needs --allow-private-network; without it Obscura blocks the fetch and the failure reads like a network error.\",\"This URL is a PDF. Neither browser tool reads a PDF's structure — rendering one measures the viewer rather than the document — so fetch the file and parse it instead.\"],\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|http://192.168.1.4:3000/report.pdf?t=1|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"notes\":[\"This is a private-network address, so it needs --allow-private-network; without it Obscura blocks the fetch and the failure reads like a network error.\",\"This URL is a PDF. Neither browser tool reads a PDF's structure — rendering one measures the viewer rather than the document — so fetch the file and parse it instead.\"],\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|https://example.com/dashboard|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"https://example.com/dashboard\"}",
  "com.google.Chrome|https://example.com/dashboard|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"https://example.com/dashboard\"}",
  "com.google.Chrome|https://example.com/dashboard|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"https://example.com/dashboard\"}",
  "com.google.Chrome|https://example.com/dashboard|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"https://example.com/dashboard\"}",
  "com.google.Chrome|https://example.com/dashboard|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"https://example.com/dashboard\"}",
  "com.google.Chrome|https://example.com/dashboard|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"https://example.com/dashboard\"}",
  "com.google.Chrome|https://example.com/dashboard|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"url\":\"https://example.com/dashboard\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|https://example.com/dashboard|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"url\":\"https://example.com/dashboard\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|https://example.com/dashboard|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"url\":\"https://example.com/dashboard\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|https://example.com/dashboard|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"url\":\"https://example.com/dashboard\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|https://example.com/dashboard|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"url\":\"https://example.com/dashboard\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "com.google.Chrome|https://example.com/dashboard|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Google Chrome\",\"bundleId\":\"com.google.Chrome\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"url\":\"https://example.com/dashboard\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|<nil>|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"AXURL was absent or empty on the web area.\"}",
  "org.mozilla.firefox|<nil>|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"AXURL was absent or empty on the web area.\"}",
  "org.mozilla.firefox|<nil>|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"AXURL was absent or empty on the web area.\"}",
  "org.mozilla.firefox|<nil>|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"AXURL was absent or empty on the web area.\"}",
  "org.mozilla.firefox|<nil>|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"AXURL was absent or empty on the web area.\"}",
  "org.mozilla.firefox|<nil>|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"AXURL was absent or empty on the web area.\"}",
  "org.mozilla.firefox|<nil>|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"AXURL was absent or empty on the web area.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|<nil>|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"AXURL was absent or empty on the web area.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|<nil>|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"AXURL was absent or empty on the web area.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|<nil>|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"AXURL was absent or empty on the web area.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|<nil>|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"AXURL was absent or empty on the web area.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|<nil>|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"AXURL was absent or empty on the web area.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|<no probe>|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"No window was named, so no page URL was read.\"}",
  "org.mozilla.firefox|<no probe>|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"No window was named, so no page URL was read.\"}",
  "org.mozilla.firefox|<no probe>|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"No window was named, so no page URL was read.\"}",
  "org.mozilla.firefox|<no probe>|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"No window was named, so no page URL was read.\"}",
  "org.mozilla.firefox|<no probe>|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"No window was named, so no page URL was read.\"}",
  "org.mozilla.firefox|<no probe>|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"urlUnavailable\":\"No window was named, so no page URL was read.\"}",
  "org.mozilla.firefox|<no probe>|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"No window was named, so no page URL was read.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|<no probe>|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"No window was named, so no page URL was read.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|<no probe>|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"No window was named, so no page URL was read.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|<no probe>|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"No window was named, so no page URL was read.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|<no probe>|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"No window was named, so no page URL was read.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|<no probe>|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"No window was named, so no page URL was read.\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|about:blank|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|about:blank|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|about:blank|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|about:blank|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|about:blank|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|about:blank|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|about:blank|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|about:blank|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|about:blank|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|about:blank|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|about:blank|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|about:blank|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome-extension://abcdefghijklmnop/options.html|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome-extension://abcdefghijklmnop/options.html|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome-extension://abcdefghijklmnop/options.html|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome-extension://abcdefghijklmnop/options.html|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome-extension://abcdefghijklmnop/options.html|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome-extension://abcdefghijklmnop/options.html|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome-extension://abcdefghijklmnop/options.html|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome-extension://abcdefghijklmnop/options.html|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome-extension://abcdefghijklmnop/options.html|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome-extension://abcdefghijklmnop/options.html|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome-extension://abcdefghijklmnop/options.html|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome-extension://abcdefghijklmnop/options.html|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://newtab|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://newtab|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://newtab|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://newtab|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://newtab|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://newtab|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://newtab|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://newtab|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://newtab|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://newtab|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://newtab|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://newtab|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://settings|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://settings|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://settings|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://settings|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://settings|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://settings|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://settings|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://settings|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://settings|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://settings|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://settings|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|chrome://settings|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|data:text/html,<p>hi</p>|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|data:text/html,<p>hi</p>|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|data:text/html,<p>hi</p>|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|data:text/html,<p>hi</p>|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|data:text/html,<p>hi</p>|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|data:text/html,<p>hi</p>|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|data:text/html,<p>hi</p>|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|data:text/html,<p>hi</p>|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|data:text/html,<p>hi</p>|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|data:text/html,<p>hi</p>|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|data:text/html,<p>hi</p>|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|data:text/html,<p>hi</p>|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|devtools://devtools/bundled/inspector.html|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|devtools://devtools/bundled/inspector.html|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|devtools://devtools/bundled/inspector.html|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|devtools://devtools/bundled/inspector.html|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|devtools://devtools/bundled/inspector.html|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|devtools://devtools/bundled/inspector.html|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|devtools://devtools/bundled/inspector.html|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|devtools://devtools/bundled/inspector.html|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|devtools://devtools/bundled/inspector.html|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|devtools://devtools/bundled/inspector.html|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|devtools://devtools/bundled/inspector.html|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|devtools://devtools/bundled/inspector.html|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"The page's URL is not http or https, so Obscura cannot open it; there is no handoff target. The page is still not a native surface, so what Proctor can prove about it is limited whichever tool is used.\"}",
  "org.mozilla.firefox|file:///Users/x/page.html|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|file:///Users/x/page.html|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|file:///Users/x/page.html|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|file:///Users/x/page.html|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|file:///Users/x/page.html|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|file:///Users/x/page.html|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|file:///Users/x/page.html|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|file:///Users/x/page.html|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|file:///Users/x/page.html|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|file:///Users/x/page.html|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|file:///Users/x/page.html|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|file:///Users/x/page.html|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"urlUnavailable\":\"This URL is a local file or an inline payload rather than a page on the network. Neither browser tool is the instrument for it: read the file, or decode the payload, directly.\"}",
  "org.mozilla.firefox|http://192.168.1.4:3000/report.pdf?t=1|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\"}",
  "org.mozilla.firefox|http://192.168.1.4:3000/report.pdf?t=1|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\"}",
  "org.mozilla.firefox|http://192.168.1.4:3000/report.pdf?t=1|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\"}",
  "org.mozilla.firefox|http://192.168.1.4:3000/report.pdf?t=1|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\"}",
  "org.mozilla.firefox|http://192.168.1.4:3000/report.pdf?t=1|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\"}",
  "org.mozilla.firefox|http://192.168.1.4:3000/report.pdf?t=1|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\"}",
  "org.mozilla.firefox|http://192.168.1.4:3000/report.pdf?t=1|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"notes\":[\"This is a private-network address, so it needs --allow-private-network; without it Obscura blocks the fetch and the failure reads like a network error.\",\"This URL is a PDF. Neither browser tool reads a PDF's structure — rendering one measures the viewer rather than the document — so fetch the file and parse it instead.\"],\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|http://192.168.1.4:3000/report.pdf?t=1|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"notes\":[\"This is a private-network address, so it needs --allow-private-network; without it Obscura blocks the fetch and the failure reads like a network error.\",\"This URL is a PDF. Neither browser tool reads a PDF's structure — rendering one measures the viewer rather than the document — so fetch the file and parse it instead.\"],\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|http://192.168.1.4:3000/report.pdf?t=1|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"notes\":[\"This is a private-network address, so it needs --allow-private-network; without it Obscura blocks the fetch and the failure reads like a network error.\",\"This URL is a PDF. Neither browser tool reads a PDF's structure — rendering one measures the viewer rather than the document — so fetch the file and parse it instead.\"],\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|http://192.168.1.4:3000/report.pdf?t=1|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"notes\":[\"This is a private-network address, so it needs --allow-private-network; without it Obscura blocks the fetch and the failure reads like a network error.\",\"This URL is a PDF. Neither browser tool reads a PDF's structure — rendering one measures the viewer rather than the document — so fetch the file and parse it instead.\"],\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|http://192.168.1.4:3000/report.pdf?t=1|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"notes\":[\"This is a private-network address, so it needs --allow-private-network; without it Obscura blocks the fetch and the failure reads like a network error.\",\"This URL is a PDF. Neither browser tool reads a PDF's structure — rendering one measures the viewer rather than the document — so fetch the file and parse it instead.\"],\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|http://192.168.1.4:3000/report.pdf?t=1|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"notes\":[\"This is a private-network address, so it needs --allow-private-network; without it Obscura blocks the fetch and the failure reads like a network error.\",\"This URL is a PDF. Neither browser tool reads a PDF's structure — rendering one measures the viewer rather than the document — so fetch the file and parse it instead.\"],\"url\":\"http://192.168.1.4:3000/report.pdf?t=1\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|https://example.com/dashboard|obscura=false|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"https://example.com/dashboard\"}",
  "org.mozilla.firefox|https://example.com/dashboard|obscura=false|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"https://example.com/dashboard\"}",
  "org.mozilla.firefox|https://example.com/dashboard|obscura=false|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"https://example.com/dashboard\"}",
  "org.mozilla.firefox|https://example.com/dashboard|obscura=false|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"https://example.com/dashboard\"}",
  "org.mozilla.firefox|https://example.com/dashboard|obscura=false|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"https://example.com/dashboard\"}",
  "org.mozilla.firefox|https://example.com/dashboard|obscura=false|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"toolUnavailable\":{\"askThePerson\":\"Proctor has no way to install it. Tell the person driving you rather than trying to install it yourself: the Proctor status window has the install command ready to copy for this Mac, and a Re-check that confirms it worked.\",\"docs\":\"https://github.com/h4ckf0r0day/obscura\",\"missing\":\"Obscura is not installed, or not anywhere Proctor can see it. Proctor runs as a launchd agent, so it checks a fixed list of locations rather than a login shell's PATH; proctor_doctor reports every path it checked.\",\"tool\":\"obscura\"},\"url\":\"https://example.com/dashboard\"}",
  "org.mozilla.firefox|https://example.com/dashboard|obscura=true|lane=enabled|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"url\":\"https://example.com/dashboard\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|https://example.com/dashboard|obscura=true|lane=enabled|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"url\":\"https://example.com/dashboard\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|https://example.com/dashboard|obscura=true|lane=off|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"url\":\"https://example.com/dashboard\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|https://example.com/dashboard|obscura=true|lane=off|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"url\":\"https://example.com/dashboard\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|https://example.com/dashboard|obscura=true|lane=unavailable|brief": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"url\":\"https://example.com/dashboard\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}",
  "org.mozilla.firefox|https://example.com/dashboard|obscura=true|lane=unavailable|full": "{\"boundary\":\"The page belongs to Obscura. The native chrome around it — toolbar, tab bar, menus, sheets, the downloads popover — stays Proctor's.\",\"browser\":\"Firefox\",\"bundleId\":\"org.mozilla.firefox\",\"caveats\":[\"Localhost and private-network addresses are blocked unless --allow-private-network is passed; a local dev server otherwise fails as an SSRF block that reads like a network error.\",\"CSS animations and transitions never execute, so there is no motion to observe.\",\"Emulation.setEmulatedMedia is accepted and inert, so there is no print pass and no reduced-motion pass; matchMedia stays false.\",\"Web fonts never load, so font fidelity is unmeasurable rather than perfect.\",\"An empty computed value means not implemented, not unset, for boxShadow, backgroundImage, textTransform, outline and flex.\",\"Read computed styles through longhand properties. Shorthands such as padding and margin resolve to 0px even when the layout is correct, which passes a spacing assertion that should fail.\",\"obscura fetch renders at a fixed 1280x720 and awaits no promise; use obscura serve plus CDP when either matters.\"],\"commands\":[\"obscura fetch <url> --dump markdown\",\"obscura fetch <url> --screenshot page.png\",\"obscura fetch <url> --eval \\\"document.title\\\"\",\"obscura --allow-private-network fetch <url>   # required for localhost and 192.168.*\",\"obscura serve --port 9222   # CDP, when a promise or a viewport other than 1280x720 matters\"],\"evidence\":\"A step into page content still travels the accessibility plane like any other, but its state hash is taken over a browser's render tree rather than an app's view hierarchy, so a determinism score across page content measures the page's own churn as much as the app's.\",\"url\":\"https://example.com/dashboard\",\"use\":\"obscura\",\"why\":\"This is the default lane. No measured Obscura limit applies to this page: the scheme is one Obscura opens, and Proctor found nothing about this page that Obscura cannot do.\"}"
}